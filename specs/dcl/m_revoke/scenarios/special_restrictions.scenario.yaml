schema_version: 1
kind: scenario
id: scenario_m_revoke_special_restrictions
name: scenario_m_revoke_special_restrictions
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_revoke
fact_refs:
- m_revoke_fact_dependent_grants
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: PUBLIC 再授权限制或下游依赖回收独立建模；不把系统权限、ANY、PUBLIC、CASCADE 放入默认对象候选。
oracles:
- kind: manual_assertion
  expected: 受限分支保持未生成，目标错误不臆造
execution_requirements:
- database_authorization
- target_oracle_calibration
