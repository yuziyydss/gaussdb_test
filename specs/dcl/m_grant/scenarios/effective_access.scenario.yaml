schema_version: 1
kind: scenario
id: scenario_m_grant_effective_access
name: scenario_m_grant_effective_access
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_grant
fact_refs:
- m_grant_fact_column_access
- m_grant_fact_admin_bypass
fixture_refs:
- fixture_m_grant_objects
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 用非管理员的专用受测身份查询/更新列，区分表级、列级、直接权限、继承权限和再授权能力；不能用 fixture 管理员身份假验权限。
oracles:
- kind: manual_assertion
  expected: 当前只验证有限 SQL；有效权限与身份切换 Oracle 仍待校准
execution_requirements:
- database_authorization
- target_oracle_calibration
