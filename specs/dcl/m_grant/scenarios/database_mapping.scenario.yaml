schema_version: 1
kind: scenario
id: scenario_m_grant_database_mapping
name: scenario_m_grant_database_mapping
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_grant
fact_refs:
- m_grant_fact_database_mapping
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 分 grant_database_nomapping 环境验证物理库与 Schema 授权映射，不能统一假设。
oracles:
- kind: manual_assertion
  expected: 数据库级授权仍未建模/执行
execution_requirements:
- database_authorization
- target_oracle_calibration
