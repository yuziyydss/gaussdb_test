schema_version: 1
kind: "fixture"
id: "fixture_revoke_table"
name: "REVOKE table"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "revoke"
requires_fixture_refs:
  - "fixture_create_role_isolated_roles"
provides:
  tables:
    -
      name: "b9_revoke_source"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: true
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TABLE b9_revoke_source (col_1 INTEGER, col_2 INTEGER);"
  teardown_sqls:
    - "DROP TABLE IF EXISTS b9_revoke_source;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
