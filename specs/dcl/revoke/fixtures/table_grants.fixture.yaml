schema_version: 1
kind: "fixture"
id: "fixture_revoke_table_grants"
name: "REVOKE table_grants"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "revoke"
requires_fixture_refs:
  - "fixture_revoke_table"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "GRANT SELECT, INSERT, UPDATE ON TABLE b9_revoke_source TO b9_role_b, b9_role_c WITH GRANT OPTION;"
  teardown_sqls:
    - "REVOKE ALL PRIVILEGES ON TABLE b9_revoke_source FROM b9_role_b, b9_role_c;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
