schema_version: 1
kind: scenario
id: scenario_grant_public_defaults
name: GRANT PUBLIC 默认权限和所有者固有权限
status: planned
description: 查询新建对象的默认 PUBLIC 权限，并验证对象所有者固有权限及 ALTER DEFAULT PRIVILEGES 影响。
factor_ref: grant
fixture_refs: [fixture_grant_core_objects]
fact_refs: [grant_fact_default_public_privileges, grant_fact_owner_implicit_privileges, grant_fact_public_semantics]
preconditions: [能够查询权限元数据, 按对象类型创建新对象]
steps:
  - {id: inspect_defaults, action: query_privilege_metadata}
  - {id: alter_defaults, action: execute_related_command, detail: "在独立场景中修改默认权限后创建新对象"}
oracles:
  - {kind: public_and_owner_privilege_matrix, expected: "默认 PUBLIC 权限及所有者固有权限符合本章描述"}
execution_requirements: [database, metadata_query, alter_default_privileges]
