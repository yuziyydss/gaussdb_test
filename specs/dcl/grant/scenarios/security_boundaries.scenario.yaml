schema_version: 1
kind: scenario
id: scenario_grant_security_boundaries
name: GRANT 管理员与安全边界
status: planned
description: 在三权分立开关与管理员身份矩阵中验证授权边界，并记录安全建议而不把建议当成 SQL 语法。
factor_ref: grant
fixture_refs: [fixture_grant_core_objects]
fact_refs: [grant_fact_security_guidance, grant_fact_role_admin_environment, grant_fact_any_environment, grant_fact_all_privileges_environment]
preconditions: [可切换三权分立模式, 初始用户、系统管理员、安全管理员和普通用户]
steps:
  - {id: authority_matrix, action: execute_grants_by_identity, detail: "角色、ANY 与 ALL PRIVILEGES 分别测试"}
oracles:
  - {kind: authorization_matrix, expected: "每种身份只在文档确认的环境边界内成功"}
execution_requirements: [database, privileged_users, separation_of_duties_variants, security_review]
