schema_version: 1
kind: fixture
id: fixture_grant_fresh_single_recipient
name: GRANT 单个全新非登录受权角色
status: needs_review
description: >-
  单接收方产生式专用输入；不创建第二角色，不预先赋予SYSADMIN或普通对象权限。
  只定义SQL，不代表角色属性修改或回滚已验证。
factor_ref: grant
provides: {tables: []}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - BEGIN;
    - CREATE ROLE grant_recipient NOSYSADMIN NOLOGIN PASSWORD DISABLE;
  teardown_sqls:
    - ROLLBACK;
  note: >-
    未来执行须独立授权、独占隔离实例、全新连接且无外层事务，grant_recipient 全局名称未占用。
    任一步失败都停止目标并回滚；不预先DROP，不复用已有角色，不DROP OWNED，不吞异常。
    禁止登录受权角色、SET ROLE或外部认证配置；必须实际核查回滚和角色权限残留。
