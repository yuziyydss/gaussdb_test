schema_version: 1
kind: fixture
id: fixture_m_alter_default_privileges_granted_tables
name: fixture_m_alter_default_privileges_granted_tables
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_default_privileges
requires_fixture_refs:
- fixture_m_create_role_existing
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE SCHEMA m_default_privilege_namespace;
  - GRANT USAGE ON SCHEMA m_default_privilege_namespace TO m_b04_role_existing;
  - ALTER DEFAULT PRIVILEGES IN SCHEMA m_default_privilege_namespace GRANT SELECT, INSERT ON TABLES TO m_b04_role_existing
    WITH GRANT OPTION;
  teardown_sqls:
  - ALTER DEFAULT PRIVILEGES IN SCHEMA m_default_privilege_namespace REVOKE ALL PRIVILEGES ON TABLES FROM m_b04_role_existing
    RESTRICT;
  - ALTER DEFAULT PRIVILEGES IN SCHEMA m_default_privilege_namespace REVOKE ALL PRIVILEGES ON SEQUENCES FROM m_b04_role_existing
    RESTRICT;
  - DROP SCHEMA m_default_privilege_namespace;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
