schema_version: 1
kind: scenario
id: scenario_m_alter_default_privileges_before_after
name: scenario_m_alter_default_privileges_before_after
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_default_privileges
fact_refs:
- m_alter_default_privileges_fact_existing
- m_alter_default_privileges_fact_cleanup
- m_alter_default_privileges_fact_object_scope
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 隔离Schema中分别在授权前后创建表、视图和序列，用非管理员目标身份检查ACL；最后恢复默认ACL再删除角色。
oracles:
- kind: manual_assertion
  expected: 已有对象不追溯变更，后建对象按默认ACL；未执行，不能用管理员访问成功证明受权角色权限
execution_requirements:
- database_authorization
- target_oracle_calibration
