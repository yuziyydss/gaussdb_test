schema_version: 1
kind: fixture
id: fixture_shared_log_fdw_catalog_server
name: log_fdw目录示例专用服务器
status: needs_review
description: CREATE/DROP FOREIGN TABLE共同使用的无地址、无凭据服务器；仅目录DDL，不承担文件或远端数据访问。
factor_ref: null
requires_fixture_refs: []
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls: ["CREATE SERVER g_a3_log_server FOREIGN DATA WRAPPER log_fdw;"]
  teardown_sqls: ["DROP SERVER g_a3_log_server RESTRICT;"]
  note: 仅离线；源自CREATE FOREIGN TABLE L101-111、CREATE SERVER L26-27。执行需另行授权且名称不存在；仅凭本次成功CREATE回执允许清理。子对象未删除时RESTRICT必须失败并保留原始错误，不盲目级联。
