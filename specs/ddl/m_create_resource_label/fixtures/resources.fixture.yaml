schema_version: 1
kind: fixture
id: fixture_m_create_resource_label_resources
name: fixture_m_create_resource_label_resources
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_resource_label
requires_fixture_refs: []
provides:
  tables:
  - name: m_label_namespace.source
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
    - name: qty
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE SCHEMA m_label_namespace;
  - CREATE TABLE m_label_namespace.source (id INTEGER, qty INTEGER);
  - CREATE VIEW m_label_namespace.source_view AS SELECT id, qty FROM m_label_namespace.source;
  teardown_sqls:
  - DROP VIEW m_label_namespace.source_view;
  - DROP TABLE m_label_namespace.source;
  - DROP SCHEMA m_label_namespace;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
