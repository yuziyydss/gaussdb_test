schema_version: 1
kind: scenario
id: scenario_m_create_resource_label_collision
name: scenario_m_create_resource_label_collision
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_resource_label
fact_refs:
- m_create_resource_label_fact_unique
- m_create_resource_label_fact_notice
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 在专用已存在标签上比较有/无IF NOT EXISTS，需目标重名错误与NOTICE校准。
oracles:
- kind: manual_assertion
  expected: 存在状态与错误/NOTICE尚未执行
execution_requirements:
- database_authorization
- target_oracle_calibration
