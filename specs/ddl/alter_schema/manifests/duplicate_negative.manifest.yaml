schema_version: 1
kind: "manifest"
id: "manifest_alter_schema_duplicate_negative"
name: "ALTER SCHEMA duplicate_negative"
status: "needs_review"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_schema"
syntax_ref: "syntax_alter_schema"
suite_type: "negative"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_schema_targets"
identifier_policy: {}
bindings:
  action:
    - "alter_schema_action_rename"
  new_name:
    - "alter_schema_new_name_collision"
expected:
  default: "error"
  scope: "syntax_and_semantics"
  oracle_status: "needs_verification"
  error_category: "duplicate_schema"
  fact_refs: []
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
violates_rule_refs:
  - "alter_schema_rule_unique"
