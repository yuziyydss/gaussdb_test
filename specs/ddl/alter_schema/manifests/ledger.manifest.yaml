schema_version: 1
kind: "manifest"
id: "manifest_alter_schema_ledger"
name: "ALTER SCHEMA ledger"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_schema"
syntax_ref: "syntax_alter_schema"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_schema_targets"
identifier_policy: {}
bindings:
  action:
    - "alter_schema_action_with"
    - "alter_schema_action_without"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "enable_ledger"
    allowed_values:
      - "on"
    fact_refs:
      - "alter_schema_fact_ledger_gate"
