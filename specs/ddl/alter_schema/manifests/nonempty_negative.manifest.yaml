schema_version: 1
kind: "manifest"
id: "manifest_alter_schema_nonempty_negative"
name: "ALTER SCHEMA nonempty_negative"
status: "needs_review"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_schema"
syntax_ref: "syntax_alter_schema"
suite_type: "negative"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_schema_targets"
identifier_policy: {}
bindings:
  action:
    - "alter_schema_action_with"
    - "alter_schema_action_without"
  schema_name:
    - "alter_schema_schema_name_occupied"
expected:
  default: "error"
  scope: "syntax_and_semantics"
  oracle_status: "needs_verification"
  error_category: "schema_contains_tables"
  fact_refs: []
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
violates_rule_refs:
  - "alter_schema_rule_empty"
environment_requirements:
  -
    key: "enable_ledger"
    allowed_values:
      - "on"
    fact_refs:
      - "alter_schema_fact_ledger_gate"
