schema_version: 1
kind: "scenario"
id: "scenario_alter_schema_charset_review"
name: "ALTER SCHEMA charset_review"
status: "planned"
description: "补完整字符集域与编码匹配；查询字符属性默认值，不能只验证 ALTER 成功。"
factor_ref: "alter_schema"
fixture_refs: []
fact_refs:
  - "alter_schema_fact_charset_gap"
  - "alter_schema_fact_charset_default"
  - "alter_schema_fact_collation_default"
  - "alter_schema_fact_encoding"
  - "alter_schema_fact_compatibility"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "补完整字符集域与编码匹配；查询字符属性默认值，不能只验证 ALTER 成功。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
