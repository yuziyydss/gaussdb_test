schema_version: 1
kind: "manifest"
id: "manifest_drop_type_unreferenced"
name: "DROP TYPE unreferenced"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_type"
syntax_ref: "syntax_drop_type"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_type_types"
identifier_policy: {}
bindings:
  if_exists:
    - "drop_type_if_exists_no"
    - "drop_type_if_exists_yes"
  behavior:
    - "drop_type_behavior_default"
    - "drop_type_behavior_restrict"
    - "drop_type_behavior_cascade"
  targets:
    - "drop_type_targets_composite"
    - "drop_type_targets_enum"
    - "drop_type_targets_both"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "type_drop_authorized"
    allowed_values:
      - "true"
    fact_refs:
      - "drop_type_fact_privilege"
