schema_version: 1
kind: "scenario"
id: "scenario_drop_type_documented_contracts"
name: "DROP TYPE documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_type"
fixture_refs: []
fact_refs:
  - "drop_type_fact_purpose"
  - "drop_type_fact_privilege"
  - "drop_type_fact_existing"
  - "drop_type_fact_missing"
  - "drop_type_fact_cascade"
  - "drop_type_fact_restrict"
  - "drop_type_fact_cascade_function"
  - "drop_type_fact_cascade_operator"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除用户定义类型。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "属主、DROP或DROP ANY TYPE授权可删；三权分立关闭时管理员默认拥有权限。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "类型存在时删除成功。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "IF EXISTS且不存在时只产生NOTICE。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "CASCADE删除依赖类型的字段、函数、操作符等。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "RESTRICT遇依赖对象拒绝删除。"
  -
    id: "cascade_function"
    action: "verify_documented_contract"
    note: "CASCADE会级联删除依赖函数。"
  -
    id: "cascade_operator"
    action: "verify_documented_contract"
    note: "CASCADE会级联删除依赖操作符。"
oracles:
  -
    kind: "documented_behavior"
    expected: "类型存在时删除成功。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS且不存在时只产生NOTICE。"
  -
    kind: "documented_behavior"
    expected: "RESTRICT遇依赖对象拒绝删除。"
execution_requirements:
  - "database"
  - "same_session"
