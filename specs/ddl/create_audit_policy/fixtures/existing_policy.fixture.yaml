schema_version: 1
kind: "fixture"
id: "fixture_create_audit_policy_existing_policy"
name: "CREATE AUDIT POLICY existing_policy"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_audit_policy"
requires_fixture_refs:
  - "fixture_create_resource_label_existing_labels"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE AUDIT POLICY b10_audit_existing PRIVILEGES CREATE DISABLE;"
  teardown_sqls:
    - "DROP AUDIT POLICY IF EXISTS b10_audit_existing;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
