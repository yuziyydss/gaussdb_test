schema_version: 1
kind: "fixture"
id: "fixture_create_audit_policy_resource_labels"
name: "CREATE AUDIT POLICY resource_labels"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_audit_policy"
requires_fixture_refs:
  - "fixture_create_resource_label_existing_labels"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "auto"
  note: "只依赖上游能力；不复制对象定义或生命周期 SQL。"
