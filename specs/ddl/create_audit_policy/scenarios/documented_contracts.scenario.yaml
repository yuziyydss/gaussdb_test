schema_version: 1
kind: "scenario"
id: "scenario_create_audit_policy_documented_contracts"
name: "CREATE AUDIT POLICY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "create_audit_policy"
fixture_refs: []
fact_refs:
  - "create_audit_policy_fact_purpose"
  - "create_audit_policy_fact_privilege"
  - "create_audit_policy_fact_switch"
  - "create_audit_policy_fact_catalogs"
  - "create_audit_policy_fact_unique"
  - "create_audit_policy_fact_dblink_sender"
  - "create_audit_policy_fact_policy_name"
  - "create_audit_policy_fact_name_limit"
  - "create_audit_policy_fact_truncate"
  - "create_audit_policy_fact_case_fold"
  - "create_audit_policy_fact_identifier_chars"
  - "create_audit_policy_fact_analyze_vacuum"
  - "create_audit_policy_fact_all_scope"
  - "create_audit_policy_fact_enabled_default"
  - "create_audit_policy_fact_identifier_start"
  - "create_audit_policy_fact_policy_catalog"
  - "create_audit_policy_fact_access_catalog"
  - "create_audit_policy_fact_privileges_catalog"
  - "create_audit_policy_fact_filters_catalog"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "创建统一审计策略。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "只有POLADMIN、SYSADMIN或初始用户可创建及维护审计策略。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "本段要求enable_security_policy=on；原文却称脱敏策略生效，需保留术语歧义。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "管理员可查询GS_AUDITING_POLICY、ACCESS、PRIVILEGES、FILTERS四类审计目录。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "策略名称唯一；IF NOT EXISTS避免重复创建。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "DATABASE LINK请求属性取服务端发送方，外部开发指南尚未提供。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "策略名称唯一。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "名称遵守标识符规范，标称最大63字符。"
  -
    id: "contract_8"
    action: "verify_documented_contract"
    note: "超长名称截断保留前63字符。"
  -
    id: "contract_9"
    action: "verify_documented_contract"
    note: "未加双引号名称折叠小写；双引号保留大写。"
  -
    id: "contract_10"
    action: "verify_documented_contract"
    note: "名称字符限字母、下划线、数字和美元符号。"
  -
    id: "contract_11"
    action: "verify_documented_contract"
    note: "PRIVILEGES ANALYZE同时审计ANALYZE与VACUUM。"
  -
    id: "contract_12"
    action: "verify_documented_contract"
    note: "ALL只展开所在PRIVILEGES或ACCESS类别，不是两类并集。"
  -
    id: "contract_13"
    action: "verify_documented_contract"
    note: "ENABLE/DISABLE控制开关；省略默认ENABLE。"
  -
    id: "contract_14"
    action: "verify_documented_contract"
    note: "名称必须以字母或下划线开头。"
  -
    id: "verify_create_audit_policy_fact_policy_catalog"
    action: "verify_documented_contract"
    note: "管理员可查询GS_AUDITING_POLICY中的策略信息。"
  -
    id: "verify_create_audit_policy_fact_access_catalog"
    action: "verify_documented_contract"
    note: "管理员可查询GS_AUDITING_POLICY_ACCESS中的访问操作。"
  -
    id: "verify_create_audit_policy_fact_privileges_catalog"
    action: "verify_documented_contract"
    note: "管理员可查询GS_AUDITING_POLICY_PRIVILEGES中的权限操作。"
  -
    id: "verify_create_audit_policy_fact_filters_catalog"
    action: "verify_documented_contract"
    note: "管理员可查询GS_AUDITING_POLICY_FILTERS中的过滤信息。"
oracles:
  -
    kind: "documented_behavior"
    expected: "管理员可查询GS_AUDITING_POLICY、ACCESS、PRIVILEGES、FILTERS四类审计目录。"
  -
    kind: "documented_behavior"
    expected: "超长名称截断保留前63字符。"
  -
    kind: "documented_behavior"
    expected: "未加双引号名称折叠小写；双引号保留大写。"
  -
    kind: "documented_behavior"
    expected: "PRIVILEGES ANALYZE同时审计ANALYZE与VACUUM。"
  -
    kind: "documented_behavior"
    expected: "ALL只展开所在PRIVILEGES或ACCESS类别，不是两类并集。"
  -
    kind: "documented_behavior"
    expected: "ENABLE/DISABLE控制开关；省略默认ENABLE。"
execution_requirements:
  - "database"
  - "same_session"
