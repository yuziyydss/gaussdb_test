schema_version: 1
kind: "fixture"
id: "fixture_alter_extension_attached_member"
name: "ALTER EXTENSION attached_member"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_extension"
requires_fixture_refs:
  - "fixture_alter_extension_member"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "ALTER EXTENSION ext_b7 ADD TABLE ext_b7_member;"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
