schema_version: 1
kind: "fixture"
id: "fixture_alter_extension_member"
name: "ALTER EXTENSION member"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_extension"
requires_fixture_refs:
  - "fixture_create_extension_existing_extension"
provides:
  tables:
    -
      name: "ext_b7_member"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: true
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TABLE ext_b7_member (col_1 INT, col_2 INT);"
    - "CREATE SCHEMA ext_b7_target;"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
