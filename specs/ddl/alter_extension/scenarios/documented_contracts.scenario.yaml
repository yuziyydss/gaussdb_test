schema_version: 1
kind: "scenario"
id: "scenario_alter_extension_documented_contracts"
name: "ALTER EXTENSION documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_extension"
fixture_refs: []
fact_refs:
  - "alter_extension_fact_internal"
  - "alter_extension_fact_enabled"
  - "alter_extension_fact_purpose"
  - "alter_extension_fact_update"
  - "alter_extension_fact_relocatable"
  - "alter_extension_fact_add"
  - "alter_extension_fact_drop"
  - "alter_extension_fact_out_ignored"
  - "alter_extension_fact_argname_ignored"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "只允许修改内部高级包或扩展。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "需support_extended_features=true。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "修改已安装扩展属性。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "UPDATE需能从当前版本到目标版本的适用更新脚本。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "SET SCHEMA需扩展支持relocatable。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "ADD已存在对象成为扩展成员，后续随扩展取消。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "DROP成员只解除成员关系不删除对象；需扩展及ADD/DROP目标权限。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "定位函数仅需输入参数，OUT不参与一致性判断。"
  -
    id: "contract_8"
    action: "verify_documented_contract"
    note: "定位函数不关心参数名，只关心数据类型。"
oracles:
  -
    kind: "documented_behavior"
    expected: "定位函数仅需输入参数，OUT不参与一致性判断。"
  -
    kind: "documented_behavior"
    expected: "定位函数不关心参数名，只关心数据类型。"
execution_requirements:
  - "database"
  - "same_session"
