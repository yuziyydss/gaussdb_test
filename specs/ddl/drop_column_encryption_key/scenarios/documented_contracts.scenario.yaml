schema_version: 1
kind: "scenario"
id: "scenario_drop_column_encryption_key_documented_contracts"
name: "DROP COLUMN ENCRYPTION KEY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_column_encryption_key"
fixture_refs: []
fact_refs:
  - "drop_column_encryption_key_fact_purpose"
  - "drop_column_encryption_key_fact_privilege"
  - "drop_column_encryption_key_fact_exists"
  - "drop_column_encryption_key_fact_missing"
  - "drop_column_encryption_key_fact_name"
  - "drop_column_encryption_key_fact_cascade_guard"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除CEK对象。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "CEK所有者或获DROP权限者可删，管理员默认有权。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "IF EXISTS目标存在时删除。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "IF EXISTS缺失仅NOTICE。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "目标须已有CEK名。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "支持CASCADE/RESTRICT语法，但密态中级联删除加密列危险，实际上不能删除依赖CEK的加密列。"
oracles:
  -
    kind: "documented_behavior"
    expected: "IF EXISTS目标存在时删除。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS缺失仅NOTICE。"
execution_requirements:
  - "database"
  - "same_session"
