schema_version: 1
kind: "fixture"
id: "fixture_drop_sequence_qualified"
name: "DROP SEQUENCE qualified"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "drop_sequence"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE SCHEMA ds_batch03;"
    - "CREATE SEQUENCE ds_batch03.seq_qualified;"
  teardown_sqls:
    - "DROP SEQUENCE IF EXISTS ds_batch03.seq_qualified;"
    - "DROP SCHEMA ds_batch03;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
