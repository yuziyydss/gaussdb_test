schema_version: 1
kind: manifest
id: manifest_drop_sequence_large_required_negative
name: drop_sequence 单规则负向 large_required_negative
status: needs_review
description: 前置条件受控，只违反目标规则；准确错误身份没有实机证据。
factor_ref: drop_sequence
syntax_ref: syntax_drop_sequence
suite_type: negative
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  large:
  - drop_sequence_large_absent
  if_exists:
  - drop_sequence_if_exists_absent
  - drop_sequence_if_exists_present
  targets:
  - drop_sequence_targets_large_one
  behavior:
  - drop_sequence_behavior_default
  - drop_sequence_behavior_restrict
violates_rule_refs:
- drop_sequence_rule_large
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: large_sequence_modifier_required
  fact_refs:
  - drop_sequence_fact_large_error
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
