schema_version: 1
kind: "manifest"
id: "manifest_drop_sequence_missing_large"
name: "DROP SEQUENCE missing_large"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_sequence"
syntax_ref: "syntax_drop_sequence"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  large:
    - "drop_sequence_large_present"
  if_exists:
    - "drop_sequence_if_exists_present"
  targets:
    - "drop_sequence_targets_large_missing"
  behavior:
    - "drop_sequence_behavior_default"
    - "drop_sequence_behavior_restrict"
    - "drop_sequence_behavior_cascade"
expected:
  default: "success"
  scope: "syntax_and_semantics"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
