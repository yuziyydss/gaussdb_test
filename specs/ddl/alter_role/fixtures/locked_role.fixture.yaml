schema_version: 1
kind: "fixture"
id: "fixture_alter_role_locked_role"
name: "ALTER ROLE locked_role"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_role"
requires_fixture_refs:
  - "fixture_alter_role_isolated_roles"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "ALTER ROLE b9_role_a ACCOUNT LOCK;"
  teardown_sqls:
    - "DROP ROLE IF EXISTS b9_role_a;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
