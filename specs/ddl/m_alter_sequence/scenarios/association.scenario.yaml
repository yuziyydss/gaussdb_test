schema_version: 1
kind: scenario
id: scenario_m_alter_sequence_association
name: scenario_m_alter_sequence_association
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_sequence
fact_refs:
- m_alter_sequence_fact_owned
- m_create_sequence::m_create_sequence_fact_owned
fixture_refs:
- fixture_m_alter_sequence_owned_source
preconditions:
- M-Compatibility；独立隔离测试空间
- 同一创建者在独占用户Schema中创建两个全新对象；未提前调用nextval，START为5；禁止并发使用该序列。
steps:
- sql: ALTER SEQUENCE m_b03_existing_seq OWNED BY NONE;
- sql: DROP TABLE m_b03_sequence_owner;
- id: after_owner_drop
  sql: SELECT nextval('m_b03_existing_seq');
oracles:
- kind: result_set
  step_id: after_owner_drop
  expected:
  - - 5
execution_requirements:
- database_authorization
- target_oracle_calibration
