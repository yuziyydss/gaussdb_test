schema_version: 1
kind: scenario
id: scenario_m_alter_sequence_cache_invalidation
name: scenario_m_alter_sequence_cache_invalidation
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_sequence
fact_refs:
- m_alter_sequence_fact_cache_clear
fixture_refs:
- fixture_m_alter_sequence_shared_sequence
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 两个独立会话预取序列 cache，再 ALTER MAXVALUE；检查其他会话重新取号
oracles:
- kind: manual_assertion
  expected: 跨会话缓存被清空，序列行为不是单次 DDL 成功
execution_requirements:
- database_authorization
- target_oracle_calibration
