schema_version: 1
kind: manifest
id: manifest_m_alter_sequence_detach_owned
name: manifest_m_alter_sequence_detach_owned
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_sequence
syntax_ref: syntax_m_alter_sequence
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_alter_sequence_owned_source
identifier_policy: {}
bindings:
  change:
  - m_alter_sequence_change_unchanged
  owned:
  - m_alter_sequence_owned_none
  if_exists:
  - m_alter_sequence_if_exists_none
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_alter_sequence_fact_mode
- key: namespace
  allowed_values:
  - isolated_user_schema
  fact_refs:
  - m_alter_sequence_fact_association_scope
- key: object_authority
  allowed_values:
  - case_object_owner
  fact_refs:
  - m_alter_sequence_fact_authority
- key: sequence_creation_authority
  allowed_values:
  - create_any_sequence
  fact_refs:
  - m_create_sequence::m_create_sequence_fact_authority
- key: table_creation_authority
  allowed_values:
  - create_any_table
  fact_refs:
  - m_create_table::m_create_table_fact_authority
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
local_rules:
- expression: change == 'm_alter_sequence_change_unchanged' and owned == 'm_alter_sequence_owned_none' and if_exists
    == 'm_alter_sequence_if_exists_none'
  rationale: 本有限清单仅解除既有关联，不混入MAXVALUE/CACHE或缺失对象分支；不是产品全域禁止其他组合。
  fact_refs:
  - m_alter_sequence_fact_syntax
  - m_alter_sequence_fact_owned
