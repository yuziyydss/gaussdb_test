schema_version: 1
kind: "scenario"
id: "scenario_drop_user_mapping_documented_contracts"
name: "DROP USER MAPPING documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_user_mapping"
fixture_refs: []
fact_refs:
  - "drop_user_mapping_fact_purpose"
  - "drop_user_mapping_fact_existing"
  - "drop_user_mapping_fact_missing"
  - "drop_user_mapping_fact_create_if_exists"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除用户到外部服务器的映射。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "存在时删除成功。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "IF EXISTS且不存在时只产生NOTICE。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "IF EXISTS只用于DROP USER MAPPING，CREATE USER MAPPING使用它会发生语法错误。"
oracles:
  -
    kind: "documented_behavior"
    expected: "存在时删除成功。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS且不存在时只产生NOTICE。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS只用于DROP USER MAPPING，CREATE USER MAPPING使用它会发生语法错误。"
execution_requirements:
  - "database"
  - "same_session"
