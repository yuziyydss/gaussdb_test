schema_version: 1
kind: "manifest"
id: "manifest_drop_user_mapping_existing"
name: "DROP USER MAPPING existing"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_user_mapping"
syntax_ref: "syntax_drop_user_mapping"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_user_mapping_mappings"
identifier_policy: {}
bindings:
  mapped_user:
    - "drop_user_mapping_mapped_user_user"
    - "drop_user_mapping_mapped_user_current"
    - "drop_user_mapping_mapped_user_public"
  if_exists:
    - "drop_user_mapping_if_exists_no"
    - "drop_user_mapping_if_exists_yes"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "mapping_target_server_owned"
    allowed_values:
      - "true"
    fact_refs:
      - "create_user_mapping::create_user_mapping_fact_server_owner"
  -
    key: "documented_fdw_available"
    allowed_values:
      - "true"
    fact_refs:
      - "create_server::create_server_fact_fdw_environment"
