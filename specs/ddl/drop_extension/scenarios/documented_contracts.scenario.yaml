schema_version: 1
kind: "scenario"
id: "scenario_drop_extension_documented_contracts"
name: "DROP EXTENSION documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_extension"
fixture_refs: []
fact_refs:
  - "drop_extension_fact_internal"
  - "drop_extension_fact_purpose"
  - "drop_extension_fact_privilege"
  - "drop_extension_fact_members"
  - "drop_extension_fact_existing"
  - "drop_extension_fact_missing"
  - "drop_extension_fact_cascade"
  - "drop_extension_fact_restrict"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "仅允许删除内部高级包或扩展。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "从数据库中删除扩展。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "仅扩展拥有者或初始用户可执行删除。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "删除扩展会级联删除其组件。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "存在时删除成功。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "IF EXISTS且不存在只产生NOTICE。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "CASCADE级联删除扩展依赖对象。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "RESTRICT遇依赖拒绝，文档列出成员及其他扩展对象同条删除的例外。"
oracles:
  -
    kind: "documented_behavior"
    expected: "存在时删除成功。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS且不存在只产生NOTICE。"
  -
    kind: "documented_behavior"
    expected: "RESTRICT遇依赖拒绝，文档列出成员及其他扩展对象同条删除的例外。"
execution_requirements:
  - "database"
  - "same_session"
