schema_version: 1
kind: "fixture"
id: "fixture_drop_extension_two_extensions"
name: "DROP EXTENSION two_extensions"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "drop_extension"
requires_fixture_refs:
  - "fixture_create_extension_existing_extension"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE EXTENSION ext_b7_second SCHEMA ext_b7_schema;"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
