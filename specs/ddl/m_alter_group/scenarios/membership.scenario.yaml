schema_version: 1
kind: scenario
id: scenario_m_alter_group_membership
name: scenario_m_alter_group_membership
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_group
fact_refs:
- m_alter_group_fact_membership
fixture_refs:
- fixture_m_alter_group_add
- fixture_m_alter_group_drop
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 分别确认 ADD 前未入组、DROP 前已入组；执行后检查成员关系与成员角色仍存在。
oracles:
- kind: manual_assertion
  expected: 只改变组成员关系，不删除成员；尚未执行
execution_requirements:
- database_authorization
- target_oracle_calibration
