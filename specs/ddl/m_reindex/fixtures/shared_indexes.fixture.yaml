schema_version: 1
kind: fixture
id: fixture_m_reindex_shared_indexes
name: fixture_m_reindex_shared_indexes
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_reindex
requires_fixture_refs:
- fixture_m_create_index_existing
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: auto
  note: 依赖真实建表/建索引；每 case 独占命名空间，禁止共享会话并行复用。
