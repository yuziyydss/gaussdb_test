schema_version: 1
kind: scenario
id: scenario_m_reindex_physical
name: scenario_m_reindex_physical
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_reindex
fact_refs:
- m_reindex_fact_force
- m_reindex_fact_invalid
- m_reindex_fact_failure
fixture_refs:
- fixture_m_reindex_shared_indexes
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 检查有效索引、真实重建结果与在线失败的确切残留；不把 FORCE 当作强制修复或扫描系统目录重建。
oracles:
- kind: manual_assertion
  expected: 物理重建/失败恢复保留待验证，不默认运行全库或系统重建
execution_requirements:
- database_authorization
- target_oracle_calibration
