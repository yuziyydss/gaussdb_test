schema_version: 1
kind: "scenario"
id: "scenario_alter_function_strict_behavior"
name: "ALTER FUNCTION strict_behavior"
status: "planned"
description: "ALTER STRICT 后空值调用跳过函数体。"
factor_ref: "alter_function"
fixture_refs:
  - "fixture_alter_function_base"
fact_refs:
  - "alter_function_fact_strict"
preconditions: []
steps:
  -
    id: "alter"
    action: "execute_sql"
    sql: "ALTER FUNCTION fp_cf_callable(INTEGER, INTEGER) STRICT;"
  -
    id: "probe"
    action: "execute_sql"
    sql: "SELECT fp_cf_callable(NULL, 1);"
oracles:
  -
    after_step: "probe"
    kind: "result_rows"
    expected:
      -
        - null
execution_requirements:
  - "database"
  - "same_session"
