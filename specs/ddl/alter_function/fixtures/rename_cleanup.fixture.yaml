schema_version: 1
kind: "fixture"
id: "fixture_alter_function_rename_cleanup"
name: "ALTER FUNCTION rename_cleanup"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_function"
requires_fixture_refs:
  - "fixture_alter_function_base"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "SELECT 1;"
  teardown_sqls:
    - "DROP FUNCTION IF EXISTS fp_af_renamed(INTEGER, INTEGER) CASCADE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
