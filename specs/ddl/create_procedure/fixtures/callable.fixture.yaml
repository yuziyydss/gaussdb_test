schema_version: 1
kind: "fixture"
id: "fixture_create_procedure_callable"
name: "CREATE PROCEDURE callable"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_procedure"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE PROCEDURE fp_proc_ready(a INTEGER) IS v INTEGER; BEGIN v := a; END;"
  teardown_sqls:
    - "DROP PROCEDURE IF EXISTS fp_proc_ready;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
