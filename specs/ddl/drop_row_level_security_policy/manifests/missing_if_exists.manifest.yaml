schema_version: 1
kind: manifest
id: manifest_drop_row_level_security_policy_missing_if_exists
name: DROP ROW LEVEL SECURITY POLICY missing_if_exists
status: needs_review
description: 一个确认缺失策略的IF EXISTS静态代表；不验证NOTICE文本或策略删除行为。
factor_ref: drop_row_level_security_policy
syntax_ref: syntax_drop_row_level_security_policy
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  long_form:
    - drop_row_level_security_policy_long_form_short
  if_exists:
    - drop_row_level_security_policy_if_exists_yes
  policy_name:
    - drop_row_level_security_policy_policy_name_missing
  behavior:
    - drop_row_level_security_policy_behavior_none
environment_requirements:
  - key: table_owner_or_admin
    allowed_values:
      - "true"
    fact_refs:
      - drop_row_level_security_policy_fact_privilege
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
