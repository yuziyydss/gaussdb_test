schema_version: 1
kind: "scenario"
id: "scenario_drop_row_level_security_policy_documented_contracts"
name: "DROP ROW LEVEL SECURITY POLICY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_row_level_security_policy"
fixture_refs: []
fact_refs:
  - "drop_row_level_security_policy_fact_purpose"
  - "drop_row_level_security_policy_fact_privilege"
  - "drop_row_level_security_policy_fact_exists"
  - "drop_row_level_security_policy_fact_missing"
  - "drop_row_level_security_policy_fact_missing_oracle"
  - "drop_row_level_security_policy_fact_no_dependents"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除表上的RLS策略。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅表所有者或管理员可删除。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "IF EXISTS目标存在时删除。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "IF EXISTS目标不存在仅NOTICE，不删除。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "CASCADE和RESTRICT仅语法适配，无对象依赖RLS策略，效果相同。"
oracles:
  -
    kind: "documented_behavior"
    expected: "IF EXISTS目标存在时删除。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS目标不存在仅NOTICE，不删除。"
  -
    kind: "documented_behavior"
    expected: "CASCADE和RESTRICT仅语法适配，无对象依赖RLS策略，效果相同。"
execution_requirements:
  - "database"
  - "same_session"
