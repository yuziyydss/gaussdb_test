schema_version: 1
kind: scenario
id: scenario_m_drop_audit_policy_missing_notice
name: scenario_m_drop_audit_policy_missing_notice
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_audit_policy
fact_refs:
- m_drop_audit_policy_fact_notice
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: DROP AUDIT POLICY IF EXISTS m_drop_audit_policy_absent;
oracles:
- kind: manual_assertion
  expected: 缺失策略NOTICE及不带IF EXISTS的目标错误需校准
execution_requirements:
- database_authorization
- target_oracle_calibration
