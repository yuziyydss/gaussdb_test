schema_version: 1
kind: "manifest"
id: "manifest_drop_synonym_cascade"
name: "DROP SYNONYM cascade"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_synonym"
syntax_ref: "syntax_drop_synonym"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  target:
    - "drop_synonym_target_dependent"
  if_exists:
    - "drop_synonym_if_exists_no"
    - "drop_synonym_if_exists_yes"
  behavior:
    - "drop_synonym_behavior_cascade"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
