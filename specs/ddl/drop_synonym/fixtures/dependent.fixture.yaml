schema_version: 1
kind: "fixture"
id: "fixture_drop_synonym_dependent"
name: "DROP SYNONYM dependent"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "drop_synonym"
requires_fixture_refs:
  - "fixture_drop_synonym_base"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE SYNONYM s_drop_dep FOR t_syn_source;"
    - "CREATE VIEW v_syn_dep AS SELECT col_1, col_2 FROM s_drop_dep;"
  teardown_sqls:
    - "DROP VIEW IF EXISTS v_syn_dep;"
    - "DROP SYNONYM IF EXISTS s_drop_dep;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
