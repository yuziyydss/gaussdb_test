schema_version: 1
kind: "scenario"
id: "scenario_alter_view_notice"
name: "ALTER VIEW notice"
status: "planned"
description: "缺失视图 IF EXISTS 捕获 NOTICE；无 IF EXISTS 校准目标错误身份，不能以 fixture 错误代替。"
factor_ref: "alter_view"
fixture_refs: []
fact_refs:
  - "alter_view_fact_if_notice"
  - "alter_view_fact_error_identity"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "缺失视图 IF EXISTS 捕获 NOTICE；无 IF EXISTS 校准目标错误身份，不能以 fixture 错误代替。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
