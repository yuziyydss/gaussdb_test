schema_version: 1
kind: "manifest"
id: "manifest_alter_view_owner"
name: "ALTER VIEW owner"
status: "needs_review"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_view"
syntax_ref: "syntax_alter_view"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_view_view"
identifier_policy: {}
bindings:
  action:
    - "alter_view_action_owner"
  if_exists:
    - "alter_view_if_exists_absent"
    - "alter_view_if_exists_present"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "prepared_owner_role"
    allowed_values:
      - "fp_av_owner"
    fact_refs:
      - "alter_view_fact_owner_permission"
  -
    key: "owner_membership_and_schema_create"
    allowed_values:
      - "true"
    fact_refs:
      - "alter_view_fact_owner_permission"
