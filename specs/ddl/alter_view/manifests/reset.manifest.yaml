schema_version: 1
kind: "manifest"
id: "manifest_alter_view_reset"
name: "ALTER VIEW reset"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_view"
syntax_ref: "syntax_alter_view"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_view_view"
identifier_policy: {}
bindings:
  action:
    - "alter_view_action_reset"
  if_exists:
    - "alter_view_if_exists_absent"
    - "alter_view_if_exists_present"
  reset_options:
    - "alter_view_reset_options_barrier"
    - "alter_view_reset_options_check"
    - "alter_view_reset_options_both"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
