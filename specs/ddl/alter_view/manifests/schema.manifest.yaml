schema_version: 1
kind: "manifest"
id: "manifest_alter_view_schema"
name: "ALTER VIEW schema"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_view"
syntax_ref: "syntax_alter_view"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_view_move"
identifier_policy: {}
bindings:
  action:
    - "alter_view_action_schema"
  if_exists:
    - "alter_view_if_exists_absent"
    - "alter_view_if_exists_present"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "target_schema_create_permission"
    allowed_values:
      - "true"
    fact_refs:
      - "alter_view_fact_schema_permission"
