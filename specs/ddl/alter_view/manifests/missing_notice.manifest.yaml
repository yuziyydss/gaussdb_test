schema_version: 1
kind: "manifest"
id: "manifest_alter_view_missing_notice"
name: "ALTER VIEW missing_notice"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_view"
syntax_ref: "syntax_alter_view"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_view_view"
identifier_policy: {}
bindings:
  action:
    - "alter_view_action_rename"
  view_name:
    - "alter_view_view_name_missing"
  if_exists:
    - "alter_view_if_exists_present"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
