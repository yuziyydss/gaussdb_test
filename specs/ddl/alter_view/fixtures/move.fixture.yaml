schema_version: 1
kind: "fixture"
id: "fixture_alter_view_move"
name: "ALTER VIEW move"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_view"
requires_fixture_refs:
  - "fixture_alter_view_view"
  - "fixture_create_schema_pair"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "DROP VIEW IF EXISTS fp_cs_one.v_av_base;"
  teardown_sqls:
    - "DROP VIEW IF EXISTS fp_cs_one.v_av_base;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
