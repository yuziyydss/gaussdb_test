schema_version: 1
kind: "fixture"
id: "fixture_alter_view_qualified"
name: "ALTER VIEW qualified"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_view"
requires_fixture_refs:
  - "fixture_create_view_source_two_ints"
  - "fixture_create_schema_pair"
provides:
  tables:
    -
      name: "fp_cs_one.v_av_qualified"
      persistence: "permanent"
      table_kind: "view"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: true
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE VIEW fp_cs_one.v_av_qualified AS SELECT col_1, col_2 FROM t_view_source;"
  teardown_sqls:
    - "DROP VIEW IF EXISTS fp_cs_one.v_av_qualified;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
