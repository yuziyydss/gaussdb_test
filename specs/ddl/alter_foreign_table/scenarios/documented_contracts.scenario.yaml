schema_version: 1
kind: "scenario"
id: "scenario_alter_foreign_table_documented_contracts"
name: "ALTER FOREIGN TABLE documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_foreign_table"
fixture_refs: []
fact_refs:
  - "alter_foreign_table_fact_purpose"
  - "alter_foreign_table_fact_sensitive_quotes"
  - "alter_foreign_table_fact_pdb_admin"
  - "alter_foreign_table_fact_table"
  - "alter_foreign_table_fact_option_operations"
  - "alter_foreign_table_fact_filename"
  - "alter_foreign_table_fact_force_not_null"
  - "alter_foreign_table_fact_external_guide"
  - "alter_foreign_table_fact_column"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "修改外表属性。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "OPTIONS敏感字段多层引号可能失去脱敏识别，禁止实际秘密落盘。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "PDB内仅初始用户可ALTER。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "目标须已有外表。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "OPTIONS用ADD/SET/DROP，省略默认ADD；名称域唯一，表列域可同名，由FDW验证名称和值。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "file_fdw filename必选且为服务端绝对路径。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "列force_not_null为true不匹配文件级NULL空串，引用COPY FORCE_NOT_NULL。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "完整file_fdw用法依赖未提供开发指南。"
  -
    id: "contract_8"
    action: "verify_documented_contract"
    note: "column_name须已有字段并符合标识符规范。"
oracles:
  -
    kind: "documented_behavior"
    expected: "列force_not_null为true不匹配文件级NULL空串，引用COPY FORCE_NOT_NULL。"
execution_requirements:
  - "database"
  - "same_session"
