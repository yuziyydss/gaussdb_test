schema_version: 1
kind: scenario
id: scenario_alter_foreign_table_log_implicit
name: log_fdw implicit独立目录Oracle待校准
status: planned
description: 绑定单个真实implicit候选；独立初始化fixture，不继承其他场景目标状态。
factor_ref: alter_foreign_table
fixture_refs: [fixture_alter_foreign_table_log_absent]
fact_refs: [alter_foreign_table_fact_option_operations, alter_foreign_table_fact_example_lines_77, 'create_server::create_server_fact_compatibility_only']
preconditions: [明确物理模式与版本, 非PDB且log_fdw目录能力核实, 独占server及schema名不存在, 前置逐对象成功回执, 当前连接ALTER权限需单独核实且权限失败不能当目标结果]
steps:
- id: implicit_option
  expected: success
  candidate: {manifest_ref: manifest_alter_foreign_table_log_implicit, params: {operation: aft_log_implicit}}
oracles:
- {kind: manual_assertion, step_id: implicit_option, expected: 校准当前版本目录查询并核对本次外表latest_files为2；前置必须无latest_files，不能以日志读取成功或任意错误替代目录选项断言。}
execution_requirements: [database_authorization, physical_mode_verification, target_oracle_calibration, ownership_scoped_cleanup]
