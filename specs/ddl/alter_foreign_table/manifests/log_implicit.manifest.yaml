schema_version: 1
kind: manifest
id: manifest_alter_foreign_table_log_implicit
name: ALTER FOREIGN TABLE log_fdw implicit有限目录候选
status: needs_review
description: 只生成文档latest_files代表操作，保留真实初始选项状态；不验证日志读取、任意FDW、文件参数或列级语法。
factor_ref: alter_foreign_table
syntax_ref: syntax_alter_foreign_table
suite_type: positive
strategy: equivalence
identifier_policy: {}
fixture_refs: [fixture_alter_foreign_table_log_absent]
bindings:
  table_name: [aft_log_table]
  operation: [aft_log_implicit]
  option_value: [aft_log_two]
environment_requirements:
- {key: documented_fdw_available, allowed_values: [log_fdw_catalog], fact_refs: ['create_server::create_server_fact_fdw_environment']}
- {key: database_scope, allowed_values: [non_pdb], fact_refs: [alter_foreign_table_fact_pdb_admin]}
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
