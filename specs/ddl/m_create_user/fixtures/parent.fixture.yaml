schema_version: 1
kind: fixture
id: fixture_m_create_user_parent
name: fixture_m_create_user_parent
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_user
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE ROLE m_create_user_parent NOLOGIN NOSYSADMIN PASSWORD DISABLE;
  teardown_sqls:
  - DROP SCHEMA IF EXISTS m_create_user_new;
  - DROP USER IF EXISTS m_create_user_new RESTRICT;
  - DROP ROLE m_create_user_parent;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
