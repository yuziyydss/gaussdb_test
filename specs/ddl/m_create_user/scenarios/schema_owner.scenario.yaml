schema_version: 1
kind: scenario
id: scenario_m_create_user_schema_owner
name: scenario_m_create_user_schema_owner
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_user
fact_refs:
- m_create_user_fact_ownership
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 在独立普通用户同名 Schema 创建专用表并检查所有权，需另配定向表清理。
oracles:
- kind: manual_assertion
  expected: 系统管理员所建对象属于普通用户；尚未执行
execution_requirements:
- database_authorization
- target_oracle_calibration
