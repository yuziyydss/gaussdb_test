schema_version: 1
kind: scenario
id: scenario_m_create_user_implicit_schema
name: scenario_m_create_user_implicit_schema
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_user
fact_refs:
- m_create_user_fact_schema
- m_create_user_fact_login
fixture_refs:
- fixture_m_create_user_parent
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 创建目标后检查同名 Schema 与角色 NOLOGIN；不把 CREATE ROLE 的无 Schema 行为套用到 USER。
oracles:
- kind: manual_assertion
  expected: Schema 自动创建且归属可检查；NOLOGIN 是本 case 显式选择，不改变 LOGIN 缺省事实
execution_requirements:
- database_authorization
- target_oracle_calibration
