schema_version: 1
kind: "scenario"
id: "scenario_alter_audit_policy_documented_contracts"
name: "ALTER AUDIT POLICY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_audit_policy"
fixture_refs: []
fact_refs:
  - "alter_audit_policy_fact_purpose"
  - "alter_audit_policy_fact_privilege"
  - "alter_audit_policy_fact_switch"
  - "alter_audit_policy_fact_unique"
  - "alter_audit_policy_fact_identifier"
  - "alter_audit_policy_fact_all_scope"
  - "alter_audit_policy_fact_enabled_behavior"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "修改统一审计策略。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "维护需要POLADMIN、SYSADMIN或初始用户。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "enable_security_policy开启才使统一审计策略生效。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "已有策略名称唯一。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "策略名遵守标识符规范。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "ALL只指所属审计类别的全部操作。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "ENABLE/DISABLE打开或关闭策略。"
oracles:
  -
    kind: "documented_behavior"
    expected: "ALL只指所属审计类别的全部操作。"
  -
    kind: "documented_behavior"
    expected: "ENABLE/DISABLE打开或关闭策略。"
execution_requirements:
  - "database"
  - "same_session"
