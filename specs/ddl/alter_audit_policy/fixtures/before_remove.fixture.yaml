schema_version: 1
kind: "fixture"
id: "fixture_alter_audit_policy_before_remove"
name: "ALTER AUDIT POLICY before_remove"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_audit_policy"
requires_fixture_refs:
  - "fixture_alter_audit_policy_existing_policy"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "ALTER AUDIT POLICY b10_audit_existing ADD PRIVILEGES (DROP);"
  teardown_sqls:
    - "DROP AUDIT POLICY IF EXISTS b10_audit_existing;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
