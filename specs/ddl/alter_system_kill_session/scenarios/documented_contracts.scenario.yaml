schema_version: 1
kind: "scenario"
id: "scenario_alter_system_kill_session_documented_contracts"
name: "ALTER SYSTEM KILL SESSION documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_system_kill_session"
fixture_refs: []
fact_refs:
  - "alter_system_kill_session_fact_purpose"
  - "alter_system_kill_session_fact_session_identity"
  - "alter_system_kill_session_fact_immediate"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "            ALTER SYSTEM KILL SESSION命令用于结束一个会话。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "SID和SERIAL需从DV_SESSIONS、PG_STAT_ACTIVITY读取；目标线程可能已结束，必须验证会话身份和时效。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "IMMEDIATE用于立即终止指定会话。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
