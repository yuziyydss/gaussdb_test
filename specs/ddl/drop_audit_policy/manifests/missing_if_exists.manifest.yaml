schema_version: 1
kind: manifest
id: manifest_drop_audit_policy_missing_if_exists
name: DROP AUDIT POLICY missing_if_exists
status: needs_review
description: 一个确认缺失策略的IF EXISTS静态代表；不验证NOTICE文本或无IF EXISTS错误SQLSTATE。
factor_ref: drop_audit_policy
syntax_ref: syntax_drop_audit_policy
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  if_exists:
    - drop_audit_policy_if_exists_yes
  policy_name:
    - drop_audit_policy_policy_name_missing
environment_requirements:
  - key: security_policy_admin
    allowed_values:
      - "true"
    fact_refs:
      - drop_audit_policy_fact_privilege
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
