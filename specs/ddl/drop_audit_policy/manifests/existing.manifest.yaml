schema_version: 1
kind: "manifest"
id: "manifest_drop_audit_policy_existing"
name: "DROP AUDIT POLICY existing"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_audit_policy"
syntax_ref: "syntax_drop_audit_policy"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_audit_policy_existing_policy"
identifier_policy: {}
bindings:
  if_exists:
    - "drop_audit_policy_if_exists_none"
    - "drop_audit_policy_if_exists_yes"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "security_policy_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "drop_audit_policy_fact_privilege"
