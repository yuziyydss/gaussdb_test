schema_version: 1
kind: "scenario"
id: "scenario_drop_audit_policy_documented_contracts"
name: "DROP AUDIT POLICY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_audit_policy"
fixture_refs: []
fact_refs:
  - "drop_audit_policy_fact_purpose"
  - "drop_audit_policy_fact_privilege"
  - "drop_audit_policy_fact_name"
  - "drop_audit_policy_fact_identifier"
  - "drop_audit_policy_fact_exists"
  - "drop_audit_policy_fact_missing_notice"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除一个审计策略。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅POLADMIN、SYSADMIN或初始用户可删除。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "策略名称唯一。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "名称遵守标识符规范。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "IF EXISTS在目标存在时删除成功。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "IF EXISTS目标不存在时仅NOTICE，无删除。"
oracles:
  -
    kind: "documented_behavior"
    expected: "IF EXISTS在目标存在时删除成功。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS目标不存在时仅NOTICE，无删除。"
execution_requirements:
  - "database"
  - "same_session"
