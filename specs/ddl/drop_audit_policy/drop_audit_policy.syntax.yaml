schema_version: 1
kind: "syntax"
id: "syntax_drop_audit_policy"
name: "DROP AUDIT POLICY 语法"
factor_ref: "drop_audit_policy"
category: "DDL"
status: "ready"
description: "语法结构来自 PDF，选值仅由 factor/manifest 提供。"
production: "DROP AUDIT POLICY {if_exists} b10_audit_existing"
rendering:
  whitespace: "collapse"
  statement_terminator: ";"
slots:
  if_exists:
    type: "token"
    optional: true
    dimension_ref: "drop_audit_policy.if_exists"
source_fact_refs:
  - "drop_audit_policy_fact_syntax"
