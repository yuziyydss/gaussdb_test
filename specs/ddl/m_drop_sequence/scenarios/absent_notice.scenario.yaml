schema_version: 1
kind: scenario
id: scenario_m_drop_sequence_absent_notice
name: scenario_m_drop_sequence_absent_notice
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_sequence
fact_refs:
- m_drop_sequence_fact_notice
fixture_refs:
- fixture_m_drop_sequence_shared_sequence
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: DROP SEQUENCE IF EXISTS m_drop_sequence_absent;
oracles:
- kind: manual_assertion
  expected: 成功并产生 notice；不是任意错误通过
execution_requirements:
- database_authorization
- target_oracle_calibration
