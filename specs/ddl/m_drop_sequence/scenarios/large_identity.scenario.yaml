schema_version: 1
kind: scenario
id: scenario_m_drop_sequence_large_identity
name: scenario_m_drop_sequence_large_identity
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_sequence
fact_refs:
- m_drop_sequence_fact_large
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 从 M AUTO_INCREMENT 原文建立隐式 LARGE 序列的发现与定向删除契约
oracles:
- kind: manual_assertion
  expected: 仅删除本 case 创建的隐式 LARGE 序列，未实现该分支
execution_requirements:
- database_authorization
- target_oracle_calibration
