schema_version: 1
kind: scenario
id: scenario_m_drop_sequence_dependent_default
name: scenario_m_drop_sequence_dependent_default
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_sequence
fact_refs:
- m_drop_sequence_fact_restrict
fixture_refs:
- fixture_m_drop_sequence_shared_sequence
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 创建 DEFAULT nextval 依赖列，分别验证 RESTRICT 拒绝与 CASCADE 依赖处置
oracles:
- kind: target_error
  expected: 目标依赖错误待校准；不在正向空依赖 fixture 中宣称覆盖
execution_requirements:
- database_authorization
- target_oracle_calibration
