schema_version: 1
kind: fixture
id: fixture_m_drop_sequence_shared_sequence
name: fixture_m_drop_sequence_shared_sequence
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_sequence
requires_fixture_refs:
- fixture_m_create_sequence_existing
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: auto
  note: 展开 CREATE SEQUENCE 包的真实序列/表前置状态，不复制 DDL。
