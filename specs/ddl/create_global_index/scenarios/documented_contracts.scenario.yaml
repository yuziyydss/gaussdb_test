schema_version: 1
kind: "scenario"
id: "scenario_create_global_index_documented_contracts"
name: "CREATE GLOBAL INDEX documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "create_global_index"
fixture_refs: []
fact_refs:
  - "create_global_index_fact_purpose"
  - "create_global_index_fact_gsi_description"
  - "create_global_index_fact_unsupported"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "GSI为全局二级索引。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "文档概念描述GSI可与基表不同分布以支持非分布列单节点查询和唯一约束。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "本PDF当前集中式形态不支持CREATE GLOBAL INDEX功能。"
oracles:
  -
    kind: "documented_behavior"
    expected: "文档概念描述GSI可与基表不同分布以支持非分布列单节点查询和唯一约束。"
execution_requirements:
  - "database"
  - "same_session"
