schema_version: 1
kind: manifest
id: manifest_m_create_group_connection_below
name: manifest_m_create_group_connection_below
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_group
syntax_ref: syntax_m_create_group
suite_type: negative
strategy: pairwise
fixture_refs:
- fixture_m_create_group_parent
identifier_policy: {}
bindings:
  connection_limit:
  - m_create_group_connection_limit_below
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_create_group_fact_mode
- key: actor_authority
  allowed_values:
  - sysadmin
  fact_refs:
  - m_create_role::m_create_role_fact_disable_authority
- key: separation_of_duties
  allowed_values:
  - 'off'
  fact_refs:
  - m_create_role::m_create_role_fact_creation_authority
violates_rule_refs:
- m_create_group_rule_connection_range
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: connection_range
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
