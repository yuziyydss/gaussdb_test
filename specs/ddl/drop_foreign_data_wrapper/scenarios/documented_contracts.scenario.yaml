schema_version: 1
kind: "scenario"
id: "scenario_drop_foreign_data_wrapper_documented_contracts"
name: "DROP FOREIGN DATA WRAPPER documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_foreign_data_wrapper"
fixture_refs: []
fact_refs:
  - "drop_foreign_data_wrapper_fact_purpose"
  - "drop_foreign_data_wrapper_fact_enabled"
  - "drop_foreign_data_wrapper_fact_existing"
  - "drop_foreign_data_wrapper_fact_missing"
  - "drop_foreign_data_wrapper_fact_cascade"
  - "drop_foreign_data_wrapper_fact_restrict"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除一个外部数据封装器。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "support_extended_features=on才可删除。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "指定封装器存在时删除成功。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "带IF EXISTS且目标不存在只产生NOTICE。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "CASCADE级联删除依赖封装器的服务器等对象。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "RESTRICT遇到依赖对象拒绝删除。"
oracles:
  -
    kind: "documented_behavior"
    expected: "指定封装器存在时删除成功。"
  -
    kind: "documented_behavior"
    expected: "带IF EXISTS且目标不存在只产生NOTICE。"
  -
    kind: "documented_behavior"
    expected: "RESTRICT遇到依赖对象拒绝删除。"
execution_requirements:
  - "database"
  - "same_session"
