schema_version: 1
kind: "manifest"
id: "manifest_alter_server_version"
name: "ALTER SERVER version"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_server"
syntax_ref: "syntax_alter_server"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_server_server"
identifier_policy: {}
bindings:
  form:
    - "alter_server_form_version"
  version:
    - "alter_server_version_one"
    - "alter_server_version_two"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "server_alter_privilege"
    allowed_values:
      - "true"
    fact_refs:
      - "alter_server_fact_privilege"
  -
    key: "documented_fdw_available"
    allowed_values:
      - "true"
    fact_refs:
      - "create_server::create_server_fact_fdw_environment"
