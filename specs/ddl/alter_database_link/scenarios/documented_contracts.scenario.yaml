schema_version: 1
kind: "scenario"
id: "scenario_alter_database_link_documented_contracts"
name: "ALTER DATABASE LINK documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_database_link"
fixture_refs: []
fact_refs:
  - "alter_database_link_fact_purpose"
  - "alter_database_link_fact_external_guide"
  - "alter_database_link_fact_gauss_only_credentials"
  - "alter_database_link_fact_password"
  - "alter_database_link_fact_visibility"
  - "alter_database_link_fact_oracle_options"
  - "alter_database_link_fact_isolation"
  - "alter_database_link_fact_max_long"
  - "alter_database_link_fact_strip_zeros"
  - "alter_database_link_fact_prefetch"
  - "alter_database_link_fact_lob_prefetch"
  - "alter_database_link_fact_timeout"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "修改DBLink对象。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "完整合同依赖开发指南。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "GaussDB后端仅可修改用户名和密码。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "修改远端连接密码需秘密运行时绑定。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "PUBLIC公共，省略默认PRIVATE。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "USING选项修改限Oracle后端。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "隔离serializable/read_committed/read_only，默认serializable。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "max_long=1..1073741823，预置不生效。"
  -
    id: "contract_8"
    action: "verify_documented_contract"
    note: "yes/on/true删除ASCII零。"
  -
    id: "contract_9"
    action: "verify_documented_contract"
    note: "prefetch=1..1000，默认50。"
  -
    id: "contract_10"
    action: "verify_documented_contract"
    note: "lob_prefetch=0..536870912，默认1048576，预置不生效。"
  -
    id: "contract_11"
    action: "verify_documented_contract"
    note: "time_out=0..2147483647，默认0不超时。"
oracles:
  -
    kind: "documented_behavior"
    expected: "PUBLIC公共，省略默认PRIVATE。"
  -
    kind: "documented_behavior"
    expected: "yes/on/true删除ASCII零。"
execution_requirements:
  - "database"
  - "same_session"
