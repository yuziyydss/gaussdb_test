schema_version: 1
kind: "fixture"
id: "fixture_create_text_search_configuration_target"
name: "CREATE TEXT SEARCH CONFIGURATION target"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_text_search_configuration"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "DROP TEXT SEARCH CONFIGURATION IF EXISTS fp_tsc_new CASCADE;"
  teardown_sqls:
    - "DROP TEXT SEARCH CONFIGURATION IF EXISTS fp_tsc_new CASCADE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
