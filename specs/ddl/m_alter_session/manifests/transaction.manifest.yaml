schema_version: 1
kind: manifest
id: manifest_m_alter_session_transaction
name: manifest_m_alter_session_transaction
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_session
syntax_ref: syntax_m_alter_session
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_alter_session_transaction
identifier_policy: {}
bindings:
  form:
  - m_alter_session_form_transaction
  characteristic:
  - m_alter_session_characteristic_read
  - m_alter_session_characteristic_write
  - m_alter_session_characteristic_committed
  - m_alter_session_characteristic_uncommitted
  - m_alter_session_characteristic_repeatable
  - m_alter_session_characteristic_serializable
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_alter_session_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_alter_session_fact_session
- key: transaction_stage
  allowed_values:
  - before_first_data_statement
  fact_refs:
  - m_alter_session_fact_transaction
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
