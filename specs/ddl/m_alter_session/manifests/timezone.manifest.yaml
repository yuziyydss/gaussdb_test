schema_version: 1
kind: manifest
id: manifest_m_alter_session_timezone
name: manifest_m_alter_session_timezone
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_session
syntax_ref: syntax_m_alter_session
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_alter_session_transaction
identifier_policy: {}
bindings:
  form:
  - m_alter_session_form_timezone
  timezone:
  - m_alter_session_timezone_prc
  - m_alter_session_timezone_default
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_alter_session_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_alter_session_fact_session
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
