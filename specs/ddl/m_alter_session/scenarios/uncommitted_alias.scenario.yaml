schema_version: 1
kind: scenario
id: scenario_m_alter_session_uncommitted_alias
name: scenario_m_alter_session_uncommitted_alias
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_session
fact_refs:
- m_alter_session_fact_alias
fixture_refs:
- fixture_m_alter_session_transaction
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 通过独立会话未提交数据验证隔离级别别名，不由语法接受推导脏读
oracles:
- kind: manual_assertion
  expected: READ UNCOMMITTED 行为同 READ COMMITTED；未执行
execution_requirements:
- database_authorization
- target_oracle_calibration
