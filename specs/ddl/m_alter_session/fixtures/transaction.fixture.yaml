schema_version: 1
kind: fixture
id: fixture_m_alter_session_transaction
name: fixture_m_alter_session_transaction
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_session
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - START TRANSACTION;
  teardown_sqls:
  - ROLLBACK;
  note: setup/目标/ROLLBACK 使用同一独占连接，在任何数据查询前设置事务特性；case 结束关闭连接恢复会话边界，不承诺所有参数可回滚。
