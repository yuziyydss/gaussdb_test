schema_version: 1
kind: fixture
id: fixture_m_create_sequence_existing
name: fixture_m_create_sequence_existing
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_sequence
requires_fixture_refs: []
provides:
  tables:
  - name: m_b03_sequence_owner
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE m_b03_sequence_owner (id INT);
  - CREATE SEQUENCE m_b03_existing_seq MINVALUE 1 MAXVALUE 100 START 5 CACHE 1;
  - CREATE SEQUENCE m_b03_existing_seq_two MINVALUE 1 MAXVALUE 100 START 5 CACHE 1;
  - SELECT nextval('m_b03_existing_seq');
  teardown_sqls:
  - DROP SEQUENCE IF EXISTS m_b03_existing_seq_two;
  - DROP SEQUENCE IF EXISTS m_b03_existing_seq;
  - DROP TABLE m_b03_sequence_owner;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
