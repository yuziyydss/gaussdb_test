schema_version: 1
kind: "scenario"
id: "scenario_create_masking_policy_documented_contracts"
name: "CREATE MASKING POLICY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "create_masking_policy"
fixture_refs: []
fact_refs:
  - "create_masking_policy_fact_purpose"
  - "create_masking_policy_fact_privilege"
  - "create_masking_policy_fact_switch"
  - "create_masking_policy_fact_dblink_sender"
  - "create_masking_policy_fact_maskall_catalog"
  - "create_masking_policy_fact_unique"
  - "create_masking_policy_fact_name_limit"
  - "create_masking_policy_fact_truncate"
  - "create_masking_policy_fact_case_fold"
  - "create_masking_policy_fact_identifier_chars"
  - "create_masking_policy_fact_all_users"
  - "create_masking_policy_fact_default_enable"
  - "create_masking_policy_fact_identifier_start"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "创建脱敏策略。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅POLADMIN、SYSADMIN或初始用户可创建。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "enable_security_policy=on才使脱敏策略生效。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "DATABASE LINK过滤发送方属性来自服务端，详细合同依赖未提供开发指南。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "maskall非预置函数，不支持\\df展示。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "脱敏策略名称唯一。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "名称符合标识符规范，标称最长63字符。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "名称超长截断为前63字符。"
  -
    id: "contract_8"
    action: "verify_documented_contract"
    note: "未引号名称转换小写，双引号保留大写。"
  -
    id: "contract_9"
    action: "verify_documented_contract"
    note: "名称字符限字母、数字、下划线、美元符号。"
  -
    id: "contract_10"
    action: "verify_documented_contract"
    note: "不指定过滤时对所有用户生效。"
  -
    id: "contract_11"
    action: "verify_documented_contract"
    note: "不指定开关默认ENABLE。"
  -
    id: "contract_12"
    action: "verify_documented_contract"
    note: "名称以字母或下划线开头。"
oracles:
  -
    kind: "documented_behavior"
    expected: "maskall非预置函数，不支持\\df展示。"
  -
    kind: "documented_behavior"
    expected: "名称超长截断为前63字符。"
  -
    kind: "documented_behavior"
    expected: "未引号名称转换小写，双引号保留大写。"
  -
    kind: "documented_behavior"
    expected: "不指定过滤时对所有用户生效。"
  -
    kind: "documented_behavior"
    expected: "不指定开关默认ENABLE。"
execution_requirements:
  - "database"
  - "same_session"
