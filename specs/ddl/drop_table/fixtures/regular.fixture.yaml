schema_version: 1
kind: fixture
id: fixture_drop_table_regular
name: DROP TABLE 普通目标表
status: ready
description: 为单表和 public 模式限定目标提供最小普通表。
factor_ref: drop_table
provides:
  tables:
    - name: t_dt_base
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
seed: {required: true, rows: [{id: 1}]}
execution: {status: ready, mode: auto, note: "每个 case 前重建、后清理目标表。"}
