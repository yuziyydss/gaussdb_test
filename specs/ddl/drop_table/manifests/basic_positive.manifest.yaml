schema_version: 1
kind: manifest
id: manifest_drop_table_basic_positive
name: DROP TABLE 基础、模式和两表组合
status: ready
description: 对无阻塞依赖的一表、模式限定和两表目标覆盖存在性、依赖模式与 PURGE。
factor_ref: drop_table
syntax_ref: syntax_drop_table_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  if_exists: [dt_if_absent, dt_if_present]
  table_profile: [dt_table_single, dt_table_schema_qualified, dt_table_two]
  dependency_clause: [dt_dependency_default, dt_dependency_restrict, dt_dependency_cascade]
  purge_clause: [dt_purge_absent, dt_purge_present]
expected: {default: success, scope: syntax_and_semantics}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
