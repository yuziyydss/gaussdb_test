schema_version: 1
kind: manifest
id: manifest_drop_table_cascade_positive
name: DROP TABLE CASCADE 依赖视图正向
status: ready
description: 用明确依赖视图验证 CASCADE 目标语句可成功执行，依赖删除结果由场景收口。
factor_ref: drop_table
syntax_ref: syntax_drop_table_v1
suite_type: positive
strategy: pairwise
fixture_refs: [fixture_drop_table_dependent_view]
identifier_policy: {}
bindings:
  if_exists: [dt_if_absent, dt_if_present]
  table_profile: [dt_table_dependent_view]
  dependency_clause: [dt_dependency_cascade]
  purge_clause: [dt_purge_absent, dt_purge_present]
expected: {default: success, scope: syntax_and_semantics}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
