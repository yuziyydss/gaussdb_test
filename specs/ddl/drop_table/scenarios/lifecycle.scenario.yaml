schema_version: 1
kind: scenario
id: scenario_drop_table_lifecycle
name: DROP TABLE 索引、例程和分区生命周期
status: planned
description: 分别验证索引删除、引用例程失效和分区表全部分区删除。
factor_ref: drop_table
fixture_refs: []
fact_refs: [dt_fact_index_lifecycle, dt_fact_routine_dependency, dt_fact_partition_lifecycle]
preconditions: [带索引普通表, 引用目标表的函数与存储过程, 多分区表]
steps: [分别删除三类目标表, 查询对象目录, 调用引用例程]
oracles: [依赖索引不存在, 函数和存储过程无法执行, 全部分区不存在]
execution_requirements: [对象目录查询, 函数与存储过程执行, 分区元数据]
