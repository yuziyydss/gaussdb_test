schema_version: 1
kind: scenario
id: scenario_drop_table_fk_lock
name: 外键表删除的触发器与八级锁
status: planned
description: 在并发会话中验证删除外键表对被引用表触发器和八级锁的影响。
factor_ref: drop_table
fixture_refs: []
fact_refs: [dt_fact_fk_trigger_lifecycle, dt_fact_fk_lock]
preconditions: [主表与外键表, 被引用表触发器, 两个会话和锁监控]
steps: [会话一持有冲突操作, 会话二删除外键表, 查询锁状态和触发器目录]
oracles: [被引用表出现文档指定锁级别, 业务可观察到阻塞, 对应触发器被级联删除]
execution_requirements: [多会话, 锁视图, 触发器元数据]
