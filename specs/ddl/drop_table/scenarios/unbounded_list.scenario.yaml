schema_version: 1
kind: scenario
id: scenario_drop_table_unbounded_list
name: DROP TABLE 无界表列表模型缺口
status: planned
description: V1 repeat 依赖有限 items，后续以生成程序补充三表以上及更长列表边界。
factor_ref: drop_table
fixture_refs: []
fact_refs: [dt_open_table_list_unbounded]
preconditions: [可动态创建任意数量的独立表]
steps: [生成三表、十表和接近 SQL 长度上限的列表, 分别组合模式限定名称]
oracles: [各列表语法可执行, 任一目标错误可精确归因, 找到实现边界或确认仅受通用 SQL 长度限制]
execution_requirements: [动态 Fixture 生成器, 真实 GaussDB]
