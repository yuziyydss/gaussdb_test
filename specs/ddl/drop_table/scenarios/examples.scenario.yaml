schema_version: 1
kind: scenario
id: scenario_drop_table_examples
name: DROP TABLE 文档示例回放
status: planned
description: 原样回放基础 IF EXISTS 与依赖视图的 RESTRICT/CASCADE 示例。
factor_ref: drop_table
fixture_refs: []
fact_refs: [dt_fact_example_basic, dt_fact_example_dependency]
preconditions: [隔离 Schema, 示例对象名可用]
steps: [创建并删除 test, 创建 test1 和 v_test1, 默认删除, CASCADE 删除]
oracles: [基础删除成功, 默认删除显示文档中的依赖错误, CASCADE NOTICE 指向 v_test1]
execution_requirements: [真实 GaussDB, NOTICE 与 ERROR 捕获]
