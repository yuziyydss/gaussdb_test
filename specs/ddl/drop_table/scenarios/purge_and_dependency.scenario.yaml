schema_version: 1
kind: scenario
id: scenario_drop_table_purge_and_dependency
name: CASCADE、RESTRICT 与 PURGE 行为
status: planned
description: 验证依赖对象边界、关联表不被级联删除及回收站开启时 PURGE 的物理删除。
factor_ref: drop_table
fixture_refs: [fixture_drop_table_dependent_view]
fact_refs: [dt_fact_dependency_modes, dt_fact_purge, dt_fact_restrict_error_message, "create_view::cv_example_basic_view"]
preconditions: [依赖视图与关联表, 开启回收站, 可查询回收站和对象目录]
steps: [默认及显式 RESTRICT 删除, CASCADE 删除, 检查关联表, 重建后使用 PURGE]
oracles: [RESTRICT 命中文档错误文本, CASCADE 删除依赖视图但保留关联表, PURGE 不进入回收站]
execution_requirements: [回收站能力, 对象目录查询, 错误捕获]
