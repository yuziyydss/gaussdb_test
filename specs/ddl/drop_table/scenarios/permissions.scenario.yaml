schema_version: 1
kind: scenario
id: scenario_drop_table_permissions
name: DROP TABLE 权限矩阵
status: planned
description: 验证表 Owner、Schema Owner、表 DROP、DROP ANY TABLE 及三权分立关闭管理员路径。
factor_ref: drop_table
fixture_refs: []
fact_refs: [dt_fact_permissions]
preconditions: [多角色与多 Owner, 可切换三权分立状态]
steps: [对每类角色分别执行 DROP TABLE, 撤销授权后重试]
oracles: [文档列出的角色成功, 其他角色命中目标权限错误]
execution_requirements: [管理员权限, 多用户, 安全配置]
