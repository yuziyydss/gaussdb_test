schema_version: 1
kind: manifest
id: manifest_create_schema_inline_index
name: CREATE SCHEMA内嵌index有限代表
status: needs_review
description: 仅命名新模式、无AUTHORIZATION或BLOCKCHAIN的一种完整子命令列表；不代表任意混合顺序或全部子命令属性。清理阶段依赖仍需独立校准。
factor_ref: create_schema
syntax_ref: syntax_create_schema
suite_type: positive
strategy: pairwise
fixture_refs: [fixture_create_schema_inline_index]
identifier_policy: {}
bindings:
  form: [create_schema_form_named]
  schema_name: [create_schema_schema_name_new]
  authorization: [create_schema_authorization_absent]
  blockchain: [create_schema_blockchain_absent]
  elements: [create_schema_elements_table_index]
environment_requirements:
  - key: schema_create_authority
    allowed_values: [database_create]
    fact_refs: [create_schema_fact_permission]
  - key: inline_create_authority
    allowed_values: [create_any_index]
    fact_refs: [create_index::ci_fact_permissions]
  - key: table_create_authority
    allowed_values: [create_any_table]
    fact_refs: [create_table::ct_fact_create_permissions]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
