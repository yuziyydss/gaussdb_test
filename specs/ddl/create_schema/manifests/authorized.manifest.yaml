schema_version: 1
kind: "manifest"
id: "manifest_create_schema_authorized"
name: "CREATE SCHEMA authorized"
status: "needs_review"
description: "预配置角色由环境提供；独立事务创建模式并回滚，不回收外部角色，仍仅生成静态候选。"
factor_ref: "create_schema"
syntax_ref: "syntax_create_schema"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: [fixture_create_schema_authorized_transaction]
identifier_policy: {}
bindings:
  form:
    - "create_schema_form_named"
    - "create_schema_form_owner"
  authorization:
    - "create_schema_authorization_present"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "prepared_schema_owner"
    allowed_values:
      - "fp_cs_owner"
    fact_refs:
      - "create_schema_fact_existing_role"
