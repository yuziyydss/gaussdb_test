schema_version: 1
kind: "manifest"
id: "manifest_create_schema_basic"
name: "CREATE SCHEMA basic"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "create_schema"
syntax_ref: "syntax_create_schema"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_create_schema_new_name"
identifier_policy: {}
bindings:
  form:
    - "create_schema_form_named"
  elements:
    - "create_schema_elements_absent"
    - "create_schema_elements_table"
    - "create_schema_elements_table_view"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
