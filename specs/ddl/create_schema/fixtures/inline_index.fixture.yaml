schema_version: 1
kind: fixture
id: fixture_create_schema_inline_index
name: 内嵌index专属资产清理合同
status: needs_review
factor_ref: create_schema
description: 目标SQL自身创建模式及资产；setup不提前创建同名对象，provides不假报执行前存在的表。
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls: ["SHOW search_path;"]
  teardown_sqls:
    - DROP TABLE fp_cs_new.t_cs_index PURGE;
    - DROP SCHEMA fp_cs_new RESTRICT;
  note: 仅获授权独占测试数据库及隔离连接；fp_cs_new初始不存在且不得与现存用户同名。创建后及清理前确认不是current_schema，防止既有search_path自动选中。记录ownership及实际成功创建的资产，失败或部分创建时不盲目执行整套teardown。不使用CASCADE、DROP OWNED、吞异常或假设ROLLBACK撤销DDL。DROP TABLE正文L8-L9说明删除表会删除依赖索引；本case仅有此普通索引，PURGE避免回收站保留表。DROP SCHEMA L34及各清理正文在source账本保留；因DROP SCHEMA包已依赖CREATE SCHEMA，清理阶段仍是正文依赖而非反向包级fact导入，phase_dependency_review未完成，不宣称完整生命周期DAG或零残留。
