schema_version: 1
kind: fixture
id: fixture_create_schema_q2_transaction
name: 第二轮独立事务命名空间
status: ready
description: BEGIN 在 CREATE SCHEMA 之前；若名称已存在，失败后回滚，不删除既有模式。仅供无外层事务的独立连接。
factor_ref: create_schema
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls: ["BEGIN;", "CREATE SCHEMA fp_q2;"]
  teardown_sqls: ["ROLLBACK;"]
  note: 静态事务形状；运行前仍须确认DDL事务支持、独立连接、失败后继续回滚。未执行数据库。
