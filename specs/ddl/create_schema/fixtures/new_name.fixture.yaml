schema_version: 1
kind: "fixture"
id: "fixture_create_schema_new_name"
name: "CREATE SCHEMA new_name"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_schema"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "DROP SCHEMA IF EXISTS fp_cs_new;"
  teardown_sqls:
    - "DROP SCHEMA IF EXISTS fp_cs_new CASCADE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
