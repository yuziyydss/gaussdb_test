schema_version: 1
kind: fixture
id: fixture_create_schema_authorized_transaction
name: CREATE SCHEMA AUTHORIZATION 独立事务
status: ready
description: 预配置角色仍由环境提供；仅通过回滚撤销本用例创建的模式，不删除角色或既有对象。
factor_ref: create_schema
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls: ["BEGIN;"]
  teardown_sqls: ["ROLLBACK;"]
  note: 需独立连接、无外层事务；失败路径同样回滚，禁止在用户已有事务中回放。
