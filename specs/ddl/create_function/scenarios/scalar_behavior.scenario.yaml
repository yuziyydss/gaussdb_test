schema_version: 1
kind: "scenario"
id: "scenario_create_function_scalar_behavior"
name: "CREATE FUNCTION scalar_behavior"
status: "planned"
description: "执行函数后验证整数相加与 STRICT 空值短路。"
factor_ref: "create_function"
fixture_refs:
  - "fixture_create_function_callable"
fact_refs:
  - "create_function_fact_strict"
  - "create_function_fact_immutable"
preconditions: []
steps:
  -
    id: "sum"
    action: "execute_sql"
    sql: "SELECT fp_cf_callable(1, 3);"
  -
    id: "null"
    action: "execute_sql"
    sql: "SELECT fp_cf_callable(NULL, 3);"
oracles:
  -
    after_step: "sum"
    kind: "result_rows"
    expected:
      -
        - 4
  -
    after_step: "null"
    kind: "result_rows"
    expected:
      -
        - null
execution_requirements:
  - "database"
  - "same_session"
