schema_version: 1
kind: "fixture"
id: "fixture_create_function_existing"
name: "CREATE FUNCTION existing"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_function"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE FUNCTION fp_cf_replace(num1 INTEGER, num2 INTEGER) RETURNS INTEGER LANGUAGE SQL AS 'SELECT $1 + $2;';"
  teardown_sqls:
    - "DROP FUNCTION IF EXISTS fp_cf_replace(INTEGER, INTEGER) CASCADE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
