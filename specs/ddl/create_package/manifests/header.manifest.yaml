schema_version: 1
kind: "manifest"
id: "manifest_create_package_header"
name: "CREATE PACKAGE header"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "create_package"
syntax_ref: "syntax_create_package"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_create_package_schema_pair"
identifier_policy: {}
bindings:
  form:
    - "create_package_form_header"
  replace:
    - "create_package_replace_none"
    - "create_package_replace_yes"
  as_is:
    - "create_package_as_is_as"
    - "create_package_as_is_is"
  authid:
    - "create_package_authid_none"
    - "create_package_authid_invoker"
    - "create_package_authid_definer"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "package_create_authorized"
    allowed_values:
      - "true"
    fact_refs:
      - "create_package_fact_create_any"
