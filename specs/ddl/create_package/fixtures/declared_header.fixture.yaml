schema_version: 1
kind: "fixture"
id: "fixture_create_package_declared_header"
name: "CREATE PACKAGE declared_header"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_package"
requires_fixture_refs:
  - "fixture_create_package_schema_pair"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE PACKAGE fp_cs_one.b10_package_new AUTHID CURRENT_USER AS g_counter INTEGER := 0; PROCEDURE p_inc; END b10_package_new;"
  teardown_sqls:
    - "DROP PACKAGE IF EXISTS fp_cs_one.b10_package_new;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
