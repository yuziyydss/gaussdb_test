schema_version: 1
kind: "fixture"
id: "fixture_create_resource_label_source_objects"
name: "CREATE RESOURCE LABEL source_objects"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_resource_label"
requires_fixture_refs:
  - "fixture_create_schema_pair"
provides:
  tables:
    -
      name: "b9_rl_source"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: true
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
    -
      name: "b9_rl_source_two"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: true
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
    - "CREATE TABLE b9_rl_source (col_1 INTEGER, col_2 INTEGER);"
    - "CREATE TABLE b9_rl_source_two (col_1 INTEGER, col_2 INTEGER);"
    - "CREATE VIEW b9_rl_view AS SELECT col_1 FROM b9_rl_source;"
    - "CREATE FUNCTION b9_rl_fn() RETURNS INTEGER AS $$ SELECT 1 $$ LANGUAGE SQL;"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
