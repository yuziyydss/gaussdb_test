schema_version: 1
kind: "scenario"
id: "scenario_create_resource_label_documented_contracts"
name: "CREATE RESOURCE LABEL documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "create_resource_label"
fixture_refs: []
fact_refs:
  - "create_resource_label_fact_purpose"
  - "create_resource_label_fact_privilege"
  - "create_resource_label_fact_if_not_exists"
  - "create_resource_label_fact_name"
  - "create_resource_label_fact_truncate"
  - "create_resource_label_fact_case"
  - "create_resource_label_fact_name_chars"
  - "create_resource_label_fact_identifier_start"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "创建资源标签。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅POLADMIN、SYSADMIN或初始用户可创建。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "同名带IF NOT EXISTS不报错，仅提示已存在。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "标签名称唯一，合法标识符且最多63字符。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "超过63字符截断。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "未引用大写转小写，双引号保留。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "字母/下划线起始，后续字母、数字、下划线、美元符号。"
  -
    id: "check_create_resource_label_fact_identifier_start"
    action: "verify_documented_contract"
    note: "名称首字符必须为字母或下划线。"
oracles:
  -
    kind: "documented_behavior"
    expected: "同名带IF NOT EXISTS不报错，仅提示已存在。"
  -
    kind: "documented_behavior"
    expected: "超过63字符截断。"
  -
    kind: "documented_behavior"
    expected: "未引用大写转小写，双引号保留。"
execution_requirements:
  - "database"
  - "same_session"
