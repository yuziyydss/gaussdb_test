schema_version: 1
kind: "scenario"
id: "scenario_alter_masking_policy_documented_contracts"
name: "ALTER MASKING POLICY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_masking_policy"
fixture_refs: []
fact_refs:
  - "alter_masking_policy_fact_purpose"
  - "alter_masking_policy_fact_privilege"
  - "alter_masking_policy_fact_switch"
  - "alter_masking_policy_fact_name"
  - "alter_masking_policy_fact_maskall_catalog"
  - "alter_masking_policy_fact_enabled_behavior"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "修改脱敏策略内容。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅POLADMIN、SYSADMIN或初始用户可维护。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "安全开关enable_security_policy开启才生效。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "策略名称唯一且遵守标识符规则。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "maskall在代码中内置，不支持\\df展示。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "ENABLE/DISABLE打开关闭，原文称省略默认ENABLE。"
oracles:
  -
    kind: "documented_behavior"
    expected: "maskall在代码中内置，不支持\\df展示。"
  -
    kind: "documented_behavior"
    expected: "ENABLE/DISABLE打开关闭，原文称省略默认ENABLE。"
execution_requirements:
  - "database"
  - "same_session"
