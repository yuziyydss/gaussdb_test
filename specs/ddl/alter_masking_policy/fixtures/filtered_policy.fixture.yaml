schema_version: 1
kind: "fixture"
id: "fixture_alter_masking_policy_filtered_policy"
name: "ALTER MASKING POLICY filtered_policy"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_masking_policy"
requires_fixture_refs:
  - "fixture_create_masking_policy_source_labels"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE MASKING POLICY b10_mask_existing maskall ON LABEL(b10_mask_a) FILTER ON IP('127.0.0.1') DISABLE;"
  teardown_sqls:
    - "DROP MASKING POLICY IF EXISTS b10_mask_existing;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
