schema_version: 1
kind: "manifest"
id: "manifest_alter_masking_policy_remove"
name: "ALTER MASKING POLICY remove"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_masking_policy"
syntax_ref: "syntax_alter_masking_policy"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_masking_policy_before_remove"
identifier_policy: {}
bindings:
  operation:
    - "alter_masking_policy_operation_remove"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "security_policy_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "alter_masking_policy_fact_privilege"
  -
    key: "enable_security_policy"
    allowed_values:
      - "on"
    fact_refs:
      - "alter_masking_policy_fact_switch"
