schema_version: 1
kind: "scenario"
id: "scenario_alter_package_documented_contracts"
name: "ALTER PACKAGE documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_package"
fixture_refs: []
fact_refs:
  - "alter_package_fact_purpose"
  - "alter_package_fact_owner_privilege"
  - "alter_package_fact_separation"
  - "alter_package_fact_initial_owner"
  - "alter_package_fact_no_separation"
  - "alter_package_fact_definer_owner"
  - "alter_package_fact_name"
  - "alter_package_fact_schema_create"
  - "alter_package_fact_existing_role"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "修改包属性或重编译。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "注意事项称目前仅支持OWNER；所有者或管理员且为新所有者成员。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "三权分立时管理员也需用户组权限，DEFINER包不允许改所有者。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "只有初始用户能将包所有者改为初始用户。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "三权分立关闭时仅系统管理员以上能改所有者。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "管理员不能将DEFINER包改归初始/运维管理员。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "目标仅单个已有PACKAGE。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "新所有者需包所在模式CREATE权限。"
  -
    id: "contract_8"
    action: "verify_documented_contract"
    note: "新所有者必须为已有角色。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
