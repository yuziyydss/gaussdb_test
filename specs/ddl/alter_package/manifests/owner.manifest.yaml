schema_version: 1
kind: "manifest"
id: "manifest_alter_package_owner"
name: "ALTER PACKAGE owner"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_package"
syntax_ref: "syntax_alter_package"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_package_owner_privilege"
identifier_policy: {}
bindings:
  operation:
    - "alter_package_operation_owner"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "sysadmin"
    allowed_values:
      - "true"
    fact_refs:
      - "alter_package_fact_no_separation"
  -
    key: "enable_separation_of_duty"
    allowed_values:
      - "off"
    fact_refs:
      - "alter_package_fact_no_separation"
