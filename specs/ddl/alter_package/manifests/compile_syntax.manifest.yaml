schema_version: 1
kind: manifest
id: manifest_alter_package_compile_syntax
name: ALTER PACKAGE compile syntax
status: needs_review
description: 四种COMPILE拼写仅作原文语法代表；不解决仅支持OWNER的来源冲突，不宣称行为通过。
factor_ref: alter_package
syntax_ref: syntax_alter_package
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  operation:
  - alter_package_operation_compile
  - alter_package_operation_compile_package
  - alter_package_operation_compile_body
  - alter_package_operation_compile_spec
environment_requirements:
- key: package_compile_support_conflict
  allowed_values:
  - syntax_only_no_behavior_claim
  fact_refs:
  - alter_package_fact_support_conflict
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
