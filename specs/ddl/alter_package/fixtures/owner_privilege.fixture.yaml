schema_version: 1
kind: "fixture"
id: "fixture_alter_package_owner_privilege"
name: "ALTER PACKAGE owner_privilege"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_package"
requires_fixture_refs:
  - "fixture_create_role_isolated_roles"
  - "fixture_create_package_existing_package"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "GRANT CREATE ON SCHEMA fp_cs_one TO b9_role_b;"
  teardown_sqls:
    - "DROP PACKAGE IF EXISTS fp_cs_one.b10_package_existing;"
    - "REVOKE CREATE ON SCHEMA fp_cs_one FROM b9_role_b;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
