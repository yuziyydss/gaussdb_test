schema_version: 1
kind: "manifest"
id: "manifest_create_event_disabled_only"
name: "CREATE EVENT disabled_only"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "create_event"
syntax_ref: "syntax_create_event"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_create_event_source"
identifier_policy: {}
bindings:
  if_not_exists:
    - "create_event_if_not_exists_none"
    - "create_event_if_not_exists_yes"
  schedule:
    - "create_event_schedule_once"
    - "create_event_schedule_repeat"
  completion:
    - "create_event_completion_none"
    - "create_event_completion_preserve"
    - "create_event_completion_not_preserve"
  disabled:
    - "create_event_disabled_disable"
    - "create_event_disabled_slave"
  comment:
    - "create_event_comment_none"
    - "create_event_comment_text"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "sql_compatibility"
    allowed_values:
      - "B"
    fact_refs:
      - "create_event_fact_compat_b"
  -
    key: "event_operator_authorized"
    allowed_values:
      - "true"
    fact_refs:
      - "create_event_fact_privilege"
