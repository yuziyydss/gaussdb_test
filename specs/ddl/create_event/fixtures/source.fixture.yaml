schema_version: 1
kind: "fixture"
id: "fixture_create_event_source"
name: "CREATE EVENT source"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_event"
requires_fixture_refs:
  - "fixture_create_schema_pair"
provides:
  tables:
    -
      name: "fp_cs_one.b10_event_source"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: true
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TABLE fp_cs_one.b10_event_source (col_1 INTEGER,col_2 INTEGER);"
  teardown_sqls:
    - "DROP EVENT IF EXISTS fp_cs_one.b10_event_new;"
    - "DROP TABLE fp_cs_one.b10_event_source;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
