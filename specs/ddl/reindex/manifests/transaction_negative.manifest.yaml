schema_version: 1
kind: "manifest"
id: "manifest_reindex_transaction_negative"
name: "REINDEX transaction_negative"
status: "needs_review"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "reindex"
syntax_ref: "syntax_reindex"
suite_type: "negative"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  target:
    - "reindex_target_in_tx"
  concurrently:
    - "reindex_concurrently_yes"
  force:
    - "reindex_force_none"
expected:
  default: "error"
  scope: "syntax_and_semantics"
  oracle_status: "needs_verification"
  error_category: "reindex_concurrently_in_transaction"
  fact_refs:
    - "reindex_fact_concurrent"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
violates_rule_refs:
  - "reindex_rule_transaction"
