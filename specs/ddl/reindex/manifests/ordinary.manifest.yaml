schema_version: 1
kind: "manifest"
id: "manifest_reindex_ordinary"
name: "REINDEX ordinary"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "reindex"
syntax_ref: "syntax_reindex"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  target:
    - "reindex_target_index"
    - "reindex_target_table"
    - "reindex_target_in_tx"
  concurrently:
    - "reindex_concurrently_none"
  force:
    - "reindex_force_none"
    - "reindex_force_yes"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
