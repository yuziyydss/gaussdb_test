schema_version: 1
kind: "scenario"
id: "scenario_drop_package_documented_contracts"
name: "DROP PACKAGE documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_package"
fixture_refs: []
fact_refs:
  - "drop_package_fact_purpose"
  - "drop_package_fact_body_invalidates"
  - "drop_package_fact_initial_owner"
  - "drop_package_fact_exists"
  - "drop_package_fact_missing"
  - "drop_package_fact_name"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除PACKAGE或PACKAGE BODY。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "删除包体使包内过程函数同时失效。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "初始用户包仅初始用户可DROP。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "IF EXISTS目标存在时删除。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "IF EXISTS缺失时仅NOTICE。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "目标为已有PACKAGE名。"
oracles:
  -
    kind: "documented_behavior"
    expected: "IF EXISTS目标存在时删除。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS缺失时仅NOTICE。"
execution_requirements:
  - "database"
  - "same_session"
