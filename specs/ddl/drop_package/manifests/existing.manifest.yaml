schema_version: 1
kind: "manifest"
id: "manifest_drop_package_existing"
name: "DROP PACKAGE existing"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_package"
syntax_ref: "syntax_drop_package"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_package_existing_package"
identifier_policy: {}
bindings:
  body:
    - "drop_package_body_package"
    - "drop_package_body_body"
  if_exists:
    - "drop_package_if_exists_none"
    - "drop_package_if_exists_yes"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "package_drop_authorized"
    allowed_values:
      - "true"
    fact_refs:
      - "drop_package_fact_initial_owner"
