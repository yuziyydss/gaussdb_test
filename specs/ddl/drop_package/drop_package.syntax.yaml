schema_version: 1
kind: "syntax"
id: "syntax_drop_package"
name: "DROP PACKAGE 语法"
factor_ref: "drop_package"
category: "DDL"
status: "ready"
description: "语法结构来自 PDF，选值仅由 factor/manifest 提供。"
production: "DROP PACKAGE {body} {if_exists} fp_cs_one.b10_package_existing"
rendering:
  whitespace: "collapse"
  statement_terminator: ";"
slots:
  body:
    type: "token"
    optional: true
    dimension_ref: "drop_package.body"
  if_exists:
    type: "token"
    optional: true
    dimension_ref: "drop_package.if_exists"
source_fact_refs:
  - "drop_package_fact_package_syntax"
  - "drop_package_fact_body_syntax"
