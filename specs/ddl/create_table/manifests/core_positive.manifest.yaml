schema_version: 1
kind: manifest
id: manifest_create_table_core_positive
name: CREATE TABLE 普通与 UNLOGGED 有界核心
status: ready
description: 仅覆盖新目标名、普通或 UNLOGGED、一至两列，以及可选 IF NOT EXISTS。
factor_ref: create_table
syntax_ref: syntax_create_table_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {table_name: {generator: deterministic, prefix: t_ct_core, unique_per_case: true}}
bindings:
  persistence_modifier: [ct_persistence_permanent, ct_persistence_unlogged]
  if_not_exists: [ct_if_absent, ct_if_present]
  column_profile: [ct_columns_integer, ct_columns_integer_varchar]
expected: {default: success, scope: syntax_and_semantics}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
