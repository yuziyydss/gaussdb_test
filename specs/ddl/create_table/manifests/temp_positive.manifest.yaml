schema_version: 1
kind: manifest
id: manifest_create_table_temp_positive
name: CREATE TABLE TEMP 同义词有界核心
status: ready
description: 仅覆盖未显式 GLOBAL/LOCAL、未带 ON COMMIT 的 TEMP 与 TEMPORARY 普通列建表。
factor_ref: create_table
syntax_ref: syntax_create_table_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {table_name: {generator: deterministic, prefix: t_ct_temp, unique_per_case: true}}
bindings:
  persistence_modifier: [ct_persistence_temp, ct_persistence_temporary]
  if_not_exists: [ct_if_absent, ct_if_present]
  column_profile: [ct_columns_integer, ct_columns_integer_varchar]
expected: {default: success, scope: syntax_and_semantics}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
