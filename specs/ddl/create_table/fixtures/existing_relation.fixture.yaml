schema_version: 1
kind: fixture
id: fixture_create_table_existing_relation
name: CREATE TABLE 已存在关系场景基线
status: ready
description: 仅供 IF NOT EXISTS 生命周期计划场景使用；核心正向生成不引用该 fixture。
factor_ref: create_table
provides:
  tables:
    - name: t_ct_existing
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
seed: {required: false, rows: []}
execution: {status: ready, mode: auto, note: "为已存在关系行为场景建立单列普通表，并在场景后清理。"}
