schema_version: 1
kind: fixture
id: fixture_create_table_drop_view_base
name: DROP TABLE 依赖视图共享基表
status: ready
description: 提供非空整数列表供依赖视图引用，目标 DROP TABLE 的 CASCADE/RESTRICT 行为由消费方定义。
factor_ref: create_table
provides:
  tables:
    - name: t_dt_dep_base
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
seed: {required: false, rows: []}
execution: {status: ready, mode: auto, note: "基表先 setup，最后 teardown；允许目标语句已删除基表。"}
