schema_version: 1
kind: fixture
id: fixture_create_table_insert_view_base
name: INSERT 视图场景共享基表
status: ready
description: 按 CREATE TABLE 的整数和字符列形式提供 INSERT 单表视图示例所需基表；由依赖它的视图 fixture 负责视图生命周期。
factor_ref: create_table
provides:
  tables:
    - name: t_insert_view_base
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: true}
        - {name: note, type: VARCHAR(64), nullable: true}
seed: {required: false, rows: []}
execution: {status: ready, mode: auto, note: "每个用例先建基表；消费者拆除视图后再清理基表。"}
