schema_version: 1
kind: "fixture"
id: "fixture_create_text_search_dictionary_simple"
name: "CREATE TEXT SEARCH DICTIONARY simple"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_text_search_dictionary"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TEXT SEARCH DICTIONARY fp_tsd_simple (TEMPLATE=simple, ACCEPT=true);"
  teardown_sqls:
    - "DROP TEXT SEARCH DICTIONARY IF EXISTS fp_tsd_simple CASCADE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
