schema_version: 1
kind: scenario
id: scenario_m_drop_extension_dependencies
name: scenario_m_drop_extension_dependencies
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_extension
fact_refs:
- m_drop_extension_fact_members
- m_drop_extension_fact_restrict
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 扩展组件清单和外部依赖需独立核验；不删除默认plpgsql，不生成CASCADE清理。
oracles:
- kind: manual_assertion
  expected: 组件随扩展删除、外部依赖阻止RESTRICT尚待Oracle
execution_requirements:
- database_authorization
- target_oracle_calibration
