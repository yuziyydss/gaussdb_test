schema_version: 1
kind: "scenario"
id: "scenario_create_server_documented_contracts"
name: "CREATE SERVER documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "create_server"
fixture_refs: []
fact_refs:
  - "create_server_fact_purpose"
  - "create_server_fact_sensitive"
  - "create_server_fact_name"
  - "create_server_fact_fdw_environment"
  - "create_server_fact_startup"
  - "create_server_fact_tuple_spelling"
  - "create_server_fact_compatibility_only"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "创建外部服务器，承载其他同构数据库信息。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "敏感OPTIONS字段使用多层引号时语义不同，不会被识别脱敏。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "服务器名符合标识符规范且不超过63个字符。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "本章列出的封装器为dist_fdw、log_fdw、file_fdw；后两种仅语法兼容，可创建外表但无实际使用意义。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "fdw_startup_cost表示扫描启动耗时估计，默认100且必须大于0。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "本章写fdw_typle_cost，表示每元组传输额外消耗，默认0.01且必须大于0。"
  -
    id: "compatibility_only"
    action: "verify_documented_contract"
    note: "log_fdw和file_fdw仅语法兼容，可以创建外表但无实际使用意义。"
oracles:
  -
    kind: "documented_behavior"
    expected: "敏感OPTIONS字段使用多层引号时语义不同，不会被识别脱敏。"
  -
    kind: "documented_behavior"
    expected: "log_fdw和file_fdw仅语法兼容，可以创建外表但无实际使用意义。"
execution_requirements:
  - "database"
  - "same_session"
