schema_version: 1
kind: "scenario"
id: "scenario_drop_database_link_documented_contracts"
name: "DROP DATABASE LINK documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_database_link"
fixture_refs: []
fact_refs:
  - "drop_database_link_fact_purpose"
  - "drop_database_link_fact_exists"
  - "drop_database_link_fact_missing"
  - "drop_database_link_fact_visibility"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除DBLink对象。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "IF EXISTS在目标存在时删除。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "IF EXISTS缺失时仅NOTICE。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "PUBLIC删除公共连接，省略删除私有连接。"
oracles:
  -
    kind: "documented_behavior"
    expected: "IF EXISTS在目标存在时删除。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS缺失时仅NOTICE。"
  -
    kind: "documented_behavior"
    expected: "PUBLIC删除公共连接，省略删除私有连接。"
execution_requirements:
  - "database"
  - "same_session"
