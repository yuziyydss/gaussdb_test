schema_version: 1
kind: "scenario"
id: "scenario_drop_masking_policy_documented_contracts"
name: "DROP MASKING POLICY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_masking_policy"
fixture_refs: []
fact_refs:
  - "drop_masking_policy_fact_purpose"
  - "drop_masking_policy_fact_privilege"
  - "drop_masking_policy_fact_name"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除脱敏策略。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅POLADMIN、SYSADMIN或初始用户可删除。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "policy_name指已有策略名，参数正文误称审计策略。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
