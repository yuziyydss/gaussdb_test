schema_version: 1
kind: scenario
id: scenario_m_create_function_overloads
name: scenario_m_create_function_overloads
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_function
fact_refs:
- m_create_function_fact_overload
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 独立建模重载标识和类型、返回值、默认值冲突的目标错误；不在当前有限域声明覆盖。
oracles:
- kind: manual_assertion
  expected: 重载冲突 Oracle 尚待校准
execution_requirements:
- database_authorization
- target_oracle_calibration
