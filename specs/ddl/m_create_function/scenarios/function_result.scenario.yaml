schema_version: 1
kind: scenario
id: scenario_m_create_function_function_result
name: scenario_m_create_function_function_result
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_function
fact_refs:
- m_create_function_fact_strict
- m_create_function_fact_return_type
fixture_refs:
- fixture_m_create_function_namespace
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 内部工具审阅后验证整数输入/NULL 返回；不从 CREATE 成功推导执行返回类型正确。
oracles:
- kind: manual_assertion
  expected: 输入1预期2、STRICT NULL预期NULL，但尚未生成调用场景或执行
execution_requirements:
- database_authorization
- target_oracle_calibration
