schema_version: 1
kind: "fixture"
id: "fixture_drop_schema_schema_contents"
name: "DROP SCHEMA schema_contents"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "drop_schema"
requires_fixture_refs:
  - "fixture_create_schema_pair"
provides:
  tables:
    -
      name: "fp_cs_two.ds_probe"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "id"
          type: "INTEGER"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "DROP SCHEMA IF EXISTS fp_ds_missing;"
    - "CREATE TABLE fp_cs_two.ds_probe (id INT);"
  teardown_sqls:
    - "DROP TABLE IF EXISTS fp_cs_two.ds_probe;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
