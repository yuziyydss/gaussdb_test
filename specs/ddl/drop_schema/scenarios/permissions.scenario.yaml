schema_version: 1
kind: "scenario"
id: "scenario_drop_schema_permissions"
name: "DROP SCHEMA permissions"
status: "planned"
description: "隔离角色预配置并验证所有者、DROP 授权和三权分立分支。"
factor_ref: "drop_schema"
fixture_refs: []
fact_refs:
  - "drop_schema_fact_permission"
  - "drop_schema_fact_sysadmin"
  - "drop_schema_fact_example_role"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "隔离角色预配置并验证所有者、DROP 授权和三权分立分支。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
