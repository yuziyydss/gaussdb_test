schema_version: 1
kind: "manifest"
id: "manifest_drop_schema_missing_negative"
name: "DROP SCHEMA missing_negative"
status: "needs_review"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_schema"
syntax_ref: "syntax_drop_schema"
suite_type: "negative"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_schema_schema_contents"
identifier_policy: {}
bindings:
  if_exists:
    - "drop_schema_if_exists_absent"
  targets:
    - "drop_schema_targets_missing"
  behavior:
    - "drop_schema_behavior_default"
    - "drop_schema_behavior_restrict"
    - "drop_schema_behavior_cascade"
expected:
  default: "error"
  scope: "syntax_and_semantics"
  oracle_status: "needs_verification"
  error_category: "undefined_schema"
  fact_refs: []
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
violates_rule_refs:
  - "drop_schema_rule_exists"
environment_requirements:
  -
    key: "search_path_excludes_test_schemas"
    allowed_values:
      - "true"
    fact_refs:
      - "drop_schema_fact_current"
