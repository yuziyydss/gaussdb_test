schema_version: 1
kind: scenario
id: scenario_m_truncate_partition_and_inheritance
name: scenario_m_truncate_partition_and_inheritance
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_truncate
fact_refs:
- m_truncate_fact_only
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 另建分区/继承契约并核验版本后补场景
oracles:
- kind: result_set
  expected: 待分区行集合与全局索引 Oracle 接入
execution_requirements:
- database_authorization
- target_oracle_calibration
