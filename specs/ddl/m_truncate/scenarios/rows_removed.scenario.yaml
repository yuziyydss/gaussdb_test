schema_version: 1
kind: scenario
id: scenario_m_truncate_rows_removed
name: scenario_m_truncate_rows_removed
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_truncate
fact_refs:
- m_truncate_fact_rows_removed
- m_truncate_fact_owner
- m_truncate_fact_purge
fixture_refs:
- fixture_m_truncate_seeded
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: TRUNCATE TABLE m_truncate_a;
- sql: SELECT COUNT(*) FROM m_truncate_a;
oracles:
- kind: result_set
  expected:
  - - 0
execution_requirements:
- database_authorization
- target_oracle_calibration
