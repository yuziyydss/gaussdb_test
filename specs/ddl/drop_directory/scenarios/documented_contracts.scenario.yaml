schema_version: 1
kind: "scenario"
id: "scenario_drop_directory_documented_contracts"
name: "DROP DIRECTORY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_directory"
fixture_refs: []
fact_refs:
  - "drop_directory_fact_purpose"
  - "drop_directory_fact_off_privilege"
  - "drop_directory_fact_on_privilege"
  - "drop_directory_fact_no_pdb"
  - "drop_directory_fact_existing"
  - "drop_directory_fact_missing"
  - "drop_directory_fact_missing_notice"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除目录对象。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "enable_access_server_directory=off时仅初始用户可删除。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "开关on时SYSADMIN、属主、DROP授权或继承gs_role_directory_drop者可删除。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "PDB内不支持目录对象。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "存在时删除成功。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "IF EXISTS且不存在只产生NOTICE。"
oracles:
  -
    kind: "documented_behavior"
    expected: "存在时删除成功。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS且不存在只产生NOTICE。"
execution_requirements:
  - "database"
  - "same_session"
