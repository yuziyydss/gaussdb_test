schema_version: 1
kind: fixture
id: fixture_create_index_ugin_text
name: CREATE INDEX UGIN trgm 文本源
status: ready
description: 对齐文档 UGIN ugin_trgm_ops 示例的 USTORE varchar 源表。
factor_ref: create_index
provides:
  tables:
    - name: t_ci_ugin_text
      persistence: permanent
      table_kind: ustore_regular
      columns:
        - {name: name, type: VARCHAR(100), nullable: true}
        - {name: note, type: VARCHAR(100), nullable: true}
seed:
  required: true
  rows:
    - {name: qwertyu0001, note: first}
    - {name: "2024新年快乐7days", note: second}
execution: {status: ready, mode: auto, note: "UGIN trgm 单键与 LIKE 行为场景共用。"}
