schema_version: 1
kind: fixture
id: fixture_create_index_ustore_regular
name: CREATE INDEX USTORE 普通目标表
status: ready
description: 为 UB-Tree、INCLUDE、INDEXSPLIT 与 INDEX_TXNTYPE 提供 USTORE 表。
factor_ref: create_index
provides:
  tables:
    - name: t_ci_ustore
      persistence: permanent
      table_kind: ustore_regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: note, type: VARCHAR(64), nullable: true}
        - {name: name, type: VARCHAR(64), nullable: true}
        - {name: postcode, type: CHAR(6), nullable: true}
seed:
  required: true
  rows:
    - {id: 1, note: one, name: Alpha, postcode: "100001"}
    - {id: 2, note: two, name: Beta, postcode: "100002"}
execution: {status: ready, mode: auto, note: "自动编译器会追加 WITH (storage_type=ustore)。"}
