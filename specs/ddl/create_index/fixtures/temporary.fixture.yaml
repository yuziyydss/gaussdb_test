schema_version: 1
kind: fixture
id: fixture_create_index_temporary
name: CREATE INDEX 临时目标表
status: ready
description: 验证临时表接受 CONCURRENTLY 语法但实际采用阻塞创建。
factor_ref: create_index
provides:
  tables:
    - name: t_ci_temp
      persistence: temporary
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: note, type: VARCHAR(64), nullable: true}
        - {name: name, type: VARCHAR(64), nullable: true}
        - {name: postcode, type: CHAR(6), nullable: true}
        - {name: payload, type: BLOB, nullable: true}
seed:
  required: true
  rows:
    - {id: 11, note: temp_one, name: TempAlpha, postcode: "200001", payload: null}
    - {id: 12, note: temp_two, name: TempBeta, postcode: "200002", payload: null}
execution:
  status: ready
  mode: auto
  note: TEMP 表由 Fixture 编译器创建并在会话/teardown 中删除。

