schema_version: 1
kind: fixture
id: fixture_create_index_visibility_fresh
name: A模式索引可见性fresh普通表
status: needs_review
description: 独占新schema中的ASTORE空表，不操作既有索引或表。
factor_ref: create_index
provides:
  tables:
  - name: g_ci_visibility
    persistence: permanent
    table_kind: regular
    columns: [{name: id, type: INTEGER, nullable: true}]
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls: ["CREATE TABLE g_ci_visibility (id INTEGER) WITH (storage_type=astore);"]
  teardown_sqls: ["DROP TABLE g_ci_visibility;"]
  note: 本case须独占fresh用户schema与新名字。仅在确认由本case建表后允许清理其表和索引；setup失败不授权盲清理。无预DROP/CASCADE，不依赖DDL回滚，尚未实机执行。
