schema_version: 1
kind: fixture
id: fixture_create_index_regular
name: CREATE INDEX 普通目标表
status: ready
description: 为普通、唯一、组合、排序、INCLUDE、表达式和 LOB 负向索引提供列能力。
factor_ref: create_index
provides:
  tables:
    - name: t_ci_regular
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: note, type: VARCHAR(64), nullable: true}
        - {name: name, type: VARCHAR(64), nullable: true}
        - {name: postcode, type: CHAR(6), nullable: true}
        - {name: payload, type: BLOB, nullable: true}
seed:
  required: true
  rows:
    - {id: 1, note: one, name: Alpha, postcode: "100001", payload: null}
    - {id: 2, note: two, name: Beta, postcode: "100002", payload: null}
execution:
  status: ready
  mode: auto
  note: 数据保持唯一，确保普通正向 manifest 可安全组合 UNIQUE。

