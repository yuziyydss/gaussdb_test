schema_version: 1
kind: fixture
id: fixture_create_index_astore_regular
name: CREATE INDEX ASTORE 普通目标表
status: ready
description: 为树索引、表达式、前缀、COLLATE、opclass、部分索引与存储参数提供稳定列契约。
factor_ref: create_index
provides:
  tables:
    - name: t_ci_astore
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: note, type: VARCHAR(64), nullable: true}
        - {name: name, type: VARCHAR(64), nullable: true}
        - {name: postcode, type: CHAR(6), nullable: true}
seed:
  required: true
  rows:
    - {id: 1, note: one, name: Alpha, postcode: "100001"}
    - {id: 2, note: two, name: Beta, postcode: "100002"}
execution: {status: ready, mode: auto, note: "普通 ASTORE 表；唯一键数据保持唯一。"}
