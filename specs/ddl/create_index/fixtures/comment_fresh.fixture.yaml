schema_version: 1
kind: fixture
id: fixture_create_index_comment_fresh
name: B模式短注释索引的fresh普通表
status: needs_review
description: 本case新建普通ASTORE单列空表，表删除时清理其索引；不触碰既有对象。
factor_ref: create_index
provides:
  tables:
  - name: g_ci_comment
    persistence: permanent
    table_kind: regular
    columns: [{name: id, type: INTEGER, nullable: true}]
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls: ["CREATE TABLE g_ci_comment (id INTEGER) WITH (storage_type=astore);"]
  teardown_sqls: ["DROP TABLE g_ci_comment;"]
  note: 须独占新鲜用户schema与本case创建者；表/索引名预先不存在。setup失败不授权盲清理，目标失败先恢复错误事务。无预DROP/CASCADE，不依赖DDL回滚，未执行数据库。
