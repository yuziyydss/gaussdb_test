schema_version: 1
kind: fixture
id: fixture_create_index_gist_point
name: CREATE INDEX GiST point 源
status: ready
description: 对齐文档 GiST point 示例的 ASTORE 表。
factor_ref: create_index
provides:
  tables:
    - name: t_ci_gist_point
      persistence: permanent
      table_kind: regular
      columns:
        - {name: location, type: POINT, nullable: true}
        - {name: id, type: INTEGER, nullable: false}
seed:
  required: true
  rows:
    - {location: "(1,2)", id: 1}
execution: {status: ready, mode: auto, note: "point 文本字面量由数据库隐式转换；仅用于生成层契约。"}
