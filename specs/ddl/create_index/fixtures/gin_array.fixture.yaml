schema_version: 1
kind: fixture
id: fixture_create_index_gin_array
name: CREATE INDEX GIN int 数组源
status: ready
description: 对齐文档 GIN 数组示例的 ASTORE int[] 表。
factor_ref: create_index
provides:
  tables:
    - name: t_ci_gin_array
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: info, type: "INT[]", nullable: true}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - DROP TABLE IF EXISTS t_ci_gin_array CASCADE;
    - CREATE TABLE t_ci_gin_array (id INTEGER, info INT[]) WITH (storage_type=astore);
    - INSERT INTO t_ci_gin_array VALUES (1, ARRAY[1, 2]);
  teardown_sqls: [DROP TABLE IF EXISTS t_ci_gin_array CASCADE;]
  note: "显式 ASTORE 数组表。"
