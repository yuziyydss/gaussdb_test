schema_version: 1
kind: fixture
id: fixture_create_index_ugin_array
name: CREATE INDEX UGIN bool 数组源
status: ready
description: USTORE bool[] 源表；数组类型由显式生命周期创建。
factor_ref: create_index
provides:
  tables:
    - name: t_ci_ugin_array
      persistence: permanent
      table_kind: ustore_regular
      columns:
        - {name: tags, type: "BOOL[]", nullable: true}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - DROP TABLE IF EXISTS t_ci_ugin_array CASCADE;
    - CREATE TABLE t_ci_ugin_array (tags BOOL[]) WITH (storage_type=ustore);
    - INSERT INTO t_ci_ugin_array VALUES (ARRAY[true, false]);
  teardown_sqls: [DROP TABLE IF EXISTS t_ci_ugin_array CASCADE;]
  note: "显式 SQL 避免自动 Fixture 类型正则把数组误判为普通类型。"
