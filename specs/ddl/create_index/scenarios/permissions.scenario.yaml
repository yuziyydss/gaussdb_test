schema_version: 1
kind: scenario
id: scenario_create_index_permissions
name: CREATE INDEX 权限与表达式执行身份
status: planned
description: 验证 CREATE ANY INDEX、表所有者、初始用户、系统管理员和三权分立组合。
factor_ref: create_index
fixture_refs: [fixture_create_index_astore_regular]
fact_refs: [ci_fact_permissions, ci_fact_expression_permissions]
preconditions: [创建多种角色, 切换三权分立状态, 创建由不同 Owner 拥有的表和 immutable 函数]
steps: [普通索引, 表达式索引, 跨模式索引, 初始用户表上的索引]
oracles: [成功或目标权限错误, 函数按表 Owner 权限执行]
execution_requirements: [多用户, 管理员权限, 安全配置]
