schema_version: 1
kind: scenario
id: scenario_create_index_ilm_behavior
name: ILM 压缩策略行为
status: planned
description: 验证 TURBO 级别/时间单位、显式 NONE、默认继承、已知不支持的方法以及 UGIN 待确认能力。
factor_ref: create_index
fixture_refs: [fixture_create_index_astore_regular, fixture_create_index_ugin_text, fixture_create_index_gin_array, fixture_create_index_gist_point]
fact_refs: [ci_fact_ilm, ci_fact_gin_capability, ci_fact_gist_capability, ci_open_ugin_ilm_compression_support]
preconditions: [可查询 ILM 策略元数据]
steps: [TURBO HIGH, 省略级别, 显式 NONE, GIN/GiST 尝试, UGIN 能力校准]
oracles: [策略级别与时间, 表策略继承, GIN/GiST 目标不支持错误, UGIN 实际成功或目标错误]
execution_requirements: [ILM 能力启用, 元数据查询]
