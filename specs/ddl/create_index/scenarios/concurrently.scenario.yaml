schema_version: 1
kind: scenario
id: scenario_create_index_concurrently
name: CONCURRENTLY 在线创建与事务限制
status: planned
description: 验证普通表 DML 不阻塞、事务内拒绝、临时表退化为阻塞创建及 ASTORE/USTORE 生命周期差异。
factor_ref: create_index
fixture_refs: [fixture_create_index_astore_regular, fixture_create_index_temporary]
fact_refs: [ci_fact_concurrently, ci_fact_online_capability, ci_fact_concurrent_performance]
preconditions: [多会话, 长时间 DML, 事务块, ASTORE 与 USTORE 表]
steps: [在线建索引并发 DML, 事务内执行, 临时表执行, 比较两种存储引擎扫描与合并]
oracles: [锁等待与 DML 可用性, 事务错误, 创建方式, 边界成功或失败]
execution_requirements: [多会话, 锁监控, 宽表 Fixture]
