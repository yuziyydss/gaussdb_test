schema_version: 1
kind: scenario
id: scenario_create_index_partial_behavior
name: 部分索引谓词行为
status: planned
description: 验证普通表 WHERE 谓词、谓词可引用非键列、禁止子查询/聚集及 GLOBAL 分区索引限制。
factor_ref: create_index
fixture_refs: [fixture_create_index_astore_regular, fixture_create_index_partitioned]
fact_refs: [ci_fact_partial_index, ci_fact_immutable, ci_fact_example_partial]
preconditions: [含满足与不满足谓词的数据]
steps: [创建部分索引, 非键列谓词, 子查询谓词, 聚集谓词, GLOBAL 分区表尝试]
oracles: [索引谓词元数据, 计划与结果, 目标错误类别]
execution_requirements: [系统目录查询, EXPLAIN]
