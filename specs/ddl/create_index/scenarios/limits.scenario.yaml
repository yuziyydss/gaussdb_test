schema_version: 1
kind: scenario
id: scenario_create_index_limits
name: 索引列数、元组长度和内存边界
status: planned
description: 验证 31/32 列、2696/2688 字节、SQL 长度 WARNING、rowid/rowno 与 XML 禁止项。
factor_ref: create_index
fixture_refs: []
fact_refs: [ci_fact_key_column_limits, ci_fact_index_size, ci_fact_sql_length_warning, ci_fact_system_column_restriction, ci_fact_xml_restriction]
preconditions: [自动生成宽表和宽值, XML 与系统列目标, 超长 CREATE INDEX 文本]
steps: [各形态边界列数, ASTORE/USTORE 宽键, INCLUDE 导致超宽, XML/系统列, 5250 字符边界]
oracles: [边界成功, 超限目标错误, 禁止项错误, WARNING 类别]
execution_requirements: [宽表 Fixture 生成器, WARNING 捕获]
