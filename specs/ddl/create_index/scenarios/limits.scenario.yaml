schema_version: 1
kind: scenario
id: scenario_create_index_limits
name: 索引列数、元组长度和内存边界
status: planned
description: 验证 31/32、28/29 列边界、2613/2617 字节边界和 maintenance_work_mem。
factor_ref: create_index
fixture_refs: []
fact_refs: [ci_fact_key_column_limits, ci_fact_concurrent_column_limits, ci_fact_index_size, ci_fact_optimization_limits]
preconditions: [自动生成宽表和宽值, 可调整 maintenance_work_mem]
steps: [各形态边界列数, NULL/非 NULL 宽键, INCLUDE 导致超宽, 不同内存上限创建]
oracles: [边界成功, 超限目标错误, 资源使用]
execution_requirements: [宽表 Fixture 生成器, GUC 权限]
