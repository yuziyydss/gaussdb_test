schema_version: 1
kind: scenario
id: scenario_create_index_partition_metadata
name: LOCAL/GLOBAL 与索引分区元数据
status: planned
description: 验证显式 scope、索引分区一一对应、LOCAL/GLOBAL 互斥和二级分区待补能力。
factor_ref: create_index
fixture_refs: [fixture_create_index_partitioned]
fact_refs: [ci_fact_partition_selection, ci_fact_partition_scope, ci_fact_index_partition_clause, ci_fact_classified_index, ci_open_subpartition_fixture]
preconditions: [一级分区表, 后续补充二级分区表]
steps: [LOCAL, GLOBAL, 命名索引分区, 数量不匹配, FOR 分类索引, 自动扩展失败回滚]
oracles: [pg_partition 元数据, 目标数量错误, sparse 属性, 自治事务残留分区]
execution_requirements: [系统目录查询, 二级分区 Fixture 待实现]
