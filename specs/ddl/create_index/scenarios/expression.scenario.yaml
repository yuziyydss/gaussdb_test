schema_version: 1
kind: scenario
id: scenario_create_index_expression
name: 表达式索引函数与可用性
status: planned
description: 验证 immutable 要求、函数调用括号省略、IS NULL 无效和执行计划使用表达式索引。
factor_ref: create_index
fixture_refs: [fixture_create_index_regular]
fact_refs: [ci_fact_immutable, ci_fact_expression, ci_fact_expression_permissions, ci_fact_plan_change]
preconditions: [创建 immutable/stable/volatile 函数, 准备匹配 upper/lower 查询]
steps: [创建各类函数索引, IS NULL 表达式, EXPLAIN 匹配查询]
oracles: [目标函数易变性错误, 索引有效状态, 计划是否使用索引]
execution_requirements: [创建函数权限, ANALYZE, EXPLAIN]

