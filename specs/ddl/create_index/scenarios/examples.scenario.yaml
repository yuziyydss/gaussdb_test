schema_version: 1
kind: scenario
id: scenario_create_index_examples
name: V10 CREATE INDEX 示例账本回放
status: planned
description: 将文档普通、唯一、表达式、部分、分区、UGIN、GIN、GiST 示例作为独立回放单元，不反推未声明规则。
factor_ref: create_index
fixture_refs: [fixture_create_index_astore_regular, fixture_create_index_partitioned, fixture_create_index_ugin_text, fixture_create_index_ugin_array, fixture_create_index_gin_array, fixture_create_index_gist_point]
fact_refs: [ci_fact_leftmost_prefix, ci_fact_example_basic, ci_fact_example_unique, ci_fact_example_expression, ci_fact_example_partial, ci_fact_example_partition, ci_fact_example_ugin, ci_fact_example_gin, ci_fact_example_gist]
preconditions: [按示例逐个创建干净对象]
steps: [逐段回放 CREATE/SELECT/DROP, 对比示例结果]
oracles: [DDL 成功, 查询结果, pg_indexes/pg_partition 元数据]
execution_requirements: [隔离 Schema, 示例命令归一化]
