schema_version: 1
kind: scenario
id: scenario_create_index_comment
name: B 模式索引 COMMENT 行为
status: planned
description: 验证 COMMENT 只在 B 兼容模式生效，并覆盖 1024 字符边界和元数据读取。
factor_ref: create_index
fixture_refs: [fixture_create_index_astore_regular]
fact_refs: [ci_fact_comment]
preconditions: [独立 B 兼容模式数据库, 准备普通 ASTORE 表]
steps: [创建带短注释索引, 创建 1024 字符注释索引, 创建超长注释索引, 查询索引注释元数据]
oracles: [合法注释创建成功, 目录注释内容一致, 超长注释返回目标错误类别]
execution_requirements: [B 兼容模式, 系统目录查询]
