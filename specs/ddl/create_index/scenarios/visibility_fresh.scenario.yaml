schema_version: 1
kind: scenario
id: scenario_create_index_visibility_fresh
name: 独立A模式可见性候选的元数据校准
status: planned
description: 由manifest维度选择器唯一绑定实际CREATE候选；目录字段与计划使用合同未校准，不能以CREATE成功或空表计划证明可见性行为。
factor_ref: create_index
fixture_refs: [fixture_create_index_visibility_fresh]
fact_refs: [ci_fact_visible_definition, ci_fact_invisible_definition, ci_fact_visible_mode, ci_fact_invisible_mode, ci_fact_visible_keyword, ci_fact_invisible_keyword, ci_fact_visible_upgrade, ci_fact_invisible_upgrade]
preconditions: [A模式数据库, disable_keyword_options为空, 非升级中, 独占fresh用户schema]
steps:
- id: visible
  candidate:
    manifest_ref: manifest_create_index_visibility_a_fresh
    params: {visibility_clause: ci_visibility_visible_a_fresh}
- id: invisible
  candidate:
    manifest_ref: manifest_create_index_visibility_a_fresh
    params: {visibility_clause: ci_visibility_invisible_a_fresh}
oracles:
- {kind: manual_assertion, step_id: visible, expected: "校准此版本元数据接口后确认此步骤实际候选索引为可见；未选择目录列名，不以索引存在或查询计划代替可见性验证。"}
- {kind: manual_assertion, step_id: invisible, expected: "校准此版本元数据接口后确认此步骤实际候选索引为不可见；未选择目录列名，不以索引存在或查询计划代替可见性验证。"}
execution_requirements: [database_authorization, target_oracle_calibration, case_owned_cleanup]
