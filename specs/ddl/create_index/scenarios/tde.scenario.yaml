schema_version: 1
kind: scenario
id: scenario_create_index_tde
name: TDE 加密索引参数
status: planned
description: 配置密钥服务和加密基表后验证 enable_tde 及自动复制参数。
factor_ref: create_index
fixture_refs: []
fact_refs: [ci_fact_tde_parameters, ci_open_tde_fixture]
preconditions: [enable_tde GUC, tde_key_info, 可用密钥服务, enable_tde 基表]
steps: [btree enable_tde on, 非 btree 尝试, 主动设置 encrypt_algo/dek_cipher/key_type/cmk_id]
oracles: [索引加密属性, 自动生成/复制字段, 目标参数错误]
execution_requirements: [密钥服务, 安全配置, 加密基表 Fixture]

