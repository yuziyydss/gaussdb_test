schema_version: 1
kind: scenario
id: scenario_create_index_method_contracts
name: 索引方法、类型与存储引擎契约
status: planned
description: 验证 default/btree/ubtree 自动映射及 UGIN/GIN/GiST 的类型、opclass、扫描方式与限制。
factor_ref: create_index
fixture_refs: [fixture_create_index_astore_regular, fixture_create_index_ustore_regular, fixture_create_index_ugin_text, fixture_create_index_ugin_array, fixture_create_index_gin_array, fixture_create_index_gist_point]
fact_refs: [ci_fact_method_catalog, ci_fact_method_storage_contract, ci_fact_ugin_capability, ci_fact_ugin_opclass, ci_fact_gin_capability, ci_fact_gist_capability, ci_open_key_profile_domain_completion]
preconditions: [ASTORE/USTORE 表, 专用列类型, ANALYZE]
steps: [创建五种方法, 交叉错误引擎, 执行文档操作符查询, EXPLAIN, 补齐并验证文档列出的类型与 opclass 代表域]
oracles: [目录访问方法, 自动转换结果, 目标错误类别, Bitmap/Index Scan 形态]
execution_requirements: [方法扩展可用, 系统目录查询, EXPLAIN]
