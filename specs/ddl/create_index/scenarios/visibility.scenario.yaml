schema_version: 1
kind: scenario
id: scenario_create_index_visibility
name: 索引可见性与关键字禁用
status: planned
description: 验证 VISIBLE/INVISIBLE 元数据、查询计划以及 disable_keyword_options。
factor_ref: create_index
fixture_refs: [fixture_create_index_regular]
fact_refs: [ci_fact_visibility, ci_fact_plan_change]
preconditions: [可修改 disable_keyword_options, 具有可选择计划的测试数据]
steps: [默认可见, VISIBLE, INVISIBLE, 分别禁用两个关键字, EXPLAIN]
oracles: [可见性目录字段, 关键字错误, 计划变化]
execution_requirements: [GUC 权限, 系统目录查询, EXPLAIN]

