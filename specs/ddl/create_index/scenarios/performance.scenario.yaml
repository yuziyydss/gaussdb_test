schema_version: 1
kind: scenario
id: scenario_create_index_performance
name: 索引收益、写入成本与在线创建性能
status: planned
description: 将文档建议作为基准场景，不把性能数字当成静态 SQL 成功 Oracle。
factor_ref: create_index
fixture_refs: [fixture_create_index_regular]
fact_refs: [ci_fact_usage_guidance, ci_fact_plan_change, ci_fact_concurrent_performance, ci_fact_include]
preconditions: [大数据集, 高频过滤/连接/排序查询, 并发写入负载]
steps: [无索引基线, 普通/组合/INCLUDE 索引, 离线与在线创建, 长事务和高 TPS]
oracles: [执行计划, 查询延迟, 导入吞吐, I/O/CPU, 创建耗时]
execution_requirements: [独立性能环境, 监控, 稳定负载]

