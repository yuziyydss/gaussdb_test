schema_version: 1
kind: scenario
id: scenario_create_index_storage_contracts
name: WITH 参数适用性与元数据
status: planned
description: 验证 storage_type、fillfactor、FASTUPDATE、pending limit、INDEXSPLIT、INDEX_TXNTYPE、ACTIVE_PAGES、DEDUPLICATION、STAT_STATE、LPI 与 buffering。
factor_ref: create_index
fixture_refs: [fixture_create_index_astore_regular, fixture_create_index_ustore_regular, fixture_create_index_partitioned, fixture_create_index_gin_array, fixture_create_index_gist_point]
fact_refs: [ci_fact_storage_parameters, ci_fact_storage_type, ci_fact_fillfactor, ci_fact_fastupdate, ci_fact_gin_pending_limit, ci_fact_indexsplit, ci_fact_index_txntype, ci_fact_active_pages, ci_fact_deduplication, ci_fact_stat_state, ci_fact_lpi_parallel, ci_fact_gist_buffering, ci_fact_ilm, ci_open_storage_profile_domain_completion, "create_table::ct_fact_storage_type_default"]
preconditions: [对应存储引擎与索引方法, 可查询 reloptions]
steps: [合法边界, 方法错配, 非法边界, VACUUM与ANALYZE后的属性, 补齐 fastupdate/INDEXSPLIT/INDEX_TXNTYPE/STAT_STATE/LPI/buffering/ILM 全部文档值]
oracles: [成功或目标参数错误, reloptions, 自动修正结果, 统计锁定状态]
execution_requirements: [系统目录查询, VACUUM, ANALYZE]
