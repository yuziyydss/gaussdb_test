schema_version: 1
kind: scenario
id: scenario_create_index_online_failure_cleanup
name: 在线创建失败与残留清理
status: planned
description: 在三个阶段注入取消、重复键、资源不足或锁超时，观察 cctmp 和 NOT VALID 索引。
factor_ref: create_index
fixture_refs: [fixture_create_index_regular]
fact_refs: [ci_fact_concurrent_lifecycle]
preconditions: [大表, 并发 DML, 可控制 delete_cctmp_table, 可取消会话或注入失败]
steps: [各阶段取消, 重启与不重启, 自动清理失败, 手动 DROP]
oracles: [cctmp 是否保留, indisvalid/ready 状态, DML 是否维护残留索引]
execution_requirements: [多会话, 故障注入, 重启权限, 系统目录查询]

