schema_version: 1
kind: scenario
id: scenario_create_index_partition_default_scope
name: 分区索引默认 LOCAL/GLOBAL 冲突验证
status: planned
description: 解决原文 L13 默认 GLOBAL 与 L81 默认 LOCAL 的直接矛盾。
factor_ref: create_index
fixture_refs: [fixture_create_index_partitioned]
fact_refs: [ci_fact_partition_selection, ci_open_partition_default_scope]
preconditions: [按目标产品版本创建两分区表]
steps: [省略 LOCAL/GLOBAL 创建非唯一索引, 创建包含分区键的 UNIQUE, 创建不包含分区键的 UNIQUE]
oracles: [系统目录中的实际 scope, 索引分区数量]
execution_requirements: [版本标识, 系统目录查询]

