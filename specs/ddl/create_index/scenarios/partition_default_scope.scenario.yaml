schema_version: 1
kind: scenario
id: scenario_create_index_partition_default_scope
name: 分区索引默认 GLOBAL 验证
status: planned
description: V10 原文功能描述与 GLOBAL 参数说明一致声明省略 scope 默认 GLOBAL。
factor_ref: create_index
fixture_refs: [fixture_create_index_partitioned]
fact_refs: [ci_fact_partition_selection, ci_fact_partition_scope, ci_open_unique_local_missing_partition_key]
preconditions: [按目标产品版本创建两分区表]
steps: [省略 LOCAL/GLOBAL 创建非唯一索引, 创建包含分区键的 UNIQUE, 创建不包含分区键的 UNIQUE, 显式 LOCAL 创建不包含分区键的 UNIQUE]
oracles: [系统目录中的实际 scope, 索引分区数量, 显式 LOCAL 与 UNIQUE 自动选型冲突时的实际结果或目标错误类别]
execution_requirements: [版本标识, 系统目录查询]
