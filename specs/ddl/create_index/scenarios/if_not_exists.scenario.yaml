schema_version: 1
kind: scenario
id: scenario_create_index_if_not_exists
name: CREATE INDEX IF NOT EXISTS 生命周期
status: planned
description: 预先创建同名索引或其他 relation，比较 NOTICE 和 ERROR。
factor_ref: create_index
fixture_refs: [fixture_create_index_astore_regular]
fact_refs: [ci_fact_if_not_exists]
preconditions: [当前 Schema 已存在同名索引, 当前 Schema 已存在其他同名 relation]
steps: [带 IF NOT EXISTS 创建, 不带 IF NOT EXISTS 创建]
oracles: [NOTICE 且不新建, 目标同名错误]
execution_requirements: [捕获 NOTICE, 对象生命周期]
