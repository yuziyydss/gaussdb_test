schema_version: 1
kind: scenario
id: scenario_create_index_ustore_local_fresh
name: USTORE LOCAL实际引擎与分区目录校准
status: planned
description: 空表只建立有限能力基线，不等于ACTIVE_PAGES手设或VACUUM/ANALYZE行为覆盖。
factor_ref: create_index
fixture_refs: [fixture_create_index_ustore_local_fresh]
fact_refs: [ci_fact_partition_scope, ci_fact_method_storage_contract, ci_fact_active_pages]
preconditions: [独占fresh用户Schema, 实际track_counts与track_activities开启, 已核本case对象归属与清理依赖]
steps:
  - id: local
    candidate: {manifest_ref: manifest_create_index_ustore_local_fresh, params: {}}
oracles:
  - {kind: manual_assertion, step_id: local, expected: 校准实际版本目录后核表为USTORE、索引为UBTree普通LOCAL且覆盖p1与p2；自动索引分区名不可预猜。此结果不证明统计页数、更新或查询路由。}
execution_requirements: [database_authorization, target_oracle_calibration, case_owned_cleanup]
