schema_version: 1
kind: scenario
id: scenario_create_index_unique_behavior
name: UNIQUE 索引写入行为
status: planned
description: 创建 UNIQUE 索引后验证 INSERT/UPDATE 重复值拒绝及 INCLUDE 列不参与唯一性。
factor_ref: create_index
fixture_refs: [fixture_create_index_regular]
fact_refs: [ci_fact_unique, ci_fact_include]
preconditions: [创建唯一索引和带 INCLUDE 的唯一索引]
steps: [插入重复键, 更新为重复键, 仅修改 INCLUDE 列]
oracles: [唯一约束错误, INCLUDE 列差异不触发唯一冲突]
execution_requirements: [DML 执行, 目标错误 Oracle]

