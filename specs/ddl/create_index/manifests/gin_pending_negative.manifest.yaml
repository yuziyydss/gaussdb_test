schema_version: 1
kind: manifest
id: manifest_create_index_gin_pending_negative
name: manifest_create_index_gin_pending_negative
status: needs_review
description: 有限倒排索引参数代表；真实前置与目标错误分开，数据库未执行。 A是本批测试环境选择；不将UGIN的非M限制推广成GIN自身限制。
factor_ref: create_index
syntax_ref: syntax_create_index_v1
suite_type: negative
strategy: pairwise
fixture_refs: []
identifier_policy:
  index_name:
    generator: deterministic
    prefix: idx_ci_gin_pending_negative
    unique_per_case: true
bindings:
  statement_form:
  - ci_statement_regular
  index_name_presence:
  - ci_index_name_present
  table_profile:
  - ci_table_gin_storage
  method:
  - ci_method_gin
  key_profile:
  - ci_key_gin_array
  storage_profile:
  - ci_gin_pending_63
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: gin_pending_list_limit_out_of_range
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - A
  fact_refs:
  - create_database::create_database_fact_compatibility_environment
- key: actor_authority
  allowed_values:
  - create_any_index
  fact_refs:
  - ci_fact_permissions
- key: case_namespace
  allowed_values:
  - isolated_user_schema
  fact_refs:
  - ci_fact_permissions
violates_rule_refs:
- ci_rule_storage_parameter_domain
