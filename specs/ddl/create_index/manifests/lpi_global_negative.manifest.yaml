schema_version: 1
kind: manifest
id: manifest_create_index_lpi_global_negative
name: CREATE INDEX GLOBAL 使用 LPI 负向
status: needs_review
description: LPI_PARALLEL_METHOD 仅 ASTORE BTREE LOCAL 非在线分区索引。
factor_ref: create_index
syntax_ref: syntax_create_index_v1
suite_type: negative
strategy: pairwise
fixture_refs: []
identifier_policy: {index_name: {generator: deterministic, prefix: idx_ci_lpi_bad, unique_per_case: true}}
bindings:
  statement_form: [ci_statement_partition]
  index_name_presence: [ci_index_name_present]
  table_profile: [ci_table_astore_partitioned]
  method: [ci_method_default]
  key_profile: [ci_key_id]
  scope_clause: [ci_scope_global]
  storage_profile: [ci_lpi_partition]
violates_rule_refs: [ci_rule_lpi_shape_supported]
expected: {default: error, scope: syntax_and_semantics, oracle_status: needs_verification, error_category: lpi_parallel_method_not_local}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
