schema_version: 1
kind: manifest
id: manifest_create_index_lpi_global_negative
name: CREATE INDEX GLOBAL 使用 LPI 参数负向
status: ready
description: 验证 LPI_PARALLEL_METHOD 仅支持 LOCAL 分区索引。
factor_ref: create_index
syntax_ref: syntax_create_index_v1
suite_type: negative
strategy: pairwise
fixture_refs: []
identifier_policy:
  index_name: {generator: deterministic, prefix: idx_ci_lpi_global, unique_per_case: true}
bindings:
  statement_form: [ci_statement_partition]
  index_name_presence: [ci_index_name_present]
  table_profile: [ci_table_partitioned]
  key_profile: [ci_key_id]
  scope_clause: [ci_scope_global]
  storage_profile: [ci_lpi_partition]
violates_rule_refs: [ci_rule_lpi_shape_supported]
expected:
  default: error
  scope: syntax_and_semantics
  error_category: lpi_parallel_method_not_local
  error_message_regex: "(?i)(lpi_parallel_method|local|global|not support)"
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}

