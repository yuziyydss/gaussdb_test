schema_version: 1
kind: manifest
id: manifest_create_index_comment_b_fresh
name: B模式短ASCII索引注释
status: needs_review
description: 只验证普通ASTORE整数BTREE行内短COMMENT语法，独立B门；不覆盖1024边界、Unicode、重复COMMENT或元数据结果。
factor_ref: create_index
syntax_ref: syntax_create_index_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {index_name: {generator: deterministic, prefix: idx_ci_comment, unique_per_case: true}}
bindings:
  statement_form: [ci_statement_regular]
  index_name_presence: [ci_index_name_present]
  table_profile: [ci_table_comment_fresh]
  method: [ci_method_btree]
  key_profile: [ci_key_id]
  comment_clause: [ci_comment_short_b]
environment_requirements:
- {key: compatibility_mode, allowed_values: [B], fact_refs: [ci_fact_comment_b_mode]}
- {key: actor_authority, allowed_values: [create_any_index], fact_refs: [ci_fact_permissions]}
- {key: case_namespace, allowed_values: [isolated_user_schema], fact_refs: [ci_fact_permissions]}
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
