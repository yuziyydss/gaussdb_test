schema_version: 1
kind: manifest
id: manifest_create_index_online_expression_negative
name: CREATE INDEX CONCURRENTLY 表达式索引负向
status: ready
description: 只违反在线创建不支持表达式索引的规则。
factor_ref: create_index
syntax_ref: syntax_create_index_v1
suite_type: negative
strategy: pairwise
fixture_refs: []
identifier_policy:
  index_name: {generator: deterministic, prefix: idx_ci_online_expr, unique_per_case: true}
bindings:
  statement_form: [ci_statement_regular]
  concurrently: [ci_concurrently]
  index_name_presence: [ci_index_name_present]
  table_profile: [ci_table_regular]
  key_profile: [ci_key_expression_lower_name]
violates_rule_refs: [ci_rule_online_shape_supported]
expected:
  default: error
  scope: syntax_and_semantics
  error_category: online_expression_index_not_supported
  error_message_regex: "(?i)(concurrently|expression|online|not support)"
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}

