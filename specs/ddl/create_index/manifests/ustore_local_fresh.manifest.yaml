schema_version: 1
kind: manifest
id: manifest_create_index_ustore_local_fresh
name: USTORE RANGE UBTree LOCAL有限能力代表
status: needs_review
description: 实际USTORE单键两分区与自动LOCAL索引；不手设ACTIVE_PAGES，不证明统计更新、数据路由或执行计划。
factor_ref: create_index
syntax_ref: syntax_create_index_v1
suite_type: positive
strategy: full_cartesian
fixture_refs: []
identifier_policy: {index_name: {generator: deterministic, prefix: idx_ci_ul, unique_per_case: true}}
bindings:
  statement_form: [ci_statement_partition]
  index_name_presence: [ci_index_name_present]
  table_profile: [ci_table_ustore_local_fresh]
  method: [ci_method_ubtree]
  key_profile: [ci_key_id]
  scope_clause: [ci_scope_local]
environment_requirements:
  - {key: actor_authority, allowed_values: [create_any_index], fact_refs: [ci_fact_permissions]}
  - {key: case_namespace, allowed_values: [isolated_user_schema], fact_refs: [ci_fact_permissions]}
  - {key: table_creation_authority, allowed_values: [create_any_table], fact_refs: ['create_table::ct_fact_create_permissions']}
  - {key: track_counts, allowed_values: ['on'], fact_refs: ['create_table::ct_fact_ustore_tracking_prerequisites']}
  - {key: track_activities, allowed_values: ['on'], fact_refs: ['create_table::ct_fact_ustore_tracking_prerequisites']}
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
