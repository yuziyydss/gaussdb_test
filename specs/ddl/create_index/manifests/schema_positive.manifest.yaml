schema_version: 1
kind: manifest
id: manifest_create_index_schema_positive
name: CREATE INDEX 模式限定索引名
status: ready
description: 独立验证 schema.index_name 渲染与索引模式归属。
factor_ref: create_index
syntax_ref: syntax_create_index_v1
suite_type: positive
strategy: pairwise
fixture_refs: [fixture_create_index_schema_qualified]
identifier_policy: {index_name: {generator: deterministic, prefix: idx_ci_schema, schema_prefix: ci_target_schema, unique_per_case: true}}
bindings:
  statement_form: [ci_statement_regular]
  index_name_presence: [ci_index_name_present]
  table_profile: [ci_table_schema_astore]
  key_profile: [ci_key_id]
expected: {default: success, scope: syntax_and_semantics}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
