schema_version: 1
kind: manifest
id: manifest_create_index_schema_positive
name: CREATE INDEX 模式限定索引名
status: ready
description: 单独验证 schema.index_name 渲染，避免在主 Pairwise 中混淆 identifier policy。
factor_ref: create_index
syntax_ref: syntax_create_index_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy:
  index_name: {generator: deterministic, prefix: idx_ci_schema, schema_prefix: public, unique_per_case: true}
bindings:
  statement_form: [ci_statement_regular]
  index_name_presence: [ci_index_name_present]
  table_profile: [ci_table_regular]
  key_profile: [ci_key_id]
expected: {default: success, scope: syntax_and_semantics}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}

