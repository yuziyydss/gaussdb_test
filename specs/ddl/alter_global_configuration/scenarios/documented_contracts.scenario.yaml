schema_version: 1
kind: "scenario"
id: "scenario_alter_global_configuration_documented_contracts"
name: "ALTER GLOBAL CONFIGURATION documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_global_configuration"
fixture_refs: []
fact_refs:
  - "alter_global_configuration_fact_upsert"
  - "alter_global_configuration_fact_initial_user"
  - "alter_global_configuration_fact_protected_names"
  - "alter_global_configuration_fact_name_domain"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "gs_global_config中键已存在则修改，不存在则新增。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅数据库初始用户可以运行。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "不得修改weak_password或undostoragetype。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "参数名为text，除两个受保护名称外没有限制。"
oracles:
  -
    kind: "documented_behavior"
    expected: "gs_global_config中键已存在则修改，不存在则新增。"
execution_requirements:
  - "database"
  - "same_session"
