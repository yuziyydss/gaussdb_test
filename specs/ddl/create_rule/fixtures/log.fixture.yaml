schema_version: 1
kind: "fixture"
id: "fixture_create_rule_log"
name: "CREATE RULE log"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_rule"
requires_fixture_refs: []
provides:
  tables:
    -
      name: "t_rule_log"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: true
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TABLE t_rule_log (col_1 INTEGER, col_2 INTEGER) WITH (STORAGE_TYPE=ASTORE, segment=off);"
  teardown_sqls:
    - "DROP TABLE IF EXISTS t_rule_log CASCADE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
