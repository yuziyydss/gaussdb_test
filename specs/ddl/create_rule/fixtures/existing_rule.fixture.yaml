schema_version: 1
kind: "fixture"
id: "fixture_create_rule_existing_rule"
name: "CREATE RULE existing_rule"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_rule"
requires_fixture_refs:
  - "fixture_create_rule_source"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE RULE fp_rule_ready AS ON INSERT TO t_rule_source DO ALSO NOTHING;"
  teardown_sqls:
    - "DROP RULE IF EXISTS fp_rule_ready ON t_rule_source;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
