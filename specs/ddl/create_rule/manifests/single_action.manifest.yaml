schema_version: 1
kind: "manifest"
id: "manifest_create_rule_single_action"
name: "CREATE RULE single_action"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "create_rule"
syntax_ref: "syntax_create_rule"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_create_rule_source"
  - "fixture_create_rule_log"
identifier_policy: {}
bindings:
  event:
    - "create_rule_event_insert"
    - "create_rule_event_update"
    - "create_rule_event_delete"
  condition:
    - "create_rule_condition_none"
    - "create_rule_condition_true"
    - "create_rule_condition_false"
  behavior:
    - "create_rule_behavior_default"
    - "create_rule_behavior_also"
    - "create_rule_behavior_instead"
  action_form:
    - "create_rule_action_form_single"
  actions:
    - "create_rule_actions_one"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
