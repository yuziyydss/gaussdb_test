schema_version: 1
kind: "scenario"
id: "scenario_create_operator_class_documented_contracts"
name: "CREATE OPERATOR CLASS documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "create_operator_class"
fixture_refs: []
fact_refs:
  - "create_operator_class_fact_purpose"
  - "create_operator_class_fact_contract"
  - "create_operator_class_fact_internal"
  - "create_operator_class_fact_unique"
  - "create_operator_class_fact_privilege"
  - "create_operator_class_fact_unchecked"
  - "create_operator_class_fact_default"
  - "create_operator_class_fact_family"
  - "create_operator_class_fact_function_types"
  - "create_operator_class_fact_storage"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "创建新操作符类。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "操作符类规定数据类型的索引策略及支持函数，使用的函数和操作符必须先定义。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "内部功能，不建议用户直接使用。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "同模式操作符类可重名但必须用于不同索引方法。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "创建者为属主，当前系统管理员可创建。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "CREATE不检查是否包含全部索引所需操作符/函数，也不检查它们是否自包含。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "DEFAULT指定默认类，同类型和访问方法最多一个。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "指定FAMILY加入已有族；省略时使用同名族，不存在则自动创建。"
  -
    id: "contract_8"
    action: "verify_documented_contract"
    note: "函数操作数类型不同于输入类型或类数据类型时须显式指定支持的操作数类型。"
  -
    id: "contract_9"
    action: "verify_documented_contract"
    note: "STORAGE指定实际索引存储类型，仅索引方法允许不同类型时才能指定。"
oracles:
  -
    kind: "documented_behavior"
    expected: "CREATE不检查是否包含全部索引所需操作符/函数，也不检查它们是否自包含。"
execution_requirements:
  - "database"
  - "same_session"
