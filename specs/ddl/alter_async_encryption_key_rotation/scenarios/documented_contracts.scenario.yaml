schema_version: 1
kind: "scenario"
id: "scenario_alter_async_encryption_key_rotation_documented_contracts"
name: "ALTER ASYNC ENCRYPTION KEY ROTATION documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_async_encryption_key_rotation"
fixture_refs: []
fact_refs:
  - "alter_async_encryption_key_rotation_fact_purpose"
  - "alter_async_encryption_key_rotation_fact_switches"
  - "alter_async_encryption_key_rotation_fact_privilege"
  - "alter_async_encryption_key_rotation_fact_no_m"
  - "alter_async_encryption_key_rotation_fact_no_audit"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "轮转透明数据异步加密密钥，不等于客户端CMK/CEK轮转。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "enable_tde和enable_tde_async_encryption必须同时开启。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "仅SYSADMIN有权限。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "M-Compatibility不支持。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "不支持记录统一审计日志。"
oracles:
  -
    kind: "documented_behavior"
    expected: "不支持记录统一审计日志。"
execution_requirements:
  - "database"
  - "same_session"
