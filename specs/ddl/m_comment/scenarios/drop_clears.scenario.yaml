schema_version: 1
kind: scenario
id: scenario_m_comment_drop_clears
name: scenario_m_comment_drop_clears
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_comment
fact_refs:
- m_comment_fact_drop
fixture_refs:
- fixture_m_comment_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 记录本 case 表 OID、注释，删除表后检查注释记录不存在
oracles:
- kind: manual_assertion
  expected: 只检查本 case OID，不扫描删除其他对象注释
execution_requirements:
- database_authorization
- target_oracle_calibration
