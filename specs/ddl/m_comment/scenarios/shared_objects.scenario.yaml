schema_version: 1
kind: scenario
id: scenario_m_comment_shared_objects
name: scenario_m_comment_shared_objects
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_comment
fact_refs:
- m_comment_fact_visible
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 数据库/角色/表空间等共享对象必须独立授权并隔离；不在本批默认清单生成
oracles:
- kind: manual_assertion
  expected: 验证可见性；不在注释中保存密钥和身份凭据
execution_requirements:
- database_authorization
- target_oracle_calibration
