schema_version: 1
kind: scenario
id: scenario_m_comment_replace_and_clear
name: scenario_m_comment_replace_and_clear
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_comment
fact_refs:
- m_comment_fact_replace
- m_comment_fact_null_token
fixture_refs:
- fixture_m_comment_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: COMMENT ON TABLE m_b01_source IS 'first';
- sql: COMMENT ON TABLE m_b01_source IS 'second';
- sql: COMMENT ON TABLE m_b01_source IS NULL;
oracles:
- kind: manual_assertion
  expected: 依次检查唯一注释为 first、second、无注释；目录 Oracle 待接入
execution_requirements:
- database_authorization
- target_oracle_calibration
