schema_version: 1
kind: "manifest"
id: "manifest_alter_trigger_rename"
name: "ALTER TRIGGER rename"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_trigger"
syntax_ref: "syntax_alter_trigger"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_trigger_existing"
identifier_policy: {}
bindings:
  new_name:
    - "alter_trigger_new_name_short"
    - "alter_trigger_new_name_max"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
