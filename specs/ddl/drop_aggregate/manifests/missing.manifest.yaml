schema_version: 1
kind: "manifest"
id: "manifest_drop_aggregate_missing"
name: "DROP AGGREGATE missing"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_aggregate"
syntax_ref: "syntax_drop_aggregate"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_aggregate_aggregate"
identifier_policy: {}
bindings:
  target:
    - "drop_aggregate_target_missing"
  if_exists:
    - "drop_aggregate_if_exists_yes"
  behavior:
    - "drop_aggregate_behavior_none"
    - "drop_aggregate_behavior_restrict"
    - "drop_aggregate_behavior_cascade"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
