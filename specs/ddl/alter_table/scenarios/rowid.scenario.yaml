schema_version: 1
kind: scenario
id: scenario_alter_table_rowid
name: SET WITH ROWID 能力矩阵
status: planned
description: 在 A 模式验证系统列、隐式序列索引、对象限制和压缩数据解压。
factor_ref: alter_table
fixture_refs: []
fact_refs: [at_fact_rowid, at_open_a_compat_fixture]
preconditions: [A 模式, 普通/临时/unlogged/外表, 冲突列名和存储参数]
steps: [SET WITH ROWID, 受限对象执行, 冲突配置执行]
oracles: [rowid/rowno 元数据, 隐式对象, 目标错误类别]
execution_requirements: [A 兼容数据库, 多对象 Fixture]

