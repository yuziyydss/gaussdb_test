schema_version: 1
kind: scenario
id: scenario_alter_table_partition_tablespace_restriction
name: 分区表 SET TABLESPACE 单故障验证
status: planned
description: 使用真实存在且不同于当前表空间的可回收表空间，验证分区表的表级 SET TABLESPACE 限制；在 Fixture 完成前不生成可执行负向 SQL。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_partitioned]
fact_refs: [at_fact_partition_restrictions, at_fact_tablespace, at_open_tablespace_fixture]
preconditions: [创建可回收的 at_partition_target_ts, 确认它与目标表当前表空间不同, 确认目标是分区表]
steps: [ALTER TABLE t_at_partitioned SET TABLESPACE at_partition_target_ts]
oracles: [命中分区表表级 TABLESPACE 限制的目标错误, 表与分区的表空间元数据未变, 无非目标前置错误]
execution_requirements: [可安全创建和删除表空间的目录, 表空间元数据查询, 目标错误 Oracle 校准]
