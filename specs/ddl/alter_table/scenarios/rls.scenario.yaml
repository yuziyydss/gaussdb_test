schema_version: 1
kind: scenario
id: scenario_alter_table_rls
name: ALTER TABLE 行访问控制开关行为
status: planned
description: 验证 ENABLE/DISABLE 与 FORCE/NO FORCE 对策略、表所有者、普通用户、临时表和外表的行为差异。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular, fixture_alter_table_temporary]
fact_refs: [at_fact_rls, at_open_external_table_fixture]
preconditions: [普通表及策略, 表所有者与普通用户, 临时表, 外表能力待补]
steps: [逐种切换 RLS 开关, 以不同角色查询, 对禁止对象尝试开启]
oracles: [可见行集合, 表所有者是否受 FORCE 影响, 禁止对象的目标错误类别]
execution_requirements: [多角色与策略 Fixture, 外表 Fixture, 捕获目标错误身份]
