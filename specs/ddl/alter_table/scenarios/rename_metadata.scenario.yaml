schema_version: 1
kind: scenario
id: scenario_alter_table_rename_metadata
name: 表列约束重命名元数据
status: planned
description: 验证数据不变、依赖对象名称与 B 5.7 特殊表名行为。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs: [at_fact_rename, at_open_b_compat_fixture]
preconditions: [依赖视图和索引, B 5.7 变体]
steps: [重命名表, 列, 约束, 同名重命名, mysql50 前缀]
oracles: [目录名称, 数据, 依赖可用性]
execution_requirements: [目录查询, B 兼容数据库]

