schema_version: 1
kind: scenario
id: scenario_alter_table_rowid_off_fresh
name: 新普通空表ROWID转换目录验证
status: planned
description: 精确绑定off到on候选；原广泛ROWID场景保持未完成。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_rowid_off_fresh]
fact_refs: [at_fact_rowid, at_open_a_compat_fixture]
preconditions:
- 独立授权A数据库、同一对象创建主体、新用户Schema；实际确认普通空表、hasrowid=off且没有同名系统列/普通列冲突
- 记录setup成功回执，目标成功后记录系统列以及自动序列/索引身份；不得以provides普通列清单代替目录证据
steps:
- id: enable_rowid
  expected: success
  candidate: {manifest_ref: manifest_alter_table_rowid_off_fresh, params: {table_profile: at_table_rowid_off_fresh, action_profile: at_action_set_rowid_fresh}}
oracles:
- {kind: manual_assertion, step_id: enable_rowid, expected: 先校准系统目录查询；验证hasrowid由off到on、rowid/rowno系统列和隐式依赖身份；不是普通同名列，未猜系统列类型。}
execution_requirements: [database_authorization, rowid_catalog_before_after_calibration, owned_implicit_sequence_index_inventory, cleanup_receipts]
