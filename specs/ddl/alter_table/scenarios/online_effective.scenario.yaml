schema_version: 1
kind: scenario
id: scenario_alter_table_online_effective
name: 在线 DDL 有效路径
status: planned
description: ONLINE manifest 只声明语法形态和环境门禁；本场景验证类型修改、表空间移动和非空/检查/主键/唯一约束是否真正在线执行。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs: [at_fact_online_mode, at_fact_online_environment, at_fact_online_parameters, at_open_tablespace_fixture]
preconditions: [事务外的单语句执行, 非存储过程, 非 M 且支持 ONLINE 的环境, 非升级观察期, 并发 DML, 足够磁盘, 无长事务]
steps: [不同在线 action, parallel_threads, max_catchup_times]
oracles: [DML 可用性, NOTICE 缺失, 最终定义]
execution_requirements: [多会话, 锁监控, NOTICE 捕获]
