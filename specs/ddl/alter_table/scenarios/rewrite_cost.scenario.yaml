schema_version: 1
kind: scenario
id: scenario_alter_table_rewrite_cost
name: 表重写与空间成本
status: planned
description: 验证非空默认、类型转换、列位置和逻辑删列后的重写与空间回收。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs: [at_fact_add_column_default, at_fact_column_position, at_fact_drop_column, at_fact_alter_type, at_fact_tablespace]
preconditions: [大表, 二进制兼容与不兼容 cast, 监控磁盘]
steps: [新增非空默认, 类型转换, FIRST/AFTER, DROP COLUMN, VACUUM]
oracles: [文件重写, 临时空间, 统计清空, 空间回收]
execution_requirements: [性能隔离环境, 存储监控]
