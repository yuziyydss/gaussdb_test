schema_version: 1
kind: scenario
id: scenario_alter_table_identity_lifecycle
name: IDENTITY 属性、隐式序列与写入行为
status: planned
description: 验证已有数值列增加、修改和删除 IDENTITY 时的类型与 DEFAULT/NULL 前置条件、三种生成模式、隐式序列生命周期及序列选项。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs:
  - at_fact_identity_drop_lifecycle
  - at_fact_identity_modify_form
  - at_fact_identity_modify_preserves_mode
  - at_fact_identity_options_override
  - at_fact_identity_add_form
  - at_fact_identity_supported_types
  - at_fact_identity_a_mode_types
  - at_fact_identity_requires_no_default
  - at_fact_identity_sequence_name
  - at_fact_identity_default_not_null
  - at_fact_identity_existing_null_failure
  - at_fact_identity_implicit_sequence
  - at_fact_identity_always_input
  - at_fact_identity_by_default_input
  - at_fact_identity_on_null_input
  - at_fact_identity_increment
  - at_fact_identity_minvalue
  - at_fact_identity_maxvalue
  - at_fact_identity_start
  - at_fact_identity_cache
  - at_fact_identity_nocache
  - at_fact_identity_cycle
preconditions: [已有数值列与非数值列, 分别含 NULL 和无 NULL 的数据, 无 DEFAULT 与有 DEFAULT 的列, A 与非 A 兼容环境]
steps: [增加三种 IDENTITY 模式, 指定隐式序列名与各序列选项, 修改模式与选项, 插入用户值、NULL 和省略值, 删除 IDENTITY]
oracles: [目标错误类别, NOT NULL 元数据, 插入所得值, 原模式保留, 隐式序列名称与参数, 删除后的属性和序列生命周期]
execution_requirements: [兼容模式隔离, 系统目录元数据查询, 捕获目标错误身份]
