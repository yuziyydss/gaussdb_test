schema_version: 1
kind: scenario
id: scenario_alter_table_inheritance_scope
name: table_name 星号与 ONLY 继承作用域
status: planned
description: 当前 SQL 清单只覆盖四种目标语法形态；本场景使用父子表单独验证默认和星号包含后代、ONLY 排除后代的行为。
factor_ref: alter_table
fixture_refs: []
fact_refs: [at_fact_target_forms, at_fact_target_scope, at_open_inheritance_scope_fixture]
preconditions: [创建父表和具有可观察后代的子表, 每个目标形式使用独立干净 Fixture]
steps: [对 table_name 执行可观察变更, 对 table_name * 执行同类变更, 对 ONLY table_name 执行同类变更, 对 ONLY (table_name) 执行同类变更]
oracles: [父表定义变化, 默认与星号的子表变化, ONLY 两种形式的子表不变, 系统目录作用域]
execution_requirements: [父子表 Fixture, 干净 Schema, 元数据 Oracle]
