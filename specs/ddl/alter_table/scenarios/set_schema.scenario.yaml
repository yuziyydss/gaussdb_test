schema_version: 1
kind: scenario
id: scenario_alter_table_set_schema
name: SET SCHEMA 依赖与权限
status: planned
description: 验证索引约束随表移动、序列阻塞、系统模式拒绝及新模式权限。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs: [at_fact_set_schema, at_fact_permissions]
preconditions: [用户模式, 系统模式, owned sequence, 多用户权限]
steps: [移动普通表, 带序列表, 无 CREATE 权限, 系统模式]
oracles: [对象模式, 序列行为, 目标权限或环境错误]
execution_requirements: [Schema 与用户生命周期]

