schema_version: 1
kind: scenario
id: scenario_alter_table_replica_identity
name: REPLICA IDENTITY 解码行为
status: planned
description: 验证主键、无主键、唯一索引和 ustore 下旧元组组成。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs: [at_fact_replica_identity]
preconditions: [逻辑复制槽, 主键与无主键表, ustore 变体]
steps: [DEFAULT, USING INDEX, FULL, NOTHING, UNIQUE 后更新删除]
oracles: [解码旧元组字段, 回放确定性]
execution_requirements: [逻辑复制环境]

