schema_version: 1
kind: scenario
id: scenario_alter_table_online_downgrade
name: 在线 DDL 降级为离线
status: planned
description: 验证不支持 action、表形态、M 模式和升级观察期忽略 ONLINE 并返回 NOTICE。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular, fixture_alter_table_temporary]
fact_refs: [at_fact_online_mode, at_fact_online_environment, at_open_online_environment_fixture]
preconditions: [M 模式, 升级观察期, 临时或受限表, 不支持 action]
steps: [逐类执行 ONLINE]
oracles: [NOTICE, 实际离线锁行为, action 最终成功]
execution_requirements: [环境生命周期, 多会话]

