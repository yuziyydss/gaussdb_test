schema_version: 1
kind: scenario
id: scenario_alter_table_if_exists
name: ALTER TABLE IF EXISTS 生命周期
status: planned
description: 对存在和缺失表分别验证 ERROR、NOTICE 与零副作用。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs: [at_fact_if_exists]
preconditions: [存在表, 缺失表]
steps: [有无 IF EXISTS 执行相同 action]
oracles: [NOTICE 或目标关系不存在错误, 无对象变化]
execution_requirements: [捕获 NOTICE]

