schema_version: 1
kind: scenario
id: scenario_alter_table_triggers
name: 触发器启停与复制角色
status: planned
description: 验证单个、ALL、USER、REPLICA、ALWAYS 在 origin/local/replica 下的行为。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs: [at_fact_triggers]
preconditions: [用户触发器, 约束触发器, 日志表]
steps: [逐种启停, 切换 session_replication_role, 执行 DML]
oracles: [触发日志与约束完整性]
execution_requirements: [触发器 Fixture, 可修改会话参数]

