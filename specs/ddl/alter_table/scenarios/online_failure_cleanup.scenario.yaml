schema_version: 1
kind: scenario
id: scenario_alter_table_online_failure_cleanup
name: 在线 DDL 故障与残留清理
status: planned
description: 验证取消、二次取消、类型转换错误和严重故障后的临时 Schema 与 append_mode。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs: [at_fact_online_lifecycle, at_open_online_environment_fixture]
preconditions: [可注入取消和连接故障, 并发写入]
steps: [单次取消, 二次取消, 转换失败, 节点断连]
oracles: [online$$ Schema, 临时对象, append_mode, 手动恢复结果]
execution_requirements: [故障注入, 管理员目录权限]

