schema_version: 1
kind: scenario
id: scenario_alter_table_b_compatibility
name: B 模式扩展 action
status: planned
description: 验证 MODIFY、CHANGE、FIRST/AFTER、AUTO_INCREMENT、字符集和约束索引差异。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs: [at_fact_modify_column, at_fact_column_position, at_fact_auto_increment, at_fact_charset, at_fact_index_constraint_compat, at_fact_on_update, at_open_b_compat_fixture, at_open_index_method_source]
preconditions: [B 模式不同 format version/dev version, ASTORE/USTORE]
steps: [逐种扩展 action, 索引方法与排序, 字符集转换]
oracles: [列顺序与定义, 自增值, 实际索引方法, 字符编码]
execution_requirements: [数据库级兼容模式生命周期]

