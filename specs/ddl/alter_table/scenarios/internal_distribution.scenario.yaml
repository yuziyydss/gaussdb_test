schema_version: 1
kind: scenario
id: scenario_alter_table_internal_distribution
name: 内部扩缩容 action
status: planned
description: 仅在 support_extended_features 环境验证 GROUP/NODE/SLICE 等内部操作。
factor_ref: alter_table
fixture_refs: []
fact_refs: [at_fact_internal_distribution, at_open_internal_distribution_environment]
preconditions: [隔离集群, support_extended_features=on, 节点与 group 拓扑]
steps: [TO GROUP, TO NODE, ADD NODE, DELETE NODE, UPDATE SLICE]
oracles: [分布元数据, 非扩展模式目标错误]
execution_requirements: [内部扩缩容测试环境]
