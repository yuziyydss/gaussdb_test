schema_version: 1
kind: scenario
id: scenario_alter_table_add_column_data
name: ADD COLUMN 数据初始化与重写
status: planned
description: 验证 NULL、常量 DEFAULT、volatile、长度边界和已有行初始化。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs: [at_fact_add_column, at_fact_add_column_default, at_fact_serial_compat]
preconditions: [有数据普通表, PG 兼容数据库变体]
steps: [无默认新增, NULL 默认新增, 常量默认新增, volatile 默认新增, SERIAL 新增]
oracles: [已有行值, 表重写证据, 目标错误类别]
execution_requirements: [元数据与存储观测, 多兼容模式]

