schema_version: 1
kind: scenario
id: scenario_alter_table_permissions
name: ALTER TABLE 权限与三权分立
status: planned
description: 验证 Owner、ALTER、ALTER ANY TABLE、模式 CREATE 和新 Owner 成员关系。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs: [at_fact_permissions, at_fact_owner]
preconditions: [多角色, 三权分立开关, 模式权限矩阵]
steps: [普通 ALTER, OWNER TO, SET SCHEMA]
oracles: [成功或目标权限错误, Owner 与 Schema 元数据]
execution_requirements: [管理员权限, 多用户]

