schema_version: 1
kind: scenario
id: scenario_alter_table_log_foreign_rls
name: 外表ENABLE RLS目标错误校准
status: planned
description: 唯一绑定实际生成候选，区分前置失败、目标拒绝和清理失败；不猜SQLSTATE。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_log_foreign_rls]
fact_refs: [at_fact_rls]
preconditions: [核实实际物理兼容模式及版本, 非PDB且log_fdw目录能力可用, 本case成功创建服务器和schema外表并取得归属回执, 当前连接拥有ALTER权限]
steps:
- id: enable_foreign_rls
  candidate: {manifest_ref: manifest_alter_table_log_foreign_rls_negative, params: {action_profile: at_action_enable_rls}}
oracles:
- {kind: manual_assertion, step_id: enable_foreign_rls, expected: 校准unsupported_rls_target对应的目标错误及SQLSTATE；前置成功后目标ENABLE被拒绝，不能以任意错误替代。核实外表仍存在且未开启行访问控制，清理结果单独报告。}
execution_requirements: [database_authorization, target_oracle_calibration, case_owned_cleanup]
