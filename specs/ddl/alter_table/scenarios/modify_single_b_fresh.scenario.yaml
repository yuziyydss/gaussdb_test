schema_version: 1
kind: scenario
id: scenario_alter_table_modify_single_b_fresh
name: B单列MODIFY扩宽的原行与列定义校准
status: planned
description: 绑定实际候选；检查扩宽后ASCII和NULL保留以及列长度，不借成功执行证明依赖重建。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_modify_single_b_fresh]
fact_refs: [at_fact_modify_single_definition, at_fact_modify_single_switch]
preconditions: [实际B物理数据库, enable_modify_column选项启用, 本case成功创建无外部依赖的独占表]
steps:
  - id: widen
    candidate: {manifest_ref: manifest_alter_table_modify_single_b_fresh, params: {action_profile: at_action_modify_single_b_fresh}}
oracles:
  - {kind: manual_assertion, step_id: widen, expected: 校准目录接口后确认note为VARCHAR96且仍可空无默认；ORDER BY id结果为1/alpha和2/NULL。统计清空、依赖重建和其他转换不在本批验证范围。}
execution_requirements: [database_authorization, target_oracle_calibration, case_owned_cleanup]
