schema_version: 1
kind: scenario
id: scenario_alter_table_constraint_behavior
name: 约束增加验证删除与延迟
status: planned
description: 验证 NOT VALID、VALIDATE、USING INDEX、约束扫描、外键动作和延迟时机。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs: [at_fact_constraint_actions, at_fact_constraint_limit, at_fact_table_constraints, at_fact_foreign_key, at_fact_deferrable, at_fact_system_columns]
preconditions: [合法与违规数据, 引用表, 唯一索引, 事务]
steps: [增加约束, 验证, USING INDEX, 删除主键, 外键动作, 延迟提交]
oracles: [数据扫描结果, NOT NULL 保留, 引用行变化, 检查时机]
execution_requirements: [多表 Fixture, 事务控制]

