schema_version: 1
kind: scenario
id: scenario_alter_table_online_using_expression
name: 在线 ALTER TYPE USING 能力判定
status: planned
description: 判定带 USING expression 的 ALTER TYPE 在目标版本中真正在线执行、降级为离线，还是被拒绝；PDF 未给出足够证据预判结果。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs: [at_fact_alter_type, at_fact_online_mode, at_open_online_using_expression]
preconditions: [足够磁盘, 无长事务, 可捕获 NOTICE, 可并发访问目标表]
steps: [并发持有可观察事务, 执行 ONLINE ALTER COLUMN amount TYPE BIGINT USING amount::BIGINT]
oracles: [SQL 执行结果, NOTICE, 锁与并发 DML 行为, 最终列类型]
execution_requirements: [多会话, NOTICE 捕获, 锁监控]
