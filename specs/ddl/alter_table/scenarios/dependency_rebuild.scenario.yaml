schema_version: 1
kind: scenario
id: scenario_alter_table_dependency_rebuild
name: 列变更依赖重建
status: planned
description: 验证索引、约束、视图、触发器、RLS 策略重建以及函数调用者不自动处理。
factor_ref: alter_table
fixture_refs: [fixture_alter_table_regular]
fact_refs:
- at_fact_modify_column
- at_fact_alter_type
- at_fact_check_generated
- at_open_encrypted_column_fixture
- at_open_external_table_fixture
- at_open_generated_column_fixture
- at_fact_change_foreign_forbidden
- at_fact_change_encrypted_condition
- at_fact_change_partition_type_collation
- at_fact_change_rule_type_collation
- at_fact_change_matview_type_collation
preconditions: [多种依赖对象, 生成列, 加密列, 外表]
steps: [兼容变更, 不兼容变更, 改名, 删除依赖列]
oracles: [对象有效性与重建, 目标依赖错误, 调用者失效]
execution_requirements: [复杂依赖 Fixture]
