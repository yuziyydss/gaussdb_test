schema_version: 1
kind: fixture
id: fixture_alter_table_multistat
name: ALTER TABLE 已存在多列统计声明
status: ready
description: 在普通表 Fixture 之后创建多列统计声明，供 DELETE/ENABLE/DISABLE action 使用。
factor_ref: alter_table
provides:
  tables: []
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "ALTER TABLE t_at_regular ADD STATISTICS ((code, amount));"
  teardown_sqls:
    - "DROP TABLE IF EXISTS t_at_regular CASCADE;"
  note: 基础表由 fixture_alter_table_regular 提供；DROP TABLE 同时清理统计声明。
