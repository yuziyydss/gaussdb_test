schema_version: 1
kind: fixture
id: fixture_alter_table_rename_fresh
name: 普通列同定义改名的独占新表
status: needs_review
description: 仅三个可空普通列，无DEFAULT、约束、索引、视图、规则或生成列；实际CREATE由共享改名合同读取。
factor_ref: alter_table
provides:
  tables:
    - name: t_at_rename_fresh
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: true}
        - {name: code, type: VARCHAR(32), nullable: true}
        - {name: note, type: VARCHAR(64), nullable: true}
seed:
  required: true
  rows:
    - {id: 1, code: alpha, note: one}
    - {id: 2, code: beta, note: two}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - CREATE TABLE t_at_rename_fresh (id INTEGER,code VARCHAR(32),note VARCHAR(64));
    - "INSERT INTO t_at_rename_fresh VALUES (1,'alpha','one'),(2,'beta','two');"
  teardown_sqls:
    - DROP TABLE t_at_rename_fresh;
  note: 只生成候选。将来独立授权后使用独占用户Schema，名字预先不存在且无并发依赖；setup未完全成功不得清理未确权对象，不预DROP或依赖DDL回滚。
