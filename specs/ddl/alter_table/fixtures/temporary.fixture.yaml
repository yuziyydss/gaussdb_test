schema_version: 1
kind: fixture
id: fixture_alter_table_temporary
name: ALTER TABLE 临时表
status: ready
description: 为临时表的普通变更与 RLS 禁止目标提供对象。
factor_ref: alter_table
provides:
  tables:
    - name: t_at_temp
      persistence: temporary
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: code, type: VARCHAR(32), nullable: false}
        - {name: note, type: VARCHAR(64), nullable: true}
        - {name: amount, type: INTEGER, nullable: false}
seed:
  required: true
  rows:
    - {id: 1, code: T001, note: temp, amount: 10}
execution:
  status: ready
  mode: auto
  note: 仅用于同一会话内的 ALTER TABLE。
