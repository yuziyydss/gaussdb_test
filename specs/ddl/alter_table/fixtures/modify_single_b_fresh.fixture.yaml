schema_version: 1
kind: fixture
id: fixture_alter_table_modify_single_b_fresh
name: B单列MODIFY无默认无依赖VARCHAR资产
status: needs_review
description: 真实原长48的可空列，ASCII和NULL两行；不含默认、索引、约束、视图或生成列。
factor_ref: alter_table
provides:
  tables:
    - name: g_at_modify_single
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: true}
        - {name: note, type: VARCHAR(48), nullable: true}
seed:
  required: true
  rows: [{id: 1, note: alpha}, {id: 2, note: null}]
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - CREATE TABLE g_at_modify_single (id INTEGER,note VARCHAR(48));
    - "INSERT INTO g_at_modify_single VALUES (1,'alpha'),(2,NULL);"
  teardown_sqls: [DROP TABLE g_at_modify_single;]
  note: 独占用户schema内新表，须确认实际B物理模式及enable_modify_column选项已启用。仅离线定义，不自动SET、不创建数据库。只有本次成功建表回执匹配时才允许清理，setup失败不盲DROP；统计信息与依赖重建不在本fixture证明范围。
