schema_version: 1
kind: "scenario"
id: "scenario_alter_directory_documented_contracts"
name: "ALTER DIRECTORY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_directory"
fixture_refs: []
fact_refs:
  - "alter_directory_fact_purpose"
  - "alter_directory_fact_only_owner"
  - "alter_directory_fact_privilege"
  - "alter_directory_fact_no_pdb"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "修改目录对象属性。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "当前仅支持修改目录所有者。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "开关off仅初始用户可修改；on时SYSADMIN或属主可改，且须为新属主成员。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "PDB内不支持目录对象。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
