schema_version: 1
kind: scenario
id: scenario_m_drop_schema_missing_notice
name: scenario_m_drop_schema_missing_notice
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_schema
fact_refs:
- m_drop_schema_fact_missing_notice
fixture_refs:
- fixture_m_drop_schema_owned_namespaces
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: DROP SCHEMA IF EXISTS m_drop_schema_absent;
oracles:
- kind: manual_assertion
  expected: 命令成功并产生 NOTICE；真实消息与 SQLSTATE 待执行校准
execution_requirements:
- database_authorization
- target_oracle_calibration
