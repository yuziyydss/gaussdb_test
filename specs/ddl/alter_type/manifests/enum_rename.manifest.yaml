schema_version: 1
kind: "manifest"
id: "manifest_alter_type_enum_rename"
name: "ALTER TYPE enum_rename"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_type"
syntax_ref: "syntax_alter_type"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_type_enum_autocommit"
identifier_policy: {}
bindings:
  form:
    - "alter_type_form_enum_rename"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "type_alter_authorized"
    allowed_values:
      - "true"
    fact_refs:
      - "alter_type_fact_privilege"
