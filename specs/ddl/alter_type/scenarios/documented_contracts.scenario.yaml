schema_version: 1
kind: "scenario"
id: "scenario_alter_type_documented_contracts"
name: "ALTER TYPE documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_type"
fixture_refs: []
fact_refs:
  - "alter_type_fact_purpose"
  - "alter_type_fact_privilege"
  - "alter_type_fact_new_enum"
  - "alter_type_fact_existing_enum"
  - "alter_type_fact_cascade"
  - "alter_type_fact_restrict"
  - "alter_type_fact_extra_privilege"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "修改类型定义。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "类型属主、ALTER/ALTER ANY TYPE授权可改；三权分立关闭时管理员默认可改，改属主/模式另有成员关系要求。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "新增枚举标签非空且不超过63字节。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "重命名的既有标签非空且不超过63字节。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "CASCADE联动关联记录及继承子表。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "RESTRICT为默认，需联动关联记录时拒绝更新。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "改模式需目标模式CREATE；改属主需成员及该模式CREATE；增改属性类型需USAGE。"
oracles:
  -
    kind: "documented_behavior"
    expected: "RESTRICT为默认，需联动关联记录时拒绝更新。"
execution_requirements:
  - "database"
  - "same_session"
