schema_version: 1
kind: "fixture"
id: "fixture_alter_type_enum_autocommit"
name: "ALTER TYPE enum_autocommit"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_type"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TYPE type_enum_alter_b7 AS ENUM ('open', 'closed');"
  teardown_sqls:
    - "DROP TYPE IF EXISTS type_enum_alter_b7;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
