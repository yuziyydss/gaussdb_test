schema_version: 1
kind: "scenario"
id: "scenario_alter_column_encryption_key_documented_contracts"
name: "ALTER COLUMN ENCRYPTION KEY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_column_encryption_key"
fixture_refs: []
fact_refs:
  - "alter_column_encryption_key_fact_purpose"
  - "alter_column_encryption_key_fact_driver"
  - "alter_column_encryption_key_fact_plaintext_unchanged"
  - "alter_column_encryption_key_fact_name"
  - "alter_column_encryption_key_fact_different_cmk"
  - "alter_column_encryption_key_fact_sm_preserved"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "轮换保护CEK的CMK，对CEK明文重新加密。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅全密态连接开关开启后支持。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "只能CMK轮转；CEK明文不变，加密列数据密文不变。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "CEK名在命名空间唯一并符合标识符规范。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "新CMK须已经存在且不等于轮转前CMK。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "原CMK为国密算法时新CMK仍必须为国密。"
oracles:
  -
    kind: "documented_behavior"
    expected: "只能CMK轮转；CEK明文不变，加密列数据密文不变。"
execution_requirements:
  - "database"
  - "same_session"
