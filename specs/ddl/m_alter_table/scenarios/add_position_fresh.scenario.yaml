schema_version: 1
kind: scenario
id: scenario_m_alter_table_add_position_fresh
name: scenario_m_alter_table_add_position_fresh
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_table
fact_refs:
- m_alter_table_fact_add
- m_alter_table_fact_add_null
- m_alter_table_fact_position
- m_alter_table_fact_position_rules
fixture_refs:
- fixture_m_alter_table_add_position_fresh
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- id: add
  manifest_ref: manifest_m_alter_table_add_position_fresh
  action: 每case独立fixture，执行一条ADD；FIRST/AFTER不可叠加执行。
- id: rows_first
  sql: SELECT * FROM m_at_add_position ORDER BY id;
  when_value_ref: m_alter_table_position_first
- id: rows_after
  sql: SELECT * FROM m_at_add_position ORDER BY id;
  when_value_ref: m_alter_table_position_after
oracles:
- kind: result_set
  step_id: rows_first
  expected:
  - - null
    - 1
    - 10
    - 100
  - - null
    - 2
    - 20
    - 200
- kind: result_set
  step_id: rows_after
  expected:
  - - 1
    - null
    - 10
    - 100
  - - 2
    - null
    - 20
    - 200
- kind: manual_assertion
  expected: 目录新增extra为可空INTEGER无默认并处于目标位置；目录接口和实际原行NULL尚待授权校准。
execution_requirements:
- database_authorization
- target_oracle_calibration
