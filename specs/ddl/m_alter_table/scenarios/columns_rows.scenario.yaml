schema_version: 1
kind: scenario
id: scenario_m_alter_table_columns_rows
name: scenario_m_alter_table_columns_rows
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_table
fact_refs:
- m_alter_table_fact_modify
- m_alter_table_fact_default_future
- m_alter_table_fact_nextval
- m_alter_table_fact_schema_sequence
- m_alter_table_fact_bundle
- m_alter_table_fact_only
- m_alter_table_fact_internal
- m_alter_table_fact_drop
- m_alter_table_fact_check_null
- m_alter_table_fact_rewrite
fixture_refs:
- fixture_m_alter_table_integer_source
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 逐case验证新增DEFAULT=7回填、SET DEFAULT不回填、NULL兼容/已有正值CHECK、类型/名称/位置、表改名/迁移后对象定位。内部扩容、ONLY继承、nextval及物理重写不作为覆盖结果。
oracles:
- kind: manual_assertion
  expected: 目录、已有行、后续INSERT、约束和准确错误Oracle均待独立执行；不以静态成功推断行为
execution_requirements:
- database_authorization
- target_oracle_calibration
