schema_version: 1
kind: scenario
id: scenario_m_alter_table_change_fresh
name: scenario_m_alter_table_change_fresh
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_table
fact_refs:
- m_alter_table_fact_change_syntax
- m_alter_table_fact_change_name
fixture_refs:
- fixture_m_alter_table_change_fresh
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- id: change
  manifest_ref: manifest_m_alter_table_change_fresh
  action: 在M隔离Schema中逐case执行一条候选，不叠加两个COLUMN拼写。
- id: rows
  sql: SELECT id, amount FROM m_at_change_fresh ORDER BY id;
oracles:
- kind: result_set
  step_id: rows
  expected:
  - - 1
    - 10
  - - 2
    - 20
- kind: manual_assertion
  expected: 目录应确认qty改名amount，INTEGER、列序、可空及无DEFAULT保持；实际目录字段与结果需授权后校准。
execution_requirements:
- database_authorization
- target_oracle_calibration
