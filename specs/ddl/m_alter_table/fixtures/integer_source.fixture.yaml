schema_version: 1
kind: fixture
id: fixture_m_alter_table_integer_source
name: fixture_m_alter_table_integer_source
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_table
requires_fixture_refs: []
provides:
  tables:
  - name: m_alter_table_namespace.source
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
    - name: qty
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE SCHEMA m_alter_table_namespace;
  - CREATE TABLE m_alter_table_namespace.source (id INTEGER, qty INTEGER DEFAULT 9) WITH (storage_type = ASTORE,
    fillfactor = 80);
  - INSERT INTO m_alter_table_namespace.source VALUES (1,10),(2,20);
  teardown_sqls:
  - DROP TABLE IF EXISTS m_alter_table_namespace.source PURGE;
  - DROP TABLE IF EXISTS m_alter_table_namespace.renamed PURGE;
  - DROP SCHEMA m_alter_table_namespace;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
