schema_version: 1
kind: scenario
id: scenario_m_alter_extension_members
name: scenario_m_alter_extension_members
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_extension
fact_refs:
- m_alter_extension_fact_detached
- m_alter_extension_fact_upgrade
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 检查成员目录与DROP后表仍存在；版本更新/移动模式待真实脚本和relocatable证据，不操作系统默认plpgsql。
oracles:
- kind: manual_assertion
  expected: 成员、更新与重定位尚未实机验证
execution_requirements:
- database_authorization
- target_oracle_calibration
