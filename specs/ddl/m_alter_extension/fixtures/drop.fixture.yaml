schema_version: 1
kind: fixture
id: fixture_m_alter_extension_drop
name: fixture_m_alter_extension_drop
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_extension
requires_fixture_refs:
- fixture_m_create_extension_installed
provides:
  tables:
  - name: m_extension_namespace.member_table
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE m_extension_namespace.member_table (id INTEGER);
  - ALTER EXTENSION security_plugin ADD TABLE m_extension_namespace.member_table;
  teardown_sqls:
  - DROP EXTENSION IF EXISTS security_plugin RESTRICT;
  - DROP TABLE IF EXISTS m_extension_namespace.member_table;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
