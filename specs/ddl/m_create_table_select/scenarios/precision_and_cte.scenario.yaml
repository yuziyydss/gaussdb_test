schema_version: 1
kind: scenario
id: scenario_m_create_table_select_precision_and_cte
name: scenario_m_create_table_select_precision_and_cte
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_table_select
fact_refs:
- m_create_table_select_fact_precision
- m_create_table_select_fact_cte
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 先绑定 enable_precision_decimal 环境，再补有输出类型合同的表达式/UNION/带括号 CTE
oracles:
- kind: manual_assertion
  expected: 本轮无该类可执行候选，不将基础直接列测试视为类型推导全覆盖
execution_requirements:
- database_authorization
- target_oracle_calibration
