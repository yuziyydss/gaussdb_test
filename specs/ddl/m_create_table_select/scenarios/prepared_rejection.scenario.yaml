schema_version: 1
kind: scenario
id: scenario_m_create_table_select_prepared_rejection
name: scenario_m_create_table_select_prepared_rejection
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_table_select
fact_refs:
- m_create_table_select_fact_prepare
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 独立 PREPARE 负向需目标错误身份及会话 fixture，不合并进 CTAS 正向
oracles:
- kind: target_error
  expected: 应拒绝预备 CREATE TABLE AS，错误身份待校准
execution_requirements:
- database_authorization
- target_oracle_calibration
