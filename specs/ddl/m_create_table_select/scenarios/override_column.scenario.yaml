schema_version: 1
kind: scenario
id: scenario_m_create_table_select_override_column
name: scenario_m_create_table_select_override_column
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_table_select
fact_refs:
- m_create_table_select_fact_same_name
fixture_refs:
- fixture_m_create_table_select_source
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: CREATE TABLE m_ctas_new(qty INT) AS SELECT id,qty FROM m_ctas_source;
- sql: SELECT * FROM m_ctas_new ORDER BY id;
oracles:
- kind: result_set
  expected:
  - - 1
    - 10
  - - 2
    - null
execution_requirements:
- database_authorization
- target_oracle_calibration
