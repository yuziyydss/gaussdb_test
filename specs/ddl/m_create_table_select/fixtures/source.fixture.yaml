schema_version: 1
kind: fixture
id: fixture_m_create_table_select_source
name: fixture_m_create_table_select_source
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_table_select
requires_fixture_refs: []
provides:
  tables:
  - name: m_ctas_source
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: false
    - name: qty
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE m_ctas_source (id INT NOT NULL DEFAULT 7, qty INT DEFAULT 9);
  - INSERT INTO m_ctas_source(id,qty) VALUES (1,10),(2,NULL);
  teardown_sqls:
  - DROP TABLE IF EXISTS m_ctas_new;
  - DROP TABLE m_ctas_source;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
