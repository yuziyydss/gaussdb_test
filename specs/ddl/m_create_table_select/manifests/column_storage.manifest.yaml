schema_version: 1
kind: manifest
id: manifest_m_create_table_select_column_storage
name: manifest_m_create_table_select_column_storage
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_table_select
syntax_ref: syntax_m_create_table_select
suite_type: negative
strategy: pairwise
fixture_refs:
- fixture_m_create_table_select_source
identifier_policy: {}
bindings:
  storage:
  - m_create_table_select_storage_column
  projection:
  - m_create_table_select_projection_two
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_create_table_select_fact_mode
violates_rule_refs:
- m_create_table_select_rule_row_only
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: row_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
