schema_version: 1
kind: "manifest"
id: "manifest_create_type_composite"
name: "CREATE TYPE composite"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "create_type"
syntax_ref: "syntax_create_type"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_create_type_transaction"
identifier_policy:
  type_name:
    generator: "deterministic"
    prefix: "typ_b7"
    unique_per_case: true
bindings:
  form:
    - "create_type_form_composite"
  attributes:
    - "create_type_attributes_empty"
    - "create_type_attributes_one"
    - "create_type_attributes_two"
  labels:
    - "create_type_labels_empty"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
