schema_version: 1
kind: "fixture"
id: "fixture_create_type_existing_types"
name: "CREATE TYPE existing_types"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_type"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
    - "CREATE TYPE type_comp_b7 AS (col_1 INTEGER, col_2 TEXT);"
    - "CREATE TYPE type_enum_b7 AS ENUM ('open', 'closed');"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
