schema_version: 1
kind: fixture
id: fixture_m_alter_resource_label_remove
name: fixture_m_alter_resource_label_remove
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_resource_label
requires_fixture_refs:
- fixture_m_create_resource_label_resources
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE RESOURCE LABEL m_alter_resource_label_existing ADD COLUMN (m_label_namespace.source.id), TABLE (m_label_namespace.source),
    COLUMN (m_label_namespace.source.qty), SCHEMA (m_label_namespace), VIEW (m_label_namespace.source_view);
  teardown_sqls:
  - DROP RESOURCE LABEL IF EXISTS m_alter_resource_label_existing;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
