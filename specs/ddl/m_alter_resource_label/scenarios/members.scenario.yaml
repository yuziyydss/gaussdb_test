schema_version: 1
kind: scenario
id: scenario_m_alter_resource_label_members
name: scenario_m_alter_resource_label_members
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_resource_label
fact_refs:
- m_alter_resource_label_fact_membership
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 检查ADD前未包含、REMOVE前包含目标资源；REMOVE后仍保留id列，避免空标签状态混入。
oracles:
- kind: manual_assertion
  expected: 资源成员变化与底层对象仍存在需独立Oracle
execution_requirements:
- database_authorization
- target_oracle_calibration
