schema_version: 1
kind: "manifest"
id: "manifest_alter_index_ordinary"
name: "ALTER INDEX ordinary"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_index"
syntax_ref: "syntax_alter_index"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  action:
    - "alter_index_action_rename"
    - "alter_index_action_set"
    - "alter_index_action_reset"
    - "alter_index_action_unusable"
  if_exists:
    - "alter_index_if_exists_absent"
    - "alter_index_if_exists_present"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
