schema_version: 1
kind: "manifest"
id: "manifest_alter_index_tablespace"
name: "ALTER INDEX tablespace"
status: "needs_review"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_index"
syntax_ref: "syntax_alter_index"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  action:
    - "alter_index_action_tablespace"
  if_exists:
    - "alter_index_if_exists_absent"
    - "alter_index_if_exists_present"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "prepared_tablespace"
    allowed_values:
      - "fp_ai_tbs"
    fact_refs:
      - "alter_index_fact_tablespace_existing"
