schema_version: 1
kind: "manifest"
id: "manifest_alter_index_partition_rebuild"
name: "ALTER INDEX partition_rebuild"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_index"
syntax_ref: "syntax_alter_index"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  action:
    - "alter_index_action_rebuild_partition"
  index_name:
    - "alter_index_index_name_local"
  partition_name:
    - "alter_index_partition_name_low"
    - "alter_index_partition_name_max"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
