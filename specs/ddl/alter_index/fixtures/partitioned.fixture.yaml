schema_version: 1
kind: "fixture"
id: "fixture_alter_index_partitioned"
name: "ALTER INDEX partitioned"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_index"
requires_fixture_refs:
  - "fixture_create_index_partitioned"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE INDEX idx_ai_local ON t_ci_partitioned(id) LOCAL (PARTITION p_ai_low, PARTITION p_ai_max);"
  teardown_sqls:
    - "DROP INDEX IF EXISTS idx_ai_local;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
