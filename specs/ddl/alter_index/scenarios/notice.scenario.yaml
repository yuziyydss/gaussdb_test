schema_version: 1
kind: "scenario"
id: "scenario_alter_index_notice"
name: "ALTER INDEX notice"
status: "planned"
description: "捕获缺失索引 NOTICE 并校准无 IF EXISTS 错误，不能任意报错通过。"
factor_ref: "alter_index"
fixture_refs: []
fact_refs:
  - "alter_index_fact_notice"
  - "alter_index_fact_error_identity"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "捕获缺失索引 NOTICE 并校准无 IF EXISTS 错误，不能任意报错通过。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
