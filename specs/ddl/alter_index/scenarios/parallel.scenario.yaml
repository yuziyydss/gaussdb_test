schema_version: 1
kind: "scenario"
id: "scenario_alter_index_parallel"
name: "ALTER INDEX parallel"
status: "planned"
description: "按方法、parallel_workers 与并行执行证据校验禁用及默认方式；不能把串行成功冒充并行覆盖。"
factor_ref: "alter_index"
fixture_refs: []
fact_refs:
  - "alter_index_fact_parallel_rebuild"
  - "alter_index_fact_parallel_default"
  - "alter_index_fact_parallel_gap"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "按方法、parallel_workers 与并行执行证据校验禁用及默认方式；不能把串行成功冒充并行覆盖。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
