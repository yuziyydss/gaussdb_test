schema_version: 1
kind: "scenario"
id: "scenario_alter_index_storage"
name: "ALTER INDEX storage"
status: "planned"
description: "补 Ustore LOCAL ACTIVE_PAGES 前置和 VACUUM/ANALYZE 更新观察；SET 返回成功不代表物理重写完成。"
factor_ref: "alter_index"
fixture_refs: []
fact_refs:
  - "alter_index_fact_set_deferred"
  - "alter_index_fact_reset_deferred"
  - "alter_index_fact_active_pages"
  - "alter_index_fact_active_scope"
  - "alter_index_fact_active_refresh"
  - "alter_index_fact_storage_gap"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "补 Ustore LOCAL ACTIVE_PAGES 前置和 VACUUM/ANALYZE 更新观察；SET 返回成功不代表物理重写完成。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
