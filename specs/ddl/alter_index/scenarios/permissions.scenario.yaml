schema_version: 1
kind: "scenario"
id: "scenario_alter_index_permissions"
name: "ALTER INDEX permissions"
status: "planned"
description: "由管理员提供表空间、权限及 GUC 能力，不自动修改配置；唯一不可用索引的 DML 另建场景。"
factor_ref: "alter_index"
fixture_refs: []
fact_refs:
  - "alter_index_fact_permission"
  - "alter_index_fact_tablespace_existing"
  - "alter_index_fact_upgrade"
  - "alter_index_fact_visible_gate"
  - "alter_index_fact_invisible_gate"
  - "alter_index_fact_unique_unusable"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "由管理员提供表空间、权限及 GUC 能力，不自动修改配置；唯一不可用索引的 DML 另建场景。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
