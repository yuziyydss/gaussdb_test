schema_version: 1
kind: scenario
id: scenario_m_create_audit_policy_behavior
name: scenario_m_create_audit_policy_behavior
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_audit_policy
fact_refs:
- m_create_audit_policy_fact_enabled
- m_create_audit_policy_fact_analyze
- m_create_audit_policy_fact_notice
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 校准默认启用、ANALYZE/VACUUM审计事件、重名NOTICE；语法生成不证明日志行为。
oracles:
- kind: manual_assertion
  expected: 目录状态、审计日志、NOTICE和目标错误尚待独立执行
execution_requirements:
- database_authorization
- target_oracle_calibration
