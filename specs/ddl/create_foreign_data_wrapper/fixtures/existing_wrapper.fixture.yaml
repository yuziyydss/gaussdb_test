schema_version: 1
kind: "fixture"
id: "fixture_create_foreign_data_wrapper_existing_wrapper"
name: "CREATE FOREIGN DATA WRAPPER existing_wrapper"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_foreign_data_wrapper"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
    - "CREATE FOREIGN DATA WRAPPER fdw_b7 NO HANDLER NO VALIDATOR OPTIONS (bar 'true');"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
