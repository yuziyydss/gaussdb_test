schema_version: 1
kind: "scenario"
id: "scenario_create_foreign_data_wrapper_documented_contracts"
name: "CREATE FOREIGN DATA WRAPPER documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "create_foreign_data_wrapper"
fixture_refs: []
fact_refs:
  - "create_foreign_data_wrapper_fact_purpose"
  - "create_foreign_data_wrapper_fact_owner"
  - "create_foreign_data_wrapper_fact_privilege"
  - "create_foreign_data_wrapper_fact_name"
  - "create_foreign_data_wrapper_fact_handler_contract"
  - "create_foreign_data_wrapper_fact_without_handler"
  - "create_foreign_data_wrapper_fact_no_handler"
  - "create_foreign_data_wrapper_fact_validator"
  - "create_foreign_data_wrapper_fact_validator_signature"
  - "create_foreign_data_wrapper_fact_options"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "创建一个新的外部数据封装器。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "创建者成为封装器所有者。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "只有初始用户和系统管理员用户可以创建封装器。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "封装器名称在数据库中必须唯一。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "HANDLER引用已注册无参数且返回fdw_handler的函数。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "无处理函数的封装器仅支持声明外表，不能访问。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "NO HANDLER不使用处理函数，外表只能声明不能访问。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "验证函数检查封装器及其服务器/映射/外表选项；省略或NO VALIDATOR时创建不检查，运行时仍可能拒绝。"
  -
    id: "contract_8"
    action: "verify_documented_contract"
    note: "验证器接收text[]选项数组和oid系统目录标识两个参数。"
  -
    id: "contract_9"
    action: "verify_documented_contract"
    note: "选项名必须唯一，合法名称和值取决于封装器并由验证器检查。"
oracles:
  -
    kind: "documented_behavior"
    expected: "无处理函数的封装器仅支持声明外表，不能访问。"
  -
    kind: "documented_behavior"
    expected: "NO HANDLER不使用处理函数，外表只能声明不能访问。"
  -
    kind: "documented_behavior"
    expected: "验证函数检查封装器及其服务器/映射/外表选项；省略或NO VALIDATOR时创建不检查，运行时仍可能拒绝。"
execution_requirements:
  - "database"
  - "same_session"
