schema_version: 1
kind: "manifest"
id: "manifest_drop_procedure_existing"
name: "DROP PROCEDURE existing"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_procedure"
syntax_ref: "syntax_drop_procedure"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  name:
    - "drop_procedure_name_existing"
  if_exists:
    - "drop_procedure_if_exists_none"
    - "drop_procedure_if_exists_yes"
environment_requirements:
  -
    key: "procedure_owner"
    allowed_values:
      - "initial_user"
    fact_refs:
      - "drop_procedure_fact_initial"
      - "drop_procedure_fact_initial_profile"
  -
    key: "session_user"
    allowed_values:
      - "initial_user"
    fact_refs:
      - "drop_procedure_fact_initial_profile"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
