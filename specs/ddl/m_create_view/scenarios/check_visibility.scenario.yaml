schema_version: 1
kind: scenario
id: scenario_m_create_view_check_visibility
name: scenario_m_create_view_check_visibility
status: planned
description: M 首批有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_view
fact_refs:
- m_create_view_fact_check
fixture_refs:
- fixture_m_create_view_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: CREATE VIEW m_b01_checked AS SELECT id,qty FROM m_b01_source WHERE id > 0 WITH CHECK OPTION;
- sql: INSERT INTO m_b01_checked VALUES (-1,2);
oracles:
- kind: target_error
  expected: 视图可见性检查错误；SQLSTATE 待校准
  stage: target
execution_requirements:
- database_authorization
- target_oracle_calibration
