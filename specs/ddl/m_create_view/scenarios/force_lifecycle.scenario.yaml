schema_version: 1
kind: scenario
id: scenario_m_create_view_force_lifecycle
name: scenario_m_create_view_force_lifecycle
status: planned
description: M 首批有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_view
fact_refs:
- m_create_view_fact_force
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: ensure_missing_dependency_in_isolated_schema
- sql: CREATE FORCE VIEW m_b01_force AS SELECT id FROM m_b01_missing;
oracles:
- kind: metadata
  expected: 无效视图；不是可查询成功
execution_requirements:
- database_authorization
- target_oracle_calibration
