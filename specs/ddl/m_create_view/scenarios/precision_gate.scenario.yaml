schema_version: 1
kind: scenario
id: scenario_m_create_view_precision_gate
name: scenario_m_create_view_precision_gate
status: planned
description: M 首批有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_view
fact_refs:
- m_create_view_fact_precision
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 建立不同精度传递配置下的受限类型计算视图；需先补具体类型 fixture
oracles:
- kind: target_error
  expected: 按精度配置分别校准受限类型创建结果，不与 INT 直投影混用
execution_requirements:
- database_authorization
- target_oracle_calibration
