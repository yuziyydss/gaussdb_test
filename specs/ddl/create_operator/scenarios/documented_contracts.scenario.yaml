schema_version: 1
kind: "scenario"
id: "scenario_create_operator_documented_contracts"
name: "CREATE OPERATOR documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "create_operator"
fixture_refs: []
fact_refs:
  - "create_operator_fact_purpose"
  - "create_operator_fact_owner"
  - "create_operator_fact_not_equal"
  - "create_operator_fact_arity"
  - "create_operator_fact_privilege"
  - "create_operator_fact_characters"
  - "create_operator_fact_comments"
  - "create_operator_fact_trailing"
  - "create_operator_fact_arrow"
  - "create_operator_fact_function"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "创建新操作符。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "创建者成为操作符所有者。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "输入!=会映射为<>，两者等价。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "双目需LEFTARG和RIGHTARG；右目仅LEFTARG；左目仅RIGHTARG。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "需参数/返回类型USAGE及底层函数EXECUTE；交换/负操作符另需其USAGE。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "操作符名由文档枚举的符号字符组成。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "操作符名不能含--或/*注释起始符。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "多字符名不能以+/-结束，除非还含文档列出的特殊符号之一。"
  -
    id: "contract_8"
    action: "verify_documented_contract"
    note: "不再使用=>作为操作符名。"
  -
    id: "contract_9"
    action: "verify_documented_contract"
    note: "底层函数必须先CREATE FUNCTION，接受匹配的一或两个参数及指定类型。"
oracles:
  -
    kind: "documented_behavior"
    expected: "输入!=会映射为<>，两者等价。"
execution_requirements:
  - "database"
  - "same_session"
