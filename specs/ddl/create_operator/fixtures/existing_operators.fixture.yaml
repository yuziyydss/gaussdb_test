schema_version: 1
kind: "fixture"
id: "fixture_create_operator_existing_operators"
name: "CREATE OPERATOR existing_operators"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_operator"
requires_fixture_refs:
  - "fixture_create_operator_functions"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE OPERATOR @#@ (PROCEDURE = fp_cf_callable, LEFTARG = INTEGER, RIGHTARG = INTEGER);"
    - "CREATE OPERATOR @#@ (PROCEDURE = op_b7_unary, RIGHTARG = INTEGER);"
    - "CREATE OPERATOR @#@ (PROCEDURE = op_b7_unary, LEFTARG = INTEGER);"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
