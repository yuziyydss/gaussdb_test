schema_version: 1
kind: "scenario"
id: "scenario_drop_llm_documented_contracts"
name: "DROP LLM documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_llm"
fixture_refs: []
fact_refs:
  - "drop_llm_fact_body_5"
  - "drop_llm_fact_body_8"
  - "drop_llm_fact_body_9"
  - "drop_llm_fact_body_16"
  - "drop_llm_fact_body_16_1"
  - "drop_llm_fact_body_16_2"
  - "create_llm::create_llm_fact_body_9"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除库内大模型服务。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "1.   drop llm命令需要拥有sysadmin的用户权限。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "2.   当前版本不支持M兼容模式。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "●    gs_model_name 必选，模型服务在GaussDB中的名称。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "该名称作为标识符，在以后的语义函数中 作为参数输入，不超过63字节。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "输入大小写不敏感，在系统中自动转写至小写字 母存储，只支持数字、字母和下划线“_”符号，且不能以数字开头，如果超过长 度则会被自动截断。"
  -
    id: "upstream_contract_0"
    action: "verify_documented_contract"
    note: "使用已导出的跨章事实 create_llm::create_llm_fact_body_9；来源变化须使本包验证快照过期，尚未执行数据库验证。"
oracles:
  -
    kind: "documented_behavior"
    expected: "删除库内大模型服务。"
  -
    kind: "documented_behavior"
    expected: "●    gs_model_name 必选，模型服务在GaussDB中的名称。"
  -
    kind: "documented_behavior"
    expected: "该名称作为标识符，在以后的语义函数中 作为参数输入，不超过63字节。"
  -
    kind: "documented_behavior"
    expected: "输入大小写不敏感，在系统中自动转写至小写字 母存储，只支持数字、字母和下划线“_”符号，且不能以数字开头，如果超过长 度则会被自动截断。"
execution_requirements:
  - "database"
  - "same_session"
