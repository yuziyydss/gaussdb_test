schema_version: 1
kind: manifest
id: manifest_drop_llm_fresh_syntax
name: DROP LLM fresh_syntax
factor_ref: drop_llm
syntax_ref: syntax_drop_llm
status: needs_review
description: 一个静态LLM名语法代表；不删除真实模型服务、不验证模型存在或目录行为。
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  model_name: [drop_llm_model_name_fresh]
environment_requirements:
  - key: llm_drop_privilege
    allowed_values: [sysadmin]
    fact_refs: [drop_llm_fact_body_8]
  - key: compatibility_mode
    allowed_values: [non_m]
    fact_refs: [drop_llm_fact_body_9]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
