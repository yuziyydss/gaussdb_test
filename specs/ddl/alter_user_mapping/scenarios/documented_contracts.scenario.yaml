schema_version: 1
kind: "scenario"
id: "scenario_alter_user_mapping_documented_contracts"
name: "ALTER USER MAPPING documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_user_mapping"
fixture_refs: []
fact_refs:
  - "alter_user_mapping_fact_privilege"
  - "alter_user_mapping_fact_keys"
  - "alter_user_mapping_fact_sensitive"
  - "alter_user_mapping_fact_options"
  - "alter_user_mapping_fact_storage"
  - "alter_user_mapping_fact_key_random"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "服务器所有者可修改任意用户映射，获USAGE用户可修改自己的映射。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "PASSWORD选项要求所有节点存在指定usermapping密钥文件。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "多层引号敏感字段可能不脱敏。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "修改映射选项，名称唯一，默认ADD，名称和值由FDW验证。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "密码加密保存PG_USER_MAPPING，需要所有节点可读密钥文件。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "-S default随机生成保护密钥的密码，也可指定密码。"
oracles:
  -
    kind: "documented_behavior"
    expected: "多层引号敏感字段可能不脱敏。"
  -
    kind: "documented_behavior"
    expected: "密码加密保存PG_USER_MAPPING，需要所有节点可读密钥文件。"
execution_requirements:
  - "database"
  - "same_session"
