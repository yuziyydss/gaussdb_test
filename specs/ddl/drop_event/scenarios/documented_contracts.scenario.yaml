schema_version: 1
kind: "scenario"
id: "scenario_drop_event_documented_contracts"
name: "DROP EVENT documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_event"
fixture_refs: []
fact_refs:
  - "drop_event_fact_purpose"
  - "drop_event_fact_compat_b"
  - "drop_event_fact_exists"
  - "drop_event_fact_missing"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除任务。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅B模式支持。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "IF EXISTS目标存在删除成功。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "IF EXISTS缺失仅NOTICE。"
oracles:
  -
    kind: "documented_behavior"
    expected: "IF EXISTS目标存在删除成功。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS缺失仅NOTICE。"
execution_requirements:
  - "database"
  - "same_session"
