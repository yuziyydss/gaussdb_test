schema_version: 1
kind: "syntax"
id: "syntax_drop_event"
name: "DROP EVENT 语法"
factor_ref: "drop_event"
category: "DDL"
status: "ready"
description: "语法结构来自 PDF，选值仅由 factor/manifest 提供。"
production: "DROP EVENT {if_exists} {event_name}"
rendering:
  whitespace: "collapse"
  statement_terminator: ";"
slots:
  if_exists:
    type: "token"
    optional: true
    dimension_ref: "drop_event.if_exists"
  event_name:
    type: "token"
    optional: false
    dimension_ref: "drop_event.event_name"
source_fact_refs:
  - "drop_event_fact_syntax"
