schema_version: 1
kind: "manifest"
id: "manifest_drop_event_existing"
name: "DROP EVENT existing"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_event"
syntax_ref: "syntax_drop_event"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_event_existing_disabled_event"
identifier_policy: {}
bindings:
  if_exists:
    - "drop_event_if_exists_none"
    - "drop_event_if_exists_yes"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "sql_compatibility"
    allowed_values:
      - "B"
    fact_refs:
      - "drop_event_fact_compat_b"
  -
    key: "event_operator_authorized"
    allowed_values:
      - "true"
    fact_refs:
      - "create_event::create_event_fact_privilege"
