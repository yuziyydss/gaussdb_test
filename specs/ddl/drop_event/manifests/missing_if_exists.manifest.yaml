schema_version: 1
kind: manifest
id: manifest_drop_event_missing_if_exists
name: DROP EVENT missing_if_exists
status: needs_review
description: 一个确认缺失任务的IF EXISTS静态代表；不验证NOTICE文本、自动删除或运行中任务行为。
factor_ref: drop_event
syntax_ref: syntax_drop_event
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  if_exists:
    - drop_event_if_exists_yes
  event_name:
    - drop_event_event_name_missing
environment_requirements:
  - key: sql_compatibility
    allowed_values:
      - "B"
    fact_refs:
      - drop_event_fact_compat_b
  - key: event_operator_authorized
    allowed_values:
      - "true"
    fact_refs:
      - create_event::create_event_fact_privilege
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
