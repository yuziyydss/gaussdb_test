schema_version: 1
kind: scenario
id: scenario_m_alter_audit_policy_state
name: scenario_m_alter_audit_policy_state
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_audit_policy
fact_refs:
- m_alter_audit_policy_fact_membership
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 校准目录成员ADD/REMOVE、启停前后值、过滤器及COMMENTS；REMOVE后保留CREATE审计项。
oracles:
- kind: manual_assertion
  expected: 前置状态已静态检查，状态变化尚未实机验证
execution_requirements:
- database_authorization
- target_oracle_calibration
