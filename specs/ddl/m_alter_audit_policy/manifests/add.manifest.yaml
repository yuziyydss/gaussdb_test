schema_version: 1
kind: manifest
id: manifest_m_alter_audit_policy_add
name: manifest_m_alter_audit_policy_add
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_audit_policy
syntax_ref: syntax_m_alter_audit_policy
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_alter_audit_policy_add
identifier_policy: {}
bindings:
  change:
  - m_alter_audit_policy_change_add_drop
  - m_alter_audit_policy_change_add_alter
  if_exists:
  - m_alter_audit_policy_if_exists_none
  - m_alter_audit_policy_if_exists_yes
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_alter_audit_policy_fact_mode
- key: actor_authority
  allowed_values:
  - sysadmin
  fact_refs:
  - m_alter_audit_policy_fact_authority
- key: enable_security_policy
  allowed_values:
  - 'on'
  fact_refs:
  - m_alter_audit_policy_fact_security_on
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
