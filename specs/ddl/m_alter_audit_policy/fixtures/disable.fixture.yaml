schema_version: 1
kind: fixture
id: fixture_m_alter_audit_policy_disable
name: fixture_m_alter_audit_policy_disable
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_audit_policy
requires_fixture_refs:
- fixture_m_create_resource_label_audit_label
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE AUDIT POLICY m_alter_audit_policy_existing PRIVILEGES CREATE ON LABEL (m_b05_audit_label) FILTER ON IP('127.0.0.1')
    ENABLE;
  teardown_sqls:
  - DROP AUDIT POLICY IF EXISTS m_alter_audit_policy_existing;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
