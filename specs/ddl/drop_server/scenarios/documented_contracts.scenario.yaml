schema_version: 1
kind: "scenario"
id: "scenario_drop_server_documented_contracts"
name: "DROP SERVER documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_server"
fixture_refs: []
fact_refs:
  - "drop_server_fact_purpose"
  - "drop_server_fact_privilege"
  - "drop_server_fact_existing"
  - "drop_server_fact_missing"
  - "drop_server_fact_cascade"
  - "drop_server_fact_restrict"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除已有数据服务器。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "属主或获DROP授权用户可删除，管理员默认拥有权限。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "存在时删除成功。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "不存在时IF EXISTS只产生NOTICE。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "CASCADE删除依赖服务器的对象。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "RESTRICT遇到依赖对象拒绝删除。"
oracles:
  -
    kind: "documented_behavior"
    expected: "存在时删除成功。"
  -
    kind: "documented_behavior"
    expected: "不存在时IF EXISTS只产生NOTICE。"
  -
    kind: "documented_behavior"
    expected: "RESTRICT遇到依赖对象拒绝删除。"
execution_requirements:
  - "database"
  - "same_session"
