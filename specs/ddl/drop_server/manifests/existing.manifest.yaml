schema_version: 1
kind: "manifest"
id: "manifest_drop_server_existing"
name: "DROP SERVER existing"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_server"
syntax_ref: "syntax_drop_server"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_server_server"
identifier_policy: {}
bindings:
  if_exists:
    - "drop_server_if_exists_no"
    - "drop_server_if_exists_yes"
  behavior:
    - "drop_server_behavior_default"
    - "drop_server_behavior_restrict"
    - "drop_server_behavior_cascade"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "server_drop_privilege"
    allowed_values:
      - "true"
    fact_refs:
      - "drop_server_fact_privilege"
  -
    key: "documented_fdw_available"
    allowed_values:
      - "true"
    fact_refs:
      - "create_server::create_server_fact_fdw_environment"
