schema_version: 1
kind: fixture
id: fixture_m_create_table_subpartition_range_hash_source
name: fixture_m_create_table_subpartition_range_hash_source
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_table_subpartition
requires_fixture_refs: []
provides:
  tables:
  - name: m_b06_sub_source
    persistence: permanent
    table_kind: range_hash_subpartitioned
    columns:
    - name: id
      type: INTEGER
      nullable: true
    - name: qty
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE m_b06_sub_source (id INTEGER, qty INTEGER) WITH (storage_type = ASTORE) PARTITION BY RANGE (id)
    SUBPARTITION BY HASH (qty) (PARTITION p_low VALUES LESS THAN (10) (SUBPARTITION s_low), PARTITION p_high VALUES
    LESS THAN (MAXVALUE) (SUBPARTITION s_high));
  - INSERT INTO m_b06_sub_source VALUES (1,10),(2,20),(11,30);
  teardown_sqls:
  - DROP TABLE m_b06_sub_source PURGE;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
