schema_version: 1
kind: scenario
id: scenario_m_alter_table_partition_metadata_data
name: scenario_m_alter_table_partition_metadata_data
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_table_partition
fact_refs:
- m_alter_table_partition_fact_hash
- m_alter_table_partition_fact_last
- m_alter_table_partition_fact_global_index
- m_alter_table_partition_fact_exchange
- m_create_table_partition::m_create_table_partition_fact_bounds
fixture_refs:
- fixture_m_alter_table_partition_shared_partition
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 检查3个真实起始分区与每分区一行；ADD上界40、SPLIT点15、连续合并和改名逐case验证。没有全局索引时不声明验证索引维护。交换另需完整契约。
oracles:
- kind: manual_assertion
  expected: 分区数据、名称/数量、统计、目标错误及索引行为待验证
execution_requirements:
- database_authorization
- target_oracle_calibration
