schema_version: 1
kind: "scenario"
id: "scenario_create_aggregate_sum_behavior"
name: "CREATE AGGREGATE sum_behavior"
status: "planned"
description: "无业务表的有限四行求和与 NULL 输入。"
factor_ref: "create_aggregate"
fixture_refs:
  - "fixture_create_aggregate_sum"
fact_refs:
  - "create_aggregate_fact_strict"
  - "create_aggregate_fact_init"
preconditions: []
steps:
  -
    id: "sum"
    action: "execute_sql"
    sql: "SELECT fp_ca_ready(v) FROM (SELECT 1 AS v UNION ALL SELECT 2 UNION ALL SELECT 3 UNION ALL SELECT 4) q;"
oracles:
  -
    after_step: "sum"
    kind: "result_rows"
    expected:
      -
        - 10
execution_requirements:
  - "database"
  - "same_session"
