schema_version: 1
kind: "fixture"
id: "fixture_create_aggregate_target"
name: "CREATE AGGREGATE target"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_aggregate"
requires_fixture_refs:
  - "fixture_create_aggregate_transition"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "DROP AGGREGATE IF EXISTS fp_ca_sum(INTEGER) CASCADE;"
  teardown_sqls:
    - "DROP AGGREGATE IF EXISTS fp_ca_sum(INTEGER) CASCADE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
