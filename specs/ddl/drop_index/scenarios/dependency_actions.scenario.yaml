schema_version: 1
kind: "scenario"
id: "scenario_drop_index_dependency_actions"
name: "DROP INDEX dependency_actions"
status: "planned"
description: "构造真实依赖索引的对象，验证默认/显式 RESTRICT 与 CASCADE；不假设所有普通索引都有可构造依赖。"
factor_ref: "drop_index"
fixture_refs: []
fact_refs:
  - "drop_index_fact_default_restrict"
  - "drop_index_fact_cascade"
  - "drop_index_fact_restrict"
preconditions: []
steps:
  -
    id: "construct"
    action: "calibrate_index_dependency_fixture"
    note: "补充约束/系统依赖正文，区分约束所有的索引和可直接删除索引；当前不能执行。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
