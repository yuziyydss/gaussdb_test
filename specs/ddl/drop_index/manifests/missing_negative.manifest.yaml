schema_version: 1
kind: manifest
id: manifest_drop_index_missing_negative
name: drop_index 单规则负向 missing_negative
status: needs_review
description: 前置条件受控，只违反目标规则；准确错误身份没有实机证据。
factor_ref: drop_index
syntax_ref: syntax_drop_index
suite_type: negative
strategy: pairwise
fixture_refs:
- fixture_drop_index_indexes
identifier_policy: {}
bindings:
  concurrently:
  - drop_index_concurrently_absent
  if_exists:
  - drop_index_if_exists_absent
  targets:
  - drop_index_targets_missing
  behavior:
  - drop_index_behavior_default
  - drop_index_behavior_restrict
  - drop_index_behavior_cascade
violates_rule_refs:
- drop_index_rule_exists
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: undefined_index
  fact_refs:
  - drop_index_fact_missing_error
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
