schema_version: 1
kind: "manifest"
id: "manifest_drop_index_online_cascade_negative"
name: "DROP INDEX online_cascade_negative"
status: "needs_review"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_index"
syntax_ref: "syntax_drop_index"
suite_type: "negative"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_index_indexes"
identifier_policy: {}
bindings:
  concurrently:
    - "drop_index_concurrently_present"
  if_exists:
    - "drop_index_if_exists_absent"
    - "drop_index_if_exists_present"
  targets:
    - "drop_index_targets_one"
  behavior:
    - "drop_index_behavior_cascade"
expected:
  default: "error"
  scope: "syntax_and_semantics"
  oracle_status: "needs_verification"
  error_category: "online_drop_cascade"
  fact_refs:
    - "drop_index_fact_online_rejection"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
violates_rule_refs:
  - "drop_index_rule_online_no_cascade"
environment_requirements:
  -
    key: "transaction_mode"
    allowed_values:
      - "autocommit"
    fact_refs:
      - "drop_index_fact_transaction"
