schema_version: 1
kind: scenario
id: scenario_m_alter_index_rename_data
name: scenario_m_alter_index_rename_data
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_index
fact_refs:
- m_alter_index_fact_rename
fixture_refs:
- fixture_m_alter_index_shared_indexes
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: ALTER INDEX m_b03_existing_index RENAME TO m_b03_renamed_index;
- sql: SELECT id,qty FROM m_b01_source ORDER BY id;
oracles:
- kind: result_set
  expected:
  - - 1
    - 10
  - - 2
    - 20
  - - 3
    - 30
execution_requirements:
- database_authorization
- target_oracle_calibration
