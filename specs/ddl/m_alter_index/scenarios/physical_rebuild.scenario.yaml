schema_version: 1
kind: scenario
id: scenario_m_alter_index_physical_rebuild
name: scenario_m_alter_index_physical_rebuild
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_index
fact_refs:
- m_alter_index_fact_rebuild
fixture_refs:
- fixture_m_alter_index_shared_indexes
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: SET/RESET 后分别检查目录参数和物理重建结果
oracles:
- kind: manual_assertion
  expected: 语句成功不等于物理索引内容已立即变化
execution_requirements:
- database_authorization
- target_oracle_calibration
