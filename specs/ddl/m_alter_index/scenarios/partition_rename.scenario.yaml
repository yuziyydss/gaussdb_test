schema_version: 1
kind: scenario
id: scenario_m_alter_index_partition_rename
name: scenario_m_alter_index_partition_rename
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_index
fact_refs:
- m_alter_index_fact_partition
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 需要真实 M LOCAL 分区索引 fixture 后再生成分区改名场景
oracles:
- kind: manual_assertion
  expected: pg_partition 名称改变；普通索引不替代该前置条件
execution_requirements:
- database_authorization
- target_oracle_calibration
