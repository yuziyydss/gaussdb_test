schema_version: 1
kind: scenario
id: scenario_m_alter_index_notice
name: scenario_m_alter_index_notice
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_index
fact_refs:
- m_alter_index_fact_notice
fixture_refs:
- fixture_m_alter_index_shared_indexes
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: ALTER INDEX IF EXISTS m_alter_index_absent RESET (fillfactor);
oracles:
- kind: manual_assertion
  expected: notice 非 error；不能用任意失败通过
execution_requirements:
- database_authorization
- target_oracle_calibration
