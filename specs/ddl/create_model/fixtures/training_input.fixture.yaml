schema_version: 1
kind: fixture
id: fixture_create_model_training_input
name: CREATE MODEL 原文训练输入
status: needs_review
description: >-
  原文 L202-L230 houses 示例的八列和十五行，只有表名改为专用测试名；
  来源由 scenario_create_model_training_input 引用的 example 事实记录。
  数据示例不是产品取值限制，也不是模型训练成功的证据。
  使用 V1 无归属共享 fixture，供 CREATE MODEL 与 PREDICT BY 的输入投影计划复用；
  保留原 ID 和位置，避免复制数据或改变原生命周期 SQL。
factor_ref: null
provides:
  tables:
    - name: fp_model_houses_input
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: true}
        - {name: tax, type: INTEGER, nullable: true}
        - {name: bedroom, type: INTEGER, nullable: true}
        - {name: bath, type: DOUBLE PRECISION, nullable: true}
        - {name: price, type: INTEGER, nullable: true}
        - {name: size, type: INTEGER, nullable: true}
        - {name: lot, type: INTEGER, nullable: true}
        - {name: mark, type: TEXT, nullable: true}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - BEGIN;
    - CREATE TABLE fp_model_houses_input (id INTEGER, tax INTEGER, bedroom INTEGER, bath DOUBLE PRECISION, price INTEGER, size INTEGER, lot INTEGER, mark TEXT);
    - >-
      INSERT INTO fp_model_houses_input(id, tax, bedroom, bath, price, size, lot, mark) VALUES
      (1,590,2,1,50000,770,22100,'a+'),
      (2,1050,3,2,85000,1410,12000,'a+'),
      (3,20,2,1,22500,1060,3500,'a-'),
      (4,870,2,2,90000,1300,17500,'a+'),
      (5,1320,3,2,133000,1500,30000,'a+'),
      (6,1350,2,1,90500,850,25700,'a-'),
      (7,2790,3,2.5,260000,2130,25000,'a+'),
      (8,680,2,1,142500,1170,22000,'a-'),
      (9,1840,3,2,160000,1500,19000,'a+'),
      (10,3680,4,2,240000,2790,20000,'a-'),
      (11,1660,3,1,87000,1030,17500,'a+'),
      (12,1620,3,2,118500,1250,20000,'a-'),
      (13,3100,3,2,140000,1760,38000,'a+'),
      (14,2090,2,3,148000,1550,14000,'a-'),
      (15,650,3,1.5,65000,1450,12000,'a-');
  teardown_sqls:
    - ROLLBACK;
  note: >-
    显式 INSERT 是 seed 的唯一执行定义。仅供授权后的独立新连接，无外层事务；
    同名对象存在则 setup 失败，不预先删除。失败也须回滚。
    ready 只表示已有生命周期 SQL，不是实机验证；不包含训练、预测或模型清理。
