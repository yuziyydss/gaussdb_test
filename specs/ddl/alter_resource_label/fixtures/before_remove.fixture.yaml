schema_version: 1
kind: "fixture"
id: "fixture_alter_resource_label_before_remove"
name: "ALTER RESOURCE LABEL before_remove"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_resource_label"
requires_fixture_refs:
  - "fixture_create_resource_label_source_objects"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE RESOURCE LABEL b9_rl_change ADD COLUMN(b9_rl_source.col_1, b9_rl_source.col_2), TABLE(b9_rl_source_two);"
  teardown_sqls:
    - "DROP RESOURCE LABEL IF EXISTS b9_rl_change;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
