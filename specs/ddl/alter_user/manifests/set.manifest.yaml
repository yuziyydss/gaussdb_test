schema_version: 1
kind: "manifest"
id: "manifest_alter_user_set"
name: "ALTER USER set"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_user"
syntax_ref: "syntax_alter_user"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_user_isolated_users"
identifier_policy: {}
bindings:
  form:
    - "alter_user_form_set"
  assignment:
    - "alter_user_assignment_to"
    - "alter_user_assignment_equal"
  value:
    - "alter_user_value_on"
    - "alter_user_value_default"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "dedicated_user_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "create_role::create_role_fact_privilege"
  -
    key: "password_disable_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "create_role::create_role_fact_disable_admin"
