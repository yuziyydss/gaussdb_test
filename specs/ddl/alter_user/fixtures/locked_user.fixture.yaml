schema_version: 1
kind: "fixture"
id: "fixture_alter_user_locked_user"
name: "ALTER USER locked_user"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_user"
requires_fixture_refs:
  - "fixture_alter_user_isolated_users"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "ALTER USER b9_user_a ACCOUNT LOCK;"
  teardown_sqls:
    - "DROP USER IF EXISTS b9_user_a RESTRICT;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
