schema_version: 1
kind: "manifest"
id: "manifest_drop_materialized_view_cascade"
name: "DROP MATERIALIZED VIEW cascade"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_materialized_view"
syntax_ref: "syntax_drop_materialized_view"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  if_exists:
    - "drop_materialized_view_if_exists_none"
    - "drop_materialized_view_if_exists_yes"
  targets:
    - "drop_materialized_view_targets_dependent"
  behavior:
    - "drop_materialized_view_behavior_cascade"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
