schema_version: 1
kind: "manifest"
id: "manifest_drop_materialized_view_restrict_negative"
name: "DROP MATERIALIZED VIEW restrict_negative"
status: "needs_review"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_materialized_view"
syntax_ref: "syntax_drop_materialized_view"
suite_type: "negative"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  if_exists:
    - "drop_materialized_view_if_exists_none"
    - "drop_materialized_view_if_exists_yes"
  targets:
    - "drop_materialized_view_targets_dependent"
  behavior:
    - "drop_materialized_view_behavior_default"
    - "drop_materialized_view_behavior_restrict"
expected:
  default: "error"
  scope: "syntax_and_semantics"
  oracle_status: "needs_verification"
  error_category: "dependent_objects_exist"
  fact_refs:
    - "drop_materialized_view_fact_restrict"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
violates_rule_refs:
  - "drop_materialized_view_rule_dependent"
