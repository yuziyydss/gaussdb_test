schema_version: 1
kind: "scenario"
id: "scenario_drop_foreign_table_documented_contracts"
name: "DROP FOREIGN TABLE documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_foreign_table"
fixture_refs: []
fact_refs:
  - "drop_foreign_table_fact_purpose"
  - "drop_foreign_table_fact_dependent_objects"
  - "drop_foreign_table_fact_pdb_admin"
  - "drop_foreign_table_fact_exists"
  - "drop_foreign_table_fact_missing"
  - "drop_foreign_table_fact_table"
  - "drop_foreign_table_fact_default_restrict"
  - "drop_foreign_table_fact_cascade"
  - "drop_foreign_table_fact_restrict"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除外表。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "正文称删除时相关索引级联，依赖函数过程无法执行。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "多租PDB仅初始用户可DROP。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "IF EXISTS目标存在删除。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "IF EXISTS缺失仅NOTICE。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "目标为已有外表名。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "CASCADE/RESTRICT可选，默认RESTRICT。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "CASCADE删除依赖对象如视图。"
  -
    id: "contract_8"
    action: "verify_documented_contract"
    note: "RESTRICT存在依赖则拒绝删除。"
oracles:
  -
    kind: "documented_behavior"
    expected: "IF EXISTS目标存在删除。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS缺失仅NOTICE。"
  -
    kind: "documented_behavior"
    expected: "CASCADE/RESTRICT可选，默认RESTRICT。"
  -
    kind: "documented_behavior"
    expected: "CASCADE删除依赖对象如视图。"
  -
    kind: "documented_behavior"
    expected: "RESTRICT存在依赖则拒绝删除。"
execution_requirements:
  - "database"
  - "same_session"
