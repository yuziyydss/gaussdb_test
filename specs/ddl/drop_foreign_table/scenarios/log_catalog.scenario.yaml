schema_version: 1
kind: scenario
id: scenario_drop_foreign_table_log_catalog
name: log_fdw目录删除元数据待校准
status: planned
description: 只观察实际创建/删除后的目录对象身份，不SELECT外表或宣称FDW数据可用。
factor_ref: drop_foreign_table
fixture_refs: [fixture_drop_foreign_table_log_catalog]
fact_refs: [drop_foreign_table_fact_purpose, 'create_server::create_server_fact_compatibility_only', 'create_foreign_table::create_foreign_table_fact_log_fdw_example']
preconditions: [单独执行授权, 非PDB, log_fdw目录DDL能力确认, 新服务器名及schema名不存在, 单独连接与逐对象归属回执]
steps:
- id: target
  candidate:
    manifest_ref: manifest_drop_foreign_table_log_catalog
    params: {table_names: drop_foreign_table_log_catalog_target}
oracles:
- kind: manual_assertion
  step_id: target
  expected: 校准当前版本目录接口，验证本次真实外表已删除且服务器尚存；不得用清理成功或任意错误替代目标结果。
execution_requirements: [database_authorization, target_oracle_calibration, isolated_connection, ownership_scoped_cleanup, preserve_stage_failures]
