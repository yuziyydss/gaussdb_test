schema_version: 1
kind: fixture
id: fixture_m_alter_user_shared_user
name: fixture_m_alter_user_shared_user
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_user
requires_fixture_refs:
- fixture_m_create_user_existing
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: auto
  note: 依赖 CREATE USER 的真实普通用户与同名 Schema；逆序定向清理，不使用 CASCADE。
