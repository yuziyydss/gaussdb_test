schema_version: 1
kind: scenario
id: scenario_m_alter_user_restricted
name: scenario_m_alter_user_restricted
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_user
fact_refs:
- m_alter_user_fact_pguser
- m_alter_user_fact_pdb
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: PGUSER 错误需校准；PDB 初始用户密码限制保留，不触碰初始身份。
oracles:
- kind: manual_assertion
  expected: 未生成或执行高权限身份变更
execution_requirements:
- database_authorization
- target_oracle_calibration
