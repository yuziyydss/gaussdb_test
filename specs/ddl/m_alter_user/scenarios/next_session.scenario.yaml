schema_version: 1
kind: scenario
id: scenario_m_alter_user_next_session
name: scenario_m_alter_user_next_session
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_user
fact_refs:
- m_alter_user_fact_next_session
fixture_refs:
- fixture_m_alter_user_shared_user
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 另外在获授权、可登录的隔离账户检查下一会话 TimeZone，不拿本批 NOLOGIN 当作登录错误 Oracle。
oracles:
- kind: manual_assertion
  expected: 目标会话参数生效边界仍待验证
execution_requirements:
- database_authorization
- target_oracle_calibration
