schema_version: 1
kind: fixture
id: fixture_m_drop_owned_granted_objects
name: fixture_m_drop_owned_granted_objects
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_owned
requires_fixture_refs:
- fixture_m_create_role_existing
provides:
  tables:
  - name: m_drop_owned_namespace.source
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE SCHEMA m_drop_owned_namespace;
  - CREATE TABLE m_drop_owned_namespace.source (id INTEGER);
  - INSERT INTO m_drop_owned_namespace.source VALUES (1);
  - GRANT USAGE ON SCHEMA m_drop_owned_namespace TO m_b04_role_existing, m_b04_role_second;
  - GRANT SELECT ON TABLE m_drop_owned_namespace.source TO m_b04_role_existing, m_b04_role_second;
  teardown_sqls:
  - DROP TABLE m_drop_owned_namespace.source;
  - DROP SCHEMA m_drop_owned_namespace;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
