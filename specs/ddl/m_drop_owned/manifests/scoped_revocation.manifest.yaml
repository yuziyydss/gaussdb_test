schema_version: 1
kind: manifest
id: manifest_m_drop_owned_scoped_revocation
name: manifest_m_drop_owned_scoped_revocation
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_owned
syntax_ref: syntax_m_drop_owned
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_drop_owned_granted_objects
identifier_policy: {}
bindings:
  roles:
  - m_drop_owned_roles_one
  - m_drop_owned_roles_two
  behavior:
  - m_drop_owned_behavior_default
  - m_drop_owned_behavior_restrict
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_drop_owned_fact_mode
- key: actor_authority
  allowed_values:
  - sysadmin
  fact_refs:
  - m_create_role::m_create_role_fact_disable_authority
- key: separation_of_duties
  allowed_values:
  - 'off'
  fact_refs:
  - m_create_role::m_create_role_fact_creation_authority
- key: drop_owned_target_isolation
  allowed_values:
  - fresh_case_roles_no_external_ownership_or_grants
  fact_refs:
  - m_drop_owned_fact_scope
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
