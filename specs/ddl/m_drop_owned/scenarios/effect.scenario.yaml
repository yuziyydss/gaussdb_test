schema_version: 1
kind: scenario
id: scenario_m_drop_owned_effect
name: scenario_m_drop_owned_effect
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_owned
fact_refs:
- m_drop_owned_fact_scope
- m_drop_owned_fact_cascade
- m_drop_owned_fact_shared_owned
fixture_refs:
- fixture_m_drop_owned_granted_objects
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 仅在新建专用无登录角色上验证撤销真实表权限；所有权删除、跨库/共享对象及级联必须另审，不作为任何fixture cleanup。
oracles:
- kind: manual_assertion
  expected: 本批只覆盖角色被授予的真实权限；更广所有权/共享对象行为未生成未执行
execution_requirements:
- database_authorization
- target_oracle_calibration
