schema_version: 1
kind: "scenario"
id: "scenario_alter_sequence_recreate"
name: "ALTER SEQUENCE recreate"
status: "planned"
description: "仅在隔离场景演示保存当前值→删除重建→setval；不将不可修改的其他参数加到 ALTER 正向。"
factor_ref: "alter_sequence"
fixture_refs: []
fact_refs:
  - "alter_sequence_fact_purpose"
  - "alter_sequence_fact_supported"
  - "alter_sequence_fact_recreate"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "仅在隔离场景演示保存当前值→删除重建→setval；不将不可修改的其他参数加到 ALTER 正向。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
