schema_version: 1
kind: "manifest"
id: "manifest_alter_sequence_missing_notice"
name: "ALTER SEQUENCE missing_notice"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_sequence"
syntax_ref: "syntax_alter_sequence"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_sequence_sequences"
identifier_policy: {}
bindings:
  sequence_name:
    - "alter_sequence_sequence_name_missing"
  if_exists:
    - "alter_sequence_if_exists_present"
  setting:
    - "alter_sequence_setting_max_next"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "execution_context"
    allowed_values:
      - "top_level_autocommit"
    fact_refs:
      - "alter_sequence_fact_max_context"
