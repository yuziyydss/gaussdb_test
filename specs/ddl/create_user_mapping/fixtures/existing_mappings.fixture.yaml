schema_version: 1
kind: "fixture"
id: "fixture_create_user_mapping_existing_mappings"
name: "CREATE USER MAPPING existing_mappings"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_user_mapping"
requires_fixture_refs:
  - "fixture_create_user_mapping_server"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE USER MAPPING FOR CURRENT_USER SERVER srv_map_b7 OPTIONS (user 'b7_remote_user');"
    - "CREATE USER MAPPING FOR PUBLIC SERVER srv_map_b7 OPTIONS (user 'b7_remote_user');"
  teardown_sqls:
    - "DROP USER MAPPING IF EXISTS FOR PUBLIC SERVER srv_map_b7;"
    - "DROP USER MAPPING IF EXISTS FOR CURRENT_USER SERVER srv_map_b7;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
