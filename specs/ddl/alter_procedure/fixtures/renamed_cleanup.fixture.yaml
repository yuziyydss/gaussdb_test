schema_version: 1
kind: "fixture"
id: "fixture_alter_procedure_renamed_cleanup"
name: "ALTER PROCEDURE renamed_cleanup"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_procedure"
requires_fixture_refs:
  - "fixture_alter_procedure_existing"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
  teardown_sqls:
    - "ROLLBACK;"
    - "DROP PROCEDURE IF EXISTS fp_proc_renamed;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
