schema_version: 1
kind: "manifest"
id: "manifest_drop_trigger_existing"
name: "DROP TRIGGER existing"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_trigger"
syntax_ref: "syntax_drop_trigger"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_trigger_existing"
identifier_policy: {}
bindings:
  if_exists:
    - "drop_trigger_if_exists_none"
    - "drop_trigger_if_exists_yes"
  behavior:
    - "drop_trigger_behavior_default"
    - "drop_trigger_behavior_cascade"
    - "drop_trigger_behavior_restrict"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
