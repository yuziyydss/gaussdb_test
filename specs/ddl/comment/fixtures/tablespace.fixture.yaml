schema_version: 1
kind: fixture
id: fixture_comment_tablespace
name: COMMENT独占空表空间
factor_ref: comment
status: needs_review
description: 实际创建一个空RELATIVE表空间作为COMMENT目标；不验证磁盘容量、IO调度或表空间内对象行为。
requires_fixture_refs: []
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "CREATE TABLESPACE g_comment_tbspc RELATIVE LOCATION 'g_comment_tbspc';"
  teardown_sqls:
    - "DROP TABLESPACE g_comment_tbspc;"
  note: 仅在预先授权且非事务块的独占测试环境中运行；表空间名与RELATIVE目录初始不存在，且删除前必须保持为空。本case实际创建并拥有该表空间的清理权；setup失败阻止目标并保留实际表空间清单。目标仅观察注释设置、替换与清除，不验证磁盘容量、IO调度、表空间内对象行为或目录身份；清理仅限本case确认创建成功的空表空间，禁止CASCADE、DROP OWNED或吞异常。
