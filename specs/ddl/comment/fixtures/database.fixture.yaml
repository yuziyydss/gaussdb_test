schema_version: 1
kind: fixture
id: fixture_comment_database
name: COMMENT独占数据库资产
factor_ref: comment
status: needs_review
description: 实际创建一个独占数据库作为COMMENT目标；不验证数据库连接、模板复制或全局元数据行为。
requires_fixture_refs: []
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "CREATE DATABASE g_comment_database;"
  teardown_sqls:
    - "DROP DATABASE g_comment_database;"
  note: 仅在预先授权且非PDB的独占测试环境中运行；数据库名初始不存在，不与postgres、template或业务库重名。本case实际创建并拥有该数据库的清理权；setup失败阻止目标并保留实际数据库清单。目标执行前后不得连接到该新建数据库；清理仅限本case确认创建成功的数据库，禁止CASCADE、DROP OWNED或吞异常。不验证模板、编码、连接限制、目录身份或全局元数据。
