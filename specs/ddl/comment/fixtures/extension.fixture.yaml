schema_version: 1
kind: fixture
id: fixture_comment_extension
name: COMMENT事务内专用测试扩展
factor_ref: comment
status: needs_review
description: 事务内安装ext_b7专用测试扩展作为COMMENT目标；不验证扩展脚本、成员对象或安装文件。
requires_fixture_refs: []
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "BEGIN;"
    - "CREATE SCHEMA g_comment_extension_ns;"
    - "CREATE EXTENSION ext_b7 SCHEMA g_comment_extension_ns;"
  teardown_sqls:
    - "ROLLBACK;"
  note: 仅在预先授权且开启enable_extension的独占测试会话中运行；ext_b7为专用测试扩展，支持文件须已按契约准备，不假装任意环境已安装。本case在事务内实际创建扩展并拥有其清理权；setup失败阻止目标并保留实际扩展清单。目标仅观察注释设置、替换与清除，不验证扩展脚本、成员对象、依赖行为或目录身份；清理使用ROLLBACK，禁止CASCADE、DROP OWNED或吞异常。
