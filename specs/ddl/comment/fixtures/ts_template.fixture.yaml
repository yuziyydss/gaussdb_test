schema_version: 1
kind: fixture
id: fixture_comment_ts_template
name: COMMENT事务内内置模板
factor_ref: comment
status: needs_review
description: 事务内为pg_catalog.simple内置模板设置注释；不创建、删除或执行模板。
requires_fixture_refs: []
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "BEGIN;"
  teardown_sqls:
    - "ROLLBACK;"
  note: 仅在预先授权的独占测试会话中运行；目标是pg_catalog.simple内置模板，不由本case创建或拥有。事务仅保证注释变更通过ROLLBACK回滚，不证明模板、词典或目录身份；禁止COMMIT、CASCADE、DROP OWNED或吞异常。
