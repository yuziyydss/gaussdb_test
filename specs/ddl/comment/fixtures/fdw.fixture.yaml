schema_version: 1
kind: fixture
id: fixture_comment_fdw
name: COMMENT外部封装器专属资产
factor_ref: comment
status: needs_review
description: 实际创建一个NO HANDLER、NO VALIDATOR的外部数据封装器；不访问外部数据，不验证目录身份。
requires_fixture_refs: []
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "CREATE FOREIGN DATA WRAPPER g_comment_fdw NO HANDLER NO VALIDATOR;"
  teardown_sqls:
    - "DROP FOREIGN DATA WRAPPER g_comment_fdw RESTRICT;"
  note: 仅在预先授权的独占测试数据库与隔离连接中运行；初始封装器不存在。本case实际创建并拥有该封装器；setup失败阻止目标并保留实际对象清单。只清理本case确认创建成功的封装器，禁止CASCADE、DROP OWNED或吞异常。不访问外部数据，不验证FDW validator或目录身份；目录身份Oracle尚待校准。
