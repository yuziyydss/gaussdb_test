schema_version: 1
kind: fixture
id: fixture_comment_ts_parser
name: COMMENT事务内内置解析器
factor_ref: comment
status: needs_review
description: 事务内为pg_catalog.default内置解析器设置注释；不创建、删除或执行解析器。
requires_fixture_refs: []
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "BEGIN;"
  teardown_sqls:
    - "ROLLBACK;"
  note: 仅在预先授权的独占测试会话中运行；目标是pg_catalog.default内置解析器，不由本case创建或拥有。事务仅保证注释变更通过ROLLBACK回滚，不证明解析、分词或目录身份；禁止COMMIT、CASCADE、DROP OWNED或吞异常。
