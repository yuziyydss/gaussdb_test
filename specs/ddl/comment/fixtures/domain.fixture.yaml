schema_version: 1
kind: fixture
id: fixture_comment_domain
name: COMMENT独占模式中的DOMAIN
factor_ref: comment
status: needs_review
description: 实际创建一个带CHECK约束的INTEGER DOMAIN；不测试域名冲突、类型转换或domain行为。
requires_fixture_refs: []
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "CREATE SCHEMA g_comment_domain_ns;"
    - "CREATE DOMAIN g_comment_domain_ns.positive_integer AS INTEGER CHECK (VALUE > 0);"
  teardown_sqls:
    - "DROP DOMAIN g_comment_domain_ns.positive_integer RESTRICT;"
    - "DROP SCHEMA g_comment_domain_ns RESTRICT;"
  note: 仅在预先授权的独占测试数据库与隔离连接中运行；初始模式不存在，domain在非当前模式中。记录schema和domain的实际创建归属，setup失败阻止目标并保留实际对象清单；只有本case确认创建成功的对象才按domain、schema逆序RESTRICT清理。禁止CASCADE、DROP OWNED或吞异常；不依赖DDL回滚。目录身份Oracle尚待校准。
