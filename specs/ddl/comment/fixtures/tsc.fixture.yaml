schema_version: 1
kind: fixture
id: fixture_comment_tsc
name: COMMENT文本搜索配置专属资产
factor_ref: comment
status: needs_review
description: 实际创建独占schema中的default解析器文本搜索配置；不调用配置分词，不验证目录身份。
requires_fixture_refs: []
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "CREATE SCHEMA g_comment_tsc_ns;"
    - "CREATE TEXT SEARCH CONFIGURATION g_comment_tsc_ns.simple (PARSER=default);"
  teardown_sqls:
    - "DROP TEXT SEARCH CONFIGURATION g_comment_tsc_ns.simple RESTRICT;"
    - "DROP SCHEMA g_comment_tsc_ns RESTRICT;"
  note: 仅在预先授权的独占测试数据库与隔离连接中运行；初始schema和配置不存在，均使用非当前模式。记录schema和配置的实际创建归属；setup失败阻止目标并保留实际对象清单。只清理本case确认创建成功的对象，按configuration、schema逆序RESTRICT清理。禁止CASCADE、DROP OWNED或吞异常。不调用配置分词，不验证配置行为、权限或目录身份；目录身份Oracle尚待校准。
