schema_version: 1
kind: fixture
id: fixture_comment_role
name: COMMENT非登录角色专属资产
factor_ref: comment
status: needs_review
description: 实际创建一个NOLOGIN且PASSWORD DISABLE的独占角色；不创建业务对象，不验证角色权限行为。
requires_fixture_refs: []
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "CREATE ROLE g_comment_role NOLOGIN PASSWORD DISABLE;"
  teardown_sqls:
    - "DROP ROLE g_comment_role;"
  note: 仅在预先授权的独占测试数据库中运行；角色名初始不存在，不能与当前用户或系统角色重名。本case实际创建并拥有该角色的清理权；setup失败阻止目标并保留实际角色清单。只清理本case确认创建成功的角色，禁止CASCADE、DROP OWNED、REVOKE OWNED或吞异常。使用NOLOGIN和PASSWORD DISABLE，不写入密码，不验证角色权限或目录身份；目录身份Oracle尚待校准。
