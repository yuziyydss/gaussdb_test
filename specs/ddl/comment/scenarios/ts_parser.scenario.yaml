schema_version: 1
kind: scenario
id: scenario_comment_ts_parser
name: COMMENT TEXT SEARCH PARSER目录身份待校准
factor_ref: comment
status: planned
description: 只观察pg_catalog.default内置解析器注释的事务内设置与回滚；不验证解析、分词或目录身份。
fixture_refs: [fixture_comment_ts_parser]
fact_refs:
  - comment_fact_authority
  - create_text_search_configuration::create_text_search_configuration_fact_internal
preconditions:
  - 独占测试会话与隔离连接
  - pg_catalog.default解析器存在
  - 当前用户具有系统管理员注释权限
  - 事务开始前记录解析器注释初始值
steps:
  - id: comment_ts_parser
    candidate:
      manifest_ref: manifest_comment_ts_parser
      params: {target: comment_target_text_search_parser_builtin, text: comment_text_plain}
oracles:
  - kind: manual_assertion
    step_id: comment_ts_parser
    expected: 校准目录中该解析器注释的事务内设置与回滚；不得用目标SQL成功或回滚成功替代目录Oracle。
execution_requirements: [database_authorization, target_oracle_calibration, isolated_connection, preserve_stage_failures]
