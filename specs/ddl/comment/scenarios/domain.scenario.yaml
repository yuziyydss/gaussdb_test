schema_version: 1
kind: scenario
id: scenario_comment_domain
name: COMMENT DOMAIN目录身份待校准
factor_ref: comment
status: planned
description: 只观察实际DOMAIN注释的设置、替换与清除；不宣称domain转换或约束行为。
fixture_refs: [fixture_comment_domain]
fact_refs:
  - comment_fact_authority
  - comment_fact_object_domain_syntax
  - comment_fact_syntax_b
preconditions:
  - 独占测试数据库与隔离连接
  - 初始domain和schema不存在
  - 记录schema和domain的实际创建归属
steps:
  - id: comment_domain
    candidate:
      manifest_ref: manifest_comment_domain
      params: {target: comment_target_domain_fresh, text: comment_text_plain}
oracles:
  - kind: manual_assertion
    step_id: comment_domain
    expected: 校准目录中该DOMAIN注释的设置、替换与清除；不得用目标SQL成功或清理成功替代目录Oracle。
execution_requirements: [database_authorization, target_oracle_calibration, isolated_connection, ownership_scoped_cleanup, preserve_stage_failures]
