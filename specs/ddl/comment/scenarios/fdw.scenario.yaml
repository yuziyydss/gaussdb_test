schema_version: 1
kind: scenario
id: scenario_comment_fdw
name: COMMENT FOREIGN DATA WRAPPER目录身份待校准
factor_ref: comment
status: planned
description: 只观察实际外部封装器注释的设置、替换与清除；不访问外部数据。
fixture_refs: [fixture_comment_fdw]
fact_refs:
  - comment_fact_authority
  - create_foreign_data_wrapper::create_foreign_data_wrapper_fact_privilege
preconditions:
  - 独占测试数据库与隔离连接
  - 初始封装器不存在
  - 记录封装器的实际创建归属
steps:
  - id: comment_fdw
    candidate:
      manifest_ref: manifest_comment_fdw
      params: {target: comment_target_fdw_fresh, text: comment_text_plain}
oracles:
  - kind: manual_assertion
    step_id: comment_fdw
    expected: 校准目录中该外部封装器注释的设置、替换与清除；不得用目标SQL成功或清理成功替代目录Oracle。
execution_requirements: [database_authorization, target_oracle_calibration, isolated_connection, ownership_scoped_cleanup, preserve_stage_failures]
