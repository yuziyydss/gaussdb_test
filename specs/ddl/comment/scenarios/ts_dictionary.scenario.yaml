schema_version: 1
kind: scenario
id: scenario_comment_ts_dictionary
name: COMMENT TEXT SEARCH DICTIONARY目录身份待校准
factor_ref: comment
status: planned
description: 只观察实际文本搜索词典注释的设置、替换与清除；不调用词典分词，不验证运行时行为。
fixture_refs: [fixture_comment_ts_dictionary]
fact_refs:
  - comment_fact_authority
  - create_text_search_dictionary::create_text_search_dictionary_fact_internal
  - create_text_search_dictionary::create_text_search_dictionary_fact_sysadmin
preconditions:
  - 独占测试数据库与隔离连接
  - 初始schema和词典不存在
  - 记录schema和词典的实际创建归属
steps:
  - id: comment_dictionary
    candidate:
      manifest_ref: manifest_comment_ts_dictionary
      params: {target: comment_target_dictionary_fresh, text: comment_text_plain}
oracles:
  - kind: manual_assertion
    step_id: comment_dictionary
    expected: 校准目录中该词典注释的设置、替换与清除；不得用目标SQL成功或清理成功替代目录Oracle。
execution_requirements: [database_authorization, target_oracle_calibration, isolated_connection, ownership_scoped_cleanup, preserve_stage_failures]
