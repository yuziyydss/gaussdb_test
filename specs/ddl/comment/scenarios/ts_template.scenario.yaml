schema_version: 1
kind: scenario
id: scenario_comment_ts_template
name: COMMENT TEXT SEARCH TEMPLATE目录身份待校准
factor_ref: comment
status: planned
description: 只观察pg_catalog.simple内置模板注释的事务内设置与回滚；不验证模板、词典或目录身份。
fixture_refs: [fixture_comment_ts_template]
fact_refs:
  - comment_fact_authority
  - create_text_search_dictionary::create_text_search_dictionary_fact_internal
preconditions:
  - 独占测试会话与隔离连接
  - pg_catalog.simple模板存在
  - 当前用户具有系统管理员注释权限
  - 事务开始前记录模板注释初始值
steps:
  - id: comment_ts_template
    candidate:
      manifest_ref: manifest_comment_ts_template
      params: {target: comment_target_text_search_template_builtin, text: comment_text_plain}
oracles:
  - kind: manual_assertion
    step_id: comment_ts_template
    expected: 校准目录中该模板注释的事务内设置与回滚；不得用目标SQL成功或回滚成功替代目录Oracle。
execution_requirements: [database_authorization, target_oracle_calibration, isolated_connection, preserve_stage_failures]
