schema_version: 1
kind: manifest
id: manifest_comment_cast
name: COMMENT CAST有限代表
factor_ref: comment
syntax_ref: syntax_comment
status: needs_review
description: 实际schema中底层函数支撑的double precision到timestamptz转换及四类注释文本；不覆盖WITHOUT FUNCTION、domain转换或目录行为。
suite_type: positive
strategy: pairwise
fixture_refs: [fixture_comment_cast]
identifier_policy: {}
bindings:
  target: [comment_target_cast_fresh]
  text: [comment_text_plain, comment_text_unicode, comment_text_quote, comment_text_null]
environment_requirements:
  - key: cast_create_privilege
    allowed_values: ["true"]
    fact_refs: [comment_fact_authority]
  - key: comment_authority
    allowed_values: [fixture_object_owner]
    fact_refs: [comment_fact_authority]
  - key: cleanup_cast
    allowed_values: ["true"]
    fact_refs: [drop_cast::drop_cast_fact_permission]
  - key: cleanup_namespace
    allowed_values: [owned_fresh_non_current_schema]
    fact_refs: [drop_schema::drop_schema_fact_permission, drop_schema::drop_schema_fact_current]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
