schema_version: 1
kind: "fixture"
id: "fixture_create_directory_existing_directory"
name: "CREATE DIRECTORY existing_directory"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_directory"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
    - "CREATE DIRECTORY dir_b7 AS '/tmp/gaussdb_b7_directory';"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
