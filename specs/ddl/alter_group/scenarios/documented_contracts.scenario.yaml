schema_version: 1
kind: "scenario"
id: "scenario_alter_group_documented_contracts"
name: "ALTER GROUP documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_group"
fixture_refs: []
fact_refs:
  - "alter_group_fact_purpose"
  - "alter_group_fact_legacy"
  - "alter_group_fact_membership"
  - "alter_group_fact_user_names"
  - "alter_group_fact_group_name"
  - "alter_group_fact_new_name"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "修改用户组属性。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "非SQL标准且不推荐使用。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "ADD/DROP USER等价于授予/回收用户或角色的成员权限，建议GRANT/REVOKE替代。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "成员必须是已有角色，含大写须双引号。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "组必须已有，含大写须双引号。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "新名合法，最多63字符，超长截断，未引用大写折叠。"
oracles:
  -
    kind: "documented_behavior"
    expected: "ADD/DROP USER等价于授予/回收用户或角色的成员权限，建议GRANT/REVOKE替代。"
execution_requirements:
  - "database"
  - "same_session"
