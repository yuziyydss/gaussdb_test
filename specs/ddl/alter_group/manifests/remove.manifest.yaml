schema_version: 1
kind: "manifest"
id: "manifest_alter_group_remove"
name: "ALTER GROUP remove"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_group"
syntax_ref: "syntax_alter_group"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_group_members"
identifier_policy: {}
bindings:
  form:
    - "alter_group_form_remove"
  members:
    - "alter_group_members_one"
    - "alter_group_members_two"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "dedicated_role_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "create_role::create_role_fact_privilege"
  -
    key: "password_disable_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "create_role::create_role_fact_disable_admin"
