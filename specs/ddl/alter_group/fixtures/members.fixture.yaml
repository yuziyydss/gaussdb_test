schema_version: 1
kind: "fixture"
id: "fixture_alter_group_members"
name: "ALTER GROUP members"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_group"
requires_fixture_refs:
  - "fixture_alter_group_isolated_roles"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "GRANT b9_role_a TO b9_role_b, b9_role_c;"
  teardown_sqls:
    - "REVOKE b9_role_a FROM b9_role_b, b9_role_c;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
