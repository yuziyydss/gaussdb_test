schema_version: 1
kind: "scenario"
id: "scenario_alter_row_level_security_policy_documented_contracts"
name: "ALTER ROW LEVEL SECURITY POLICY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_row_level_security_policy"
fixture_refs: []
fact_refs:
  - "alter_row_level_security_policy_fact_purpose"
  - "alter_row_level_security_policy_fact_privilege"
  - "alter_row_level_security_policy_fact_boolean"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "修改已有RLS策略名、目标用户或USING表达式。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅表所有者或管理员可修改。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "USING表达式须类似WHERE的布尔条件。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
