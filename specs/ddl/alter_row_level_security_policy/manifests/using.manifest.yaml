schema_version: 1
kind: "manifest"
id: "manifest_alter_row_level_security_policy_using"
name: "ALTER ROW LEVEL SECURITY POLICY using"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_row_level_security_policy"
syntax_ref: "syntax_alter_row_level_security_policy"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_row_level_security_policy_existing_policy"
identifier_policy: {}
bindings:
  long_form:
    - "alter_row_level_security_policy_long_form_short"
    - "alter_row_level_security_policy_long_form_long"
  operation:
    - "alter_row_level_security_policy_operation_using"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "table_owner_or_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "alter_row_level_security_policy_fact_privilege"
