schema_version: 1
kind: "fixture"
id: "fixture_alter_row_level_security_policy_rename_cleanup"
name: "ALTER ROW LEVEL SECURITY POLICY rename_cleanup"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_row_level_security_policy"
requires_fixture_refs:
  - "fixture_create_row_level_security_policy_source"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE ROW LEVEL SECURITY POLICY b10_rls_existing ON b10_rls_source USING (col_1 > 0);"
  teardown_sqls:
    - "DROP ROW LEVEL SECURITY POLICY IF EXISTS b10_rls_renamed ON b10_rls_source;"
    - "DROP ROW LEVEL SECURITY POLICY IF EXISTS b10_rls_existing ON b10_rls_source;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
