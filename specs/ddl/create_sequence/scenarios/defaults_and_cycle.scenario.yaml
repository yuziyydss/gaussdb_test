schema_version: 1
kind: scenario
id: scenario_create_sequence_defaults_and_cycle
name: CREATE SEQUENCE 默认值、缓存与循环行为
status: planned
description: 验证 int64/int128、正负步长、边界默认、缓存空洞、耗尽错误及循环序列非唯一性。
factor_ref: create_sequence
fixture_refs: []
fact_refs: [cs_fact_sequence_nature, cs_fact_generate_series_calls, cs_fact_large_maximum, cs_fact_min_defaults, cs_fact_max_defaults, cs_fact_cache_guidance, cs_fact_no_cycle_exhaustion, cs_fact_cycle_uniqueness]
preconditions: [隔离 Schema, 可反复调用 nextval, 可捕获错误与序列元数据]
steps: [创建普通和 LARGE 的递增及递减序列, 查询边界与起始元数据, 调用 nextval 至耗尽或循环, 用 generate_series 校验 N+1 次调用, 比较 CACHE 造成的空洞]
oracles: [位宽和默认边界符合原文, NO CYCLE 耗尽时报目标错误, CYCLE 返回重复值, generate_series 调用次数与原文一致]
execution_requirements: [真实 GaussDB, 系统目录查询, 错误 Oracle 校准]
