schema_version: 1
kind: scenario
id: scenario_create_sequence_examples
name: CREATE SEQUENCE 文档示例回放
status: planned
description: 逐段回放递增、显式列默认自增和循环示例，不由示例外推未声明规则。
factor_ref: create_sequence
fixture_refs: []
fact_refs: [cs_fact_example_increment, cs_fact_example_owned_by, cs_fact_example_cycle]
preconditions: [隔离 Schema, 示例对象名可用]
steps: [回放 seq1 的 101 和 111, 回放 OWNED BY 加显式 nextval 默认值, 回放最大值 3 的循环序列]
oracles: [结果分别与文档的 101/111、1/2/3 和 1/2/3/1/2 一致, 清理后对象不存在]
execution_requirements: [真实 GaussDB, 结果集断言, 生命周期清理]
