schema_version: 1
kind: scenario
id: scenario_create_sequence_temporary_lifecycle
name: 临时序列会话隔离与遮蔽
status: planned
description: 在 PG 模式多会话中验证临时模式、跨会话访问、永久对象遮蔽及会话退出清理。
factor_ref: create_sequence
fixture_refs: []
fact_refs: [cs_fact_temp_pg_mode, cs_fact_temp_lifecycle, cs_fact_temp_schema, cs_fact_temp_access, cs_fact_temp_shadowing]
preconditions: [PG 兼容模式, 两个独立会话, 同名永久序列]
steps: [分别在预建和未预建临时模式时创建临时序列, 从另一会话访问, 比较限定和不限定名称, 退出创建会话]
oracles: [每会话仅一个临时模式, 跨会话访问报错, 不限定名称命中临时对象, 会话退出后对象消失]
execution_requirements: [多会话, 捕获错误, 系统目录查询]
