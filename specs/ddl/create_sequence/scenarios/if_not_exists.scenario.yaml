schema_version: 1
kind: scenario
id: scenario_create_sequence_if_not_exists
name: CREATE SEQUENCE IF NOT EXISTS 生命周期
status: planned
description: 预建同名 relation 后比较 NOTICE、不新建和无保护 ERROR 行为。
factor_ref: create_sequence
fixture_refs: []
fact_refs: [cs_fact_if_not_exists]
preconditions: [当前 Schema 中分别预建同名序列、表、索引、视图或外表]
steps: [带 IF NOT EXISTS 创建, 不带 IF NOT EXISTS 创建, 检查原 relation 身份和序列数量]
oracles: [保护形态返回 NOTICE 且不新建, 无保护形态返回 ERROR, 原对象不被替换]
execution_requirements: [对象生命周期, NOTICE 与 ERROR 捕获]
