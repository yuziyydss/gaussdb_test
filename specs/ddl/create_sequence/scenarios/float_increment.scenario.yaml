schema_version: 1
kind: scenario
id: scenario_create_sequence_float_increment
name: 浮点 increment 兼容模式校准
status: planned
description: V1 环境门禁不参与 factor 规则计算，故在场景中分别验证 B 模式转换与其他模式拒绝。
factor_ref: create_sequence
fixture_refs: []
fact_refs: [cs_fact_float_increment_mode, cs_open_float_increment_generation]
preconditions: [独立 B 与 PG 兼容模式数据库]
steps: [在 B 模式创建 INCREMENT 1.5 序列并读取元数据及 nextval, 在 PG 模式执行同一语句]
oracles: [B 模式步长转换为整数, PG 模式返回经校准的目标错误身份]
execution_requirements: [多兼容模式环境, 系统目录查询, 错误 Oracle 校准]
