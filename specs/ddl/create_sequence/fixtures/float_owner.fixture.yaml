schema_version: 1
kind: fixture
id: fixture_create_sequence_float_owner
name: 浮点序列有限候选的fresh从属表
status: needs_review
description: 只创建本case普通空表；目标必须OWNED BY本表id，同模式同创建用户。
factor_ref: create_sequence
provides:
  tables:
  - name: t_cs_float_owner
    persistence: permanent
    table_kind: regular
    columns:
    - {name: id, type: INTEGER, nullable: true}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls: ["CREATE TABLE t_cs_float_owner (id INTEGER);"]
  teardown_sqls: ["DROP TABLE t_cs_float_owner;"]
  note: 必须fresh user schema与同一创建用户；序列名及本表须不存在。setup失败不授权清理。成功目标通过OWNED BY从属，删除本表自动删除序列；PG负例失败后仅清理已创建表，先恢复错误事务。无预DROP/CASCADE，不依赖DDL回滚；此声明不是数据库已执行。
