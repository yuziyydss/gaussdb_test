schema_version: 1
kind: fixture
id: fixture_create_sequence_system_owner
name: A模式hasrowid系统列前置
status: needs_review
description: 只声明普通id列；系统rowid/rowno由真实hasrowid DDL产生，不冒充普通列或猜测类型。
factor_ref: create_sequence
provides:
  tables:
  - name: g_a3_cs_system_owner
    persistence: permanent
    table_kind: regular
    columns: [{name: id, type: INTEGER, nullable: true}]
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls: ["CREATE TABLE g_a3_cs_system_owner (id INTEGER) WITH (hasrowid = on);"]
  teardown_sqls: ["DROP TABLE g_a3_cs_system_owner RESTRICT;"]
  note: >-
    获授权的A模式独占新schema，同一会话同一所有者；本表及目标序列名必须不存在。
    hasrowid自动关联的序列/索引须运行时记录归属和依赖。仅本次成功CREATE才能清理。
    目标意外成功时其序列身份另记入失败清理计划，不能凭本表DROP推定全部残留消失。
    不执行任何数据库SQL，不将系统目录身份、目标错误或清理声明为已验证。
