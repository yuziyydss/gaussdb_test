schema_version: 1
kind: fixture
id: fixture_create_sequence_owner_table
name: CREATE SEQUENCE OWNED BY 目标表
status: ready
description: 为同模式、同创建用户的 OWNED BY 列关联提供最小普通表。
factor_ref: create_sequence
provides:
  tables:
    - name: t_cs_owner
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
seed: {required: false, rows: []}
execution: {status: ready, mode: auto, note: "fixture 与序列均由当前用户在当前模式创建。"}
