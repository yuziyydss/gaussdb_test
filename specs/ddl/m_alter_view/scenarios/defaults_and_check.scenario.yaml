schema_version: 1
kind: scenario
id: scenario_m_alter_view_defaults_and_check
name: scenario_m_alter_view_defaults_and_check
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_view
fact_refs:
- m_alter_view_fact_defaults_noop
- m_alter_view_fact_check
fixture_refs:
- fixture_m_alter_view_view
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 独立验证 DEFAULT 的无实际意义和 CHECK OPTION DML 拒绝，不能从 ALTER 成功推导赋值行为。
oracles:
- kind: manual_assertion
  expected: 保留与共享 DEFAULT 列合同之间的模式/命令边界；行为未执行
execution_requirements:
- database_authorization
- target_oracle_calibration
