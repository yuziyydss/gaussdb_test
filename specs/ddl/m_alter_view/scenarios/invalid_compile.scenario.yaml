schema_version: 1
kind: scenario
id: scenario_m_alter_view_invalid_compile
name: scenario_m_alter_view_invalid_compile
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_view
fact_refs:
- m_alter_view_fact_compile
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 补齐 enable_view_invalidation 的环境来源和删除依赖/补依赖/COMPILE 生命周期
oracles:
- kind: manual_assertion
  expected: 当前 COMPILE 仅针对已有效视图；不等价失效重编译验证
execution_requirements:
- database_authorization
- target_oracle_calibration
