schema_version: 1
kind: fixture
id: fixture_m_alter_view_moved_view
name: fixture_m_alter_view_moved_view
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_view
requires_fixture_refs:
- fixture_m_create_table_source
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE SCHEMA m_alter_view_destination;
  - CREATE VIEW m_alter_view_target AS SELECT id,qty FROM m_b01_source;
  teardown_sqls:
  - DROP VIEW IF EXISTS m_alter_view_destination.m_alter_view_target;
  - DROP VIEW IF EXISTS m_alter_view_target;
  - DROP SCHEMA m_alter_view_destination;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
