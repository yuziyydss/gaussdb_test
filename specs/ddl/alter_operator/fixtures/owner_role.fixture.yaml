schema_version: 1
kind: fixture
id: fixture_alter_operator_owner_role
name: ALTER OPERATOR owner_role
status: needs_review
description: 创建专用NOLOGIN操作符属主角色并授予当前会话用户成员关系；不验证所有权行为。
factor_ref: alter_operator
requires_fixture_refs:
  - fixture_alter_operator_target_schema
provides: {}
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "CREATE ROLE b7_operator_owner NOLOGIN PASSWORD DISABLE;"
    - "GRANT b7_operator_owner TO CURRENT_USER;"
  teardown_sqls:
    - "ROLLBACK;"
  note: 仅用于静态语法代表；依赖上游操作符fixture的事务边界，执行前必须使用独占非生产会话并确认当前操作符所有权、成员关系和模式权限。不验证操作符所有权变化、权限失败矩阵、索引契约或角色生命周期。
