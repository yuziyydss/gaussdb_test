schema_version: 1
kind: "fixture"
id: "fixture_alter_operator_target_schema"
name: "ALTER OPERATOR target_schema"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_operator"
requires_fixture_refs:
  - "fixture_create_operator_existing_operators"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE SCHEMA op_b7_target;"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
