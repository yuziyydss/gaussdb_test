schema_version: 1
kind: "manifest"
id: "manifest_alter_operator_schema"
name: "ALTER OPERATOR schema"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_operator"
syntax_ref: "syntax_alter_operator"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_operator_target_schema"
identifier_policy: {}
bindings:
  action:
    - "alter_operator_action_schema"
  signature:
    - "alter_operator_signature_binary"
    - "alter_operator_signature_prefix"
    - "alter_operator_signature_postfix"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "operator_owner_authorized"
    allowed_values:
      - "true"
    fact_refs:
      - "alter_operator_fact_owner_privilege"
  -
    key: "operator_creator_authorized"
    allowed_values:
      - "true"
    fact_refs:
      - "create_operator::create_operator_fact_privilege"
