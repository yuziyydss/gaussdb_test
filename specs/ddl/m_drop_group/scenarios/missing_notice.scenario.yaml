schema_version: 1
kind: scenario
id: scenario_m_drop_group_missing_notice
name: scenario_m_drop_group_missing_notice
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_group
fact_refs:
- m_drop_group_fact_missing
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: DROP GROUP IF EXISTS m_drop_group_absent;
oracles:
- kind: manual_assertion
  expected: 不存在目标的 NOTICE 需管理场景审阅后验证；不推导普通用户支持
execution_requirements:
- database_authorization
- target_oracle_calibration
