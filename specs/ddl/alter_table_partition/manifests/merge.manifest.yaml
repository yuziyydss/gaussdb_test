schema_version: 1
kind: "manifest"
id: "manifest_alter_table_partition_merge"
name: "ALTER TABLE PARTITION merge"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_table_partition"
syntax_ref: "syntax_alter_table_partition"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_table_partition_partition_source"
identifier_policy: {}
bindings:
  if_exists:
    - "alter_table_partition_if_exists_off"
    - "alter_table_partition_if_exists_on"
  target:
    - "alter_table_partition_target_plain"
    - "alter_table_partition_target_only"
    - "alter_table_partition_target_only_parenthesized"
  action:
    - "alter_table_partition_action_merge"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
