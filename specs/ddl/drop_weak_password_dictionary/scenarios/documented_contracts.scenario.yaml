schema_version: 1
kind: "scenario"
id: "scenario_drop_weak_password_dictionary_documented_contracts"
name: "DROP WEAK PASSWORD DICTIONARY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_weak_password_dictionary"
fixture_refs: []
fact_refs:
  - "drop_weak_password_dictionary_fact_purpose"
  - "drop_weak_password_dictionary_fact_privilege"
  - "drop_weak_password_dictionary_fact_all_entries"
preconditions:
  - "只在另行授权的可销毁独占环境评审全局安全策略；新Schema或普通独占数据库不自动证明字典状态隔离。"
  - "须先取得保密字典基线及精确恢复方案；不将现有口令写入日志。DROP清空全部弱口令，不能当单个测试值的通用teardown。"
  - "多值主语法与示例形态仍有歧义；没有已校准Oracle和恢复证据时不自动执行。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "清空弱口令字典。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅初始用户、系统管理员、安全管理员可执行。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "清空gs_global_config中的全部弱口令，而非某个测试值。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
  - "global_security_state_authorization"
  - "confidential_dictionary_baseline"
  - "exclusive_disposable_environment"
  - "precise_restore_review"
