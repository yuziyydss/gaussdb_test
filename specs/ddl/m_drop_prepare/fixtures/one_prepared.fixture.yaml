schema_version: 1
kind: fixture
id: fixture_m_drop_prepare_one_prepared
name: fixture_m_drop_prepare_one_prepared
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_prepare
requires_fixture_refs: []
provides:
  tables:
  - name: m_drop_prepare_data
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE m_drop_prepare_data (id INT);
  - PREPARE m_drop_prepare_stmt FROM 'SELECT id FROM m_drop_prepare_data';
  teardown_sqls:
  - DROP TABLE m_drop_prepare_data;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。 成功 target 已释放唯一预备语句，不能
    teardown 再次 DEALLOCATE；目标失败时必须关闭此 case 独占会话释放残留，不能复用连接继续。
