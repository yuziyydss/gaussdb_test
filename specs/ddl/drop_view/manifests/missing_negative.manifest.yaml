schema_version: 1
kind: manifest
id: manifest_drop_view_missing_negative
name: drop_view 单规则负向 missing_negative
status: needs_review
description: 前置条件受控，只违反目标规则；准确错误身份没有实机证据。
factor_ref: drop_view
syntax_ref: syntax_drop_view
suite_type: negative
strategy: pairwise
fixture_refs:
- fixture_drop_view_views
identifier_policy: {}
bindings:
  if_exists:
  - drop_view_if_exists_absent
  targets:
  - drop_view_targets_missing
  behavior:
  - drop_view_behavior_default
  - drop_view_behavior_restrict
  - drop_view_behavior_cascade
violates_rule_refs:
- drop_view_rule_exists
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: undefined_view
  fact_refs:
  - drop_view_fact_missing_error
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
