schema_version: 1
kind: "manifest"
id: "manifest_drop_view_positive"
name: "DROP VIEW positive"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_view"
syntax_ref: "syntax_drop_view"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_view_views"
identifier_policy: {}
bindings:
  if_exists:
    - "drop_view_if_exists_absent"
    - "drop_view_if_exists_present"
  targets:
    - "drop_view_targets_one"
    - "drop_view_targets_two"
    - "drop_view_targets_missing"
    - "drop_view_targets_dependent"
  behavior:
    - "drop_view_behavior_default"
    - "drop_view_behavior_restrict"
    - "drop_view_behavior_cascade"
expected:
  default: "success"
  scope: "syntax_and_semantics"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
