schema_version: 1
kind: "scenario"
id: "scenario_alter_session_rollback_contract"
name: "ALTER SESSION rollback_contract"
status: "planned"
description: "通过SET跨章事实验证事务回滚恢复参数，异常退出丢弃专用会话。"
factor_ref: "alter_session"
fixture_refs: []
fact_refs:
  - "set::set_fact_session_rollback"
  - "alter_session_fact_session_scope"
preconditions: []
steps:
  -
    id: "rollback"
    action: "verify_parameter_state_before_after_rollback"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
