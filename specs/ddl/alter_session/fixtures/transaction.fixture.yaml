schema_version: 1
kind: "fixture"
id: "fixture_alter_session_transaction"
name: "ALTER SESSION transaction"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_session"
requires_fixture_refs:
  - "fixture_set_transaction"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "auto"
  note: "只依赖上游能力；不复制对象定义或生命周期 SQL。"
