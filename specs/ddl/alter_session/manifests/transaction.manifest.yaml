schema_version: 1
kind: "manifest"
id: "manifest_alter_session_transaction"
name: "ALTER SESSION transaction"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_session"
syntax_ref: "syntax_alter_session"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_session_transaction"
identifier_policy: {}
bindings:
  form:
    - "alter_session_form_transaction"
  characteristics:
    - "alter_session_characteristics_current"
    - "alter_session_characteristics_session"
  transaction_modes:
    - "alter_session_transaction_modes_read_only"
    - "alter_session_transaction_modes_read_write"
    - "alter_session_transaction_modes_committed"
    - "alter_session_transaction_modes_uncommitted"
    - "alter_session_transaction_modes_combined"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
