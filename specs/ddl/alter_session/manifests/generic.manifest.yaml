schema_version: 1
kind: "manifest"
id: "manifest_alter_session_generic"
name: "ALTER SESSION generic"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_session"
syntax_ref: "syntax_alter_session"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_session_transaction"
identifier_policy: {}
bindings:
  form:
    - "alter_session_form_generic"
  assignment:
    - "alter_session_assignment_to"
    - "alter_session_assignment_equal"
  generic_value:
    - "alter_session_generic_value_postgres"
    - "alter_session_generic_value_default"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
