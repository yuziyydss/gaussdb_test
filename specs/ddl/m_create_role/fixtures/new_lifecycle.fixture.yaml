schema_version: 1
kind: fixture
id: fixture_m_create_role_new_lifecycle
name: fixture_m_create_role_new_lifecycle
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_role
requires_fixture_refs:
- fixture_m_create_role_parent
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: auto
  note: 目标名和父角色名预先不存在，逐 case 串行；仅删除成功创建的目标，随后逆序删除父角色。负向不以任何非目标错误通过。
