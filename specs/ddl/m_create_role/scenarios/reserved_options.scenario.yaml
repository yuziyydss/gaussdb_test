schema_version: 1
kind: scenario
id: scenario_m_create_role_reserved_options
name: scenario_m_create_role_reserved_options
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_role
fact_refs:
- m_create_role_fact_reserved
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 另建忽略 PROFILE/DEFAULT TABLESPACE 的对照，不从语法接受推断配置成功
oracles:
- kind: manual_assertion
  expected: 保留选项被忽略的文档行为；无运行验证
execution_requirements:
- database_authorization
- target_oracle_calibration
