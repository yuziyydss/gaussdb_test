schema_version: 1
kind: scenario
id: scenario_m_create_role_membership_and_login
name: scenario_m_create_role_membership_and_login
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_role
fact_refs:
- m_create_role_fact_membership
- m_create_role_fact_no_login
fixture_refs:
- fixture_m_create_role_new_lifecycle
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 创建目标后检查 pg_roles.rolcanlogin 与成员关系；不尝试连接禁用登录的角色。
oracles:
- kind: manual_assertion
  expected: NOLOGIN 生效且属于指定父角色，目标 Oracle 尚未执行
execution_requirements:
- database_authorization
- target_oracle_calibration
