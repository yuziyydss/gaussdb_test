schema_version: 1
kind: fixture
id: fixture_m_alter_table_subpartition_shared_partition
name: fixture_m_alter_table_subpartition_shared_partition
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_table_subpartition
requires_fixture_refs:
- fixture_m_create_table_subpartition_range_hash_source
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: auto
  note: 复用创建分区包的真实表与seed；按依赖拓扑setup、逆序定向teardown，不复制前置或全局清理。
