schema_version: 1
kind: scenario
id: scenario_m_create_table_generated_recompute
name: scenario_m_create_table_generated_recompute
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_table
fact_refs:
- m_create_table_fact_generated_recompute
fixture_refs:
- fixture_m_create_table_target_lifecycle
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: CREATE TABLE m_b01_created(id INT, qty INT, g INT GENERATED ALWAYS AS (id + qty) STORED);
- sql: INSERT INTO m_b01_created(id,qty) VALUES (2,9);
- sql: UPDATE m_b01_created SET qty=10 WHERE id=2;
oracles:
- kind: result_set
  sql: SELECT id,qty,g FROM m_b01_created ORDER BY id;
  expected:
  - - 2
    - 10
    - 12
execution_requirements:
- database_authorization
- target_oracle_calibration
