schema_version: 1
kind: scenario
id: scenario_m_create_table_like_independence
name: scenario_m_create_table_like_independence
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_table
fact_refs:
- m_create_table_fact_like
fixture_refs:
- fixture_m_create_table_target_lifecycle
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: CREATE TABLE m_b01_created LIKE m_b01_like_source;
- sql: INSERT INTO m_b01_like_source VALUES (1,2);
oracles:
- kind: result_set
  sql: SELECT * FROM m_b01_created;
  expected: []
execution_requirements:
- database_authorization
- target_oracle_calibration
