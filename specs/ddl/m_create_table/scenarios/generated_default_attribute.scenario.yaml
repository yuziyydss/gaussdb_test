schema_version: 1
kind: scenario
id: scenario_m_create_table_generated_default_attribute
name: scenario_m_create_table_generated_default_attribute
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_table
fact_refs:
- m_create_table_fact_generated
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: CREATE TABLE m_b01_invalid (id INT, g INT GENERATED ALWAYS AS (id + 1) STORED DEFAULT 9);
oracles:
- kind: target_error
  expected: 拒绝生成列 DEFAULT 属性；目标 SQLSTATE 待校准
execution_requirements:
- database_authorization
- target_oracle_calibration
