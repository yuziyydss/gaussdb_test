schema_version: 1
kind: manifest
id: manifest_m_create_table_column_storage_negative
name: manifest_m_create_table_column_storage_negative
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_table
syntax_ref: syntax_m_create_table
suite_type: negative
strategy: pairwise
fixture_refs:
- fixture_m_create_table_target_lifecycle
identifier_policy: {}
bindings:
  orientation:
  - m_create_table_orientation_column
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_create_table_fact_mode
violates_rule_refs:
- m_create_table_rule_row_only
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: row_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
