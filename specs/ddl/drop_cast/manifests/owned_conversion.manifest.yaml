schema_version: 1
kind: "manifest"
id: "manifest_drop_cast_owned_conversion"
name: "DROP CAST owned_conversion"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_cast"
syntax_ref: "syntax_drop_cast"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_cast_existing"
identifier_policy: {}
bindings:
  if_exists:
    - "drop_cast_if_exists_none"
    - "drop_cast_if_exists_yes"
  conversion:
    - "drop_cast_conversion_existing"
  behavior:
    - "drop_cast_behavior_none"
    - "drop_cast_behavior_cascade"
    - "drop_cast_behavior_restrict"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "cast_type_usage_authorized"
    allowed_values:
      - "true"
    fact_refs:
      - "drop_cast_fact_permission"
