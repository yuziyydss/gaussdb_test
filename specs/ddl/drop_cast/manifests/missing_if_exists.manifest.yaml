schema_version: 1
kind: manifest
id: manifest_drop_cast_missing_if_exists
name: DROP CAST missing_if_exists
status: needs_review
description: 一个专用缺失类型对的IF EXISTS静态代表；不验证NOTICE文本或转换删除行为。
factor_ref: drop_cast
syntax_ref: syntax_drop_cast
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  if_exists:
    - drop_cast_if_exists_yes
  conversion:
    - drop_cast_conversion_missing
  behavior:
    - drop_cast_behavior_none
environment_requirements:
  - key: cast_type_usage_authorized
    allowed_values:
      - "true"
    fact_refs:
      - drop_cast_fact_permission
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
