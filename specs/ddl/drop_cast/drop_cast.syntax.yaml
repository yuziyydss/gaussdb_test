schema_version: 1
kind: "syntax"
id: "syntax_drop_cast"
name: "DROP CAST 语法"
factor_ref: "drop_cast"
category: "DDL"
status: "ready"
description: "语法结构来自 PDF，选值仅由 factor/manifest 提供。"
production: "DROP CAST {if_exists} (double precision AS timestamp with time zone) {behavior}"
rendering:
  whitespace: "collapse"
  statement_terminator: ";"
slots:
  if_exists:
    type: "token"
    optional: true
    dimension_ref: "drop_cast.if_exists"
  behavior:
    type: "token"
    optional: true
    dimension_ref: "drop_cast.behavior"
source_fact_refs:
  - "drop_cast_fact_syntax"
  - "drop_cast_fact_if_exists"
  - "drop_cast_fact_source"
  - "drop_cast_fact_target"
