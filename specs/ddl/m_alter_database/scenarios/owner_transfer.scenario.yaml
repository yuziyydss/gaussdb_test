schema_version: 1
kind: scenario
id: scenario_m_alter_database_owner_transfer
name: scenario_m_alter_database_owner_transfer
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_database
fact_refs:
- m_alter_database_fact_owner
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 另建 case 专用角色与成员关系后测试 OWNER TO，先删自身命名空间再删角色，不操作系统模式。
oracles:
- kind: manual_assertion
  expected: 所有者身份与权限变化需独立校准，本批字符集清单不声称已覆盖
execution_requirements:
- database_authorization
- target_oracle_calibration
