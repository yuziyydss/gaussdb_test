schema_version: 1
kind: scenario
id: scenario_m_drop_index_concurrent_failures
name: scenario_m_drop_index_concurrent_failures
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_index
fact_refs:
- m_drop_index_fact_temp
- m_drop_index_fact_failure
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 专用多会话 fixture 校准全局临时表和在线删除失败状态
oracles:
- kind: manual_assertion
  expected: 未执行；仅清理本 case 创建的确切索引
execution_requirements:
- database_authorization
- target_oracle_calibration
