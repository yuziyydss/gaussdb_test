schema_version: 1
kind: scenario
id: scenario_m_drop_index_notice
name: scenario_m_drop_index_notice
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_index
fact_refs:
- m_drop_index_fact_notice
fixture_refs:
- fixture_m_drop_index_shared_indexes
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: DROP INDEX IF EXISTS m_drop_index_absent;
oracles:
- kind: manual_assertion
  expected: 成功及 notice，非任意错误
execution_requirements:
- database_authorization
- target_oracle_calibration
