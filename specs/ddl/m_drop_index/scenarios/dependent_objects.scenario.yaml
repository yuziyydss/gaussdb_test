schema_version: 1
kind: scenario
id: scenario_m_drop_index_dependent_objects
name: scenario_m_drop_index_dependent_objects
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_index
fact_refs:
- m_drop_index_fact_dependency
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 构造真实依赖对象后分开验证 CASCADE/RESTRICT
oracles:
- kind: target_error
  expected: 只接受依赖对象错误，不接受缺索引或语法错误
execution_requirements:
- database_authorization
- target_oracle_calibration
