schema_version: 1
kind: "fixture"
id: "fixture_create_role_new_role"
name: "CREATE ROLE new_role"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_role"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "SELECT 1 / (1 - COUNT(*)) AS assert_role_absent FROM gs_roles WHERE rolname = 'b9_role_new';"
  teardown_sqls:
    - "DROP ROLE IF EXISTS b9_role_new;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
