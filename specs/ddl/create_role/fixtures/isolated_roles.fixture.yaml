schema_version: 1
kind: "fixture"
id: "fixture_create_role_isolated_roles"
name: "CREATE ROLE isolated_roles"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_role"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE ROLE b9_role_a NOLOGIN PASSWORD DISABLE;"
    - "CREATE ROLE b9_role_b NOLOGIN PASSWORD DISABLE;"
    - "CREATE ROLE b9_role_c NOLOGIN PASSWORD DISABLE;"
  teardown_sqls:
    - "DROP ROLE IF EXISTS b9_role_c;"
    - "DROP ROLE IF EXISTS b9_role_b;"
    - "DROP ROLE IF EXISTS b9_role_a;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
