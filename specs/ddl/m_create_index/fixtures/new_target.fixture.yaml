schema_version: 1
kind: fixture
id: fixture_m_create_index_new_target
name: fixture_m_create_index_new_target
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_index
requires_fixture_refs:
- fixture_m_create_table_source
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: auto
  note: 独占基表由依赖创建；目标索引随该基表删除，无占位 setup。
