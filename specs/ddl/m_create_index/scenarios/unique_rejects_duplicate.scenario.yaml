schema_version: 1
kind: scenario
id: scenario_m_create_index_unique_rejects_duplicate
name: scenario_m_create_index_unique_rejects_duplicate
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_index
fact_refs:
- m_create_index_fact_unique
fixture_refs:
- fixture_m_create_index_new_target
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: CREATE UNIQUE INDEX m_create_index_new ON m_b01_source(id);
- sql: INSERT INTO m_b01_source(id,qty) VALUES (1,99);
oracles:
- kind: target_error
  expected: 只接受重复键目标错误；SQLSTATE 待校准
execution_requirements:
- database_authorization
- target_oracle_calibration
