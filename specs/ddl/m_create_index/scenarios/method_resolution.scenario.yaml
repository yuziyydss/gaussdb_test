schema_version: 1
kind: scenario
id: scenario_m_create_index_method_resolution
name: scenario_m_create_index_method_resolution
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_index
fact_refs:
- m_create_index_fact_engine
fixture_refs:
- fixture_m_create_index_new_target
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 在显式 ASTORE/USTORE 两种真实表能力下分别创建 BTREE，并检查最终访问方法
oracles:
- kind: manual_assertion
  expected: ASTORE→BTREE、USTORE→UBTREE；本批默认存储表不宣称覆盖两种引擎
execution_requirements:
- database_authorization
- target_oracle_calibration
