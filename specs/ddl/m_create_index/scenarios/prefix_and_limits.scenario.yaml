schema_version: 1
kind: scenario
id: scenario_m_create_index_prefix_and_limits
name: scenario_m_create_index_prefix_and_limits
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_index
fact_refs:
- m_create_index_fact_prefix
- m_create_index_fact_limits
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 补字符/二进制列和 31/32/33 键列专用 fixture，分别检验长度与 GLOBAL 边界
oracles:
- kind: manual_assertion
  expected: 未建模边界不由当前两列整数 fixture 代替
execution_requirements:
- database_authorization
- target_oracle_calibration
