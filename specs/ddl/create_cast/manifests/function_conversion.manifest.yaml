schema_version: 1
kind: "manifest"
id: "manifest_create_cast_function_conversion"
name: "CREATE CAST function_conversion"
status: "ready"
description: "仅隔离测试数据库中、源目标类型转换不存在且已获授权时执行；创建包装函数及转换均置于事务内回滚，不删除既有系统转换。"
factor_ref: "create_cast"
syntax_ref: "syntax_create_cast"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_create_cast_transaction_function"
identifier_policy: {}
bindings:
  context:
    - "create_cast_context_explicit"
    - "create_cast_context_assignment"
    - "create_cast_context_implicit"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
