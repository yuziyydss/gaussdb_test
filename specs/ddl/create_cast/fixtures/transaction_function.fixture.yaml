schema_version: 1
kind: "fixture"
id: "fixture_create_cast_transaction_function"
name: "CREATE CAST transaction_function"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_cast"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
    - "CREATE FUNCTION fp_cast_convert(double precision) RETURNS timestamp with time zone AS 'SELECT to_timestamp($1);' LANGUAGE SQL STRICT;"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
