schema_version: 1
kind: manifest
id: manifest_create_llm_fresh_syntax
name: CREATE LLM fresh_syntax
factor_ref: create_llm
syntax_ref: syntax_create_llm
status: needs_review
description: 三个非秘密静态LLM语法代表；不连接外部服务、不注入真实API Key、不验证模型登记行为。
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  model_kind: [create_llm_model_kind_query, create_llm_model_kind_embed, create_llm_model_kind_rerank]
  model_name: [create_llm_model_name_fresh]
  options: [create_llm_options_static]
environment_requirements:
  - key: actor_authority
    allowed_values: [sysadmin]
    fact_refs: [create_llm_fact_body_9]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
