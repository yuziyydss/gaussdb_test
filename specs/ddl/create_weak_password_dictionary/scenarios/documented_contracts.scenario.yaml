schema_version: 1
kind: "scenario"
id: "scenario_create_weak_password_dictionary_documented_contracts"
name: "CREATE WEAK PASSWORD DICTIONARY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "create_weak_password_dictionary"
fixture_refs: []
fact_refs:
  - "create_weak_password_dictionary_fact_purpose"
  - "create_weak_password_dictionary_fact_privilege"
  - "create_weak_password_dictionary_fact_storage"
  - "create_weak_password_dictionary_fact_default_empty"
  - "create_weak_password_dictionary_fact_dedup"
preconditions:
  - "只在另行授权的可销毁独占环境评审全局安全策略；新Schema或普通独占数据库不自动证明字典状态隔离。"
  - "须先取得保密字典基线及精确恢复方案；不将现有口令写入日志。DROP清空全部弱口令，不能当单个测试值的通用teardown。"
  - "多值主语法与示例形态仍有歧义；没有已校准Oracle和恢复证据时不自动执行。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "默认空字典，可向GS_GLOBAL_CONFIG添加一条或多条弱口令。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅初始用户、系统管理员或安全管理员可执行。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "弱口令存于gs_global_config。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "默认空，可新增一条或多条。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "重复插入已有弱口令时仅保留一条。"
oracles:
  -
    kind: "documented_behavior"
    expected: "弱口令存于gs_global_config。"
  -
    kind: "documented_behavior"
    expected: "重复插入已有弱口令时仅保留一条。"
execution_requirements:
  - "database"
  - "same_session"
  - "global_security_state_authorization"
  - "confidential_dictionary_baseline"
  - "exclusive_disposable_environment"
  - "precise_restore_review"
