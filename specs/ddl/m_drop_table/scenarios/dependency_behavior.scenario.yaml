schema_version: 1
kind: scenario
id: scenario_m_drop_table_dependency_behavior
name: scenario_m_drop_table_dependency_behavior
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_table
fact_refs:
- m_drop_table_fact_dependency_version
fixture_refs:
- fixture_m_drop_table_temporary
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 在已核验 m_format_dev_version 的隔离环境创建真实依赖视图，验证依赖删除
oracles:
- kind: target_error
  expected: s1+ 不能把 CASCADE 当作依赖删除保证；需目标错误校准
execution_requirements:
- database_authorization
- target_oracle_calibration
