schema_version: 1
kind: "fixture"
id: "fixture_alter_aggregate_schema"
name: "ALTER AGGREGATE schema"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_aggregate"
requires_fixture_refs:
  - "fixture_alter_aggregate_aggregate"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE SCHEMA fp_aa_schema;"
  teardown_sqls:
    - "DROP SCHEMA IF EXISTS fp_aa_schema CASCADE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
