schema_version: 1
kind: "manifest"
id: "manifest_alter_aggregate_changes"
name: "ALTER AGGREGATE changes"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_aggregate"
syntax_ref: "syntax_alter_aggregate"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  name:
    - "alter_aggregate_name_ready"
  action:
    - "alter_aggregate_action_rename"
    - "alter_aggregate_action_schema"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
