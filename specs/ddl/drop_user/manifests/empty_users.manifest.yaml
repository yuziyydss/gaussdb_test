schema_version: 1
kind: "manifest"
id: "manifest_drop_user_empty_users"
name: "DROP USER empty_users"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_user"
syntax_ref: "syntax_drop_user"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_user_isolated_users"
identifier_policy: {}
bindings:
  if_exists:
    - "drop_user_if_exists_none"
    - "drop_user_if_exists_yes"
  targets:
    - "drop_user_targets_one"
    - "drop_user_targets_two"
  behavior:
    - "drop_user_behavior_default"
    - "drop_user_behavior_restrict"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "user_drop_authorized"
    allowed_values:
      - "true"
    fact_refs:
      - "drop_user_fact_privilege_schema"
  -
    key: "password_disable_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "create_role::create_role_fact_disable_admin"
