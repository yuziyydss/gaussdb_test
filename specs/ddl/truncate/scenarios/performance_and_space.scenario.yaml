schema_version: 1
kind: scenario
id: scenario_truncate_performance_and_space
name: TRUNCATE、DELETE 与 DROP 的数据、空间和日志差异
status: planned
description: 在同规模表上比较行数、表定义、物理空间、扫描与事务日志行为。
factor_ref: truncate
fixture_refs: [fixture_truncate_regular]
fact_refs: [tr_fact_delete_equivalence, tr_fact_no_scan_performance, tr_fact_logging_behavior, tr_fact_statement_differences]
preconditions: [三份同结构大表, 空间和事务日志监控]
steps: [无 WHERE DELETE, TRUNCATE TABLE, DROP TABLE, 比较执行计划或扫描、日志量和对象目录]
oracles: [DELETE 与 TRUNCATE 行数均归零, TRUNCATE 不做表扫描且释放空间, DROP 额外删除定义, 日志记录粒度符合原文]
execution_requirements: [性能隔离环境, 日志与空间监控, 对象目录查询]
