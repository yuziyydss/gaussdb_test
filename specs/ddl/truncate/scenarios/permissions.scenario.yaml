schema_version: 1
kind: scenario
id: scenario_truncate_permissions
name: TRUNCATE 权限矩阵
status: planned
description: 验证表 Owner、表 TRUNCATE、TRUNCATE ANY TABLE 与三权分立关闭管理员路径。
factor_ref: truncate
fixture_refs: [fixture_truncate_regular]
fact_refs: [tr_fact_permissions]
preconditions: [多角色与不同表 Owner, 可切换三权分立状态]
steps: [逐角色执行 TRUNCATE, 授予和撤销两类权限后重试]
oracles: [文档列出的角色成功, 其他角色命中目标权限错误]
execution_requirements: [管理员权限, 多用户, 安全配置]
