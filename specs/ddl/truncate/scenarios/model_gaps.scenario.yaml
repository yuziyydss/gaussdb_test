schema_version: 1
kind: scenario
id: scenario_truncate_model_gaps
name: TRUNCATE 无界列表与 IF EXISTS 行为缺口
status: planned
description: 补齐 V1 有限 repeat 不能穷尽的表/分区值列表，并校准分区形式 IF EXISTS。两值 PARTITION FOR 需真实双列分区 fixture，不使用单列分区表伪装可执行覆盖。
factor_ref: truncate
fixture_refs: []
fact_refs: [tr_open_table_list_unbounded, tr_open_partition_values_unbounded, tr_open_partition_if_exists_behavior]
preconditions: [动态表与双列及更多列分区 Fixture 生成器, 已存在和不存在的分区表名称]
steps: [生成三表、十表和更长整表列表, 从两列开始生成多列分区键值列表并覆盖 PARTITION FOR (1, 10), 对不存在表执行分区 IF EXISTS]
oracles: [记录列表实际边界, 每个 FOR 值组唯一定位分区, IF EXISTS 的 NOTICE/ERROR 和分区解析顺序被精确记录]
execution_requirements: [动态 Fixture 生成器, 真实 GaussDB, NOTICE 与 ERROR 捕获]
