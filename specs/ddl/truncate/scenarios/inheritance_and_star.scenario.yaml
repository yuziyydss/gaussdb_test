schema_version: 1
kind: scenario
id: scenario_truncate_inheritance_and_star
name: ONLY、省略 ONLY 与星号的继承范围
status: planned
description: 验证已确认的 ONLY 行为，并校准 table_name * 及 ONLY 与星号同时出现的未说明语义。
factor_ref: truncate
fixture_refs: [fixture_truncate_inheritance]
fact_refs: [tr_fact_only_scope, tr_open_star_semantics]
preconditions: [父表和至少两级子表分别有可区分数据]
steps: [TRUNCATE ONLY 父表, TRUNCATE 父表, TRUNCATE 父表星号, TRUNCATE ONLY 父表星号]
oracles: [ONLY 只清空父表, 省略 ONLY 清空全部子表, 星号两种组合记录实际作用域或目标错误]
execution_requirements: [继承表, 行数断言, 错误 Oracle 校准]
