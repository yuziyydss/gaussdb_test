schema_version: 1
kind: scenario
id: scenario_truncate_examples
name: TRUNCATE 文档示例回放
status: planned
description: 回放 reason 空间对比与 reason_p 分区清理示例，不由示例外推其他环境。
factor_ref: truncate
fixture_refs: []
fact_refs: [tr_fact_example_full_table, tr_fact_example_partition]
preconditions: [隔离 Schema, 能查询关系大小和分区元数据]
steps: [创建并填充 reason, 比较 DELETE 与 TRUNCATE 后大小, 创建 reason_p, 清空 p_05_before 并更新全局索引, 清空整表]
oracles: [DELETE 后示例大小约 8192 bytes, TRUNCATE 后示例大小为 0 bytes, 分区与整表清理均成功]
execution_requirements: [真实 GaussDB, 关系大小查询, 分区元数据]
