schema_version: 1
kind: scenario
id: scenario_truncate_purge
name: TRUNCATE PURGE 回收站行为
status: planned
description: 在回收站开启环境中比较默认清理与 PURGE 直接清理。
factor_ref: truncate
fixture_refs: [fixture_truncate_regular]
fact_refs: [tr_fact_purge]
preconditions: [回收站功能开启, 可查询回收站对象]
steps: [默认 TRUNCATE, 查询回收站, 重建数据后 TRUNCATE PURGE, 再次查询]
oracles: [默认数据进入回收站, PURGE 数据不进入回收站且被直接清理]
execution_requirements: [回收站配置, 回收站目录查询]
