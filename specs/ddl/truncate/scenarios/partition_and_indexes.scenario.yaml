schema_version: 1
kind: scenario
id: scenario_truncate_partition_and_indexes
name: 分区定位与全局索引有效性
status: planned
description: 比较命名分区和 PARTITION FOR，并验证 UPDATE GLOBAL INDEX 的两个结果分支。
factor_ref: truncate
fixture_refs: [fixture_truncate_partitioned]
fact_refs: [tr_fact_partition_name, tr_fact_partition_value, tr_fact_partition_for_behavior, tr_fact_update_global_index, "create_index::ci_fact_partition_scope"]
preconditions: [单键分区表, 每分区可区分数据, 可用全局索引和状态目录]
steps: [按 p_low 清理, 按 FOR 一值清理, 分别省略和声明 UPDATE GLOBAL INDEX, 查询剩余数据和索引状态]
oracles: [定位目标分区的全部行被清空且其他分区保留, 声明时全局索引可正确查询, 省略时全部全局索引失效]
execution_requirements: [全局索引, 分区和索引元数据, 结果集断言]
