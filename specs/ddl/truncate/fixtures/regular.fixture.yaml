schema_version: 1
kind: fixture
id: fixture_truncate_regular
name: TRUNCATE 普通目标表
status: ready
description: 为单表和 public 模式限定整表清理提供带数据普通表。
factor_ref: truncate
provides:
  tables:
    - name: t_tr_regular
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: note, type: VARCHAR(32), nullable: true}
seed: {required: true, rows: [{id: 1, note: one}, {id: 2, note: two}]}
execution: {status: ready, mode: auto, note: "每个 case 前重建并插入两行。"}
