schema_version: 1
kind: fixture
id: fixture_truncate_schema_qualified
name: TRUNCATE 专用模式限定目标
status: ready
description: 在专用测试模式中显式创建与 schema-qualified profile 完全同名的目标表，不依赖 search_path 或 public。
factor_ref: truncate
provides:
  tables:
    - name: tr_fixture_schema.t_tr_regular
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: note, type: VARCHAR(32), nullable: true}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - DROP SCHEMA IF EXISTS tr_fixture_schema CASCADE;
    - CREATE SCHEMA tr_fixture_schema;
    - CREATE TABLE tr_fixture_schema.t_tr_regular (id INTEGER NOT NULL, note VARCHAR(32));
    - INSERT INTO tr_fixture_schema.t_tr_regular (id, note) VALUES (1, 'one'), (2, 'two');
  teardown_sqls:
    - DROP SCHEMA IF EXISTS tr_fixture_schema CASCADE;
  note: 每个 case 前重建专用模式；目标 SQL、provides 和实际建表名称完全一致。
