schema_version: 1
kind: manifest
id: manifest_truncate_restrict_negative
name: TRUNCATE RESTRICT 外键负向
status: needs_review
description: 仅违反外键 RESTRICT 规则；PDF 未提供可确认的 SQLSTATE 或错误文本。
factor_ref: truncate
syntax_ref: syntax_truncate_v1
suite_type: negative
strategy: pairwise
fixture_refs: [fixture_truncate_foreign_key]
identifier_policy: {}
bindings:
  statement_form: [tr_form_full]
  table_keyword: [tr_table_keyword_absent, tr_table_keyword_present]
  full_scope: [tr_scope_all]
  full_table_profile: [tr_table_fk_parent]
  identity_clause: [tr_identity_default]
  dependency_clause: [tr_dependency_default, tr_dependency_restrict]
  purge_clause: [tr_purge_absent]
violates_rule_refs: [tr_rule_restrict_requires_no_foreign_key]
expected: {default: error, scope: syntax_and_semantics, oracle_status: needs_verification, error_category: truncate_foreign_key_restrict}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
