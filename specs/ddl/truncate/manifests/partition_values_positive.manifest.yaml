schema_version: 1
kind: manifest
id: manifest_truncate_partition_values_positive
name: ALTER TABLE TRUNCATE PARTITION FOR 单键值列表
status: ready
description: 对一列分区键 fixture 生成一个 PARTITION FOR 值；不对 PDF 未说明的数量错配推断错误 Oracle。
factor_ref: truncate
syntax_ref: syntax_truncate_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  statement_form: [tr_form_partition]
  partition_if_exists: [tr_partition_if_absent, tr_partition_if_present]
  partition_table_profile: [tr_partition_table_plain, tr_partition_table_only_prefix, tr_partition_table_star, tr_partition_table_only_paren]
  partition_selector: [tr_partition_by_values]
  partition_value_profile: [tr_partition_values_one]
  update_global_index: [tr_update_index_absent, tr_update_index_present]
expected: {default: success, scope: syntax_and_semantics}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
