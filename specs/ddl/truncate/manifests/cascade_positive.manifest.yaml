schema_version: 1
kind: manifest
id: manifest_truncate_cascade_positive
name: TRUNCATE CASCADE 外键组正向
status: ready
description: fixture 建立有效外键引用，CASCADE 是唯一放行该能力的依赖模式。
factor_ref: truncate
syntax_ref: syntax_truncate_v1
suite_type: positive
strategy: pairwise
fixture_refs: [fixture_truncate_foreign_key]
identifier_policy: {}
bindings:
  statement_form: [tr_form_full]
  table_keyword: [tr_table_keyword_absent, tr_table_keyword_present]
  full_scope: [tr_scope_all, tr_scope_only]
  full_table_profile: [tr_table_fk_parent]
  identity_clause: [tr_identity_default, tr_identity_continue]
  dependency_clause: [tr_dependency_cascade]
  purge_clause: [tr_purge_absent, tr_purge_present]
expected: {default: success, scope: syntax_and_semantics}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
