schema_version: 1
kind: manifest
id: manifest_truncate_inheritance_syntax_positive
name: TRUNCATE ONLY 与星号继承语法
status: needs_review
description: 产生式明确允许 ONLY 和 table_name *，但二者交互行为未解释，故只作 syntax_only 覆盖。
factor_ref: truncate
syntax_ref: syntax_truncate_v1
suite_type: positive
strategy: pairwise
fixture_refs: [fixture_truncate_inheritance]
identifier_policy: {}
bindings:
  statement_form: [tr_form_full]
  table_keyword: [tr_table_keyword_absent, tr_table_keyword_present]
  full_scope: [tr_scope_all, tr_scope_only]
  full_table_profile: [tr_table_inheritance_parent, tr_table_inheritance_star]
  identity_clause: [tr_identity_default, tr_identity_continue]
  dependency_clause: [tr_dependency_default]
  purge_clause: [tr_purge_absent]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
