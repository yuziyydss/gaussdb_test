schema_version: 1
kind: "scenario"
id: "scenario_drop_function_missing_notice"
name: "DROP FUNCTION missing_notice"
status: "planned"
description: "确认 IF EXISTS 对缺失函数只发 NOTICE。"
factor_ref: "drop_function"
fixture_refs: []
fact_refs:
  - "drop_function_fact_if_exists"
preconditions: []
steps:
  -
    id: "drop"
    action: "execute_sql"
    sql: "DROP FUNCTION IF EXISTS fp_df_missing;"
oracles:
  -
    after_step: "drop"
    kind: "notice"
    expected: "missing function"
execution_requirements:
  - "database"
  - "same_session"
