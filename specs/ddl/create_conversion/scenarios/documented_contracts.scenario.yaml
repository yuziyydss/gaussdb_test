schema_version: 1
kind: "scenario"
id: "scenario_create_conversion_documented_contracts"
name: "CREATE CONVERSION documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "create_conversion"
fixture_refs: []
fact_refs:
  - "create_conversion_fact_purpose"
  - "create_conversion_fact_internal_only"
  - "create_conversion_fact_bidirectional"
  - "create_conversion_fact_privilege"
  - "create_conversion_fact_ascii"
  - "create_conversion_fact_drop"
  - "create_conversion_fact_default"
  - "create_conversion_fact_signature"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "创建源字符编码到目标字符编码的转换规则。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅系统内部可创建，用户无法直接使用和创建。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "DEFAULT用于客户端服务器默认转换时需要定义A到B和B到A双向转换。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "需转换函数EXECUTE及目标模式CREATE权限。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "源和目标编码都不能使用SQL_ASCII。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "DROP CONVERSION可移除用户自定义转换。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "每模式每编码对仅允许一个默认转换。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "转换函数参数按序为integer、integer、cstring、internal、integer，返回void。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
