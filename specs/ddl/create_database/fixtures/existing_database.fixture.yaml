schema_version: 1
kind: "fixture"
id: "fixture_create_database_existing_database"
name: "CREATE DATABASE existing_database"
status: "ready"
description: "ALTER/DROP DATABASE共用的既存数据库资产；保留兼容ID与原SQL，独立于CREATE命令包的抽取顺序。旧PURGE生命周期未获得执行授权，不能作为新编码清单的清理模板。"
factor_ref: null
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE DATABASE b8_database TEMPLATE = template0 ENCODING = 'UTF8' LC_COLLATE = 'C' LC_CTYPE = 'C' DBCOMPATIBILITY = 'PG';"
  teardown_sqls:
    - "DROP DATABASE IF EXISTS b8_database PURGE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
