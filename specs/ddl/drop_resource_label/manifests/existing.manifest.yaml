schema_version: 1
kind: "manifest"
id: "manifest_drop_resource_label_existing"
name: "DROP RESOURCE LABEL existing"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_resource_label"
syntax_ref: "syntax_drop_resource_label"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_resource_label_existing_labels"
identifier_policy: {}
bindings:
  if_exists:
    - "drop_resource_label_if_exists_none"
    - "drop_resource_label_if_exists_yes"
  names:
    - "drop_resource_label_names_one"
    - "drop_resource_label_names_two"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "resource_label_authorized"
    allowed_values:
      - "true"
    fact_refs:
      - "drop_resource_label_fact_privilege"
