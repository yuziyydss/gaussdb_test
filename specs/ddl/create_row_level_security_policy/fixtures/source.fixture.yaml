schema_version: 1
kind: "fixture"
id: "fixture_create_row_level_security_policy_source"
name: "CREATE ROW LEVEL SECURITY POLICY source"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_row_level_security_policy"
requires_fixture_refs: []
provides:
  tables:
    -
      name: "b10_rls_source"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: true
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
    - "CREATE TABLE b10_rls_source (col_1 INTEGER, col_2 INTEGER);"
    - "INSERT INTO b10_rls_source VALUES (1,2),(0,3),(NULL,4);"
    - "ALTER TABLE b10_rls_source ENABLE ROW LEVEL SECURITY;"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
