schema_version: 1
kind: "scenario"
id: "scenario_drop_global_configuration_documented_contracts"
name: "DROP GLOBAL CONFIGURATION documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_global_configuration"
fixture_refs: []
fact_refs:
  - "drop_global_configuration_fact_purpose"
  - "drop_global_configuration_fact_initial_user"
  - "drop_global_configuration_fact_protected_names"
  - "drop_global_configuration_fact_must_exist"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除gs_global_config中的参数值。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅初始用户运行。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "名称不能是weak_password、undostoragetype。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "名称必须已存在，删除不存在的参数报错。"
oracles:
  -
    kind: "documented_behavior"
    expected: "名称必须已存在，删除不存在的参数报错。"
execution_requirements:
  - "database"
  - "same_session"
