schema_version: 1
kind: scenario
id: scenario_m_create_table_partition_routing_and_metadata
name: scenario_m_create_table_partition_routing_and_metadata
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_table_partition
fact_refs:
- m_create_table_partition_fact_implicit
- m_create_table_partition_fact_routing
- m_create_table_partition_fact_key_types
- m_create_table_partition_fact_start_end
- m_create_table_partition_fact_list
- m_create_table_partition_fact_hash_key
- m_create_table_partition_fact_count_wording
fixture_refs:
- fixture_m_create_table_partition_target_namespace
- fixture_m_create_table_partition_range_source
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: CREATE后再seed边界行并检查pg_partition与分区路由；START(0)额外产生第4分区，不能按声明项3个断言实际3个。列表DEFAULT、HASH自动名和失败上边界须独立核验。
oracles:
- kind: manual_assertion
  expected: 有限INTEGER模型；分区数/路由/负向SQLSTATE未执行。多列、表达式、生成列、外键/索引、表空间、全类型尚未建模
execution_requirements:
- database_authorization
- target_oracle_calibration
