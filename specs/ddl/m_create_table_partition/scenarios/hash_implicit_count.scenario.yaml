schema_version: 1
kind: scenario
id: scenario_m_create_table_partition_hash_implicit_count
name: scenario_m_create_table_partition_hash_implicit_count
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_table_partition
fact_refs:
- m_create_table_partition_fact_partitions
fixture_refs:
- fixture_m_create_table_partition_target_namespace
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: HASH/KEY既不指定数量也不列出定义时，应自动创建一个分区；不能当作auto_count的产品错误。
oracles:
- kind: manual_assertion
  expected: 目标表对应的实际分区数为1；当前仅生成合法候选，目录断言尚未执行。
execution_requirements:
- database_authorization
- target_oracle_calibration
