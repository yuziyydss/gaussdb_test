schema_version: 1
kind: manifest
id: manifest_m_create_table_partition_descending_negative
name: manifest_m_create_table_partition_descending_negative
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_table_partition
syntax_ref: syntax_m_create_table_partition
suite_type: negative
strategy: pairwise
fixture_refs:
- fixture_m_create_table_partition_target_namespace
identifier_policy: {}
bindings:
  form:
  - m_create_table_partition_form_range_less
  bounds:
  - m_create_table_partition_bounds_descending
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_create_table_partition_fact_mode
- key: storage_environment
  allowed_values:
  - M_row_store_astore_available
  fact_refs:
  - m_create_table_partition_fact_row
violates_rule_refs:
- m_create_table_partition_rule_ascending_bounds
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: ascending_bounds
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
