schema_version: 1
kind: fixture
id: fixture_m_drop_function_existing
name: fixture_m_drop_function_existing
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_function
requires_fixture_refs:
- fixture_m_create_function_existing
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  mode: auto
  status: ready
  note: 依赖唯一 INTEGER 函数，非重载；先定向清理函数，再清理 Schema。
