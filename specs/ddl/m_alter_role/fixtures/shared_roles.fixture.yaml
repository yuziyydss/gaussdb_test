schema_version: 1
kind: fixture
id: fixture_m_alter_role_shared_roles
name: fixture_m_alter_role_shared_roles
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_role
requires_fixture_refs:
- fixture_m_create_role_existing
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: auto
  note: 依赖 CREATE ROLE 包的真实无登录角色；只允许本 case 创建的角色，不执行 DROP OWNED 或清理任何共享身份。
