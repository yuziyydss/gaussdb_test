schema_version: 1
kind: scenario
id: scenario_m_alter_role_restricted_changes
name: scenario_m_alter_role_restricted_changes
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_role
fact_refs:
- m_alter_role_fact_pguser
- m_alter_role_fact_pdb
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: PGUSER 变更目标错误需校准；初始用户/PDB 密码变化仅保留限制，不生成默认候选。
oracles:
- kind: manual_assertion
  expected: 不触碰共享或初始身份，不通过环境错误倒推目标错误
execution_requirements:
- database_authorization
- target_oracle_calibration
