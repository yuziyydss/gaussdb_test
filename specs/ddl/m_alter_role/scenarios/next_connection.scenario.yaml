schema_version: 1
kind: scenario
id: scenario_m_alter_role_next_connection
name: scenario_m_alter_role_next_connection
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_role
fact_refs:
- m_alter_role_fact_next_session
- m_alter_role_fact_locked
fixture_refs:
- fixture_m_alter_role_shared_roles
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 用额外有登录能力且获授权的隔离身份验证下次会话配置与锁定，不用本批 NOLOGIN 角色误测登录失败。
oracles:
- kind: manual_assertion
  expected: 区分 NOLOGIN 与 ACCOUNT LOCK；连接和 Oracle 均未执行
execution_requirements:
- database_authorization
- target_oracle_calibration
