schema_version: 1
kind: manifest
id: manifest_m_alter_role_connection_below
name: manifest_m_alter_role_connection_below
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_alter_role
syntax_ref: syntax_m_alter_role
suite_type: negative
strategy: pairwise
fixture_refs:
- fixture_m_alter_role_shared_roles
identifier_policy: {}
bindings:
  form:
  - m_alter_role_form_options
  option:
  - m_alter_role_option_below
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_alter_role_fact_mode
- key: actor_authority
  allowed_values:
  - sysadmin
  fact_refs:
  - m_create_role::m_create_role_fact_disable_authority
- key: separation_of_duties
  allowed_values:
  - 'off'
  fact_refs:
  - m_create_role::m_create_role_fact_creation_authority
violates_rule_refs:
- m_alter_role_rule_connection_range
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: connection_range
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
