schema_version: 1
kind: scenario
id: scenario_m_create_extension_assets
name: scenario_m_create_extension_assets
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_create_extension
fact_refs:
- m_create_extension_fact_files
- m_create_extension_fact_schema
- m_create_extension_fact_collision
- m_create_extension_fact_notice
- m_create_extension_fact_example
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 审批并核验security_plugin.control、默认版本脚本、组件清单、Schema适配和目标库未安装；不安装/下载支持文件，不用IF NOT EXISTS掩盖版本差异。
oracles:
- kind: manual_assertion
  expected: 外部支持文件可用性未验证，版本/旧式FROM/碰撞与已存在NOTICE未建模
execution_requirements:
- database_authorization
- target_oracle_calibration
