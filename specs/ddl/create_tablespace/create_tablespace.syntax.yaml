schema_version: 1
kind: "syntax"
id: "syntax_create_tablespace"
name: "CREATE TABLESPACE 语法"
factor_ref: "create_tablespace"
category: "DDL"
status: "draft"
description: "未实现服务端独占目录的空目录、权限、磁盘及失败残留清理契约；不把示例路径当可用路径。"
production: "CREATE TABLESPACE {tablespace_name} {owner} {relative} LOCATION {directory} {maxsize} {options}"
rendering:
  whitespace: "collapse"
  statement_terminator: ";"
slots:
  tablespace_name:
    type: "token"
    optional: false
  owner:
    type: "token"
    optional: true
  relative:
    type: "token"
    optional: true
  directory:
    type: "token"
    optional: false
  maxsize:
    type: "token"
    optional: true
  options:
    type: "token"
    optional: true
source_fact_refs:
  - "create_tablespace_fact_grammar_line_27"
  - "create_tablespace_fact_grammar_line_28"
  - "create_tablespace_fact_grammar_line_29"
