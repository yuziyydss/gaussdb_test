schema_version: 1
kind: manifest
id: manifest_drop_tablespace_fresh_empty
name: DROP TABLESPACE fresh_empty
factor_ref: drop_tablespace
syntax_ref: syntax_drop_tablespace
status: needs_review
description: 一个专用空RELATIVE表空间的DROP TABLESPACE静态代表；不覆盖缺失对象、非空表空间或目录清理。
suite_type: positive
strategy: pairwise
fixture_refs: [fixture_drop_tablespace_fresh_empty]
identifier_policy: {}
bindings:
  if_exists: [drop_tablespace_if_exists_none, drop_tablespace_if_exists_yes]
  tablespace_name: [drop_tablespace_tablespace_name_fresh]
environment_requirements:
  - key: tablespace_drop_privilege
    allowed_values: ["true"]
    fact_refs: [drop_tablespace_fact_body_8]
  - key: dedicated_directory_cleanup
    allowed_values: [fresh_empty_relative_tablespace]
    fact_refs: [drop_tablespace_fact_runtime_contract]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
