schema_version: 1
kind: "syntax"
id: "syntax_drop_tablespace"
name: "DROP TABLESPACE 语法"
factor_ref: "drop_tablespace"
category: "DDL"
status: "ready"
description: "受限静态代表使用专用空表空间；跨数据库空表空间与目录清理契约仍单独留账。"
production: "DROP TABLESPACE {if_exists} {tablespace_name}"
rendering:
  whitespace: "collapse"
  statement_terminator: ";"
slots:
  if_exists:
    type: "token"
    optional: true
    dimension_ref: "drop_tablespace.if_exists"
  tablespace_name:
    type: "token"
    optional: false
    dimension_ref: "drop_tablespace.tablespace_name"
source_fact_refs:
  - "drop_tablespace_fact_grammar_line_18"
