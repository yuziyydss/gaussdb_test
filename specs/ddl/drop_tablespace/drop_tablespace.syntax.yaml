schema_version: 1
kind: "syntax"
id: "syntax_drop_tablespace"
name: "DROP TABLESPACE 语法"
factor_ref: "drop_tablespace"
category: "DDL"
status: "draft"
description: "需要跨数据库空表空间证明与专用目录清理；不删除现存业务表空间。"
production: "DROP TABLESPACE {if_exists} {tablespace_name}"
rendering:
  whitespace: "collapse"
  statement_terminator: ";"
slots:
  if_exists:
    type: "token"
    optional: true
  tablespace_name:
    type: "token"
    optional: false
source_fact_refs:
  - "drop_tablespace_fact_grammar_line_18"
