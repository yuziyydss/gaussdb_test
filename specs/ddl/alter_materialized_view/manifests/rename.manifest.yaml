schema_version: 1
kind: "manifest"
id: "manifest_alter_materialized_view_rename"
name: "ALTER MATERIALIZED VIEW rename"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_materialized_view"
syntax_ref: "syntax_alter_materialized_view"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_materialized_view_full"
identifier_policy: {}
bindings:
  if_exists:
    - "alter_materialized_view_if_exists_none"
    - "alter_materialized_view_if_exists_yes"
  action:
    - "alter_materialized_view_action_column"
    - "alter_materialized_view_action_column_short"
    - "alter_materialized_view_action_name"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
