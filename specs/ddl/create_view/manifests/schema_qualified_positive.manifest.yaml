schema_version: 1
kind: manifest
id: manifest_create_view_schema_qualified_positive
name: CREATE VIEW 模式修饰名称正向语法
status: ready
description: 覆盖文档明确支持的 schema-qualified view_name，不推断权限环境。
factor_ref: create_view
syntax_ref: syntax_create_view
suite_type: positive
strategy: pairwise
fixture_refs: []

identifier_policy:
  view_name:
    generator: deterministic
    prefix: v_cv_schema
    schema_prefix: public
    unique_per_case: true

bindings:
  or_replace: [or_replace_absent]
  temp_modifier: [temp_absent]
  force_modifier: [force_absent]
  column_list: [column_list_inherited]
  view_options_profile: [view_options_none]
  query_profile: [query_simple_two_columns]
  post_query_option: [post_query_none]

expected:
  default: success
  scope: syntax_only

coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
