schema_version: 1
kind: fixture
id: fixture_create_view_flashback_source
name: CREATE VIEW Ustore 闪回源表
status: ready
description: 为 TIMECAPSULE 闪回 query profile 声明两列 Ustore 永久表能力；当前仅用于静态 SQL 生成。
factor_ref: create_view

provides:
  tables:
    - name: t_cv_flashback_source
      persistence: permanent
      table_kind: ustore_regular
      columns:
        - name: col_1
          type: INTEGER
          nullable: true
        - name: col_2
          type: INTEGER
          nullable: true

seed:
  required: true
  rows:
    - {col_1: 1, col_2: 2}
    - {col_1: 3, col_2: 4}

execution:
  status: ready
  mode: auto
  note: 自动创建 Ustore 表和 seed；有效历史时间点仍由闪回 scenario 制造。
