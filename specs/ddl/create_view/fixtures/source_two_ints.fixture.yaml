schema_version: 1
kind: fixture
id: fixture_create_view_source_two_ints
name: CREATE VIEW 两列整数源表
status: ready
description: 为基础视图、过滤视图和非可更新查询 profile 提供最小源表能力。
factor_ref: create_view

provides:
  tables:
    - name: t_view_source
      persistence: permanent
      table_kind: regular
      columns:
        - name: col_1
          type: INTEGER
          nullable: true
        - name: col_2
          type: INTEGER
          nullable: true

seed:
  required: false
  rows:
    - {col_1: 1, col_2: 2}
    - {col_1: 3, col_2: 4}

execution:
  status: ready
  mode: auto
  note: 由 Fixture 编译器生成幂等 DROP、CREATE、seed 和 teardown。
