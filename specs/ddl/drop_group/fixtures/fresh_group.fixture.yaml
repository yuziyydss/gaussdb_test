schema_version: 1
kind: fixture
id: fixture_drop_group_fresh_group
name: DROP GROUP专用测试组
factor_ref: drop_group
status: needs_review
description: 创建一个NOLOGIN且PASSWORD DISABLE的专用组作为DROP GROUP目标；不验证组权限、成员关系或目录行为。
requires_fixture_refs: []
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "CREATE GROUP g_drop_group NOLOGIN PASSWORD DISABLE;"
  teardown_sqls:
    - "DROP ROLE IF EXISTS g_drop_group;"
  note: 仅在预先授权、三权分立关闭且允许管理工具接口的独占测试会话中运行；组名初始不存在，不与当前用户或系统角色重名。本case实际创建NOLOGIN且PASSWORD DISABLE的组并拥有其清理权；setup失败阻止目标并保留实际角色清单。目标仅证明语法与静态候选，不验证组权限、成员关系、目录身份或全局状态变更；清理仅限本case确认创建成功的组，禁止CASCADE、DROP OWNED、REVOKE OWNED或吞异常。
