schema_version: 1
kind: "scenario"
id: "scenario_drop_group_documented_contracts"
name: "DROP GROUP documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_group"
fixture_refs: []
fact_refs:
  - "drop_group_fact_purpose"
  - "drop_group_fact_tool_interface"
  - "drop_group_fact_not_recommended"
  - "drop_group_fact_if_exists"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除用户组，与DROP ROLE同义。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "GaussDB管理工具封装接口，用于数据库管理。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "不建议直接使用，以免影响数据库状态。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "IF EXISTS存在则删，不存在仅NOTICE。"
oracles:
  -
    kind: "documented_behavior"
    expected: "IF EXISTS存在则删，不存在仅NOTICE。"
execution_requirements:
  - "database"
  - "same_session"
