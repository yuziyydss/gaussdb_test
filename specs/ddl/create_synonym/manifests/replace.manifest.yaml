schema_version: 1
kind: "manifest"
id: "manifest_create_synonym_replace"
name: "CREATE SYNONYM replace"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "create_synonym"
syntax_ref: "syntax_create_synonym"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_create_synonym_existing"
identifier_policy: {}
bindings:
  replace:
    - "create_synonym_replace_present"
  name:
    - "create_synonym_name_existing"
  target:
    - "create_synonym_target_view"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
