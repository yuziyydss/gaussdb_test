schema_version: 1
kind: "scenario"
id: "scenario_create_synonym_resolution"
name: "CREATE SYNONYM resolution"
status: "planned"
description: "分别验证函数和普通对象搜索顺序；显式转引到用户模式的 PUBLIC 特例单列，不把客户端元命令当 SQL。"
factor_ref: "create_synonym"
fixture_refs: []
fact_refs:
  - "create_synonym_fact_resolution_function"
  - "create_synonym_fact_qualified_resolution"
  - "create_synonym_fact_unqualified_resolution"
  - "create_synonym_fact_public_resolution"
  - "create_synonym_fact_public_search_ambiguity"
  - "create_synonym_fact_ddl"
  - "create_synonym_fact_client_meta"
  - "create_synonym_fact_dml"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "分别验证函数和普通对象搜索顺序；显式转引到用户模式的 PUBLIC 特例单列，不把客户端元命令当 SQL。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
