schema_version: 1
kind: "fixture"
id: "fixture_create_synonym_existing"
name: "CREATE SYNONYM existing"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_synonym"
requires_fixture_refs:
  - "fixture_create_synonym_objects"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE SYNONYM s_syn_replace FOR t_syn_source;"
  teardown_sqls:
    - "DROP SYNONYM IF EXISTS s_syn_replace;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
