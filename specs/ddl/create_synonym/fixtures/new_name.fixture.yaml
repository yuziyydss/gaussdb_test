schema_version: 1
kind: "fixture"
id: "fixture_create_synonym_new_name"
name: "CREATE SYNONYM new_name"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_synonym"
requires_fixture_refs:
  - "fixture_create_synonym_objects"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
  teardown_sqls:
    - "ROLLBACK;"
    - "DROP SYNONYM IF EXISTS s_syn_create;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
