schema_version: 1
kind: "fixture"
id: "fixture_create_synonym_objects"
name: "CREATE SYNONYM objects"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_synonym"
requires_fixture_refs:
  - "fixture_create_synonym_source"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE VIEW v_syn_source AS SELECT col_1, col_2 FROM t_syn_source;"
    - "CREATE SEQUENCE seq_syn_source;"
    - "CREATE SYNONYM s_syn_source FOR t_syn_source;"
  teardown_sqls:
    - "DROP SYNONYM IF EXISTS s_syn_source;"
    - "DROP SEQUENCE IF EXISTS seq_syn_source;"
    - "DROP VIEW IF EXISTS v_syn_source;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
