schema_version: 1
kind: "manifest"
id: "manifest_drop_role_owned_empty_roles"
name: "DROP ROLE owned_empty_roles"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_role"
syntax_ref: "syntax_drop_role"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_role_isolated_roles"
identifier_policy: {}
bindings:
  if_exists:
    - "drop_role_if_exists_none"
    - "drop_role_if_exists_yes"
  targets:
    - "drop_role_targets_one"
    - "drop_role_targets_two"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "dedicated_role_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "create_role::create_role_fact_privilege"
  -
    key: "password_disable_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "create_role::create_role_fact_disable_admin"
