schema_version: 1
kind: "scenario"
id: "scenario_drop_role_documented_contracts"
name: "DROP ROLE documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_role"
fixture_refs: []
fact_refs:
  - "drop_role_fact_purpose"
  - "drop_role_fact_if_exists"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除指定角色。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "IF EXISTS存在则删除，不存在不删且仅NOTICE。"
oracles:
  -
    kind: "documented_behavior"
    expected: "IF EXISTS存在则删除，不存在不删且仅NOTICE。"
execution_requirements:
  - "database"
  - "same_session"
