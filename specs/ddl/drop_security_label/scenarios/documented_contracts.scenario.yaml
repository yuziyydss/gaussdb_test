schema_version: 1
kind: "scenario"
id: "scenario_drop_security_label_documented_contracts"
name: "DROP SECURITY LABEL documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_security_label"
fixture_refs: []
fact_refs:
  - "drop_security_label_fact_purpose"
  - "drop_security_label_fact_privilege"
  - "drop_security_label_fact_must_exist"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除当前库的安全标签。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "初始用户、SYSADMIN或gs_role_seclabel继承者可删除。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "目标标签必须在数据库中存在。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
