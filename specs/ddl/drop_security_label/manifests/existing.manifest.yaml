schema_version: 1
kind: "manifest"
id: "manifest_drop_security_label_existing"
name: "DROP SECURITY LABEL existing"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_security_label"
syntax_ref: "syntax_drop_security_label"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_security_label_existing_labels"
identifier_policy: {}
bindings:
  target:
    - "drop_security_label_target_exists"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "security_label_authorized"
    allowed_values:
      - "true"
    fact_refs:
      - "drop_security_label_fact_privilege"
