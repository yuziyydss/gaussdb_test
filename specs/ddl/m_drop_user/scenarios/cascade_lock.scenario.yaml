schema_version: 1
kind: scenario
id: scenario_m_drop_user_cascade_lock
name: scenario_m_drop_user_cascade_lock
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_user
fact_refs:
- m_drop_user_fact_kill_query
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 仅保留锁/线程影响风险，不在本循环生成或执行 CASCADE 清理。
oracles:
- kind: manual_assertion
  expected: 特殊环境行为未执行
execution_requirements:
- database_authorization
- target_oracle_calibration
