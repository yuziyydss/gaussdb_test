schema_version: 1
kind: scenario
id: scenario_m_drop_user_dependencies
name: scenario_m_drop_user_dependencies
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_user
fact_refs:
- m_drop_user_fact_schema
- m_drop_user_fact_restrict
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 独立创建同名 Schema/目标对象，验证 RESTRICT 拒绝的目标错误与空 Schema 自动删除边界；不要任意错误即通过。
oracles:
- kind: manual_assertion
  expected: RESTRICT 目标错误及隐式 Schema 生命周期需校准
execution_requirements:
- database_authorization
- target_oracle_calibration
