schema_version: 1
kind: scenario
id: scenario_m_drop_user_missing_notice
name: scenario_m_drop_user_missing_notice
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_drop_user
fact_refs:
- m_drop_user_fact_missing
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: DROP USER IF EXISTS m_drop_user_absent RESTRICT;
oracles:
- kind: manual_assertion
  expected: 缺失用户发 NOTICE，尚未执行
execution_requirements:
- database_authorization
- target_oracle_calibration
