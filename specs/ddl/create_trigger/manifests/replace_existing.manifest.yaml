schema_version: 1
kind: "manifest"
id: "manifest_create_trigger_replace_existing"
name: "CREATE TRIGGER replace_existing"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "create_trigger"
syntax_ref: "syntax_create_trigger"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_create_trigger_replace_trigger"
identifier_policy: {}
bindings:
  replace:
    - "create_trigger_replace_replace"
  timing:
    - "create_trigger_timing_before"
  event:
    - "create_trigger_event_insert"
  level:
    - "create_trigger_level_row"
  when:
    - "create_trigger_when_none"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
