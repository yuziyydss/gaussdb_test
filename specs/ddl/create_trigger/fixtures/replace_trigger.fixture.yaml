schema_version: 1
kind: "fixture"
id: "fixture_create_trigger_replace_trigger"
name: "CREATE TRIGGER replace_trigger"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_trigger"
requires_fixture_refs:
  - "fixture_create_trigger_functions"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TRIGGER fp_trigger AFTER INSERT ON t_trigger_source FOR EACH ROW EXECUTE PROCEDURE fp_trigger_new();"
  teardown_sqls:
    - "DROP TRIGGER IF EXISTS fp_trigger ON t_trigger_source;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
