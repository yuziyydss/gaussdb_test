schema_version: 1
kind: "fixture"
id: "fixture_create_security_label_existing_labels"
name: "CREATE SECURITY LABEL existing_labels"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_security_label"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
    - "CREATE SECURITY LABEL b9_sec_a 'L1:G1';"
    - "CREATE SECURITY LABEL b9_sec_b 'L2:G2';"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
