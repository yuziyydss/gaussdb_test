schema_version: 1
kind: manifest
id: manifest_create_security_label_invalid_content
name: CREATE SECURITY LABEL invalid_content
status: needs_review
description: 仅保留原文给出具体错误的缺失范围负向代表；其他非法内容形态待独立错误身份校准。
factor_ref: create_security_label
syntax_ref: syntax_create_security_label
suite_type: negative
strategy: pairwise
fixture_refs:
- fixture_create_security_label_transaction
identifier_policy: {}
bindings:
  content:
  - create_security_label_content_empty_range
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: confirmed
  error_category: invalid_security_label_content
  fact_refs:
  - create_security_label_fact_invalid_content
  error_message_regex: in label text ".*", there at least have one level and one group
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
violates_rule_refs:
- create_security_label_rule_content_valid
environment_requirements:
- key: security_label_authorized
  allowed_values:
  - 'true'
  fact_refs:
  - create_security_label_fact_privilege
