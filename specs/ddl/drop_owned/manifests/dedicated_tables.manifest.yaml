schema_version: 1
kind: "manifest"
id: "manifest_drop_owned_dedicated_tables"
name: "DROP OWNED dedicated_tables"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_owned"
syntax_ref: "syntax_drop_owned"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_owned_owned_tables"
identifier_policy: {}
bindings:
  owners:
    - "drop_owned_owners_one"
    - "drop_owned_owners_two"
  behavior:
    - "drop_owned_behavior_default"
    - "drop_owned_behavior_restrict"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "dedicated_roles_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "create_role::create_role_fact_privilege"
  -
    key: "password_disable_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "create_role::create_role_fact_disable_admin"
