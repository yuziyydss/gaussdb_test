schema_version: 1
kind: "scenario"
id: "scenario_alter_synonym_ownership"
name: "ALTER SYNONYM ownership"
status: "planned"
description: "检查普通同义词 owner 变化；确认 fp_syn_owner 存在且有当前模式 CREATE 权限。系统管理员执行，禁止新建角色/用户或 DROP OWNED 兜底。"
factor_ref: "alter_synonym"
fixture_refs: []
fact_refs:
  - "alter_synonym_fact_owner"
  - "alter_synonym_fact_admin"
  - "alter_synonym_fact_schema_create"
  - "alter_synonym_fact_new_owner"
  - "alter_synonym_fact_not_public"
  - "create_synonym::create_synonym_fact_missing_object"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "检查普通同义词 owner 变化；确认 fp_syn_owner 存在且有当前模式 CREATE 权限。系统管理员执行，禁止新建角色/用户或 DROP OWNED 兜底。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
