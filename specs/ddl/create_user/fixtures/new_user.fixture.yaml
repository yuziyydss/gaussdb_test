schema_version: 1
kind: "fixture"
id: "fixture_create_user_new_user"
name: "CREATE USER new_user"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_user"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "SELECT 1 / (1 - COUNT(*)) AS assert_user_absent FROM gs_roles WHERE rolname = 'b9_user_new';"
  teardown_sqls:
    - "DROP USER IF EXISTS b9_user_new RESTRICT;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
