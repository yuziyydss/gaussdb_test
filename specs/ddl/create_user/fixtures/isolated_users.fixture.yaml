schema_version: 1
kind: "fixture"
id: "fixture_create_user_isolated_users"
name: "CREATE USER isolated_users"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_user"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE USER b9_user_a NOLOGIN PASSWORD DISABLE;"
    - "CREATE USER b9_user_b NOLOGIN PASSWORD DISABLE;"
  teardown_sqls:
    - "DROP USER IF EXISTS b9_user_b RESTRICT;"
    - "DROP USER IF EXISTS b9_user_a RESTRICT;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
