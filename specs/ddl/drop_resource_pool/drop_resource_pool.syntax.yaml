schema_version: 1
kind: "syntax"
id: "syntax_drop_resource_pool"
name: "DROP RESOURCE POOL 语法"
factor_ref: "drop_resource_pool"
category: "DDL"
status: "ready"
description: "语法结构来自 PDF，选值仅由 factor/manifest 提供。"
production: "DROP RESOURCE POOL {if_exists} b9_pool"
rendering:
  whitespace: "collapse"
  statement_terminator: ";"
slots:
  if_exists:
    type: "token"
    optional: true
    dimension_ref: "drop_resource_pool.if_exists"
source_fact_refs:
  - "drop_resource_pool_fact_syntax"
  - "drop_resource_pool_fact_name"
