schema_version: 1
kind: "manifest"
id: "manifest_drop_resource_pool_unbound"
name: "DROP RESOURCE POOL unbound"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "drop_resource_pool"
syntax_ref: "syntax_drop_resource_pool"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_drop_resource_pool_unbound_pool"
identifier_policy: {}
bindings:
  if_exists:
    - "drop_resource_pool_if_exists_none"
    - "drop_resource_pool_if_exists_yes"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "resource_pool_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "drop_resource_pool_fact_privilege"
  -
    key: "multitenant"
    allowed_values:
      - "false"
    fact_refs:
      - "drop_resource_pool_fact_no_multitenant"
  -
    key: "upgrade_in_progress"
    allowed_values:
      - "false"
    fact_refs:
      - "drop_resource_pool_fact_no_upgrade"
