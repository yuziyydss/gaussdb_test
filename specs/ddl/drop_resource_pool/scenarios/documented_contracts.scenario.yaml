schema_version: 1
kind: "scenario"
id: "scenario_drop_resource_pool_documented_contracts"
name: "DROP RESOURCE POOL documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_resource_pool"
fixture_refs: []
fact_refs:
  - "drop_resource_pool_fact_purpose"
  - "drop_resource_pool_fact_unbound"
  - "drop_resource_pool_fact_privilege"
  - "drop_resource_pool_fact_no_multitenant"
  - "drop_resource_pool_fact_no_upgrade"
  - "drop_resource_pool_fact_if_exists"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除资源池。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "角色已关联资源池时不能删除。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "仅SYSADMIN或初始用户可用。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "多租场景不支持，使用RESOURCE_MANAGER。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "升级期间不支持。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "IF EXISTS不存在时仅NOTICE，存在则删除。"
oracles:
  -
    kind: "documented_behavior"
    expected: "IF EXISTS不存在时仅NOTICE，存在则删除。"
execution_requirements:
  - "database"
  - "same_session"
