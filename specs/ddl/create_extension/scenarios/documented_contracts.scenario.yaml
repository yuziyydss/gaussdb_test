schema_version: 1
kind: "scenario"
id: "scenario_create_extension_documented_contracts"
name: "CREATE EXTENSION documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "create_extension"
fixture_refs: []
fact_refs:
  - "create_extension_fact_internal"
  - "create_extension_fact_purpose"
  - "create_extension_fact_support_files"
  - "create_extension_fact_unique"
  - "create_extension_fact_script"
  - "create_extension_fact_privilege"
  - "create_extension_fact_object_collision"
  - "create_extension_fact_enabled"
  - "create_extension_fact_schema_special"
  - "create_extension_fact_if_not_exists"
  - "create_extension_fact_schema"
  - "create_extension_fact_scope"
  - "create_extension_fact_from"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "扩展为内部功能，只允许数据库内部高级包或扩展。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "安装扩展。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "安装前必须已有扩展支持文件。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "安装新扩展须无已安装同名扩展。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "安装会执行扩展脚本并创建SQL实体。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "安装需有组件创建权限，多数需数据库所有者权限，执行者成为扩展所有者。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "存在同名PACKAGE、同义词、操作符、目录、函数、过程、视图或表时安装失败。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "扩展可能导致兼容风险，创建必须设置enable_extension=true。"
  -
    id: "contract_8"
    action: "verify_documented_contract"
    note: "特殊字符开关off且脚本使用@extschema@时，control schema不能包含列出的特殊字符。"
  -
    id: "contract_9"
    action: "verify_documented_contract"
    note: "已存在扩展时IF NOT EXISTS给提示而非直接报错，不保证与当前脚本定义相同。"
  -
    id: "contract_10"
    action: "verify_documented_contract"
    note: "SCHEMA须已存在；控制文件与命令均未指定时使用默认模式。"
  -
    id: "contract_11"
    action: "verify_documented_contract"
    note: "扩展本身不属于模式，扩展实例属于模式。"
  -
    id: "contract_12"
    action: "verify_documented_contract"
    note: "FROM old_version向已有模块装入缺少内容而非创建新实体，须指定其已有实体所在模式。"
oracles:
  -
    kind: "documented_behavior"
    expected: "存在同名PACKAGE、同义词、操作符、目录、函数、过程、视图或表时安装失败。"
  -
    kind: "documented_behavior"
    expected: "已存在扩展时IF NOT EXISTS给提示而非直接报错，不保证与当前脚本定义相同。"
  -
    kind: "documented_behavior"
    expected: "扩展本身不属于模式，扩展实例属于模式。"
execution_requirements:
  - "database"
  - "same_session"
