schema_version: 1
kind: "scenario"
id: "scenario_alter_default_privileges_acl_paths"
name: "ALTER DEFAULT PRIVILEGES acl_paths"
status: "planned"
description: "默认授予不代表拥有对象上的最终有效权限，需检查其他授权路径。"
factor_ref: "alter_default_privileges"
fixture_refs: []
fact_refs:
  - "revoke::revoke_fact_effective_privileges"
  - "alter_default_privileges_fact_future_objects"
preconditions: []
steps:
  -
    id: "future_acl"
    action: "implement_before_after_creation_acl_oracles"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
