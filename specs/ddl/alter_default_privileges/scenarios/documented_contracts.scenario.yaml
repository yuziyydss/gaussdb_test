schema_version: 1
kind: "scenario"
id: "scenario_alter_default_privileges_documented_contracts"
name: "ALTER DEFAULT PRIVILEGES documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_default_privileges"
fixture_refs: []
fact_refs:
  - "alter_default_privileges_fact_future_objects"
  - "alter_default_privileges_fact_schema"
  - "alter_default_privileges_fact_schema_create"
  - "alter_default_privileges_fact_recipient"
  - "alter_default_privileges_fact_default_acl_cleanup"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "修改以后创建对象的默认权限，不影响已有对象权限。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "IN SCHEMA必须为已有模式。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "target_role必须有schema_name的CREATE权限。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "受赠/被回收角色必须已存在。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "删除拥有默认权限记录的角色前须恢复缺省权限或由DROP OWNED处理记录。"
oracles:
  -
    kind: "documented_behavior"
    expected: "修改以后创建对象的默认权限，不影响已有对象权限。"
execution_requirements:
  - "database"
  - "same_session"
