schema_version: 1
kind: manifest
id: manifest_create_resource_pool_max_dop_syntax
name: CREATE RESOURCE POOL max_dop syntax
status: needs_review
description: 一个MAX_DOP语法代表；补充来源称仅扩容使用，不宣称集中式支持或行为通过。
factor_ref: create_resource_pool
syntax_ref: syntax_create_resource_pool
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  options:
  - create_resource_pool_options_dop_one
environment_requirements:
- key: max_dop_support_context
  allowed_values:
  - expansion_or_authoritative_contract
  fact_refs:
  - create_resource_pool_fact_dop_centralized_conflict
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
