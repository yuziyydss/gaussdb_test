schema_version: 1
kind: "fixture"
id: "fixture_create_resource_pool_existing_pool"
name: "CREATE RESOURCE POOL existing_pool"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "create_resource_pool"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE RESOURCE POOL b9_pool;"
  teardown_sqls:
    - "DROP RESOURCE POOL IF EXISTS b9_pool;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
