schema_version: 1
kind: "scenario"
id: "scenario_drop_client_master_key_documented_contracts"
name: "DROP CLIENT MASTER KEY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "drop_client_master_key"
fixture_refs: []
fact_refs:
  - "drop_client_master_key_fact_purpose"
  - "drop_client_master_key_fact_privilege"
  - "drop_client_master_key_fact_metadata_only"
  - "drop_client_master_key_fact_exists"
  - "drop_client_master_key_fact_missing"
  - "drop_client_master_key_fact_name"
  - "drop_client_master_key_fact_default_restrict"
  - "drop_client_master_key_fact_cascade"
  - "drop_client_master_key_fact_restrict"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除CMK对象。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "CMK所有者或获DROP权限者可删，管理员默认有权。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "只删数据库目录元数据，不删除客户端或外部密钥服务中的密钥实体。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "IF EXISTS目标存在时删除。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "IF EXISTS缺失仅NOTICE。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "目标须已有CMK名。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "默认RESTRICT。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "CASCADE级联删除依赖CMK对象。"
  -
    id: "contract_8"
    action: "verify_documented_contract"
    note: "RESTRICT有依赖时拒绝删除。"
oracles:
  -
    kind: "documented_behavior"
    expected: "只删数据库目录元数据，不删除客户端或外部密钥服务中的密钥实体。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS目标存在时删除。"
  -
    kind: "documented_behavior"
    expected: "IF EXISTS缺失仅NOTICE。"
  -
    kind: "documented_behavior"
    expected: "默认RESTRICT。"
  -
    kind: "documented_behavior"
    expected: "CASCADE级联删除依赖CMK对象。"
  -
    kind: "documented_behavior"
    expected: "RESTRICT有依赖时拒绝删除。"
execution_requirements:
  - "database"
  - "same_session"
