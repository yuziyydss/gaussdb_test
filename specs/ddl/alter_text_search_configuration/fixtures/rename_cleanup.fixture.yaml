schema_version: 1
kind: "fixture"
id: "fixture_alter_text_search_configuration_rename_cleanup"
name: "ALTER TEXT SEARCH CONFIGURATION rename_cleanup"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_text_search_configuration"
requires_fixture_refs:
  - "fixture_alter_text_search_configuration_empty"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "SELECT 1;"
  teardown_sqls:
    - "DROP TEXT SEARCH CONFIGURATION IF EXISTS fp_atc_renamed CASCADE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
