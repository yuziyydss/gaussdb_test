schema_version: 1
kind: "fixture"
id: "fixture_alter_text_search_configuration_mapped"
name: "ALTER TEXT SEARCH CONFIGURATION mapped"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "alter_text_search_configuration"
requires_fixture_refs:
  - "fixture_alter_text_search_configuration_empty"
  - "fixture_create_text_search_dictionary_simple"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "ALTER TEXT SEARCH CONFIGURATION fp_tsc_empty ADD MAPPING FOR word WITH simple;"
  teardown_sqls:
    - "SELECT 1;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
