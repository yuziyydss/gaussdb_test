schema_version: 1
kind: "scenario"
id: "scenario_alter_event_documented_contracts"
name: "ALTER EVENT documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "alter_event"
fixture_refs: []
fact_refs:
  - "alter_event_fact_purpose"
  - "alter_event_fact_compat_b"
  - "alter_event_fact_privilege"
  - "alter_event_fact_owner_metadata"
  - "alter_event_fact_owner_transition"
  - "alter_event_fact_definer_inherited"
  - "alter_event_fact_admin_transition"
  - "alter_event_fact_definer"
  - "alter_event_fact_rename"
  - "alter_event_fact_completion"
  - "alter_event_fact_enabled"
  - "alter_event_fact_comments"
  - "alter_event_fact_underscore_gate"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "修改已有任务属性。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅B模式支持。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "仅任务所有者可改，管理员默认可改全部。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "SHOW EVENTS或PG_JOB.log_user可查所有者。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "修改成功所有者变为当前用户，指定DEFINER则变为DEFINER。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "DEFINER限制同CREATE EVENT。"
  -
    id: "contract_6"
    action: "verify_documented_contract"
    note: "管理员修改他人任务后所有者与执行权限切为管理员。"
  -
    id: "contract_7"
    action: "verify_documented_contract"
    note: "默认创建者权限或指定者权限；指定DEFINER需SYSADMIN。"
  -
    id: "contract_8"
    action: "verify_documented_contract"
    note: "RENAME TO更新任务名。"
  -
    id: "contract_9"
    action: "verify_documented_contract"
    note: "完成默认删除，PRESERVE覆盖。"
  -
    id: "contract_10"
    action: "verify_documented_contract"
    note: "默认ENABLE，DISABLE ON SLAVE与DISABLE相同。"
  -
    id: "contract_11"
    action: "verify_documented_contract"
    note: "COMMENT存GS_JOB_ATTRIBUTE。"
  -
    id: "contract_12"
    action: "verify_documented_contract"
    note: "下划线单位仅B模式且5.7/s1配置。"
oracles:
  -
    kind: "documented_behavior"
    expected: "SHOW EVENTS或PG_JOB.log_user可查所有者。"
  -
    kind: "documented_behavior"
    expected: "管理员修改他人任务后所有者与执行权限切为管理员。"
  -
    kind: "documented_behavior"
    expected: "完成默认删除，PRESERVE覆盖。"
  -
    kind: "documented_behavior"
    expected: "默认ENABLE，DISABLE ON SLAVE与DISABLE相同。"
  -
    kind: "documented_behavior"
    expected: "COMMENT存GS_JOB_ATTRIBUTE。"
execution_requirements:
  - "database"
  - "same_session"
