schema_version: 1
kind: "manifest"
id: "manifest_alter_event_comment"
name: "ALTER EVENT comment"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "alter_event"
syntax_ref: "syntax_alter_event"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_alter_event_existing_disabled_event"
identifier_policy: {}
bindings:
  operation:
    - "alter_event_operation_comment"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "sql_compatibility"
    allowed_values:
      - "B"
    fact_refs:
      - "alter_event_fact_compat_b"
  -
    key: "event_owner_or_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "alter_event_fact_privilege"
