schema_version: 1
kind: manifest
id: manifest_m_autohint_purge_dedicated_history
name: manifest_m_autohint_purge_dedicated_history
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_autohint_purge
syntax_ref: syntax_m_autohint_purge
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_autohint_purge_history
identifier_policy: {}
bindings: {}
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_autohint_purge_fact_mode
- key: actor_authority
  allowed_values:
  - sysadmin
  fact_refs:
  - m_autohint_purge_fact_authority
- key: instance_effect_scope
  allowed_values:
  - dedicated_disposable_instance_no_foreign_hint_models
  fact_refs:
  - m_autohint_purge_fact_authority
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
