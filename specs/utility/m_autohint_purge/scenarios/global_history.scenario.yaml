schema_version: 1
kind: scenario
id: scenario_m_autohint_purge_global_history
name: scenario_m_autohint_purge_global_history
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_autohint_purge
fact_refs:
- m_autohint_purge_fact_scope
- m_autohint_purge_fact_result
- m_autohint::m_autohint_fact_analyze
fixture_refs:
- fixture_m_autohint_purge_history
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 未来先检查整个隔离实例不存在其他用户历史模型，再建立本case模型、核对前置实际数量，授权后才能执行PURGE；Schema不足以隔离全局历史。
oracles:
- kind: manual_assertion
  expected: 全局历史数量、清空结果与0条协议状态未验证；实例条件不满足时禁止运行
execution_requirements:
- database_authorization
- target_oracle_calibration
