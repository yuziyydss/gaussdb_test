schema_version: 1
kind: "fixture"
id: "fixture_set_transaction_active"
name: "SET TRANSACTION active"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "set_transaction"
requires_fixture_refs:
  - fixture_start_transaction_active_connection
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "auto"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
