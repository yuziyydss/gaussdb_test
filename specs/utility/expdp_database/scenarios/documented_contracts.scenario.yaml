schema_version: 1
kind: "scenario"
id: "scenario_expdp_database_documented_contracts"
name: "EXPDP DATABASE documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "expdp_database"
fixture_refs: []
fact_refs:
  - "expdp_database_fact_purpose"
preconditions:
  - "先取得专用备份恢复工具的真实调用合同及独立授权；新Schema、修改application_name或虚构目录不能替代工具身份。"
  - "auxdb专用工具调用；导出全部物理文件，不是仅表定义。普通会话错误不是备份成功。"
  - "物理文件身份、来源库/表、完整性、阶段成功回执、部分失败恢复与目录归属仍需实现；本planned场景不授权读写/覆盖/删除任何备份文件。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "导出数据库全部物理文件。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
  - "authoritative_backup_tool_context"
  - "physical_backup_artifact_inventory"
  - "import_export_phase_identity"
  - "partial_failure_recovery_review"
