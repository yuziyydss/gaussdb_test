schema_version: 1
kind: "fixture"
id: "fixture_security_label_on_marked_source"
name: "SECURITY LABEL ON marked_source"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "security_label_on"
requires_fixture_refs:
  - "fixture_security_label_on_source"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "SECURITY LABEL ON TABLE b9_sec_table IS 'b9_sec_a';"
    - "SECURITY LABEL ON COLUMN b9_sec_table.col_1 IS 'b9_sec_a';"
  teardown_sqls:
    - "SECURITY LABEL ON COLUMN b9_sec_table.col_1 IS NULL;"
    - "SECURITY LABEL ON TABLE b9_sec_table IS NULL;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
