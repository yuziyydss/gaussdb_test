schema_version: 1
kind: "fixture"
id: "fixture_security_label_on_source"
name: "SECURITY LABEL ON source"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "security_label_on"
requires_fixture_refs:
  - "fixture_create_security_label_existing_labels"
provides:
  tables:
    -
      name: "b9_sec_table"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: true
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TABLE b9_sec_table (col_1 INTEGER, col_2 INTEGER);"
  teardown_sqls:
    - "DROP TABLE IF EXISTS b9_sec_table;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
