schema_version: 1
kind: "fixture"
id: "fixture_explain_transaction"
name: "EXPLAIN transaction"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "explain"
requires_fixture_refs:
  - "fixture_explain_source"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
    - "SET LOCAL explain_perf_mode=normal;"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
