schema_version: 1
kind: "fixture"
id: "fixture_set_role_session"
name: "SET ROLE session"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "set_role"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
  teardown_sqls:
    - "RESET ROLE;"
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
