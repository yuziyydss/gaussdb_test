schema_version: 1
kind: fixture
id: fixture_set_role_fresh_switch
name: SET ROLE fresh_switch
status: needs_review
description: 创建一个专用LOGIN角色并授予当前会话用户；密码为静态一次性非秘密占位符，不用于真实环境。
factor_ref: set_role
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "CREATE ROLE set_role_fresh LOGIN PASSWORD 'SetRole_2026_Aa9';"
    - "GRANT set_role_fresh TO CURRENT_USER;"
  teardown_sqls:
    - "RESET ROLE;"
    - "REVOKE set_role_fresh FROM CURRENT_USER;"
    - "DROP ROLE IF EXISTS set_role_fresh;"
  note: 仅用于静态语法代表；执行前必须使用独占会话和专用初始用户，确认密码策略、成员关系和清理权限。PASSWORD字面量不是秘密，不用于生产或共享环境；不验证权限变化、会话身份、密码策略或LDAP限制。
