schema_version: 1
kind: "fixture"
id: "fixture_declare_two_cursors"
name: "DECLARE two_cursors"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "declare"
requires_fixture_refs:
  - "fixture_declare_open_cursor"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "DECLARE c_cursor_second CURSOR FOR SELECT col_1, col_2 FROM t_cursor_source ORDER BY col_1;"
  teardown_sqls:
    - "ROLLBACK;"
  note: "独占事务内创建/定位的游标由 ROLLBACK 清理，包含目标语句失败的中止事务。上层重复 ROLLBACK 仅产生事务外提示，不清理业务会话。尚未执行数据库。"
