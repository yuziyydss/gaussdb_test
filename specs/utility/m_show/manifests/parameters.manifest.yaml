schema_version: 1
kind: manifest
id: manifest_m_show_parameters
name: manifest_m_show_parameters
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_show
syntax_ref: syntax_m_show
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_show_session_parameters
identifier_policy: {}
bindings:
  form:
  - m_show_form_parameters
  parameter:
  - m_show_parameter_timezone
  - m_show_parameter_isolation
  - m_show_parameter_identity
  - m_show_parameter_all
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_show_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_set::m_set_fact_session
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
