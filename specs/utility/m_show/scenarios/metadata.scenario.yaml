schema_version: 1
kind: scenario
id: scenario_m_show_metadata
name: scenario_m_show_metadata
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_show
fact_refs:
- m_show_fact_definition
- m_show_fact_version
- m_show_fact_tables
- m_show_fact_key
- m_show_fact_metadata_gap
fixture_refs:
- fixture_m_show_shared_view
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 比较 SHOW FULL COLUMNS、SHOW CREATE 与实际 fixture 目录元数据；按版本归一化 DDL，不固定完整返回字符串。
oracles:
- kind: manual_assertion
  expected: 列名/DEFAULT/可空性/索引身份与 fixture 一致；SHOW TABLES 有序且无临时表。待专用索引及临时表场景
execution_requirements:
- database_authorization
- target_oracle_calibration
