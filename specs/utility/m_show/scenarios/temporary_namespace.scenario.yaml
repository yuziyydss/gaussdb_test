schema_version: 1
kind: scenario
id: scenario_m_show_temporary_namespace
name: scenario_m_show_temporary_namespace
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_show
fact_refs:
- m_show_fact_temporary
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 创建 case 独占临时表和索引，发现真实临时 Schema 后再绑定 SHOW INDEX FROM/IN
oracles:
- kind: manual_assertion
  expected: 普通表索引 fixture 不算覆盖临时 Schema 路径
execution_requirements:
- database_authorization
- target_oracle_calibration
