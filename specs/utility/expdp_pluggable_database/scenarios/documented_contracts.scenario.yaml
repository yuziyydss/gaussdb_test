schema_version: 1
kind: "scenario"
id: "scenario_expdp_pluggable_database_documented_contracts"
name: "EXPDP PLUGGABLE DATABASE documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "expdp_pluggable_database"
fixture_refs: []
fact_refs:
  - "expdp_pluggable_database_fact_purpose"
  - "expdp_pluggable_database_fact_tool_only"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "导出PDB的全部物理文件。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "由备份恢复工具调用，用户直接调用报Only auxdb is allowed to execute expdp cmd。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
