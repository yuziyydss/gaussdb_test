schema_version: 1
kind: "scenario"
id: "scenario_show_events_documented_contracts"
name: "SHOW EVENTS documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "show_events"
fixture_refs: []
fact_refs:
  - "show_events_fact_purpose"
  - "show_events_fact_compat_b"
  - "show_events_fact_schema_default"
  - "show_events_fact_like"
  - "show_events_fact_where"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "展示指定Schema的任务基本信息。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅B模式支持。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "FROM/IN指定Schema，省略当前Schema。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "LIKE匹配任务名，省略显示当前Schema所有任务。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "WHERE布尔表达式用于行过滤，不满足条件不返回。"
oracles:
  -
    kind: "documented_behavior"
    expected: "展示指定Schema的任务基本信息。"
  -
    kind: "documented_behavior"
    expected: "FROM/IN指定Schema，省略当前Schema。"
  -
    kind: "documented_behavior"
    expected: "LIKE匹配任务名，省略显示当前Schema所有任务。"
execution_requirements:
  - "database"
  - "same_session"
