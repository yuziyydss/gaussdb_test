schema_version: 1
kind: "manifest"
id: "manifest_show_events_empty_schema"
name: "SHOW EVENTS empty_schema"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "show_events"
syntax_ref: "syntax_show_events"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_show_events_schema_pair"
identifier_policy: {}
bindings:
  scope:
    - "show_events_scope_current"
    - "show_events_scope_from"
    - "show_events_scope_in"
  filter:
    - "show_events_filter_none"
    - "show_events_filter_like"
    - "show_events_filter_where"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "sql_compatibility"
    allowed_values:
      - "B"
    fact_refs:
      - "show_events_fact_compat_b"
