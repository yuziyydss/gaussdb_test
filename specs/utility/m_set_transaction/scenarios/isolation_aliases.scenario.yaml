schema_version: 1
kind: scenario
id: scenario_m_set_transaction_isolation_aliases
name: scenario_m_set_transaction_isolation_aliases
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set_transaction
fact_refs:
- m_set_transaction_fact_uncommitted_alias
- m_set_transaction_fact_serializable_alias
fixture_refs:
- fixture_m_set_transaction_fresh_transaction
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 在两个独立会话验证别名隔离级别的可见性，不以接受关键字证明 SERIALIZABLE 功能。
oracles:
- kind: manual_assertion
  expected: READ UNCOMMITTED≈READ COMMITTED；SERIALIZABLE≈REPEATABLE READ，待并发执行
execution_requirements:
- database_authorization
- target_oracle_calibration
