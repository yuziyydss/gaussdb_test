schema_version: 1
kind: scenario
id: scenario_m_set_transaction_global_and_next
name: scenario_m_set_transaction_global_and_next
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set_transaction
fact_refs:
- m_set_transaction_fact_global_reconnect
- m_set_transaction_fact_s2_next
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: GLOBAL 需要获授权专用物理库和新连接；s2 next-transaction 另建无事务前置与目标错误场景。
oracles:
- kind: manual_assertion
  expected: 当前有限清单不修改全局特性，不把 s2 next 与 SESSION 混为同一语义
execution_requirements:
- database_authorization
- target_oracle_calibration
