schema_version: 1
kind: fixture
id: fixture_m_set_transaction_fresh_transaction
name: fixture_m_set_transaction_fresh_transaction
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set_transaction
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - START TRANSACTION;
  teardown_sqls:
  - ROLLBACK;
  note: 必须在首条数据查询/修改前；setup/目标/ROLLBACK 同一独占连接。会话默认特性可能改变，case 完成后必须关闭连接，不仅依赖事务回滚。GLOBAL 与 s2 无修饰分支不在当前清单。
