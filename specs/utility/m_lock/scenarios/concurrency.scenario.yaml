schema_version: 1
kind: scenario
id: scenario_m_lock_concurrency
name: scenario_m_lock_concurrency
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_lock
fact_refs:
- m_lock_fact_order
- m_lock_fact_nowait
fixture_refs:
- fixture_m_lock_transaction
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 需要两个会话分别申请冲突锁并验证 NOWAIT/等待和顺序；当前两表采用同一锁模式，不代表所有混合模式。
oracles:
- kind: manual_assertion
  expected: 跨会话锁冲突尚未执行
execution_requirements:
- database_authorization
- target_oracle_calibration
