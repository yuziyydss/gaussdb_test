schema_version: 1
kind: "scenario"
id: "scenario_reset_special_state"
name: "RESET special_state"
status: "planned"
description: "在独占连接分别验证模式、事务隔离、会话身份和 ALL 的影响；保存执行前状态、捕获缺省上下文，并验证失败路径恢复。"
factor_ref: "reset"
fixture_refs: []
fact_refs:
  - "reset_fact_syntax"
  - "reset_fact_parameter_domain"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "在独占连接分别验证模式、事务隔离、会话身份和 ALL 的影响；保存执行前状态、捕获缺省上下文，并验证失败路径恢复。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
