schema_version: 1
kind: "fixture"
id: "fixture_reset_changed_timezone"
name: "RESET changed_timezone"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "reset"
requires_fixture_refs:
  - "fixture_reset_transaction"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "SET timezone TO 'Europe/Rome';"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
