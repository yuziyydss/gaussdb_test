schema_version: 1
kind: fixture
id: fixture_m_timecapsule_table_truncate_ddl
name: fixture_m_timecapsule_table_truncate_ddl
status: needs_review
description: TRUNCATE后执行ALTER TABLE新增列，用于DDL阻断闪回的负向代表。
factor_ref: m_timecapsule_table
requires_fixture_refs: []
provides:
  tables:
  - name: m_timecapsule_namespace.source
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
    - name: qty
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE SCHEMA m_timecapsule_namespace;
  - CREATE TABLE m_timecapsule_namespace.source (id INTEGER, qty INTEGER);
  - INSERT INTO m_timecapsule_namespace.source VALUES (1,10),(2,20);
  - TRUNCATE TABLE m_timecapsule_namespace.source;
  - ALTER TABLE m_timecapsule_namespace.source ADD COLUMN ddl_marker INTEGER;
  teardown_sqls:
  - DROP TABLE IF EXISTS m_timecapsule_namespace.source PURGE;
  - DROP TABLE IF EXISTS m_timecapsule_namespace.restored PURGE;
  - DROP SCHEMA m_timecapsule_namespace;
  note: 仅静态产物；不执行TIMECAPSULE，不声称回收站或DDL行为已验证。对象名须预先不存在，失败时精确报告残留。
