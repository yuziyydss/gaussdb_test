schema_version: 1
kind: fixture
id: fixture_m_timecapsule_table_drop
name: fixture_m_timecapsule_table_drop
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_timecapsule_table
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE SCHEMA m_timecapsule_namespace;
  - CREATE TABLE m_timecapsule_namespace.source (id INTEGER, qty INTEGER);
  - INSERT INTO m_timecapsule_namespace.source VALUES (1,10),(2,20);
  - DROP TABLE m_timecapsule_namespace.source;
  teardown_sqls:
  - DROP TABLE IF EXISTS m_timecapsule_namespace.source PURGE;
  - DROP TABLE IF EXISTS m_timecapsule_namespace.restored PURGE;
  - DROP SCHEMA m_timecapsule_namespace;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。 仅普通永久整数表；两个候选名称在setup前均须不存在，DROP后不声明活表。失败时另报精确回收站残留，不用PURGE
    RECYCLEBIN兜底。
