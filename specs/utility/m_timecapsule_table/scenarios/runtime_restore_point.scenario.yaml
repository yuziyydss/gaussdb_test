schema_version: 1
kind: scenario
id: scenario_m_timecapsule_table_runtime_restore_point
name: scenario_m_timecapsule_table_runtime_restore_point
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_timecapsule_table
fact_refs:
- m_timecapsule_table_fact_version_point
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 未来运行器必须运行时捕获并传递CSN/时间点和UNDO保留条件；本批不填历史示例25391或2024时间戳。
oracles:
- kind: manual_assertion
  expected: CSN/TIMESTAMP、旧版本回收和约3秒时间偏差另行建模
execution_requirements:
- database_authorization
- target_oracle_calibration
