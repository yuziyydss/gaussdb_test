schema_version: 1
kind: fixture
id: fixture_close_transaction
name: CLOSE 独占事务
status: ready
description: 通过本包入口引用 DECLARE 的事务前置，不复制建表和事务 SQL。
factor_ref: close
requires_fixture_refs: [fixture_declare_transaction]
provides: {}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: auto
  note: 依赖先 setup，逆序 teardown；本包无新增对象。
