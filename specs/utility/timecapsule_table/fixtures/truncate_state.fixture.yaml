schema_version: 1
kind: "fixture"
id: "fixture_timecapsule_table_truncate_state"
name: "TIMECAPSULE TABLE truncate_state"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "timecapsule_table"
requires_fixture_refs:
  - "fixture_create_schema_pair"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TABLE fp_cs_one.b11_flash_source (col_1 INT, col_2 INT) WITH (storage_type=astore);"
    - "INSERT INTO fp_cs_one.b11_flash_source VALUES (1,10),(2,20);"
    - "TRUNCATE TABLE fp_cs_one.b11_flash_source;"
  teardown_sqls:
    - "DROP TABLE IF EXISTS fp_cs_one.b11_flash_source PURGE;"
    - "DROP TABLE IF EXISTS fp_cs_one.b11_flash_restored PURGE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
