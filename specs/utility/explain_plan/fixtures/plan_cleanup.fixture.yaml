schema_version: 1
kind: "fixture"
id: "fixture_explain_plan_plan_cleanup"
name: "EXPLAIN PLAN plan_cleanup"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "explain_plan"
requires_fixture_refs:
  - "fixture_explain_plan_source"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "SELECT 1;"
  teardown_sqls:
    - "DELETE FROM plan_table WHERE statement_id IN ('fp_ep_basic', 'xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx');"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
