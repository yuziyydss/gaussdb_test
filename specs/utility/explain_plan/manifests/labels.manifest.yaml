schema_version: 1
kind: "manifest"
id: "manifest_explain_plan_labels"
name: "EXPLAIN PLAN labels"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "explain_plan"
syntax_ref: "syntax_explain_plan"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_explain_plan_plan_cleanup"
identifier_policy: {}
bindings:
  label:
    - "explain_plan_label_default"
    - "explain_plan_label_basic"
    - "explain_plan_label_ascii30"
  query:
    - "explain_plan_query_all"
    - "explain_plan_query_where"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  - key: execution_session
    allowed_values: [fresh_dedicated]
    fact_refs: [explain_plan_fact_isolation]
