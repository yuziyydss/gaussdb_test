schema_version: 1
kind: manifest
id: manifest_explain_autohint_user_call_negative
name: EXPLAIN AUTOHINT user call negative
status: needs_review
description: 两条原文示例的用户直接调用负向代表；不执行数据库，不模拟内核AUTOHINT任务。
factor_ref: explain_autohint
syntax_ref: syntax_explain_autohint
suite_type: negative
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  mode:
  - explain_autohint_mode_plan
  - explain_autohint_mode_execute
environment_requirements:
- key: autohint_task_context
  allowed_values:
  - not_in_kernel_autohint_task
  fact_refs:
  - explain_autohint_fact_runtime_contract
violates_rule_refs:
- explain_autohint_rule_kernel_task
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: confirmed
  error_category: user_call_outside_autohint_task
  error_message_regex: 'Can not get exploration cache for cache id: 2'
  fact_refs:
  - explain_autohint_fact_user_call_error
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
