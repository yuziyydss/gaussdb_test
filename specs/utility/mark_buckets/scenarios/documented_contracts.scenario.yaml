schema_version: 1
kind: "scenario"
id: "scenario_mark_buckets_documented_contracts"
name: "MARK BUCKETS documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "mark_buckets"
fixture_refs: []
fact_refs:
  - "mark_buckets_fact_purpose"
  - "mark_buckets_fact_unsupported"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "           用于扩容工具通知内核哪些bucket已经完成搬迁。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "当前版本不支持此特性。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
