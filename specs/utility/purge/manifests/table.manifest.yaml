schema_version: 1
kind: "manifest"
id: "manifest_purge_table"
name: "PURGE table"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "purge"
syntax_ref: "syntax_purge"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_purge_table_recycled"
identifier_policy: {}
bindings:
  target_kind:
    - "purge_target_kind_table"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "enable_recyclebin"
    allowed_values:
      - "on"
    fact_refs:
      - "purge_fact_body_39"
