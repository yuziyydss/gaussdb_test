schema_version: 1
kind: "fixture"
id: "fixture_purge_table_recycled"
name: "PURGE table_recycled"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "purge"
requires_fixture_refs:
  - "fixture_create_schema_pair"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TABLE fp_cs_one.b11_purge_source (col_1 INT, col_2 INT) WITH (storage_type=ustore);"
    - "DROP TABLE fp_cs_one.b11_purge_source;"
  teardown_sqls:
    - "DROP TABLE IF EXISTS fp_cs_one.b11_purge_source PURGE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
