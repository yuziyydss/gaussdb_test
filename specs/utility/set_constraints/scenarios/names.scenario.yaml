schema_version: 1
kind: "scenario"
id: "scenario_set_constraints_names"
name: "SET CONSTRAINTS names"
status: "planned"
description: "从 pg_constraint核验两条 UNIQUE，后续补跨模式同名和其他约束类型。"
factor_ref: "set_constraints"
fixture_refs: []
fact_refs:
  - "set_constraints_fact_named"
  - "set_constraints_fact_name"
  - "set_constraints_fact_constraint_domain"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "从 pg_constraint核验两条 UNIQUE，后续补跨模式同名和其他约束类型。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
