schema_version: 1
kind: "scenario"
id: "scenario_set_constraints_outside"
name: "SET CONSTRAINTS outside"
status: "planned"
description: "事务外执行只验证无效果；不得按 LOCK 的事务外报错逻辑生成负向。"
factor_ref: "set_constraints"
fixture_refs: []
fact_refs:
  - "set_constraints_fact_outside"
  - "set_constraints_fact_deferred_example"
  - "set_constraints_fact_immediate_example"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "事务外执行只验证无效果；不得按 LOCK 的事务外报错逻辑生成负向。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
