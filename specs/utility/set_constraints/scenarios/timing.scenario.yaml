schema_version: 1
kind: "scenario"
id: "scenario_set_constraints_timing"
name: "SET CONSTRAINTS timing"
status: "planned"
description: "验证每语句/提交检查、切 IMMEDIATE 的追溯检查及失败后模式；错误后事务状态需用保存点与专用执行器处理。"
factor_ref: "set_constraints"
fixture_refs: []
fact_refs:
  - "set_constraints_fact_purpose"
  - "set_constraints_fact_immediate"
  - "set_constraints_fact_deferred"
  - "set_constraints_fact_initial"
  - "set_constraints_fact_not_deferrable"
  - "set_constraints_fact_all"
  - "set_constraints_fact_retroactive"
  - "set_constraints_fact_violation"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "验证每语句/提交检查、切 IMMEDIATE 的追溯检查及失败后模式；错误后事务状态需用保存点与专用执行器处理。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
