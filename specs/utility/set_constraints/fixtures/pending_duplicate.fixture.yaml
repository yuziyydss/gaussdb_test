schema_version: 1
kind: "fixture"
id: "fixture_set_constraints_pending_duplicate"
name: "SET CONSTRAINTS pending_duplicate"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "set_constraints"
requires_fixture_refs:
  - "fixture_set_constraints_constraints"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
    - "SET CONSTRAINTS uq_sc_two DEFERRED;"
    - "INSERT INTO t_constraints VALUES (1, 2), (3, 2);"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
