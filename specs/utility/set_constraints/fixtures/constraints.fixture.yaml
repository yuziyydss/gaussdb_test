schema_version: 1
kind: "fixture"
id: "fixture_set_constraints_constraints"
name: "SET CONSTRAINTS constraints"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "set_constraints"
requires_fixture_refs: []
provides:
  tables:
    -
      name: "t_constraints"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: true
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TABLE t_constraints (col_1 INTEGER, col_2 INTEGER, CONSTRAINT uq_sc_one UNIQUE (col_1) DEFERRABLE INITIALLY IMMEDIATE, CONSTRAINT uq_sc_two UNIQUE (col_2) DEFERRABLE INITIALLY DEFERRED);"
  teardown_sqls:
    - "DROP TABLE IF EXISTS t_constraints;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
