schema_version: 1
kind: "manifest"
id: "manifest_set_constraints_named"
name: "SET CONSTRAINTS named"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "set_constraints"
syntax_ref: "syntax_set_constraints"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  form:
    - "set_constraints_form_named"
  names:
    - "set_constraints_names_one"
    - "set_constraints_names_two"
  mode:
    - "set_constraints_mode_deferred"
    - "set_constraints_mode_immediate"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
