schema_version: 1
kind: "manifest"
id: "manifest_set_constraints_all"
name: "SET CONSTRAINTS all"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "set_constraints"
syntax_ref: "syntax_set_constraints"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_set_constraints_transaction"
identifier_policy: {}
bindings:
  form:
    - "set_constraints_form_all"
  mode:
    - "set_constraints_mode_deferred"
    - "set_constraints_mode_immediate"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
