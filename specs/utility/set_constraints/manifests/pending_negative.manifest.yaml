schema_version: 1
kind: "manifest"
id: "manifest_set_constraints_pending_negative"
name: "SET CONSTRAINTS pending_negative"
status: "needs_review"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "set_constraints"
syntax_ref: "syntax_set_constraints"
suite_type: "negative"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  form:
    - "set_constraints_form_named"
  names:
    - "set_constraints_names_pending"
  mode:
    - "set_constraints_mode_immediate"
expected:
  default: "error"
  scope: "syntax_and_semantics"
  oracle_status: "needs_verification"
  error_category: "deferred_unique_violation"
  fact_refs:
    - "set_constraints_fact_violation"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
violates_rule_refs:
  - "set_constraints_rule_immediate_valid"
