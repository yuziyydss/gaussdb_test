schema_version: 1
kind: scenario
id: scenario_m_table_pagination
name: scenario_m_table_pagination
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_table
fact_refs:
- m_table_fact_pagination
fixture_refs:
- fixture_m_table_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: TABLE m_b01_source ORDER BY id LIMIT 1,2;
oracles:
- kind: result_set
  expected:
  - - 2
    - 20
  - - 3
    - 30
execution_requirements:
- database_authorization
- target_oracle_calibration
