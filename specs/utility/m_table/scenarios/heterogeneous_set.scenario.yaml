schema_version: 1
kind: scenario
id: scenario_m_table_heterogeneous_set
name: scenario_m_table_heterogeneous_set
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_table
fact_refs:
- m_table_fact_set_contract
fixture_refs:
- fixture_m_table_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 待接入共享输出列合同后，扩展不同表、不同列数/类型的正负向集合用例
oracles:
- kind: target_error
  expected: 不兼容输出拒绝，目标错误尚未校准
execution_requirements:
- database_authorization
- target_oracle_calibration
