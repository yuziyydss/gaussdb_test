schema_version: 1
kind: scenario
id: scenario_m_load_data_local_protocol
name: scenario_m_load_data_local_protocol
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_load_data
fact_refs:
- m_load_data_fact_local
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 另审LOCAL远程传输开关、客户端路径及单语句协议，当前只生成服务端INFILE分支。
oracles:
- kind: manual_assertion
  expected: LOCAL、分区、用户变量、特殊字符与类型转换边界尚未建模
execution_requirements:
- database_authorization
- target_oracle_calibration
