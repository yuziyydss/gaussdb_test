schema_version: 1
kind: "scenario"
id: "scenario_impdp_pluggable_database_recover_documented_contracts"
name: "IMPDP PLUGGABLE DATABASE RECOVER documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "impdp_pluggable_database_recover"
fixture_refs: []
fact_refs:
  - "impdp_pluggable_database_recover_fact_purpose"
  - "impdp_pluggable_database_recover_fact_tool_only"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "导入PDB的修复阶段。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "由备份恢复工具调用，不推荐用户直接调用。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
