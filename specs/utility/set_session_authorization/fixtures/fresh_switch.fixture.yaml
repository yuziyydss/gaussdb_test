schema_version: 1
kind: fixture
id: fixture_set_session_authorization_fresh_switch
name: SET SESSION AUTHORIZATION fresh_switch
status: needs_review
description: 创建一个专用LOGIN角色；密码为静态一次性非秘密占位符，不用于真实环境。
factor_ref: set_session_authorization
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "CREATE ROLE set_session_auth_fresh LOGIN PASSWORD 'SetSessionAuth_2026_Aa9';"
  teardown_sqls:
    - "RESET SESSION AUTHORIZATION;"
    - "DROP ROLE IF EXISTS set_session_auth_fresh;"
  note: 仅用于静态语法代表；执行前必须由专用初始系统管理员会话运行，确认密码策略、目标角色身份和清理权限。PASSWORD字面量不是秘密，不用于生产或共享环境；不验证会话用户、当前用户、权限变化、密码策略或LDAP限制。
