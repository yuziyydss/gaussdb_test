schema_version: 1
kind: "scenario"
id: "scenario_autohint_drop_model_documented_contracts"
name: "AUTOHINT DROP MODEL documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "autohint_drop_model"
fixture_refs: []
fact_refs:
  - "autohint_drop_model_fact_body_5"
  - "autohint_drop_model_fact_body_8"
  - "autohint_drop_model_fact_body_11"
  - "autohint_drop_model_fact_body_17"
  - "autohint_drop_model_fact_body_19"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "删除指定SQL的历史Hint探索结果。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅支持SELECT语句，并且需要使用者拥有super权限或管理员权限。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "删除指定SQL的历史HINT探索结果。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "statement"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "指定删除探索记录的SQL语句（仅支持SELECT语句）。"
oracles:
  -
    kind: "documented_behavior"
    expected: "删除指定SQL的历史Hint探索结果。"
  -
    kind: "documented_behavior"
    expected: "删除指定SQL的历史HINT探索结果。"
  -
    kind: "documented_behavior"
    expected: "指定删除探索记录的SQL语句（仅支持SELECT语句）。"
execution_requirements:
  - "database"
  - "same_session"
