schema_version: 1
kind: "syntax"
id: "syntax_autohint_drop_model"
name: "AUTOHINT DROP MODEL 语法"
factor_ref: "autohint_drop_model"
category: "UTILITY"
status: "draft"
description: "需要同一专用SELECT及其已生成历史，不以全局PURGE兜底。"
production: "AUTOHINT DROP MODEL {query}"
rendering:
  whitespace: "collapse"
  statement_terminator: ";"
slots:
  query:
    type: "token"
    optional: true
source_fact_refs:
  - "autohint_drop_model_fact_grammar_line_13"
