schema_version: 1
kind: manifest
id: manifest_autohint_drop_model_fresh_syntax
name: AUTOHINT DROP MODEL fresh_syntax
factor_ref: autohint_drop_model
syntax_ref: syntax_autohint_drop_model
status: needs_review
description: 一个无表SELECT静态语法代表；不删除历史、不验证SQL身份或归属。
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  query: [autohint_drop_model_query_select_one]
environment_requirements:
  - key: actor_authority
    allowed_values: [super_or_admin]
    fact_refs: [autohint_drop_model_fact_body_8]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
