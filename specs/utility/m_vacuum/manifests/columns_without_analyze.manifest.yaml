schema_version: 1
kind: manifest
id: manifest_m_vacuum_columns_without_analyze
name: manifest_m_vacuum_columns_without_analyze
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_vacuum
syntax_ref: syntax_m_vacuum
suite_type: negative
strategy: pairwise
fixture_refs:
- fixture_m_vacuum_dead_rows
identifier_policy: {}
bindings:
  form:
  - m_vacuum_form_options
  options:
  - m_vacuum_options_verbose
  columns:
  - m_vacuum_columns_id
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_vacuum_fact_mode
- key: execution_context
  allowed_values:
  - top_level_autocommit
  fact_refs:
  - m_vacuum_fact_transaction
- key: table_authority
  allowed_values:
  - fixture_table_creator
  fact_refs:
  - m_vacuum_fact_owner
violates_rule_refs:
- m_vacuum_rule_columns_require_analyze
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: columns_require_analyze
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
