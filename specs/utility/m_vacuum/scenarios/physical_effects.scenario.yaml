schema_version: 1
kind: scenario
id: scenario_m_vacuum_physical_effects
name: scenario_m_vacuum_physical_effects
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_vacuum
fact_refs:
- m_vacuum_fact_full
- m_vacuum_fact_partial
fixture_refs:
- fixture_m_vacuum_dead_rows
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 分别检查维护阶段、副作用与空间恢复时机；禁止把错误自动当回滚或对共享表做维护。
oracles:
- kind: manual_assertion
  expected: 物理空间、锁与失败阶段尚未实机验证
execution_requirements:
- database_authorization
- target_oracle_calibration
