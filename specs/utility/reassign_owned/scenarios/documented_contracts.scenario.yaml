schema_version: 1
kind: "scenario"
id: "scenario_reassign_owned_documented_contracts"
name: "REASSIGN OWNED documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "reassign_owned"
fixture_refs: []
fact_refs:
  - "reassign_owned_fact_purpose"
  - "reassign_owned_fact_transfer"
  - "reassign_owned_fact_pre_drop"
  - "reassign_owned_fact_privilege"
  - "reassign_owned_fact_initial_owner"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "修改对象所有者。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "将旧角色的数据库对象所有权改为新角色。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "常用于删除角色前的准备。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "需要原角色和目标角色上的权限。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "只有初始用户可将属主改为初始化用户。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
