schema_version: 1
kind: "manifest"
id: "manifest_reassign_owned_dedicated_tables"
name: "REASSIGN OWNED dedicated_tables"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "reassign_owned"
syntax_ref: "syntax_reassign_owned"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_reassign_owned_owned_tables"
identifier_policy: {}
bindings:
  owners:
    - "reassign_owned_owners_one"
    - "reassign_owned_owners_two"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "dedicated_roles_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "create_role::create_role_fact_privilege"
  -
    key: "password_disable_admin"
    allowed_values:
      - "true"
    fact_refs:
      - "create_role::create_role_fact_disable_admin"
  -
    key: "both_role_permissions"
    allowed_values:
      - "true"
    fact_refs:
      - "reassign_owned_fact_privilege"
