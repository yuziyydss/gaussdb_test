schema_version: 1
kind: fixture
id: fixture_m_clean_connection_no_target_sessions
name: fixture_m_clean_connection_no_target_sessions
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_clean_connection
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - SELECT 1 / CASE WHEN current_database() = 'm_factor_test' THEN 1 ELSE 0 END AS clean_physical_database_verified;
  - CREATE USER m_clean_connection_user NOLOGIN NOSYSADMIN PASSWORD DISABLE;
  teardown_sqls:
  - DROP SCHEMA m_clean_connection_user;
  - DROP USER m_clean_connection_user RESTRICT;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
