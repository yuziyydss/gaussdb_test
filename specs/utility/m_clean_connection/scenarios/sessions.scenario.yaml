schema_version: 1
kind: scenario
id: scenario_m_clean_connection_sessions
name: scenario_m_clean_connection_sessions
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_clean_connection
fact_refs:
- m_clean_connection_fact_nodes
- m_clean_connection_fact_check
- m_clean_connection_fact_force
- m_create_user::m_create_user_fact_schema
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 当前仅无目标用户会话的语法候选，不声称成功清理连接；有目标会话、CHECK冲突和FORCE终止必须另行授权多会话运行器。
oracles:
- kind: manual_assertion
  expected: 节点限制与活动会话/终止行为未执行，不能据空会话结果宣称清理功能覆盖
execution_requirements:
- database_authorization
- target_oracle_calibration
