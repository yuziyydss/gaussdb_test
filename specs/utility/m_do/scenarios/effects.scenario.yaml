schema_version: 1
kind: scenario
id: scenario_m_do_effects
name: scenario_m_do_effects
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_do
fact_refs:
- m_do_fact_execution
fixture_refs:
- fixture_m_do_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 在内部工具场景审阅后验证匿名块修改 qty 或插入行；解析与执行同阶段，不能按普通 PREPARE 处理。
oracles:
- kind: manual_assertion
  expected: UPDATE id=1 后 qty=11；INSERT 增加(4,40)，均未执行
execution_requirements:
- database_authorization
- target_oracle_calibration
