schema_version: 1
kind: "manifest"
id: "manifest_set_current_schema"
name: "SET current_schema"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "set"
syntax_ref: "syntax_set"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_set_schema_transaction"
identifier_policy: {}
bindings:
  form:
    - "set_form_current_schema"
  scope:
    - "set_scope_implicit"
    - "set_scope_session"
    - "set_scope_local"
  operator:
    - "set_operator_to"
    - "set_operator_equal"
  schema_name:
    - "set_schema_name_one"
    - "set_schema_name_default"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "session_ownership"
    allowed_values:
      - "exclusive_test_connection"
    fact_refs:
      - "set_fact_session_scope"
