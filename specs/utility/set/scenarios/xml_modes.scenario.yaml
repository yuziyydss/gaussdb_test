schema_version: 1
kind: "scenario"
id: "scenario_set_xml_modes"
name: "SET xml_modes"
status: "planned"
description: "补 XML 输入正文和表达式后，对单根及多根分别验证 DOCUMENT/CONTENT 行为，不把 SET 成功视为 XML 解析验证。"
factor_ref: "set"
fixture_refs: []
fact_refs:
  - "set_fact_xml_modes"
  - "set_fact_xml_example"
  - "set_fact_xml_content"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "补 XML 输入正文和表达式后，对单根及多根分别验证 DOCUMENT/CONTENT 行为，不把 SET 成功视为 XML 解析验证。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
