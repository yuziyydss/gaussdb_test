schema_version: 1
kind: "scenario"
id: "scenario_set_b_expression"
name: "SET b_expression"
status: "planned"
description: "独占既有 B 环境验证 @@ 数值表达式；不建库、不设置 GLOBAL，也不将歧义条件套到全部 SET SESSION。"
factor_ref: "set"
fixture_refs: []
fact_refs:
  - "set_fact_b_context"
  - "set_fact_b_variables_gate"
  - "set_fact_double_at_space"
  - "set_fact_b_expr_example"
  - "set_fact_b_database_example"
  - "set_fact_global_comment"
  - "set_fact_b_db_cleanup"
  - "set_fact_session_scope_ambiguity"
  - "set_fact_global_gap"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "独占既有 B 环境验证 @@ 数值表达式；不建库、不设置 GLOBAL，也不将歧义条件套到全部 SET SESSION。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
