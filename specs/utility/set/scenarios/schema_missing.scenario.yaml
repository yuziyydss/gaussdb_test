schema_version: 1
kind: "scenario"
id: "scenario_set_schema_missing"
name: "SET schema_missing"
status: "planned"
description: "单独验证存在与不存在的模式；不存在应为空，不能改成负向 SQLSTATE 用例。"
factor_ref: "set"
fixture_refs: []
fact_refs:
  - "set_fact_schema_domain"
  - "set_fact_schema_string"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "单独验证存在与不存在的模式；不存在应为空，不能改成负向 SQLSTATE 用例。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
