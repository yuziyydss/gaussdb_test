schema_version: 1
kind: "manifest"
id: "manifest_cursor_ordinary"
name: "CURSOR ordinary"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "cursor"
syntax_ref: "syntax_cursor"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_cursor_transaction"
identifier_policy: {}
bindings:
  binary:
    - "cursor_binary_text"
    - "cursor_binary_binary"
  scroll:
    - "cursor_scroll_auto"
    - "cursor_scroll_no"
  hold:
    - "cursor_hold_default"
    - "cursor_hold_with"
    - "cursor_hold_without"
  query:
    - "cursor_query_values"
    - "cursor_query_select"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "cursor_transaction_ready"
    allowed_values:
      - "true"
    fact_refs:
      - "cursor_fact_transaction"
