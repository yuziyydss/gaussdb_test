schema_version: 1
kind: manifest
id: manifest_m_purge_owned_table
name: manifest_m_purge_owned_table
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_purge
syntax_ref: syntax_m_purge
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_purge_dropped_table
identifier_policy: {}
bindings: {}
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_purge_fact_mode
- key: actor_authority
  allowed_values:
  - fixture_table_and_schema_creator
  fact_refs:
  - m_purge_fact_authority
- key: enable_recyclebin
  allowed_values:
  - 'on'
  fact_refs:
  - m_purge_fact_recyclebin
- key: recyclebin_state
  allowed_values:
  - new_case_namespace_no_name_collision_retention_not_expired
  fact_refs:
  - m_purge_fact_recyclebin
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
