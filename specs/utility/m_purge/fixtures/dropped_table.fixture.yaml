schema_version: 1
kind: fixture
id: fixture_m_purge_dropped_table
name: fixture_m_purge_dropped_table
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_purge
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE SCHEMA m_purge_namespace;
  - CREATE TABLE m_purge_namespace.source (id INTEGER, qty INTEGER);
  - INSERT INTO m_purge_namespace.source VALUES (1,10),(2,20);
  - DROP TABLE m_purge_namespace.source;
  teardown_sqls:
  - DROP SCHEMA m_purge_namespace;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。 setup后表已入回收站，不声明为仍可查询的provides.tables。目标失败后需核验本case回收站残留，不允许扩大清理范围。
