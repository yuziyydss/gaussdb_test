schema_version: 1
kind: scenario
id: scenario_m_purge_effects
name: scenario_m_purge_effects
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_purge
fact_refs:
- m_purge_fact_released
- m_purge_fact_global
- m_purge_fact_index_example
fixture_refs:
- fixture_m_purge_dropped_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 前置DROP后查询gs_recyclebin确认精确Schema/原始名/对象身份，再检查目标PURGE仅删除该对象及空间效果；INDEX和全局RECYCLEBIN不进入当前manifest。
oracles:
- kind: manual_assertion
  expected: 回收站条目、空间释放、失败残留与索引示例冲突未实机验证
execution_requirements:
- database_authorization
- target_oracle_calibration
