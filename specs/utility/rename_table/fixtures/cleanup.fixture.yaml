schema_version: 1
kind: "fixture"
id: "fixture_rename_table_cleanup"
name: "RENAME TABLE cleanup"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "rename_table"
requires_fixture_refs:
  - "fixture_rename_table_one"
  - "fixture_rename_table_two"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "SELECT 1;"
  teardown_sqls:
    - "DROP TABLE IF EXISTS t_renamed_two CASCADE;"
    - "DROP TABLE IF EXISTS t_renamed_one CASCADE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
