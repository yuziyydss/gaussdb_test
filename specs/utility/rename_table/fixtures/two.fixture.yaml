schema_version: 1
kind: "fixture"
id: "fixture_rename_table_two"
name: "RENAME TABLE two"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "rename_table"
requires_fixture_refs: []
provides:
  tables:
    -
      name: "t_rename_two"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: true
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TABLE t_rename_two (col_1 INTEGER, col_2 INTEGER) WITH (STORAGE_TYPE=ASTORE, segment=off);"
    - "INSERT INTO t_rename_two VALUES (1, 2), (3, 4);"
  teardown_sqls:
    - "DROP TABLE IF EXISTS t_rename_two CASCADE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
