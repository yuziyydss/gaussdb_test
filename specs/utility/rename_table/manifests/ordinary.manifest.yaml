schema_version: 1
kind: "manifest"
id: "manifest_rename_table_ordinary"
name: "RENAME TABLE ordinary"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "rename_table"
syntax_ref: "syntax_rename_table"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_rename_table_cleanup"
identifier_policy: {}
bindings:
  keyword:
    - "rename_table_keyword_table"
    - "rename_table_keyword_tables"
  targets:
    - "rename_table_targets_one"
    - "rename_table_targets_two"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
