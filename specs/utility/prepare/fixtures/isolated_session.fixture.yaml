schema_version: 1
kind: "fixture"
id: "fixture_prepare_isolated_session"
name: "PREPARE isolated_session"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "prepare"
requires_fixture_refs: []
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "DEALLOCATE ALL;"
  teardown_sqls:
    - "DEALLOCATE ALL;"
  note: "只允许 runner 新建的独占测试连接。DEALLOCATE ALL 清理该连接全部预备语句；禁止复用业务或用户连接，本阶段不执行。"
