schema_version: 1
kind: "fixture"
id: "fixture_prepare_table"
name: "PREPARE table"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "prepare"
requires_fixture_refs: []
provides:
  tables:
    -
      name: "t_prepare_source"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "id"
          type: "INTEGER"
          nullable: false
        -
          name: "note"
          type: "VARCHAR(10)"
          nullable: true
        -
          name: "detail"
          type: "VARCHAR(10)"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TABLE t_prepare_source (id INTEGER NOT NULL, note VARCHAR(10), detail VARCHAR(10));"
    - "INSERT INTO t_prepare_source VALUES (1, 'seed', 'seed');"
  teardown_sqls:
    - "DROP TABLE t_prepare_source;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
