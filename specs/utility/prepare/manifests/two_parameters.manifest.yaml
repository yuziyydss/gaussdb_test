schema_version: 1
kind: "manifest"
id: "manifest_prepare_two_parameters"
name: "PREPARE two_parameters"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "prepare"
syntax_ref: "syntax_prepare"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_prepare_isolated_session"
identifier_policy: {}
bindings:
  statement_form:
    - "prepare_statement_form_select_two"
  signature:
    - "prepare_signature_inferred"
    - "prepare_signature_two"
  expression:
    - "prepare_expression_cast"
    - "prepare_expression_plus"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "session_ownership"
    allowed_values:
      - "exclusive_test_connection"
    fact_refs:
      - "prepare_fact_session_scope"
