schema_version: 1
kind: "manifest"
id: "manifest_prepare_duplicate_negative"
name: "PREPARE duplicate_negative"
status: "needs_review"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "prepare"
syntax_ref: "syntax_prepare"
suite_type: "negative"
strategy: "pairwise"
fixture_refs:
  - "fixture_prepare_statements"
identifier_policy: {}
bindings:
  name:
    - "prepare_name_duplicate"
  statement_form:
    - "prepare_statement_form_select"
  signature:
    - "prepare_signature_one"
  expression:
    - "prepare_expression_cast"
expected:
  default: "error"
  scope: "syntax_and_semantics"
  oracle_status: "needs_verification"
  error_category: "duplicate_prepared_statement"
  fact_refs: []
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
violates_rule_refs:
  - "prepare_rule_unique_name"
environment_requirements:
  -
    key: "session_ownership"
    allowed_values:
      - "exclusive_test_connection"
    fact_refs:
      - "prepare_fact_session_scope"
