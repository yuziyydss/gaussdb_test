schema_version: 1
kind: scenario
id: scenario_m_rename_table_preserve_rows
name: scenario_m_rename_table_preserve_rows
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_rename_table
fact_refs:
- m_rename_table_fact_preserve_rows
fixture_refs:
- fixture_m_rename_table_owned_tables
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: RENAME TABLE m_rename_table_a TO m_rename_table_x;
- sql: SELECT id FROM m_rename_table_x;
oracles:
- kind: result_set
  expected:
  - - 7
execution_requirements:
- database_authorization
- target_oracle_calibration
