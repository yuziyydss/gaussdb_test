schema_version: 1
kind: scenario
id: scenario_m_explain_plan_and_opteval
name: scenario_m_explain_plan_and_opteval
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_explain
fact_refs:
- m_explain_fact_plan
- m_explain_fact_opteval
fixture_refs:
- fixture_m_explain_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 独立 suite 验证 PLAN 排他和 OPTEVAL 白名单，不混入默认选项组合
oracles:
- kind: manual_assertion
  expected: OPTEVAL已有固定TRUE与三可共存选项的有限候选，实际计划块及目标错误未验证；PLAN的plan_table前置与生命周期仍缺失。
execution_requirements:
- database_authorization
- target_oracle_calibration
