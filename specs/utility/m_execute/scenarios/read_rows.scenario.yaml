schema_version: 1
kind: scenario
id: scenario_m_execute_read_rows
name: scenario_m_execute_read_rows
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_execute
fact_refs:
- m_execute_fact_same_session
- m_prepare::m_prepare_fact_session
fixture_refs:
- fixture_m_execute_prepared
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: EXECUTE m_execute_read;
oracles:
- kind: result_set
  expected:
  - - 1
    - 10
  - - 2
    - 20
execution_requirements:
- database_authorization
- target_oracle_calibration
- isolated_connection
