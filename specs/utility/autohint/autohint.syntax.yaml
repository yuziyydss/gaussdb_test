schema_version: 1
kind: "syntax"
id: "syntax_autohint"
name: "AUTOHINT 语法"
factor_ref: "autohint"
category: "UTILITY"
status: "ready"
description: "固定有限语法；仅用于syntax_only静态代表，不执行探索或查询。"
production: "AUTOHINT ({options}) {query}"
rendering:
  whitespace: "collapse"
  statement_terminator: ";"
slots:
  options:
    type: "token"
    optional: true
    dimension_ref: "autohint.options"
  query:
    type: "token"
    optional: true
    dimension_ref: "autohint.query"
source_fact_refs:
  - "autohint_fact_body_16_1"
