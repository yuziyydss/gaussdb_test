schema_version: 1
kind: scenario
id: scenario_m_use_missing_usage
name: scenario_m_use_missing_usage
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_use
fact_refs:
- m_use_fact_usage
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 隔离低权限角色，无目标模式 USAGE，执行 USE 后调用 database()
oracles:
- kind: target_error
  expected: USE 可能不报错，但 database() 提示无当前模式；待校准
execution_requirements:
- database_authorization
- target_oracle_calibration
