schema_version: 1
kind: "scenario"
id: "scenario_impdp_pluggable_database_create_documented_contracts"
name: "IMPDP PLUGGABLE DATABASE CREATE documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "impdp_pluggable_database_create"
fixture_refs: []
fact_refs:
  - "impdp_pluggable_database_create_fact_purpose"
  - "impdp_pluggable_database_create_fact_tool_only"
preconditions:
  - "先取得专用备份恢复工具的真实调用合同及独立授权；新Schema、修改application_name或虚构目录不能替代工具身份。"
  - "PDB导入执行阶段：原文禁止普通用户直接调用，可能异常重启；必须先有真实导出文件和异常重启恢复控制。"
  - "物理文件身份、来源库/表、完整性、阶段成功回执、部分失败恢复与目录归属仍需实现；本planned场景不授权读写/覆盖/删除任何备份文件。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "导入PDB的执行阶段。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "由备份恢复工具调用；直接用户调用可能异常重启，禁止直接调用。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
  - "authoritative_backup_tool_context"
  - "physical_backup_artifact_inventory"
  - "import_export_phase_identity"
  - "partial_failure_recovery_review"
