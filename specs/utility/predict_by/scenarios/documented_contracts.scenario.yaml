schema_version: 1
kind: "scenario"
id: "scenario_predict_by_documented_contracts"
name: "PREDICT BY documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "predict_by"
fixture_refs: []
fact_refs:
  - "predict_by_fact_body_5"
  - "predict_by_fact_body_8"
  - "predict_by_fact_body_17"
  - "predict_by_fact_body_17_1"
  - "predict_by_fact_body_20"
  - "predict_by_fact_body_20_1"
  - "create_model::create_model_fact_body_23_1"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "利用完成训练的模型进行推测任务。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "调用的模型名称在系统表gs_model_warehouse中可查看到。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "●    model_name 用于推测任务的模型名称。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "取值范围：字符串，需要符合标识符命名规范。"
  -
    id: "contract_4"
    action: "verify_documented_contract"
    note: "●    attribute 推测任务的输入特征列名。"
  -
    id: "contract_5"
    action: "verify_documented_contract"
    note: "取值范围：字符串，需要符合标识符命名规范。"
  -
    id: "upstream_contract_0"
    action: "verify_documented_contract"
    note: "使用已导出的跨章事实 create_model::create_model_fact_body_23_1；来源变化须使本包验证快照过期，尚未执行数据库验证。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
