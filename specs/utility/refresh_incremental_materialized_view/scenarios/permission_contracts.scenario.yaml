schema_version: 1
kind: "scenario"
id: "scenario_refresh_incremental_materialized_view_permission_contracts"
name: "REFRESH INCREMENTAL MATERIALIZED VIEW permission_contracts"
status: "planned"
description: "基表SELECT权限必须独立验证，不能以fixture失败通过负向。"
factor_ref: "refresh_incremental_materialized_view"
fixture_refs: []
fact_refs:
  - "refresh_incremental_materialized_view_fact_select_privilege"
  - "refresh_incremental_materialized_view_fact_setup_example"
  - "refresh_incremental_materialized_view_fact_refresh_example"
  - "refresh_incremental_materialized_view_fact_cleanup_example"
  - "refresh_incremental_materialized_view_fact_privilege_profile"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "基表SELECT权限必须独立验证，不能以fixture失败通过负向。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
