schema_version: 1
kind: "fixture"
id: "fixture_refresh_incremental_materialized_view_changed"
name: "REFRESH INCREMENTAL MATERIALIZED VIEW changed"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "refresh_incremental_materialized_view"
requires_fixture_refs:
  - "fixture_refresh_incremental_materialized_view_incremental"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "INSERT INTO t_imv_source VALUES (5, 6);"
  teardown_sqls:
    - "SELECT 1;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
