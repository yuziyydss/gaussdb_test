schema_version: 1
kind: "fixture"
id: "fixture_refresh_incremental_materialized_view_full"
name: "REFRESH INCREMENTAL MATERIALIZED VIEW full"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "refresh_incremental_materialized_view"
requires_fixture_refs:
  - "fixture_create_materialized_view_full"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "auto"
  note: "只依赖上游能力；不复制对象定义或生命周期 SQL。"
