schema_version: 1
kind: "fixture"
id: "fixture_lock_one"
name: "LOCK one"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "lock"
requires_fixture_refs: []
provides:
  tables:
    -
      name: "t_lock_one"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: true
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
seed:
  required: true
  rows:
    -
      col_1: 1
      col_2: 2
    -
      col_1: 3
      col_2: 4
execution:
  status: "ready"
  mode: "auto"
  note: "只用于独占测试命名空间，普通两列整数源。"
