schema_version: 1
kind: manifest
id: manifest_m_checkpoint_isolated_instance
name: manifest_m_checkpoint_isolated_instance
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_checkpoint
syntax_ref: syntax_m_checkpoint
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_checkpoint_shared_table
identifier_policy: {}
bindings:
  command:
  - m_checkpoint_command_plain
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_checkpoint_fact_mode
- key: actor_authority
  allowed_values:
  - sysadmin_or_operations_admin
  fact_refs:
  - m_checkpoint_fact_authority
- key: instance_effect_scope
  allowed_values:
  - disposable_instance_checkpoint_explicitly_reviewed
  fact_refs:
  - m_checkpoint_fact_scope
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
