schema_version: 1
kind: scenario
id: scenario_m_checkpoint_wal
name: scenario_m_checkpoint_wal
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_checkpoint
fact_refs:
- m_checkpoint_fact_effect
- m_checkpoint_fact_scope
fixture_refs:
- fixture_m_checkpoint_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 在另行授权的独占可丢弃实例上确认WAL/脏页与检查点；DDL teardown不能撤销已发生的实例级刷新。
oracles:
- kind: manual_assertion
  expected: 仅有限SQL生成，未执行CHECKPOINT或改检查点配置
execution_requirements:
- database_authorization
- target_oracle_calibration
