schema_version: 1
kind: "fixture"
id: "fixture_analyze_analyse_index"
name: "ANALYZE | ANALYSE index"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "analyze_analyse"
requires_fixture_refs:
  - "fixture_analyze_analyse_source"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE INDEX i_analyze_source ON t_analyze_source USING btree (col_1);"
  teardown_sqls:
    - "DROP INDEX IF EXISTS i_analyze_source;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
