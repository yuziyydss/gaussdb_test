schema_version: 1
kind: "scenario"
id: "scenario_expdp_table_documented_contracts"
name: "EXPDP TABLE documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "expdp_table"
fixture_refs: []
fact_refs:
  - "expdp_table_fact_purpose"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "导出表相关索引、sequence、分区、toast及toast index全部文件。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
