schema_version: 1
kind: manifest
id: manifest_m_set_role_reset_only
name: manifest_m_set_role_reset_only
status: needs_review
description: 仅 DEFAULT 重置产生式的有限静态模型；不覆盖需要密码的指定身份切换。
factor_ref: m_set_role
syntax_ref: syntax_m_set_role
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_set_role_transaction
identifier_policy: {}
bindings:
  reset_form:
  - m_set_role_reset_form_default
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_set_role_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_new_connection
  fact_refs:
  - m_set_role_fact_mode
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
