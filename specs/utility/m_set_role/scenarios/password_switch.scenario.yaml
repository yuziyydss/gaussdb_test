schema_version: 1
kind: scenario
id: scenario_m_set_role_password_switch
name: scenario_m_set_role_password_switch
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set_role
fact_refs:
- m_set_role_fact_authority
- m_set_role_fact_password
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 待安全运行时凭证绑定与日志脱敏合同接入，再设置目标身份并校验成员权限、SESSION/LOCAL 与恢复。不得在规格或快照内保存实际密码。
oracles:
- kind: manual_assertion
  expected: 指定身份切换仍未生成/执行；不借重置用例宣称整个命令覆盖完成
execution_requirements:
- database_authorization
- target_oracle_calibration
