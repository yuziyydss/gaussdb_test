schema_version: 1
kind: scenario
id: scenario_m_set_role_reset_identity
name: scenario_m_set_role_reset_identity
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set_role
fact_refs:
- m_set_role_fact_reset
- m_set_role_fact_scope
fixture_refs:
- fixture_m_set_role_transaction
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 在已授权隔离连接记录 current_user/session_user，验证 DEFAULT 重置及事务结束的恢复边界。
oracles:
- kind: manual_assertion
  expected: 按本命令的当前/会话/初始身份区别判定；尚未执行
execution_requirements:
- database_authorization
- target_oracle_calibration
