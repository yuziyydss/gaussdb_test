schema_version: 1
kind: fixture
id: fixture_m_set_role_transaction
name: fixture_m_set_role_transaction
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set_role
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - START TRANSACTION;
  teardown_sqls:
  - ROLLBACK;
  note: 在独占新连接的同一事务生成目标与 ROLLBACK；只重置默认身份，不切换至其他角色。结束关闭连接，不能复用业务会话。
