schema_version: 1
kind: scenario
id: scenario_load_data_two_int_rows
name: LOAD DATA受控三行文件导入结果
status: planned
description: 仅一个文件/新表的具体Oracle定义，未部署、未执行，不能计行为通过。
factor_ref: load_data
fixture_refs: [fixture_load_data_two_int]
fact_refs: [load_data_fact_body_5, load_data_fact_body_13, load_data_fact_body_80, load_data_fact_body_113_1, load_data_fact_runtime_contract]
preconditions:
  - 独立授权的B模式非PDB连接，b_format_version=5.7与b_format_dev_version=s2；SYSADMIN且有表INSERT/DELETE权限。
  - 独占新表；服务器专属文件由人工部署并核验所有权、字节哈希、safe_data_path及读取权限；当前尚未满足。
steps:
  - sql: "LOAD DATA INFILE '/tmp/factor_assets/general/load_data/two_int.tsv' INTO TABLE g_load_data_two_int FIELDS TERMINATED BY '\t' LINES TERMINATED BY '\n' (id,qty);"
oracles:
  - {kind: result_set, sql: 'SELECT id,qty FROM g_load_data_two_int ORDER BY id;', expected: [[1,10], [2,20], [3,30]]}
execution_requirements: [database_authorization, isolated_connection, explicit_file_deployment, target_only_oracle, owned_asset_cleanup]
