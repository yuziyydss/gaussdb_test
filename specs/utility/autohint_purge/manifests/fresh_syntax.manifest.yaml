schema_version: 1
kind: manifest
id: manifest_autohint_purge_fresh_syntax
name: AUTOHINT PURGE fresh_syntax
factor_ref: autohint_purge
syntax_ref: syntax_autohint_purge
status: needs_review
description: 一个固定命令静态语法代表；不清除历史推荐、不验证权限或结果计数。
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  command:
    - "autohint_purge_command_fixed"
environment_requirements:
  - key: actor_authority
    allowed_values: [super_or_admin]
    fact_refs: [autohint_purge_fact_body_9]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
