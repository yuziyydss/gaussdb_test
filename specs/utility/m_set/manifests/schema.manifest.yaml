schema_version: 1
kind: manifest
id: manifest_m_set_schema
name: manifest_m_set_schema
status: needs_review
description: M当前模式三种完整拼写与三scope，仅已存在专属模式；不包含DEFAULT或缺失模式预期。
factor_ref: m_set
syntax_ref: syntax_m_set
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_set_schema
identifier_policy: {}
bindings:
  form:
  - m_set_form_schema_to
  - m_set_form_schema_equals
  - m_set_form_schema_string
  scope:
  - m_set_scope_default
  - m_set_scope_session
  - m_set_scope_local
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_set_fact_mode
- key: namespace_create_authority
  allowed_values:
  - database_create
  fact_refs:
  - m_create_schema::m_create_schema_fact_authority
- key: namespace_identity
  allowed_values:
  - fresh_non_system_non_user_named_schema
  fact_refs:
  - m_create_schema::m_create_schema_fact_namespace
  - m_create_schema::m_create_schema_fact_same_name_owner
- key: namespace_drop_authority
  allowed_values:
  - actual_case_schema_owner
  fact_refs:
  - m_drop_schema::m_drop_schema_fact_authority
- key: transaction_owner
  allowed_values:
  - same_connection_transaction_creator
  fact_refs:
  - m_commit::m_commit_fact_authority
- key: schema_lifecycle
  allowed_values:
  - fresh_owned_non_current_namespace_restore_before_drop
  fact_refs:
  - m_set_fact_session
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_set_fact_session
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
