schema_version: 1
kind: manifest
id: manifest_m_set_timezone
name: manifest_m_set_timezone
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set
syntax_ref: syntax_m_set
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_set_transaction
identifier_policy: {}
bindings:
  scope:
  - m_set_scope_default
  - m_set_scope_session
  - m_set_scope_local
  timezone:
  - m_set_timezone_prc
  - m_set_timezone_local
  - m_set_timezone_default
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_set_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_set_fact_session
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
