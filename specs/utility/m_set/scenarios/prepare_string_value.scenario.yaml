schema_version: 1
kind: scenario
id: scenario_m_set_prepare_string_value
name: scenario_m_set_prepare_string_value
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set
fact_refs:
- m_set_fact_user_variable_types
fixture_refs:
- fixture_m_set_prepare_string
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: SELECT @m_prepare_sql;
oracles:
- kind: result_set
  sql: SELECT @m_prepare_sql;
  expected:
  - - SELECT 1
execution_requirements:
- database_authorization
- target_oracle_calibration
- isolated_connection
- close_case_connection
