schema_version: 1
kind: scenario
id: scenario_m_set_local_rollback
name: scenario_m_set_local_rollback
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set
fact_refs:
- m_set_fact_local
- m_set_fact_session
fixture_refs:
- fixture_m_set_transaction
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 先记录会话 TimeZone，再 SET LOCAL TIME ZONE PRC，ROLLBACK 后与原值比较；不将原值猜成固定 PRC。
oracles:
- kind: manual_assertion
  expected: 事务结束恢复事前会话 TimeZone；状态采集与 Oracle 待执行适配
execution_requirements:
- database_authorization
- target_oracle_calibration
