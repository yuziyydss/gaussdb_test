schema_version: 1
kind: scenario
id: scenario_m_set_separate_capabilities
name: scenario_m_set_separate_capabilities
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set
fact_refs:
- m_set_fact_global
- m_set_fact_charset
- m_set_fact_schema_absent
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 独立建模 @@global 拒绝、SET NAMES 字符集匹配、缺失 CURRENT_SCHEMA 的空值；不把任意错误算通过。
oracles:
- kind: manual_assertion
  expected: 各分支需要自己的环境与预期，不由时区有限模型推导覆盖
execution_requirements:
- database_authorization
- target_oracle_calibration
