schema_version: 1
kind: scenario
id: scenario_m_set_user_variable_integer_values
name: scenario_m_set_user_variable_integer_values
status: planned
description: 字符串初始值到正整数、再到负整数的有限逐步读回；每步Oracle必须在下一步赋值前读取。只比较数值，不由JSON数值推断服务端int8身份或驱动类型，实际返回表示待校准；不宣称全数值转换域。
factor_ref: m_set
fact_refs:
- m_set_fact_user_variable_integer
- m_set_fact_user_variable_profile_gap
fixture_refs:
- fixture_m_set_user_variable
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- id: positive
  sql: SET @m_set_value = 7;
- id: negative
  sql: SET @m_set_value := -7;
oracles:
- kind: result_set
  step_id: positive
  sql: SELECT @m_set_value;
  expected:
  - - 7
- kind: result_set
  step_id: negative
  sql: SELECT @m_set_value;
  expected:
  - - -7
execution_requirements:
- database_authorization
- target_oracle_calibration
- isolated_connection
- close_case_connection
- per_step_oracle
