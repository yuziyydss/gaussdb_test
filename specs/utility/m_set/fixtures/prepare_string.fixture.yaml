schema_version: 1
kind: fixture
id: fixture_m_set_prepare_string
name: fixture_m_set_prepare_string
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - SET @m_prepare_sql := 'SELECT 1';
  teardown_sqls:
  - SET @m_prepare_sql := NULL;
  note: 仅用于新鲜M独占连接；创建本case字符串变量，清理为NULL后关闭连接。不改全局参数，不对他人会话或已有变量执行恢复猜测。
