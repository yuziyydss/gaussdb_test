schema_version: 1
kind: fixture
id: fixture_m_set_user_variable_subquery
name: fixture_m_set_user_variable_subquery
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - SET @m_set_subquery_value := 'initial subquery value';
  teardown_sqls:
  - SET @m_set_subquery_value := NULL;
  note: 仅在已授权M物理数据库的独占新连接内初始化本case变量，不创建或伪报表。目标只取单列无FROM的常量SELECT；实际运行前需确认M模式与专属连接。清理本变量为NULL后关闭连接，不用ROLLBACK假装恢复用户变量，不更改全局参数。
