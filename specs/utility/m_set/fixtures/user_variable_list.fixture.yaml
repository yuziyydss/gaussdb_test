schema_version: 1
kind: fixture
id: fixture_m_set_user_variable_list
name: fixture_m_set_user_variable_list
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - SET @m_set_first := 'initial first';
  - SET @m_set_second := 'initial second';
  teardown_sqls:
  - SET @m_set_first := NULL;
  - SET @m_set_second := NULL;
  note: 仅用于独占新M连接中的两个case专属变量，逐个初始化并清为NULL后关闭连接。不能复用用户原有变量，不假定ROLLBACK能恢复变量，不改变全局参数。两个表达式独立且无互相读取，不推断连续赋值、列表求值顺序或类型转换。
