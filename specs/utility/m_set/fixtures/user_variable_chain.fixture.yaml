schema_version: 1
kind: fixture
id: fixture_m_set_user_variable_chain
name: fixture_m_set_user_variable_chain
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - SET @m_set_chain_left := 'initial left';
  - SET @m_set_chain_right := 'initial right';
  teardown_sqls:
  - SET @m_set_chain_left := NULL;
  - SET @m_set_chain_right := NULL;
  note: 只用于独占新M连接中的本case两个变量，初始化为不同值，目标后逐个清NULL并关闭连接。不使用ROLLBACK假定恢复变量，不改全局参数，不复用用户原有变量。连续赋值与逗号列表分开，只有首位运算符可选=，第二位固定:=；更长链和类型转换未覆盖。
