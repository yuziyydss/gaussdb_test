schema_version: 1
kind: fixture
id: fixture_m_set_schema
name: fixture_m_set_schema
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE SCHEMA m_set_owned_namespace;
  - COMMIT;
  - START TRANSACTION;
  teardown_sqls:
  - ROLLBACK;
  - DROP SCHEMA m_set_owned_namespace;
  - COMMIT;
  note: 只用于已授权M物理数据库中的独占新连接，确认事前无事务且没有其他用户工作。专属模式须事前不存在、不在当前搜索路径、非系统模式、非同名用户模式；记录实际创建成功和对象归属。CREATE SCHEMA后显式COMMIT完成DDL阶段，再START
    TRANSACTION包围目标SET；不依赖DDL回滚。目标后ROLLBACK恢复事前会话模式，核验模式不再是当前模式且为空、归属仍为本case才DROP，再COMMIT完成清理。任一setup失败不得运行目标或按名称清理他人资产；恢复/身份不能确认时停止清理并报告，关闭case连接。目录身份/事务阶段执行适配仍待校准，此静态列表不是无条件安全执行许可。
