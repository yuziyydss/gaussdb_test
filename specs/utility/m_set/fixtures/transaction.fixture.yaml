schema_version: 1
kind: fixture
id: fixture_m_set_transaction
name: fixture_m_set_transaction
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - START TRANSACTION;
  teardown_sqls:
  - ROLLBACK;
  note: 真实事务前置；SET/SET LOCAL 在回滚后撤销。连接须独占并在 case 后关闭，不能复用未知状态。无占位表。
