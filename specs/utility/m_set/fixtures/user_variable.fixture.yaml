schema_version: 1
kind: fixture
id: fixture_m_set_user_variable
name: fixture_m_set_user_variable
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_set
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - SET @m_set_value := 'initial value';
  teardown_sqls:
  - SET @m_set_value := NULL;
  note: 只用于独占新M连接且本case拥有@m_set_value；不借用时区事务、不假定ROLLBACK能恢复变量。目标后清为NULL并关闭case连接；变量赋值不清理表、不改变全局参数。
