schema_version: 1
kind: "manifest"
id: "manifest_execute_parameters_negative"
name: "EXECUTE parameters_negative"
status: "needs_review"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "execute"
syntax_ref: "syntax_execute"
suite_type: "negative"
strategy: "pairwise"
fixture_refs:
  - "fixture_execute_prepared_session"
identifier_policy: {}
bindings:
  name:
    - "execute_name_one"
    - "execute_name_three"
  parameters:
    - "execute_parameters_absent"
    - "execute_parameters_integer"
    - "execute_parameters_expression"
    - "execute_parameters_null"
    - "execute_parameters_three"
    - "execute_parameters_text"
    - "execute_parameters_two"
expected:
  default: "error"
  scope: "syntax_and_semantics"
  oracle_status: "needs_verification"
  error_category: "incompatible_prepared_parameters"
  fact_refs:
    - "execute_fact_parameter_error"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
violates_rule_refs:
  - "execute_rule_compatible"
environment_requirements:
  -
    key: "session_ownership"
    allowed_values:
      - "exclusive_test_connection"
    fact_refs:
      - "execute_fact_current_session"
