schema_version: 1
kind: "scenario"
id: "scenario_execute_schema_change"
name: "EXECUTE schema_change"
status: "planned"
description: "补充依赖 DDL 前后的计划失效观测和类型域校准；不将可能出现的异常结果写成固定 SQLSTATE。"
factor_ref: "execute"
fixture_refs: []
fact_refs:
  - "execute_fact_schema_change"
  - "execute_fact_type_domain_gap"
  - "execute_fact_rownum_oracle"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "补充依赖 DDL 前后的计划失效观测和类型域校准；不将可能出现的异常结果写成固定 SQLSTATE。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
