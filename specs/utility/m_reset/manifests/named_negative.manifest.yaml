schema_version: 1
kind: manifest
id: manifest_m_reset_named_negative
name: manifest_m_reset_named_negative
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_reset
syntax_ref: syntax_m_reset
suite_type: negative
strategy: pairwise
fixture_refs:
- fixture_m_reset_transaction
identifier_policy: {}
bindings:
  target:
  - m_reset_target_named
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_reset_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_reset_fact_rollback
  - m_set::m_set_fact_session
violates_rule_refs:
- m_reset_rule_only_all
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: only_all
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
