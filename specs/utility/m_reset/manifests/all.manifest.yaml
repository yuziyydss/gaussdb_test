schema_version: 1
kind: manifest
id: manifest_m_reset_all
name: manifest_m_reset_all
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_reset
syntax_ref: syntax_m_reset
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_reset_transaction
identifier_policy: {}
bindings:
  target:
  - m_reset_target_all
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_reset_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_reset_fact_rollback
  - m_set::m_set_fact_session
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
