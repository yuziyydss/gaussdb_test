schema_version: 1
kind: fixture
id: fixture_m_reset_transaction
name: fixture_m_reset_transaction
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_reset
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - START TRANSACTION;
  - SET LOCAL TIME ZONE 'PRC';
  teardown_sqls:
  - ROLLBACK;
  note: RESET ALL 可能重置 search_path 等会话状态；唯一清理 SQL 是 ROLLBACK，不访问未限定对象，结束后关闭独占连接。
