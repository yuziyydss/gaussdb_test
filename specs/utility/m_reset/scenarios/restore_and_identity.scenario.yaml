schema_version: 1
kind: scenario
id: scenario_m_reset_restore_and_identity
name: scenario_m_reset_restore_and_identity
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_reset
fact_refs:
- m_reset_fact_defaults
- m_reset_fact_identity
- m_reset_fact_rollback
fixture_refs:
- fixture_m_reset_transaction
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 记录配置缺省与调用前 role/session authorization/TimeZone/search_path；执行 RESET ALL；再回滚比较原会话状态。
oracles:
- kind: manual_assertion
  expected: 重置按配置缺省；身份不被重置；回滚撤销变更。预期采集与执行尚未实现
execution_requirements:
- database_authorization
- target_oracle_calibration
