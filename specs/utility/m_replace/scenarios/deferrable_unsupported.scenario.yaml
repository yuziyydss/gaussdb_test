schema_version: 1
kind: scenario
id: scenario_m_replace_deferrable_unsupported
name: scenario_m_replace_deferrable_unsupported
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_replace
fact_refs:
- m_replace_fact_deferrable
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 先证明专用DEFERRABLE唯一约束前置可建立，再独立验证REPLACE目标拒绝；当前前置和错误身份待核实。
oracles:
- kind: manual_assertion
  expected: 不得把建表失败当作REPLACE目标错误，不填猜测SQLSTATE。
execution_requirements:
- database_authorization
- target_oracle_calibration
