schema_version: 1
kind: scenario
id: scenario_m_replace_default_value
name: scenario_m_replace_default_value
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_replace
fact_refs:
- m_replace_fact_default
fixture_refs:
- fixture_m_replace_conflict
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: REPLACE INTO m_replace_target VALUES (1,DEFAULT);
- sql: SELECT qty FROM m_replace_target WHERE id=1;
oracles:
- kind: result_set
  expected:
  - - 9
execution_requirements:
- database_authorization
- target_oracle_calibration
