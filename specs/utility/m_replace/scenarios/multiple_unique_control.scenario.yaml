schema_version: 1
kind: scenario
id: scenario_m_replace_multiple_unique_control
name: scenario_m_replace_multiple_unique_control
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_replace
fact_refs:
- m_replace_fact_multi_conflict
- m_replace_fact_conflict
- m_replace_fact_count
fixture_refs:
- fixture_m_replace_multiple_unique
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- id: replace_row
  sql: REPLACE INTO m_replace_multi (id,qty) VALUES (3,30);
- id: read_rows
  sql: SELECT id,qty FROM m_replace_multi ORDER BY id;
oracles:
- kind: result_set
  step_id: read_rows
  sql: SELECT id,qty FROM m_replace_multi ORDER BY id;
  expected:
  - - 1
    - 10
  - - 2
    - 20
  - - 3
    - 30
- kind: manual_assertion
  step_id: replace_row
  expected: 目标命令标签 REPLACE 0 1；待实机校准，不能只数新插入行。
execution_requirements:
- database_authorization
- target_oracle_calibration
