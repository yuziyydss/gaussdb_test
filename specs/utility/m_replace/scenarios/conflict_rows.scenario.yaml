schema_version: 1
kind: scenario
id: scenario_m_replace_conflict_rows
name: scenario_m_replace_conflict_rows
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_replace
fact_refs:
- m_replace_fact_conflict
- m_replace_fact_count
fixture_refs:
- fixture_m_replace_conflict
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: REPLACE INTO m_replace_target(id,qty) VALUES (1,99);
- sql: SELECT id,qty FROM m_replace_target ORDER BY id;
oracles:
- kind: result_set
  expected:
  - - 1
    - 99
  - - 2
    - 20
- kind: manual_assertion
  expected: 目标命令标签 REPLACE 0 2，不是仅 INSERT 1 行
execution_requirements:
- database_authorization
- target_oracle_calibration
