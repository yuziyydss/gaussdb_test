schema_version: 1
kind: scenario
id: scenario_m_replace_set_default_lineage
name: scenario_m_replace_set_default_lineage
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_replace
fact_refs:
- m_replace_fact_left_to_right
fixture_refs:
- fixture_m_replace_conflict
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: REPLACE INTO m_replace_target SET id=id+1,qty=id;
- sql: SELECT id,qty FROM m_replace_target WHERE id=3;
oracles:
- kind: result_set
  expected:
  - - 3
    - 3
execution_requirements:
- database_authorization
- target_oracle_calibration
