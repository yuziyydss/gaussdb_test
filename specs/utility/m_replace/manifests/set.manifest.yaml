schema_version: 1
kind: manifest
id: manifest_m_replace_set
name: manifest_m_replace_set
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_replace
syntax_ref: syntax_m_replace
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_replace_conflict
identifier_policy: {}
bindings:
  into:
  - m_replace_into_none
  - m_replace_into_yes
  target_profile:
  - m_replace_target_profile_implicit
  source_profile:
  - m_replace_source_profile_set_literal
  - m_replace_source_profile_set_default
  - m_replace_source_profile_set_chain
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_replace_fact_mode
- key: object_authority
  allowed_values:
  - case_object_owner
  fact_refs:
  - m_replace_fact_permissions
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
