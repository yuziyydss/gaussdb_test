schema_version: 1
kind: fixture
id: fixture_m_replace_conflict
name: fixture_m_replace_conflict
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_replace
requires_fixture_refs: []
provides:
  tables:
  - name: m_replace_target
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: false
    - name: qty
      type: INTEGER
      nullable: true
  - name: m_replace_source
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
    - name: qty
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE m_replace_target(id INT PRIMARY KEY DEFAULT 2,qty INT DEFAULT 9);
  - INSERT INTO m_replace_target(id,qty) VALUES (1,10),(2,20);
  - CREATE TABLE m_replace_source(id INT,qty INT);
  - INSERT INTO m_replace_source(id,qty) VALUES (1,99),(4,40);
  teardown_sqls:
  - DROP TABLE m_replace_source;
  - DROP TABLE m_replace_target;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
