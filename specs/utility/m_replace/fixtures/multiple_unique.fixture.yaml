schema_version: 1
kind: fixture
id: fixture_m_replace_multiple_unique
name: fixture_m_replace_multiple_unique
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_replace
requires_fixture_refs: []
provides:
  tables:
  - name: m_replace_multi
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: false
    - name: qty
      type: INTEGER
      nullable: true
seed:
  required: true
  rows:
  - id: 1
    qty: 10
  - id: 2
    qty: 20
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE m_replace_multi (id INT PRIMARY KEY, qty INT UNIQUE);
  - INSERT INTO m_replace_multi (id,qty) VALUES (1,10),(2,20);
  teardown_sqls:
  - DROP TABLE m_replace_multi;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
