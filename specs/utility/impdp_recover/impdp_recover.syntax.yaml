schema_version: 1
kind: "syntax"
id: "syntax_impdp_recover"
name: "IMPDP RECOVER 语法"
factor_ref: "impdp_recover"
category: "UTILITY"
status: "ready"
description: "语法结构来自 PDF，选值仅由 factor/manifest 提供。"
production: "IMPDP DATABASE RECOVER SOURCE = {directory} OWNER = {owner} {local}"
rendering:
  whitespace: "collapse"
  statement_terminator: ";"
slots:
  directory:
    type: "token"
    optional: true
    dimension_ref: "impdp_recover.directory"
  owner:
    type: "token"
    optional: true
    dimension_ref: "impdp_recover.owner"
  local:
    type: "token"
    optional: true
    dimension_ref: "impdp_recover.local"
source_fact_refs:
  - "impdp_recover_fact_syntax"
  - "impdp_recover_fact_directory"
  - "impdp_recover_fact_owner"
  - "impdp_recover_fact_local"
