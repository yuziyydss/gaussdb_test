schema_version: 1
kind: "scenario"
id: "scenario_shutdown_documented_contracts"
name: "SHUTDOWN documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "shutdown"
fixture_refs: []
fact_refs:
  - "shutdown_fact_purpose"
  - "shutdown_fact_privilege"
  - "shutdown_fact_fast"
  - "shutdown_fact_immediate"
preconditions:
  - "只能在另行授权的可销毁独占节点评审，不能用独占数据库或Schema推定对其他连接无影响。"
  - "必须有带外重启控制和节点健康恢复Oracle；FAST回滚事务并断开客户端，IMMEDIATE下次启动会故障恢复，连接断开本身不证明目标正确。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "关闭当前连接的数据库节点。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅管理员可执行。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "FAST断开所有客户端连接，回滚活跃事务，然后关闭节点。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "IMMEDIATE立即关闭，后续重启需要故障恢复。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
  - "exclusive_disposable_node"
  - "out_of_band_restart_control"
  - "client_transaction_impact_review"
