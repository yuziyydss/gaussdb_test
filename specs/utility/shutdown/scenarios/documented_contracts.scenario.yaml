schema_version: 1
kind: "scenario"
id: "scenario_shutdown_documented_contracts"
name: "SHUTDOWN documented_contracts"
status: "planned"
description: "逐条保留本章环境、约束与对象行为；尚未实现执行Oracle，不能计入行为覆盖。"
factor_ref: "shutdown"
fixture_refs: []
fact_refs:
  - "shutdown_fact_purpose"
  - "shutdown_fact_privilege"
  - "shutdown_fact_fast"
  - "shutdown_fact_immediate"
preconditions:
  - "独占测试数据库；先逐条实现前置条件及可机器执行的Oracle。"
steps:
  -
    id: "contract_0"
    action: "verify_documented_contract"
    note: "关闭当前连接的数据库节点。"
  -
    id: "contract_1"
    action: "verify_documented_contract"
    note: "仅管理员可执行。"
  -
    id: "contract_2"
    action: "verify_documented_contract"
    note: "FAST断开所有客户端连接，回滚活跃事务，然后关闭节点。"
  -
    id: "contract_3"
    action: "verify_documented_contract"
    note: "IMMEDIATE立即关闭，后续重启需要故障恢复。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
