schema_version: 1
kind: "manifest"
id: "manifest_show_like"
name: "SHOW like"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "show"
syntax_ref: "syntax_show"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  form:
    - "show_form_like"
  pattern:
    - "show_pattern_var"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
