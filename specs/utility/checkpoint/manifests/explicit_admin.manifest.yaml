schema_version: 1
kind: "manifest"
id: "manifest_checkpoint_explicit_admin"
name: "CHECKPOINT explicit_admin"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "checkpoint"
syntax_ref: "syntax_checkpoint"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings: {}
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "checkpoint_admin_authorized"
    allowed_values:
      - "true"
    fact_refs:
      - "checkpoint_fact_permission"
  -
    key: "container_kind"
    allowed_values:
      - "non_pdb"
    fact_refs:
      - "checkpoint_fact_no_pdb"
