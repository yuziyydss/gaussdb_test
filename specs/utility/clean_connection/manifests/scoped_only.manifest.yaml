schema_version: 1
kind: "manifest"
id: "manifest_clean_connection_scoped_only"
name: "CLEAN CONNECTION scoped_only"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "clean_connection"
syntax_ref: "syntax_clean_connection"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  target:
    - "clean_connection_target_all"
  check:
    - "clean_connection_check_none"
    - "clean_connection_check_check"
  force:
    - "clean_connection_force_none"
    - "clean_connection_force_force"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "dedicated_clean_connection_database"
    allowed_values:
      - "fp_cc_isolated"
    fact_refs:
      - "clean_connection_fact_database_exists"
  -
    key: "dedicated_clean_connection_user"
    allowed_values:
      - "fp_cc_test_user"
    fact_refs:
      - "clean_connection_fact_user_exists"
