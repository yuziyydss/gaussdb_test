schema_version: 1
kind: "scenario"
id: "scenario_clean_connection_connection_lifecycle"
name: "CLEAN CONNECTION connection_lifecycle"
status: "planned"
description: "外部预配置专用数据库与用户；分别验证空闲/活动连接、CHECK 拒绝与 FORCE 信号，不创建环境、不实际清理连接。"
factor_ref: "clean_connection"
fixture_refs: []
fact_refs:
  - "clean_connection_fact_purpose"
  - "clean_connection_fact_all_only"
  - "clean_connection_fact_active"
  - "clean_connection_fact_check"
  - "clean_connection_fact_force"
  - "clean_connection_fact_node"
  - "clean_connection_fact_db_scope"
  - "clean_connection_fact_database_exists"
  - "clean_connection_fact_user_scope"
  - "clean_connection_fact_user_exists"
  - "clean_connection_fact_example"
  - "clean_connection_fact_scope_profile"
  - "clean_connection_fact_user_profile"
  - "clean_connection_fact_nodes_profile"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "外部预配置专用数据库与用户；分别验证空闲/活动连接、CHECK 拒绝与 FORCE 信号，不创建环境、不实际清理连接。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
