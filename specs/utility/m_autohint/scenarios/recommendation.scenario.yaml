schema_version: 1
kind: scenario
id: scenario_m_autohint_recommendation
name: scenario_m_autohint_recommendation
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_autohint
fact_refs:
- m_autohint_fact_parentheses
- m_autohint_fact_analyze
- m_autohint_fact_verbose
- m_autohint_fact_patch
- m_autohint_fact_test
- m_autohint_fact_debug
- m_autohint_fact_existing_hint
fixture_refs:
- fixture_m_autohint_history
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 先校验前置探索是否产生目标查询模型，再比较推荐输出。当前显式TEST/SQLPATCH FALSE，不安装输出的SQL PATCH；探索本身可能执行查询。
oracles:
- kind: manual_assertion
  expected: 历史模型数量、具体Hint、时间、性能提升、默认选项和已有Hint忽略行为均尚未验证
execution_requirements:
- database_authorization
- target_oracle_calibration
