schema_version: 1
kind: "fixture"
id: "fixture_cluster_indexed"
name: "CLUSTER indexed"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "cluster"
requires_fixture_refs:
  - "fixture_cluster_source"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE INDEX ix_cluster_source ON t_cluster_source USING btree (col_1);"
  teardown_sqls:
    - "DROP INDEX IF EXISTS ix_cluster_source;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
