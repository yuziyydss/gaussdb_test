schema_version: 1
kind: "fixture"
id: "fixture_cluster_clustered"
name: "CLUSTER clustered"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "cluster"
requires_fixture_refs:
  - "fixture_cluster_indexed"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CLUSTER t_cluster_source USING ix_cluster_source;"
  teardown_sqls:
    - "SELECT 1;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
