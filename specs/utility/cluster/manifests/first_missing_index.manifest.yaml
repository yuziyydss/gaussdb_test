schema_version: 1
kind: "manifest"
id: "manifest_cluster_first_missing_index"
name: "CLUSTER first_missing_index"
status: "needs_review"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "cluster"
syntax_ref: "syntax_cluster"
suite_type: "negative"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  target:
    - "cluster_target_unset"
  index:
    - "cluster_index_none"
  verbose:
    - "cluster_verbose_none"
expected:
  default: "error"
  scope: "syntax_and_semantics"
  oracle_status: "needs_verification"
  error_category: "cluster_missing_recorded_index"
  fact_refs:
    - "cluster_fact_index_required"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
violates_rule_refs:
  - "cluster_rule_first_index"
environment_requirements:
  -
    key: "autocommit_no_transaction"
    allowed_values:
      - "true"
    fact_refs:
      - "cluster_fact_no_transaction"
  -
    key: "cluster_disk_capacity_ready"
    allowed_values:
      - "true"
    fact_refs:
      - "cluster_fact_disk"
