schema_version: 1
kind: fixture
id: fixture_m_prepare_analyze_row_target
name: fixture_m_prepare_analyze_row_target
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_prepare
requires_fixture_refs: []
provides:
  tables:
  - name: m_prepare_data
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
    - name: qty
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE m_prepare_data (id INT,qty INT) WITH (ORIENTATION = ROW);
  - INSERT INTO m_prepare_data VALUES (1,10),(2,20);
  teardown_sqls:
  - DEALLOCATE PREPARE m_prepare_stmt;
  - DROP TABLE m_prepare_data;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
