schema_version: 1
kind: fixture
id: fixture_m_prepare_create_namespace
name: fixture_m_prepare_create_namespace
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_prepare
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - SHOW search_path;
  teardown_sqls:
  - DEALLOCATE PREPARE m_prepare_stmt;
  note: 在已授权的M物理数据库独占连接内记录search_path；不提前创建目标模式，不用SELECT 1充当前置。目标模式须不存在、非系统名、不与任何用户同名且不在search_path中，以免创建后隐式成为当前模式。本候选只PREPARE，不DROP尚未创建的模式；prepared仅成功创建后按本case归属释放。setup或目标失败须记录实际阶段，不盲目清理；关闭本case连接，不靠ROLLBACK恢复预备语句。
