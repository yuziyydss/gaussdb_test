schema_version: 1
kind: fixture
id: fixture_m_prepare_create_table_namespace
name: fixture_m_prepare_create_table_namespace
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_prepare
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE SCHEMA m_prepare_ct_namespace;
  teardown_sqls:
  - DEALLOCATE PREPARE m_prepare_stmt;
  - DROP SCHEMA m_prepare_ct_namespace;
  note: 只创建独占新模式，不预建目标表；模式名不得与任何用户同名，避免对象归属偏移。只PREPARE的用例清理预备语句和空模式，不DROP尚未创建的表。若目标失败须确认prepared归属，若未来EXECUTE场景中途失败须记录实际创建阶段，只清理本case拥有资产；不得CASCADE或DROP
    OWNED兜底。
