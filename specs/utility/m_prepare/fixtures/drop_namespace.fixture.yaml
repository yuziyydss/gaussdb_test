schema_version: 1
kind: fixture
id: fixture_m_prepare_drop_namespace
name: fixture_m_prepare_drop_namespace
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_prepare
requires_fixture_refs: []
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - SHOW search_path;
  - CREATE SCHEMA m_prepare_drop_namespace;
  teardown_sqls:
  - DEALLOCATE PREPARE m_prepare_stmt;
  - DROP SCHEMA IF EXISTS m_prepare_drop_namespace;
  note: 仅在已授权M物理测试数据库独占连接内新建目标空模式，名称初始不存在、非系统名、非任何用户同名且不在search_path中。真实setup不创建表，不提前PREPARE或EXECUTE；全程不USE此模式。目标只PREPARE，所以模式应仍存在且为空。清理必须先确认本case资产归属与实际阶段；prepared仅成功创建后释放。仅本case新建且仍为空、非当前、无外部依赖的模式可清理。IF
    EXISTS不是归属证明，不得清理同名外部或并发重建的模式；setup失败不能盲目执行teardown。场景EXECUTE成功后模式已消失则跳过该清理，不把任意异常当已删除；关闭专属连接，ROLLBACK不能释放prepared。
