schema_version: 1
kind: fixture
id: fixture_m_prepare_prepare_string
name: fixture_m_prepare_prepare_string
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_prepare
requires_fixture_refs:
- fixture_m_set_prepare_string
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: auto
  note: 借用M SET的真实字符串变量前置；目标名必须不存在。无论目标结果如何均关闭本case独占连接；不能对预计不存在的预备语句无条件DEALLOCATE，也不能复用残留连接。
