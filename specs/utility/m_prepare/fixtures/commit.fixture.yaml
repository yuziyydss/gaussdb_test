schema_version: 1
kind: fixture
id: fixture_m_prepare_commit
name: fixture_m_prepare_commit
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_prepare
requires_fixture_refs:
- fixture_m_commit_transaction
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - SELECT COUNT(*) FROM m_commit_data;
  teardown_sqls:
  - DEALLOCATE PREPARE m_prepare_stmt;
  note: 依赖COMMIT包真实建表/种子/BEGIN/未提交INSERT，COUNT记录前置行数，不代替结果断言。目标只PREPARE；先DEALLOCATE，再逆序由依赖ROLLBACK、DROP自己的表。须独占同会话、事务创建者，setup失败不授权清理。
