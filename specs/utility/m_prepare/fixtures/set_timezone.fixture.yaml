schema_version: 1
kind: fixture
id: fixture_m_prepare_set_timezone
name: fixture_m_prepare_set_timezone
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_prepare
requires_fixture_refs:
- fixture_m_set_transaction
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - SHOW TimeZone;
  teardown_sqls:
  - DEALLOCATE PREPARE m_prepare_stmt;
  note: 先展开M SET的START TRANSACTION，再SHOW TimeZone验证当前会话参数可读；本包只PREPARE，不提前执行SET。清理先DEALLOCATE再由共享Fixture ROLLBACK，最后关闭独占连接；预备语句不会靠ROLLBACK释放。
