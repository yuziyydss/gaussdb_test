schema_version: 1
kind: scenario
id: scenario_m_prepare_duplicate_and_variable
name: 同名 PREPARE 负例（保留历史场景ID）
status: planned
description: 只接受目标步骤对应错误，setup失败不能代替；SQLSTATE/类别匹配尚未校准，仍未执行。
factor_ref: m_prepare
fact_refs:
- m_prepare_fact_duplicate
- m_prepare_fact_session
fixture_refs:
- fixture_m_prepare_duplicate_prepared
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- id: target
  sql: PREPARE m_prepare_duplicate FROM 'SELECT 2';
  expected: error
oracles:
- kind: target_error
  step_id: target
  stage: target
  expected: error
  error_category: duplicate_prepared_statement
  oracle_status: needs_verification
  sqlstates: []
execution_requirements:
- database_authorization
- target_oracle_calibration
- isolated_connection
- close_case_connection
