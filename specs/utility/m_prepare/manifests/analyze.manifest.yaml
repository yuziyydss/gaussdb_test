schema_version: 1
kind: manifest
id: manifest_m_prepare_analyze
name: manifest_m_prepare_analyze
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_prepare
syntax_ref: syntax_m_prepare
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_prepare_analyze_row_target
identifier_policy: {}
bindings:
  body:
  - m_prepare_body_analyze
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_prepare_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_prepare_fact_session
- key: table_authority
  allowed_values:
  - fixture_table_creator
  fact_refs:
  - m_analyze::m_analyze_fact_owner
- key: execution_context
  allowed_values:
  - ordinary_analyze_prepare
  fact_refs:
  - m_analyze::m_analyze_fact_transaction
- key: table_storage
  allowed_values:
  - row
  fact_refs:
  - m_analyze::m_analyze_fact_row_storage
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
