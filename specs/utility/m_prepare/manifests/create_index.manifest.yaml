schema_version: 1
kind: manifest
id: manifest_m_prepare_create_index
name: manifest_m_prepare_create_index
status: needs_review
description: 仅PREPARE，不EXECUTE；实际执行需重新建立目标对象及清理契约。新索引/视图名必须未占用；独占public或用户测试模式，不能访问系统模式或既有对象。
factor_ref: m_prepare
syntax_ref: syntax_m_prepare
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_prepare_target
identifier_policy: {}
bindings:
  body:
  - m_prepare_body_create_index
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_prepare_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_prepare_fact_session
- key: ddl_authority
  allowed_values:
  - create_any_index
  fact_refs:
  - m_create_index::m_create_index_fact_authority
- key: namespace_scope
  allowed_values:
  - isolated_public_or_user_schema
  fact_refs:
  - m_create_index::m_create_index_fact_authority
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
