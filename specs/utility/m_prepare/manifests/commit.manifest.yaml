schema_version: 1
kind: manifest
id: manifest_m_prepare_commit
name: manifest_m_prepare_commit
status: needs_review
description: 只PREPARE COMMIT，不在setup或目标阶段EXECUTE或COMMIT；真实提交效果由planned阶段场景验证。
factor_ref: m_prepare
syntax_ref: syntax_m_prepare
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_prepare_commit
identifier_policy: {}
bindings:
  body:
  - m_prepare_body_commit
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_prepare_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_prepare_fact_session
- key: transaction_authority
  allowed_values:
  - transaction_creator
  fact_refs:
  - m_commit::m_commit_fact_authority
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
