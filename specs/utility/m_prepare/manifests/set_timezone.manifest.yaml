schema_version: 1
kind: manifest
id: manifest_m_prepare_set_timezone
name: manifest_m_prepare_set_timezone
status: needs_review
description: 仅预备SESSION时区PRC语句，不EXECUTE；不推广至用户变量、其他配置或编码。实际时区结果待独立阶段Oracle验证。
factor_ref: m_prepare
syntax_ref: syntax_m_prepare
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_prepare_set_timezone
identifier_policy: {}
bindings:
  body:
  - m_prepare_body_set_timezone
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_prepare_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_prepare_fact_session
  - m_set::m_set_fact_session
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
