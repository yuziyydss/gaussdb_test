schema_version: 1
kind: manifest
id: manifest_m_prepare_drop_namespace
name: manifest_m_prepare_drop_namespace
status: needs_review
description: 只PREPARE本case独占空模式的DROP SCHEMA，不EXECUTE；不是物理数据库删除，不覆盖DATABASE同义拼写或非空模式。
factor_ref: m_prepare
syntax_ref: syntax_m_prepare
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_prepare_drop_namespace
identifier_policy: {}
bindings:
  body:
  - m_prepare_body_drop_namespace
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_prepare_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_prepare_fact_session
- key: namespace_authority
  allowed_values:
  - database_create_non_user_named_fresh_schema
  fact_refs:
  - m_create_schema::m_create_schema_fact_authority
  - m_create_schema::m_create_schema_fact_namespace
  - m_create_schema::m_create_schema_fact_same_name_owner
- key: drop_authority
  allowed_values:
  - fixture_namespace_creator
  fact_refs:
  - m_drop_schema::m_drop_schema_fact_authority
- key: asset_scope
  allowed_values:
  - fresh_non_system_non_user_namespace_outside_search_path
  fact_refs:
  - m_create_schema::m_create_schema_fact_namespace
  - m_create_schema::m_create_schema_fact_same_name_owner
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
