schema_version: 1
kind: manifest
id: manifest_m_prepare_create_table
name: manifest_m_prepare_create_table
status: needs_review
description: 仅PREPARE专属模式两INTEGER列普通新表，目标表尚不存在；创建行为、行值和对象目录由独立planned场景检查。
factor_ref: m_prepare
syntax_ref: syntax_m_prepare
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_prepare_create_table_namespace
identifier_policy: {}
bindings:
  body:
  - m_prepare_body_create_table
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_prepare_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_prepare_fact_session
- key: namespace_authority
  allowed_values:
  - database_create_non_user_named_fresh_schema
  fact_refs:
  - m_create_schema::m_create_schema_fact_authority
  - m_create_schema::m_create_schema_fact_namespace
  - m_create_schema::m_create_schema_fact_same_name_owner
- key: ddl_authority
  allowed_values:
  - create_any_table
  fact_refs:
  - m_create_table::m_create_table_fact_authority
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
