schema_version: 1
kind: manifest
id: manifest_m_prepare_drop_view
name: manifest_m_prepare_drop_view
status: needs_review
description: 仅PREPARE本case新建对象的DROP，不EXECUTE；删除结果与实际资产清理由独立planned阶段场景验证。
factor_ref: m_prepare
syntax_ref: syntax_m_prepare
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_prepare_drop_view
identifier_policy: {}
bindings:
  body:
  - m_prepare_body_drop_view
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_prepare_fact_mode
- key: session_lifecycle
  allowed_values:
  - isolated_connection
  fact_refs:
  - m_prepare_fact_session
- key: namespace_authority
  allowed_values:
  - database_create_non_user_named_fresh_schema
  fact_refs:
  - m_create_schema::m_create_schema_fact_authority
  - m_create_schema::m_create_schema_fact_namespace
  - m_create_schema::m_create_schema_fact_same_name_owner
- key: ddl_authority
  allowed_values:
  - create_any_table
  fact_refs:
  - m_create_table::m_create_table_fact_authority
- key: drop_authority
  allowed_values:
  - fixture_object_creator
  fact_refs:
  - m_drop_view::m_drop_view_fact_authority
- key: cleanup_authority
  allowed_values:
  - fixture_table_and_namespace_creator
  fact_refs:
  - m_drop_table::m_drop_table_fact_authority
  - m_drop_schema::m_drop_schema_fact_authority
- key: asset_scope
  allowed_values:
  - fresh_owned_objects_no_external_dependencies
  fact_refs:
  - m_create_schema::m_create_schema_fact_namespace
  - m_create_schema::m_create_schema_fact_same_name_owner
- key: nested_object_create_authority
  allowed_values:
  - create_any_table
  fact_refs:
  - m_create_view::m_create_view_fact_authority
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
