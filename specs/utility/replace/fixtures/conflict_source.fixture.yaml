schema_version: 1
kind: "fixture"
id: "fixture_replace_conflict_source"
name: "REPLACE conflict_source"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "replace"
requires_fixture_refs:
  - "fixture_create_schema_pair"
provides:
  tables:
    -
      name: "fp_cs_one.b11_replace_target"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: false
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
    -
      name: "fp_cs_one.b11_replace_source"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "col_1"
          type: "INTEGER"
          nullable: true
        -
          name: "col_2"
          type: "INTEGER"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
    - "CREATE TABLE fp_cs_one.b11_replace_target (col_1 INT PRIMARY KEY DEFAULT 0, col_2 INT DEFAULT 2);"
    - "CREATE TABLE fp_cs_one.b11_replace_source (col_1 INT, col_2 INT);"
    - "INSERT INTO fp_cs_one.b11_replace_target VALUES (1,10),(2,20);"
    - "INSERT INTO fp_cs_one.b11_replace_source VALUES (1,11),(3,30);"
  teardown_sqls:
    - "ROLLBACK;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
