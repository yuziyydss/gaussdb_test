schema_version: 1
kind: "manifest"
id: "manifest_replace_values"
name: "REPLACE values"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "replace"
syntax_ref: "syntax_replace"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_replace_conflict_source"
identifier_policy: {}
bindings:
  form:
    - "replace_form_values"
  into:
    - "replace_into_off"
    - "replace_into_on"
  columns:
    - "replace_columns_off"
    - "replace_columns_on"
  rows:
    - "replace_rows_one"
    - "replace_rows_two"
    - replace_rows_three
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
