schema_version: 1
kind: "manifest"
id: "manifest_replace_set"
name: "REPLACE set"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "replace"
syntax_ref: "syntax_replace"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_replace_conflict_source"
identifier_policy: {}
bindings:
  form:
    - "replace_form_set"
  into:
    - "replace_into_off"
    - "replace_into_on"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
