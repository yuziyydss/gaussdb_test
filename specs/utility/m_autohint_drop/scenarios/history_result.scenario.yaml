schema_version: 1
kind: scenario
id: scenario_m_autohint_drop_history_result
name: scenario_m_autohint_drop_history_result
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_autohint_drop
fact_refs:
- m_autohint_drop_fact_removed
- m_autohint::m_autohint_fact_analyze
fixture_refs:
- fixture_m_autohint_drop_history
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 前置AUTOHINT探索须证明目标模型已产生；删除后检查精确模型和二次删除0条的消息/成功状态，不设任意错误为通过。
oracles:
- kind: manual_assertion
  expected: 模型存在性、删除数量、缺失历史的协议状态需实机校准
execution_requirements:
- database_authorization
- target_oracle_calibration
