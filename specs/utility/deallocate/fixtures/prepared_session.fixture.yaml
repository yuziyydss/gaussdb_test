schema_version: 1
kind: "fixture"
id: "fixture_deallocate_prepared_session"
name: "DEALLOCATE prepared_session"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "deallocate"
requires_fixture_refs:
  - "fixture_prepare_statements"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "auto"
  note: "只依赖上游能力；不复制对象定义或生命周期 SQL。"
