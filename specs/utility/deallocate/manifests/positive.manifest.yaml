schema_version: 1
kind: "manifest"
id: "manifest_deallocate_positive"
name: "DEALLOCATE positive"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "deallocate"
syntax_ref: "syntax_deallocate"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_deallocate_prepared_session"
identifier_policy: {}
bindings:
  prepare_keyword:
    - "deallocate_prepare_keyword_absent"
    - "deallocate_prepare_keyword_present"
  target:
    - "deallocate_target_zero"
    - "deallocate_target_one"
    - "deallocate_target_all"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "session_ownership"
    allowed_values:
      - "exclusive_test_connection"
    fact_refs:
      - "prepare::prepare_fact_session_scope"
