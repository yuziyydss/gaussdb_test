schema_version: 1
kind: "scenario"
id: "scenario_savepoint_outside_transaction"
name: "SAVEPOINT outside_transaction"
status: "planned"
description: "事务外负向场景先校准目标错误身份，不得把其他错误算作命中。"
factor_ref: "savepoint"
fixture_refs: []
fact_refs:
  - "savepoint_fact_transaction_required"
  - "savepoint_fact_error_identity"
preconditions:
  - "无活动事务"
  - "客户端禁用隐式 BEGIN"
steps:
  -
    id: "savepoint"
    action: "execute_sql"
    sql: "SAVEPOINT sp_test"
oracles:
  -
    after_step: "savepoint"
    kind: "target_error"
    expected: "savepoint_requires_transaction"
    oracle_status: "needs_verification"
execution_requirements:
  - "database"
  - "same_session"
