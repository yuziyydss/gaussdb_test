schema_version: 1
kind: "manifest"
id: "manifest_prepare_transaction_finite_identifiers"
name: "PREPARE TRANSACTION finite_identifiers"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "prepare_transaction"
syntax_ref: "syntax_prepare_transaction"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  transaction_id:
    - "prepare_transaction_transaction_id_short"
    - "prepare_transaction_transaction_id_max_valid"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "internal_two_phase_testing"
    allowed_values:
      - "true"
    fact_refs:
      - "prepare_transaction_fact_internal"
  -
    key: "prepared_transactions_capacity_ready"
    allowed_values:
      - "true"
    fact_refs:
      - "prepare_transaction_fact_capacity"
