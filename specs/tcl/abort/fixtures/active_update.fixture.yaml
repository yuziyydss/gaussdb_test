schema_version: 1
kind: "fixture"
id: "fixture_abort_active_update"
name: "ABORT active_update"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "abort"
requires_fixture_refs:
  - "fixture_rollback_table"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "INSERT INTO t_rollback_test VALUES (1, 'original');"
    - "BEGIN;"
    - "UPDATE t_rollback_test SET name = 'pending' WHERE id = 1;"
  teardown_sqls:
    - "ROLLBACK;"
    - "DELETE FROM t_rollback_test;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
