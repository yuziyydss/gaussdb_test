schema_version: 1
kind: "manifest"
id: "manifest_abort_positive"
name: "ABORT positive"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "abort"
syntax_ref: "syntax_abort"
suite_type: "positive"
strategy: "full_cartesian"
fixture_refs:
  - "fixture_abort_active_update"
identifier_policy: {}
bindings:
  compatibility_keyword:
    - "abort_compatibility_keyword_absent"
    - "abort_compatibility_keyword_work"
    - "abort_compatibility_keyword_transaction"
expected:
  default: "success"
  scope: "syntax_and_semantics"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
