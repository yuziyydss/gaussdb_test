schema_version: 1
kind: "scenario"
id: "scenario_abort_documented_example"
name: "ABORT documented_example"
status: "planned"
description: "文档大表缩为等价两列表，只验证更新被撤销，不扩大示例规则。"
factor_ref: "abort"
fixture_refs:
  - "fixture_abort_active_update"
fact_refs:
  - "abort_fact_example_table"
  - "abort_fact_example_seed"
  - "abort_fact_example_begin"
  - "abort_fact_example_update"
  - "abort_fact_example_abort"
  - "abort_fact_example_result"
preconditions:
  - "fixture 基线值 original 对应文档 DOCTOR DEGREE"
steps:
  -
    id: "abort"
    action: "execute_sql"
    sql: "ABORT"
  -
    id: "check"
    action: "query"
    sql: "SELECT name FROM t_rollback_test WHERE id = 1"
oracles:
  -
    after_step: "check"
    kind: "result_rows"
    expected:
      -
        - "original"
execution_requirements:
  - "database"
  - "same_session"
