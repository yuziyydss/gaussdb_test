schema_version: 1
kind: "manifest"
id: "manifest_commit_prepared_owned_transaction"
name: "COMMIT PREPARED owned_transaction"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "commit_prepared"
syntax_ref: "syntax_commit_prepared"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  transaction_id:
    - "commit_prepared_transaction_id_owned"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "internal_two_phase_testing"
    allowed_values:
      - "true"
    fact_refs:
      - "commit_prepared_fact_internal"
  -
    key: "prepared_transactions_capacity_ready"
    allowed_values:
      - "true"
    fact_refs:
      - "prepare_transaction::prepare_transaction_fact_capacity"
  -
    key: "prepared_owner_or_sysadmin"
    allowed_values:
      - "true"
    fact_refs:
      - "commit_prepared_fact_permission"
