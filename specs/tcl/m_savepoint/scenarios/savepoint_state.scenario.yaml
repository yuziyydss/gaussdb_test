schema_version: 1
kind: scenario
id: scenario_m_savepoint_savepoint_state
name: scenario_m_savepoint_savepoint_state
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_savepoint
fact_refs:
- m_savepoint_fact_savepoint_state
fixture_refs:
- fixture_m_savepoint_transaction
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: SAVEPOINT m_savepoint_sp1;
- sql: INSERT INTO m_savepoint_data VALUES (2);
- sql: ROLLBACK TO SAVEPOINT m_savepoint_sp1;
- sql: SELECT id FROM m_savepoint_data ORDER BY id;
oracles:
- kind: result_set
  expected:
  - - 0
  - - 1
execution_requirements:
- database_authorization
- target_oracle_calibration
