schema_version: 1
kind: "scenario"
id: "scenario_start_transaction_documented_examples"
name: "START TRANSACTION documented_examples"
status: "planned"
description: "示例对应的两种默认起始形式后查询并 END，尚未执行。"
factor_ref: "start_transaction"
fixture_refs:
  - "fixture_start_transaction_dependency_begin_transaction_table"
fact_refs:
  - "start_transaction_fact_example_setup"
  - "start_transaction_fact_example_table"
  - "start_transaction_fact_example_default_start"
  - "start_transaction_fact_example_default_begin"
preconditions: []
steps:
  -
    id: "run"
    action: "variant_transaction"
    start:
      - "START TRANSACTION"
      - "BEGIN"
    query: "SELECT * FROM t_begin_txn"
    end: "END"
oracles:
  -
    after_step: "run"
    kind: "transaction_finished"
    expected: true
execution_requirements:
  - "database"
  - "same_session"
