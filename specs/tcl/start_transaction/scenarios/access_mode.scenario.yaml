schema_version: 1
kind: "scenario"
id: "scenario_start_transaction_access_mode"
name: "START TRANSACTION access_mode"
status: "planned"
description: "只读/读写模式分别检查受控写入；错误身份尚未校准，不以任意错误为通过。"
factor_ref: "start_transaction"
fixture_refs:
  - "fixture_start_transaction_dependency_begin_transaction_table"
fact_refs:
  - "start_transaction_fact_access_mode"
preconditions:
  - "独占连接"
steps:
  -
    id: "matrix"
    action: "transaction_access_matrix"
    modes:
      - "READ ONLY"
      - "READ WRITE"
oracles:
  -
    after_step: "matrix"
    kind: "access_mode_matches_request"
    expected: true
execution_requirements:
  - "database"
  - "same_session"
