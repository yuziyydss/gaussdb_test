schema_version: 1
kind: "scenario"
id: "scenario_start_transaction_timing_review"
name: "START TRANSACTION timing_review"
status: "planned"
description: "先核实 SELECT 触发差异，再实现逐命令的隔离级别变更时序验证。"
factor_ref: "start_transaction"
fixture_refs: []
fact_refs:
  - "start_transaction_fact_isolation_timing"
  - "start_transaction_fact_timing_difference"
preconditions: []
steps:
  -
    id: "review"
    action: "compare_pdf_chapters"
    chapters:
      - "START TRANSACTION"
      - "BEGIN"
oracles:
  -
    after_step: "review"
    kind: "timing_difference_resolved"
    expected: true
execution_requirements:
  - "database"
  - "same_session"
