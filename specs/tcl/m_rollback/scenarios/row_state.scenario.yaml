schema_version: 1
kind: scenario
id: scenario_m_rollback_row_state
name: scenario_m_rollback_row_state
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_rollback
fact_refs:
- m_rollback_fact_row_state
fixture_refs:
- fixture_m_rollback_transaction
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: ROLLBACK;
- sql: SELECT id FROM m_rollback_data ORDER BY id;
oracles:
- kind: result_set
  expected:
  - - 0
execution_requirements:
- database_authorization
- target_oracle_calibration
