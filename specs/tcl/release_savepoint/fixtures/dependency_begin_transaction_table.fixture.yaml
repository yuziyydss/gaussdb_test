schema_version: 1
kind: fixture
id: fixture_release_savepoint_dependency_begin_transaction_table
name: release_savepoint 共享前置依赖
status: ready
description: 对象只由依赖维护；本包不重复定义。
factor_ref: release_savepoint
requires_fixture_refs:
- fixture_begin_transaction_table
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: auto
  note: 仅依赖层；拓扑 setup，逆序 teardown。
