schema_version: 1
kind: scenario
id: scenario_release_savepoint_resource_release
name: RELEASE 保存点资源释放观测
status: planned
description: 数据保留断言不能证明资源释放；先校准本版本可观测指标与噪声基线。
factor_ref: release_savepoint
fixture_refs: [fixture_release_savepoint_dependency_savepoint_defined]
fact_refs: [release_savepoint_fact_resource_release]
preconditions: [独占会话，不以全实例内存下降代替保存点资源观测]
steps:
  - id: calibrate
    action: calibrate_savepoint_resource_metric
    note: 确认保存点内部资源可观测项、采样权限和基线，之后才编写 RELEASE 前后断言。
oracles: []
execution_requirements: [database, same_session, calibrated_resource_observer]
