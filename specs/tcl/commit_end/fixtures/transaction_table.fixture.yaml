schema_version: 1
kind: fixture
id: fixture_commit_end_table
name: COMMIT | END 可见性最小表
status: ready
description: 为提交前后跨会话可见性场景提供最小两列表。
factor_ref: commit_end
provides:
  tables:
    - name: t_commit_end
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: note, type: VARCHAR(40), nullable: true}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: auto
  note: 由 Fixture 编译器生成幂等建表和清理语句。
