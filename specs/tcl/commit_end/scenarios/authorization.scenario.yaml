schema_version: 1
kind: scenario
id: scenario_commit_end_authorization
name: COMMIT 执行者与跨会话授权
status: planned
description: 验证事务创建者、系统管理员和其他用户执行提交时的授权边界；跨会话交接机制仍待确认。
factor_ref: commit_end
fixture_refs: [fixture_commit_end_table]
fact_refs: [commit_end_fact_executor_authorization, commit_end_fact_cross_session, commit_end_open_cross_session_mechanism]
preconditions: [多用户, 多会话, 可识别或接管待提交事务的环境机制]
steps:
  - {id: create_transaction, action: start_transaction_as_creator}
  - {id: commit_variants, action: execute_as_identity, variants: [creator, system_administrator, unrelated_user]}
oracles:
  - {kind: authorization_matrix, expected: "创建者或系统管理员成功，其他用户被拒绝；目标错误身份待环境校准"}
execution_requirements: [database, multiple_users, multiple_sessions, transaction_handoff, target_error_calibration]
