schema_version: 1
kind: syntax
id: syntax_commit_end
name: COMMIT | END 主语法
category: TCL
status: ready
factor_ref: commit_end
description: 只表达两个命令关键字和无实际作用的兼容关键字。
production: "{command_keyword} {compatibility_keyword}"
rendering: {whitespace: collapse, statement_terminator: ";"}
slots:
  command_keyword: {type: choice, optional: false, dimension_ref: commit_end.command_keyword}
  compatibility_keyword: {type: token, optional: true, dimension_ref: commit_end.compatibility_keyword}
source_fact_refs: [commit_end_fact_syntax, commit_end_fact_compatibility_tokens]
