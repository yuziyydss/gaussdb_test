schema_version: 1
kind: syntax
id: syntax_rollback
name: ROLLBACK 主语法
description: 固定命令和单一可选关键字槽位。
factor_ref: rollback
category: TCL
status: ready
production: "ROLLBACK {compatibility_keyword}"
rendering: {whitespace: collapse, statement_terminator: ";"}
slots:
  compatibility_keyword: {type: token, optional: true, dimension_ref: rollback.compatibility_keyword}
source_fact_refs: [rollback_fact_syntax, rollback_fact_compatibility]
