schema_version: 1
kind: fixture
id: fixture_rollback_table
name: ROLLBACK 场景基础表
description: 为事务回滚及文档 DDL 示例提供独占两列表。
factor_ref: rollback
status: ready
provides:
  tables:
    - name: t_rollback_test
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: true}
        - {name: name, type: TEXT, nullable: true}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: auto
  note: 表在事务开始前创建，场景结束事务后才清理；本批不连接数据库。
