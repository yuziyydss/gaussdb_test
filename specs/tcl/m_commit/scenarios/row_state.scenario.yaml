schema_version: 1
kind: scenario
id: scenario_m_commit_row_state
name: scenario_m_commit_row_state
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_commit
fact_refs:
- m_commit_fact_row_state
fixture_refs:
- fixture_m_commit_transaction
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: COMMIT;
- sql: SELECT id FROM m_commit_data ORDER BY id;
oracles:
- kind: result_set
  expected:
  - - 0
  - - 1
execution_requirements:
- database_authorization
- target_oracle_calibration
