schema_version: 1
kind: scenario
id: scenario_m_start_transaction_isolation_alias
name: scenario_m_start_transaction_isolation_alias
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_start_transaction
fact_refs:
- m_start_transaction_fact_isolation_alias
- m_start_transaction_fact_uncommitted_alias
fixture_refs:
- fixture_m_start_transaction_transaction
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 分别开启别名隔离级别，读取实际隔离级别并验证并发可见性
oracles:
- kind: manual_assertion
  expected: 语法成功不代表支持真正的 SERIALIZABLE 隔离
execution_requirements:
- database_authorization
- target_oracle_calibration
