schema_version: 1
kind: fixture
id: fixture_m_start_transaction_transaction
name: fixture_m_start_transaction_transaction
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_start_transaction
requires_fixture_refs: []
provides:
  tables:
  - name: m_start_transaction_data
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE m_start_transaction_data (id INT);
  - INSERT INTO m_start_transaction_data VALUES (0);
  teardown_sqls:
  - ROLLBACK;
  - DROP TABLE m_start_transaction_data;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
