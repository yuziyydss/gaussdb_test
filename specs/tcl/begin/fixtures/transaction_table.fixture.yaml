schema_version: 1
kind: fixture
id: fixture_begin_transaction_table
name: BEGIN 事务行为最小表
status: ready
description: 为计划中的事务写入和并发可见性场景提供两列整数表。
factor_ref: begin
provides:
  tables:
    - name: t_begin_txn
      persistence: permanent
      table_kind: regular
      columns:
        - {name: col1, type: INTEGER, nullable: true}
        - {name: col2, type: INTEGER, nullable: true}
seed:
  required: true
  rows:
    - {col1: 1, col2: 1}
    - {col1: 2, col2: 2}
execution:
  status: ready
  mode: auto
  note: 由 Fixture 编译器生成幂等建表、种子和清理语句。
