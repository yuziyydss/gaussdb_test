schema_version: 1
kind: fixture
id: fixture_update_target
name: UPDATE 基础目标表
status: ready
description: 为单表、ONLY/*、别名、SET、排序、LIMIT、FROM 自连接与 RETURNING 提供最小三列表。
factor_ref: update
provides:
  tables:
    - name: t_update_target
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: note, type: VARCHAR(64), nullable: true}
        - {name: qty, type: INTEGER, nullable: true}
seed:
  required: true
  rows:
    - {id: 1, note: keep, qty: 10}
    - {id: 2, note: update_me, qty: 20}
    - {id: 3, note: cte_source, qty: 30}
execution:
  status: ready
  mode: auto
  note: 由 Fixture 编译器生成幂等 DROP、CREATE、INSERT 与 teardown。
