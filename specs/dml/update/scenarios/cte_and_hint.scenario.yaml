schema_version: 1
kind: scenario
id: scenario_update_cte_and_hint
name: UPDATE CTE 物化与首 Hint 块
status: planned
description: 验证 CTE 的物化/内联计划差异及 UPDATE 后首 Hint 块规则；具体 Hint 等待 catalog 内权威 profile。
factor_ref: update
fixture_refs: [fixture_update_target, fixture_update_aux]
fact_refs: [update_fact_with_materialization, update_fact_plan_hint_position, update_fact_plan_hint_first_block, update_open_plan_hint_profile, update_open_recursive_cte_depth]
preconditions: [提供目标版本可用 Hint, 可捕获执行计划]
steps: [执行默认/MATERIALIZED/NOT MATERIALIZED CTE, 对比多个 Hint 块, 执行有限递归 CTE]
oracles: [CTE Scan 或内联计划, 仅首 Hint 块生效, 递归结果]
execution_requirements: [EXPLAIN 计划捕获, Hint 专家确认]
