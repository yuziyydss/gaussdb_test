schema_version: 1
kind: scenario
id: scenario_update_generated_and_system_encoding
name: UPDATE 生成列与系统编码保护
status: planned
description: 分离验证生成列直接赋值/DEFAULT 与禁止经系统表 UPDATE 修改字符编码；不把环境行为塞入默认正向 SQL。
factor_ref: update
fixture_refs: []
fact_refs: [update_fact_generated_column, update_fact_system_encoding, update_open_generated_fixture]
preconditions: [由目标版本专家提供生成列表定义, 允许安全隔离地观察系统目录保护]
steps: [对生成列直接赋值, 对生成列指定 DEFAULT, 尝试通过受控系统表路径修改编码]
oracles: [直接赋值被拒绝, DEFAULT 按文档允许, 系统编码更新被拒绝且数据未变化]
execution_requirements: [隔离数据库, 生成列 Fixture 审核, 禁止在生产环境执行]
