schema_version: 1
kind: scenario
id: scenario_update_stream_concurrency
name: UPDATE STREAM 计划并发同一行
status: planned
description: 构造确认产生 STREAM 的子查询 UPDATE，并发竞争同一行，验证文档限制而不猜测错误身份。
factor_ref: update
fixture_refs: [fixture_update_target, fixture_update_source]
fact_refs: [update_fact_stream_concurrency]
preconditions: [可确认 STREAM 执行计划, 两个受控并发会话]
steps: [确认子查询计划含 STREAM, 两会话并发更新同一目标行, 记录事务结果与诊断]
oracles: [不允许该并发更新组合, 原子性与最终行状态]
execution_requirements: [并发调度器, 计划捕获, 事务日志]
