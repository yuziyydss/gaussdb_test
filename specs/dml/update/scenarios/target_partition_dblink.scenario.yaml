schema_version: 1
kind: scenario
id: scenario_update_target_partition_dblink
name: UPDATE ONLY、分区、远端与表类型
status: planned
description: 验证继承范围、分区并集、DATABASE LINK 及文档列出的非普通基表类型；缺失契约前不生成默认正向 SQL。
factor_ref: update
fixture_refs: [fixture_update_target]
fact_refs: [update_fact_only_semantics, update_fact_partition_grammar, update_fact_partition_union, update_fact_table_target, update_fact_supported_table_types, update_open_partition_fixture, update_open_dblink_profile, update_open_table_type_fixtures]
preconditions: [建立父子表, 提供一级/二级分区建表契约, 配置受控 DATABASE LINK, 提供各表类型]
steps: [对比 ONLY 与省略 ONLY, 按名称和 FOR 值更新分区, 验证混合分区并集, 更新远端表, 遍历非普通表类型]
oracles: [受影响表范围, 受影响分区集合, 远端行变化, 各表类型能力]
execution_requirements: [目标版本数据库, 分区与远端环境, 专家审核 Fixture]
