schema_version: 1
kind: scenario
id: scenario_update_assignment_subquery_environment
name: UPDATE 多列子查询与兼容环境
status: planned
description: 验证多列映射、共享 subplan GUC、明确列名建议及 Oracle 外连接兼容项，不把计划环境混入普通正向清单。
factor_ref: update
fixture_refs: [fixture_update_target, fixture_update_source]
fact_refs: [update_fact_set_grammar, update_fact_default, update_fact_condition_behavior, update_fact_shared_set_update, update_fact_subquery_star_advice, update_fact_ora_join_compat, update_open_assignment_arity_type_model]
preconditions: [可切换 enable_shared_set_update, 可切换 behavior_compat_options, 可捕获计划]
steps: [执行 DEFAULT 与真假条件, 执行匹配二列子查询, 测试列数/类型边界, 对比共享开关计划, 比较明确列名与 *, 测试 + 操作符兼容项]
oracles: [无缺省字段变为 NULL 且仅 true 行更新, 列映射结果, 列数和类型诊断, subplan 数量差异, 兼容项生效差异]
execution_requirements: [GUC 控制, EXPLAIN 计划捕获, 错误与结果集捕获]
