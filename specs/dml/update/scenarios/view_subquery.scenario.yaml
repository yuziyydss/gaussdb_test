schema_version: 1
kind: scenario
id: scenario_update_view_subquery
name: UPDATE 视图与子查询完整约束
status: planned
description: 扩展单表代表，验证保留键、CHECK OPTION、连接列、只读目标、系统视图、禁止子句及多种基表类型。
factor_ref: update
fixture_refs: [fixture_update_view, fixture_update_subquery]
fact_refs: [update_fact_subquery_target, update_fact_view_subquery_restrictions, update_fact_multi_one_base, update_fact_preserved_key_restriction, update_fact_system_view_restriction, update_fact_read_only_error, update_fact_virtual_column_error, update_open_preserved_key_definition, "create_view::cv_fact_key_preserved_definition", "create_view::cv_fact_read_only_behavior"]
preconditions: [按引用的 CREATE VIEW 保留键定义构造目标, 可创建连接与只读目标]
steps: [更新单表目标, 更新连接目标, 更新 CHECK OPTION 连接列, 更新 READ ONLY 和系统视图, 逐项构造禁止顶层子句]
oracles: [仅一张保留键基表改变, 文档两条精确错误文本, 系统视图与禁止结构被拒绝]
execution_requirements: [数据库执行, 对象依赖与结果快照]
