schema_version: 1
kind: manifest
id: manifest_update_declared_default_positive
name: UPDATE 已声明默认值赋值
status: needs_review
description: 在带显式默认值及非默认值种子行的独立表上生成 SET note = DEFAULT，并枚举 RETURNING。仅为有限代表和生成契约，实际值与未更新列的保持行为尚未执行验证。
factor_ref: update
syntax_ref: syntax_update_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  update_form: [update_form_single]
  single_target_profile: [update_target_declared_defaults]
  set_profile: [update_set_default]
  from_clause: [update_from_none]
  predicate: [update_predicate_where]
  order_clause: [update_order_none]
  limit_clause: [update_limit_none]
  returning_clause: [update_returning_none, update_returning_all, update_returning_expression]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
