schema_version: 1
kind: manifest
id: manifest_update_alias_positive
name: UPDATE 目标别名限定赋值
status: needs_review
description: 覆盖 AS 与裸别名目标，以及文档明确支持的 alias.column_name 左值。
factor_ref: update
syntax_ref: syntax_update_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  update_form: [update_form_single]
  single_target_profile: [update_target_alias_as, update_target_alias_bare]
  set_profile: [update_set_alias_qualified]
  from_clause: [update_from_none]
  predicate: [update_predicate_alias]
  order_clause: [update_order_none]
  limit_clause: [update_limit_none]
  returning_clause: [update_returning_none, update_returning_expression]
expected: {default: success, scope: syntax_and_semantics}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
