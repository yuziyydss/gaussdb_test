schema_version: 1
kind: manifest
id: manifest_update_from_self_positive
name: UPDATE FROM 目标自连接别名
status: needs_review
description: 目标表在 FROM 中重复时使用不同别名，并用关联条件引用该别名。
factor_ref: update
syntax_ref: syntax_update_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  update_form: [update_form_single]
  single_target_profile: [update_target_table]
  set_profile: [update_set_literal]
  from_clause: [update_from_self_alias]
  predicate: [update_predicate_self_join]
  order_clause: [update_order_none]
  limit_clause: [update_limit_none]
  returning_clause: [update_returning_none]
expected: {default: success, scope: syntax_and_semantics}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
