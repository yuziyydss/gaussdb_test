schema_version: 1
kind: manifest
id: manifest_update_cte_syntax_positive
name: UPDATE WITH 有限语法展开
status: needs_review
description: 生成 PDF 枚举的 SELECT、VALUES、INSERT、UPDATE、DELETE CTE 与物化修饰；递归值只到 max_depth=2。
factor_ref: update
syntax_ref: syntax_update_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  with_clause: [update_with_select, update_with_recursive_select, update_with_materialized_select, update_with_not_materialized_select, update_with_values, update_with_insert, update_with_update, update_with_delete]
  update_form: [update_form_single]
  single_target_profile: [update_target_table]
  set_profile: [update_set_literal]
  from_clause: [update_from_none]
  predicate: [update_predicate_where]
  order_clause: [update_order_none]
  limit_clause: [update_limit_none]
  returning_clause: [update_returning_none]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
