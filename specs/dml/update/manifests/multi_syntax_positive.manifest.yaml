schema_version: 1
kind: manifest
id: manifest_update_multi_syntax_positive
name: UPDATE 多表有限语法
status: needs_review
description: 覆盖普通与带 ONLY/*/subquery item 的 table_list，并覆盖多表产生式的可选 FROM；本章没有多表执行示例，故只声明 syntax_only。
factor_ref: update
syntax_ref: syntax_update_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  update_form: [update_form_multi]
  multi_target_profile: [update_multi_targets_basic, update_multi_targets_modified]
  set_profile: [update_set_alias_qualified]
  from_clause: [update_from_none, update_from_source]
  predicate: [update_predicate_none]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
