schema_version: 1
kind: manifest
id: manifest_update_partition_payload_positive
name: UPDATE 分区业务列有限候选
status: needs_review
description: 业务列与分区键分离；事务命名空间只在本用例内存在。仅语法及静态契约，未执行行为Oracle。
factor_ref: update
syntax_ref: syntax_update_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  update_form: [update_form_single]
  single_target_profile: [update_target_payload_partition_name, update_target_payload_partition_value, update_target_payload_subpartition_name, update_target_payload_subpartition_value]
  set_profile: [update_set_payload_literal, update_set_payload_increment, update_set_payload_tuple]
  predicate: [update_predicate_none, update_predicate_partition]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
