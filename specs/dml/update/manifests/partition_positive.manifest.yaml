schema_version: 1
kind: manifest
id: manifest_update_partition_positive
name: UPDATE 一级/二级 RANGE 分区有限代表
status: needs_review
description: 复用同 PDF 分区提供者；赋值保持分区键值不变，不代表实际数据变更、多分区并集、其他策略或数据库行为。
factor_ref: update
syntax_ref: syntax_update_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  update_form: [update_form_single]
  single_target_profile: [update_target_partition_name, update_target_partition_value, update_target_subpartition_name, update_target_subpartition_value]
  set_profile: [update_set_partition_int]
  predicate: [update_predicate_none, update_predicate_partition]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
