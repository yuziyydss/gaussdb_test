schema_version: 1
kind: manifest
id: manifest_update_from_source_positive
name: UPDATE FROM 关联来源
status: needs_review
description: 覆盖 FROM 表字段参与 WHERE 的文档能力，来源表列名避免与目标列产生非目标歧义。
factor_ref: update
syntax_ref: syntax_update_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  update_form: [update_form_single]
  single_target_profile: [update_target_table]
  set_profile: [update_set_literal, update_set_multiple]
  from_clause: [update_from_source]
  predicate: [update_predicate_source_join]
  order_clause: [update_order_none]
  limit_clause: [update_limit_none]
  returning_clause: [update_returning_none, update_returning_expression]
expected: {default: success, scope: syntax_and_semantics}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
