schema_version: 1
kind: manifest
id: manifest_update_multi_subquery_order_limit_negative
name: UPDATE 多列 SET 子查询 ORDER/LIMIT 负向
status: needs_review
description: 多列赋值子查询使用 ORDER BY 与 LIMIT，专门违反本章明示限制；PDF 未给可核验错误身份。
factor_ref: update
syntax_ref: syntax_update_v1
suite_type: negative
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  update_form: [update_form_single]
  single_target_profile: [update_target_table]
  set_profile: [update_set_tuple_subquery_order_limit_invalid]
  from_clause: [update_from_none]
  predicate: [update_predicate_where]
  order_clause: [update_order_none]
  limit_clause: [update_limit_none]
  returning_clause: [update_returning_none]
violates_rule_refs: [update_rule_multi_subquery_no_order_limit]
expected: {default: error, scope: syntax_and_semantics, oracle_status: needs_verification, error_category: multi_column_set_subquery_order_limit_not_supported}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
