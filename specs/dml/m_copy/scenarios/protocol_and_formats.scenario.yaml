schema_version: 1
kind: scenario
id: scenario_m_copy_protocol_and_formats
name: scenario_m_copy_protocol_and_formats
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_copy
fact_refs:
- m_copy_fact_styles
- m_copy_fact_header
- m_copy_fact_generated
- m_copy_fact_files
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 验证COPY流与普通结果集分离；补STDIN载荷协议、文件生命周期、生成列排除及HEADER/fileheader关系后才扩域。
oracles:
- kind: manual_assertion
  expected: STDOUT候选非普通cursor.fetchall结果；实际字节与导入行为尚未验证
execution_requirements:
- database_authorization
- target_oracle_calibration
