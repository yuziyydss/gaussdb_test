schema_version: 1
kind: "scenario"
id: "scenario_fetch_negative_identity"
name: "FETCH negative_identity"
status: "planned"
description: "NO SCROLL 反向失败必须校准目标错误身份；不得把环境失败、缺游标或 setup 失败计作命中。"
factor_ref: "fetch"
fixture_refs: []
fact_refs:
  - "fetch_fact_no_scroll"
  - "fetch_fact_reverse_gap"
  - "fetch_fact_error_identity"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "NO SCROLL 反向失败必须校准目标错误身份；不得把环境失败、缺游标或 setup 失败计作命中。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
