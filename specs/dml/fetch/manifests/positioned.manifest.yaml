schema_version: 1
kind: "manifest"
id: "manifest_fetch_positioned"
name: "FETCH positioned"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "fetch"
syntax_ref: "syntax_fetch"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  direction:
    - "fetch_direction_prior"
    - "fetch_direction_first"
    - "fetch_direction_last"
    - "fetch_direction_backward"
    - "fetch_direction_backward_all"
  connector:
    - "fetch_connector_from"
    - "fetch_connector_in"
  cursor:
    - "fetch_cursor_auto"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "cursor_reverse_supported"
    allowed_values:
      - "true"
    fact_refs:
      - "declare::declare_fact_scroll_plan"
