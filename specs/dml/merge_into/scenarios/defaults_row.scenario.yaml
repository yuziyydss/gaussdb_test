schema_version: 1
kind: scenario
id: scenario_merge_defaults_row
name: MERGE整行DEFAULT VALUES不复制源列
status: planned
description: 从原始seed开始；仅源id=2不匹配，插入的目标id没有默认所以为NULL，不应误抄为2。原id=1和3两行保持不变。
factor_ref: merge_into
fixture_refs: [fixture_merge_constant_defaults]
fact_refs: [merge_fact_not_matched_behavior, merge_fact_default_behavior, 'create_table::ct_fact_default_semantics', 'drop_table::dt_fact_dependency_modes', 'drop_table::dt_fact_purge']
preconditions: [独占非系统测试模式及隔离连接，两表初始不存在，固定current_schema/search_path，逐步记录本case资产归属]
steps:
  - id: merge
    sql: "MERGE INTO g_merge_default_target AS dst USING g_merge_default_source AS src ON (dst.id = src.id) WHEN NOT MATCHED THEN INSERT DEFAULT VALUES;"
oracles:
  - kind: result_set
    step_id: merge
    sql: SELECT id, name, category FROM g_merge_default_target ORDER BY id NULLS LAST;
    expected: [[1, old-one, old], [3, keep-three, keep], [null, fallback-name, null]]
execution_requirements: [database_authorization, general_mode, isolated_connection, fresh_fixture_per_scenario, ownership_scoped_cleanup, per_step_oracle, target_oracle_calibration]
