schema_version: 1
kind: fixture
id: fixture_merge_target
name: MERGE 普通目标表
status: ready
description: 三列普通目标表，种子同时提供可匹配行与保留行。
factor_ref: merge_into
provides:
  tables:
    - name: t_merge_target
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: true}
        - {name: name, type: "VARCHAR(64)", nullable: true}
        - {name: category, type: "VARCHAR(64)", nullable: true}
seed:
  required: true
  rows:
    - {id: 1, name: old-one, category: old}
    - {id: 3, name: keep-three, category: keep}
execution: {status: ready, mode: auto, note: 自动创建、播种并清理普通目标表。}
