schema_version: 1
kind: fixture
id: fixture_merge_source
name: MERGE 普通源表
status: ready
description: 三列普通源表，包含一个匹配键和一个新键。
factor_ref: merge_into
provides:
  tables:
    - name: t_merge_source
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: name, type: "VARCHAR(64)", nullable: true}
        - {name: category, type: "VARCHAR(64)", nullable: true}
seed:
  required: true
  rows:
    - {id: 1, name: new-one, category: updated}
    - {id: 2, name: new-two, category: inserted}
execution: {status: ready, mode: auto, note: 自动创建、播种并清理普通源表。}
