schema_version: 1
kind: "manifest"
id: "manifest_move_positioned"
name: "MOVE positioned"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "move"
syntax_ref: "syntax_move"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  direction:
    - "move_direction_prior"
    - "move_direction_first"
    - "move_direction_last"
    - "move_direction_backward"
    - "move_direction_backward_all"
  connector:
    - "move_connector_absent"
    - "move_connector_from"
    - "move_connector_in"
  cursor:
    - "move_cursor_auto"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "cursor_reverse_supported"
    allowed_values:
      - "true"
    fact_refs:
      - "declare::declare_fact_scroll_plan"
