schema_version: 1
kind: "fixture"
id: "fixture_move_cursor"
name: "MOVE cursor"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "move"
requires_fixture_refs:
  - "fixture_declare_open_cursor"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "auto"
  note: "只依赖上游能力；不复制对象定义或生命周期 SQL。"
