schema_version: 1
kind: "scenario"
id: "scenario_move_negative_identity"
name: "MOVE negative_identity"
status: "planned"
description: "独立校准 MOVE 的 NO SCROLL 目标错误，不把缺游标或事务错误记为命中。"
factor_ref: "move"
fixture_refs: []
fact_refs:
  - "fetch::fetch_fact_no_scroll"
  - "move_fact_error_identity"
preconditions: []
steps:
  -
    id: "review"
    action: "implement_and_verify_documented_scenario"
    note: "独立校准 MOVE 的 NO SCROLL 目标错误，不把缺游标或事务错误记为命中。"
oracles: []
execution_requirements:
  - "database"
  - "same_session"
