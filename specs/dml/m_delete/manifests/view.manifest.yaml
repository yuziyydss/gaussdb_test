schema_version: 1
kind: manifest
id: manifest_m_delete_view
name: manifest_m_delete_view
status: needs_review
description: M 首批有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_delete
syntax_ref: syntax_m_delete
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_delete_shared_view
identifier_policy: {}
bindings:
  target:
  - m_delete_target_view
  where:
  - m_delete_where_none
  - m_delete_where_id
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_delete_fact_mode
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
