schema_version: 1
kind: fixture
id: fixture_m_delete_shared_table
name: fixture_m_delete_shared_table
status: needs_review
description: M 首批有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_delete
requires_fixture_refs:
- fixture_m_create_table_source
provides:
  tables: []
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: auto
  note: 仅组合依赖，不复制 DDL；依赖拓扑生成 setup，逆序生成 teardown。
