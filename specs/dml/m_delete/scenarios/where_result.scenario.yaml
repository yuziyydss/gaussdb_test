schema_version: 1
kind: scenario
id: scenario_m_delete_where_result
name: scenario_m_delete_where_result
status: planned
description: M 首批有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_delete
fact_refs:
- m_delete_fact_where
fixture_refs:
- fixture_m_delete_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: DELETE FROM m_b01_source WHERE id=2;
oracles:
- kind: result_set
  sql: SELECT id FROM m_b01_source ORDER BY id;
  expected:
  - - 1
  - - 3
execution_requirements:
- database_authorization
- target_oracle_calibration
