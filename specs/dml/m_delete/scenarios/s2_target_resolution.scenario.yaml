schema_version: 1
kind: scenario
id: scenario_m_delete_s2_target_resolution
name: scenario_m_delete_s2_target_resolution
status: planned
description: M 首批有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_delete
fact_refs:
- m_delete_fact_s2_match
fixture_refs: []
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 补 s2 gate 和同名跨 schema 表，分别检查唯一匹配与歧义
oracles:
- kind: target_error
  expected: 只有目标不能唯一匹配时报告目标解析错误
execution_requirements:
- database_authorization
- target_oracle_calibration
