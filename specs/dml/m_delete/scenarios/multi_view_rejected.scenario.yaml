schema_version: 1
kind: scenario
id: scenario_m_delete_multi_view_rejected
name: scenario_m_delete_multi_view_rejected
status: planned
description: M 首批有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_delete
fact_refs:
- m_delete_fact_multi_view
fixture_refs:
- fixture_m_delete_shared_view
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 补独立第二张表及多表 DELETE 产生式，不使用拼接占位符
oracles:
- kind: target_error
  expected: 视图多表删除被拒绝；目标 SQLSTATE 待校准
execution_requirements:
- database_authorization
- target_oracle_calibration
