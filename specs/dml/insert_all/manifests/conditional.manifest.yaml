schema_version: 1
kind: "manifest"
id: "manifest_insert_all_conditional"
name: "INSERT ALL conditional"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "insert_all"
syntax_ref: "syntax_insert_all"
suite_type: "positive"
strategy: "pairwise"
fixture_refs:
  - "fixture_insert_all_three_tables"
identifier_policy: {}
bindings:
  branch_mode:
    - "insert_all_branch_mode_conditional"
  match_mode:
    - "insert_all_match_mode_default"
    - "insert_all_match_mode_all"
    - "insert_all_match_mode_first"
  targets:
    - "insert_all_targets_one"
    - "insert_all_targets_two"
expected:
  default: "success"
  scope: "syntax_only"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
environment_requirements:
  -
    key: "sql_compatibility"
    allowed_values:
      - "A"
    fact_refs:
      - "insert_all_fact_body_15"
