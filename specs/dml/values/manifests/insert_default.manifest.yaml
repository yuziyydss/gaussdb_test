schema_version: 1
kind: "manifest"
id: "manifest_values_insert_default"
name: "VALUES insert_default"
status: "ready"
description: "仅覆盖所选有限值域；静态候选不等于实机验证。"
factor_ref: "values"
syntax_ref: "syntax_values"
suite_type: "positive"
strategy: "pairwise"
fixture_refs: []
identifier_policy: {}
bindings:
  context:
    - "values_context_insert"
  first_row:
    - "values_first_row_default"
expected:
  default: "success"
  scope: "syntax_and_semantics"
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
