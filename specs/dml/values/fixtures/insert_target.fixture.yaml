schema_version: 1
kind: "fixture"
id: "fixture_values_insert_target"
name: "VALUES insert_target"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "values"
requires_fixture_refs: []
provides:
  tables:
    -
      name: "t_values_test"
      persistence: "permanent"
      table_kind: "regular"
      columns:
        -
          name: "id"
          type: "INTEGER"
          nullable: true
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "CREATE TABLE t_values_test (id INTEGER DEFAULT 7);"
  teardown_sqls:
    - "DROP TABLE t_values_test;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
