schema_version: 1
kind: fixture
id: fixture_m_select_count_all_null_source
name: fixture_m_select_count_all_null_source
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_select
requires_fixture_refs: []
provides:
  tables:
  - name: m_select_count_ns.source_table
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
    - name: qty
      type: INTEGER
      nullable: true
seed:
  required: true
  rows:
  - id: 2
    qty: null
  - id: 2
    qty: null
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE SCHEMA m_select_count_ns;
  - CREATE TABLE m_select_count_ns.source_table (id INTEGER, qty INTEGER);
  - INSERT INTO m_select_count_ns.source_table (id,qty) VALUES (2,NULL),(2,NULL);
  teardown_sqls:
  - DROP TABLE m_select_count_ns.source_table RESTRICT;
  - DROP SCHEMA m_select_count_ns;
  note: 与六行count_source互斥的独占新模式/隔离连接fixture，不同时组合。两列真实INTEGER可空，只有两行(id=2,qty=NULL)的完整literal seed；不是空表，不解释DELETE/WHERE后的状态。仅M物理数据库授权后执行，模式初始不存在且非系统非用户同名；实际创建者记录归属。setup失败阻止目标和盲目teardown，只逆序清理本case实际创建成功的表及空模式。无CASCADE/DROP
    OWNED/吞异常/DDL回滚兜底，实际计数和BIGINT元数据仍须校准。
