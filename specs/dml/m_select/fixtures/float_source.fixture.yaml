schema_version: 1
kind: fixture
id: fixture_m_select_float_source
name: fixture_m_select_float_source
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_select
requires_fixture_refs: []
provides:
  tables:
  - name: m_select_approximate_ns.float_source
    persistence: permanent
    table_kind: regular
    columns:
    - name: qty
      type: FLOAT
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE SCHEMA m_select_approximate_ns;
  - CREATE TABLE m_select_approximate_ns.float_source (qty FLOAT);
  - INSERT INTO m_select_approximate_ns.float_source (qty) VALUES (1.5),(2.5);
  teardown_sqls:
  - DROP TABLE m_select_approximate_ns.float_source RESTRICT;
  - DROP SCHEMA m_select_approximate_ns;
  note: 仅已授权M物理数据库中的独占新模式/连接；模式事前不存在、非系统且非同名用户模式。实际声明裸FLOAT，不靠provides假报INTEGER；两个seed为有限1.5/2.5，不测舍入或越界。只按创建成功及实际归属记录逆序清理本case空模式/表，无CASCADE或DROP
    OWNED，不依赖DDL回滚。失败时不能继续目标或按名称删除他人对象；模式不再拥有本case之外对象才允许DROP。真实函数解析、数据插入/元数据与驱动结果仍待授权执行校准。
