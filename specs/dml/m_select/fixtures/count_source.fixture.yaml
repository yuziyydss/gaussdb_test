schema_version: 1
kind: fixture
id: fixture_m_select_count_source
name: fixture_m_select_count_source
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_select
requires_fixture_refs: []
provides:
  tables:
  - name: m_select_count_ns.source_table
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
    - name: qty
      type: INTEGER
      nullable: true
seed:
  required: true
  rows:
  - id: 1
    qty: null
  - id: 1
    qty: 1
  - id: 1
    qty: 2
  - id: 1
    qty: 2
  - id: 2
    qty: null
  - id: 2
    qty: null
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE SCHEMA m_select_count_ns;
  - CREATE TABLE m_select_count_ns.source_table (id INTEGER, qty INTEGER);
  - INSERT INTO m_select_count_ns.source_table (id,qty) VALUES (1,NULL),(1,1),(1,2),(1,2),(2,NULL),(2,NULL);
  teardown_sqls:
  - DROP TABLE m_select_count_ns.source_table RESTRICT;
  - DROP SCHEMA m_select_count_ns;
  note: 仅已授权M物理数据库中的独占新模式/隔离连接；模式初始不存在、非系统非用户同名。两个真实INTEGER可空列与六行seed逐项一致；不把缺失字段、NULL常量或空结果集当成列计数。只按实际创建成功与归属记录逆序清理本case表和空模式；setup失败阻止目标和盲目teardown。不用CASCADE、DROP
    OWNED、吞异常或DDL回滚兜底；不能清理他人对象。实际seed内容、函数解析及BIGINT驱动元数据仍须授权验证，本fixture不是执行许可。
