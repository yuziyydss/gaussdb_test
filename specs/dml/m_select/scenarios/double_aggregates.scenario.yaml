schema_version: 1
kind: scenario
id: scenario_m_select_double_aggregates
name: scenario_m_select_double_aggregates
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_select
fact_refs:
- m_select_fact_sum_result
- m_select_fact_min_result
- m_select_fact_max_result
- m_select_fact_approximate_source_gap
fixture_refs:
- fixture_m_select_double_source
preconditions:
- M-Compatibility；独立隔离测试空间
- 每一步实际函数身份分别核为M内建SUM/MIN/MAX；实际FLOAT/DOUBLE源列元数据已核验。
steps:
- id: sum
  sql: SELECT SUM(qty) AS result FROM m_select_approximate_ns.double_source;
- id: min
  sql: SELECT MIN(qty) AS result FROM m_select_approximate_ns.double_source;
- id: max
  sql: SELECT MAX(qty) AS result FROM m_select_approximate_ns.double_source;
oracles:
- kind: result_set
  step_id: sum
  expected:
  - - 4.0
- kind: manual_assertion
  step_id: sum
  expected: 在已核实M内建SUM身份下，返回列声明类型为DOUBLE；目录/驱动类型接口待校准，不以Python数值类型替代。
- kind: result_set
  step_id: min
  expected:
  - - 1.5
- kind: manual_assertion
  step_id: min
  expected: 在已核实M内建MIN身份下，返回列声明类型为DOUBLE；目录/驱动类型接口待校准，不以Python数值类型替代。
- kind: result_set
  step_id: max
  expected:
  - - 2.5
- kind: manual_assertion
  step_id: max
  expected: 在已核实M内建MAX身份下，返回列声明类型为DOUBLE；目录/驱动类型接口待校准，不以Python数值类型替代。
execution_requirements:
- database_authorization
- target_oracle_calibration
- isolated_connection
- close_case_connection
- per_step_oracle
