schema_version: 1
kind: scenario
id: scenario_m_select_avg_double
name: scenario_m_select_avg_double
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_select
fact_refs:
- m_select_fact_avg_result
- m_select_fact_avg_gap
fixture_refs:
- fixture_m_select_double_source
preconditions:
- M-Compatibility；独立隔离测试空间
- function_resolution=m_builtin_avg；实际源列和种子须与fixture一致，真实类型元数据待校准。
steps:
- id: qty
  sql: SELECT AVG(qty) AS result FROM m_select_approximate_ns.double_source;
oracles:
- kind: result_set
  step_id: qty
  expected:
  - - 2.0
- kind: manual_assertion
  step_id: qty
  expected: 实际解析M内建AVG，输出列类型应为DOUBLE；须校准目录/驱动元数据，不以Python值类型代替。
execution_requirements:
- database_authorization
- target_oracle_calibration
- isolated_connection
- close_case_connection
- per_step_oracle
