schema_version: 1
kind: scenario
id: scenario_m_select_count_all_null
name: scenario_m_select_count_all_null
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_select
fact_refs:
- m_select_fact_count_result
- m_select_fact_count_gap
fixture_refs:
- fixture_m_select_count_all_null_source
preconditions:
- M-Compatibility；独立隔离测试空间
- function_resolution=m_builtin_count；先核两行完整seed为(2,NULL),(2,NULL)，不得借六行或空表结果。
steps:
- id: count_qty_plain
  sql: SELECT COUNT(qty) AS result FROM m_select_count_ns.source_table;
- id: count_qty_all
  sql: SELECT COUNT(ALL qty) AS result FROM m_select_count_ns.source_table;
- id: count_qty_distinct
  sql: SELECT COUNT(DISTINCT qty) AS result FROM m_select_count_ns.source_table;
- id: count_star
  sql: SELECT COUNT(*) AS result FROM m_select_count_ns.source_table;
oracles:
- kind: result_set
  step_id: count_qty_plain
  expected:
  - - 0
- kind: manual_assertion
  step_id: count_qty_plain
  expected: 实际M内建COUNT输出BIGINT；两行qty均NULL不是空表，星号计2、字段计0，驱动/目录类型仍须校准。
- kind: result_set
  step_id: count_qty_all
  expected:
  - - 0
- kind: manual_assertion
  step_id: count_qty_all
  expected: 实际M内建COUNT输出BIGINT；两行qty均NULL不是空表，星号计2、字段计0，驱动/目录类型仍须校准。
- kind: result_set
  step_id: count_qty_distinct
  expected:
  - - 0
- kind: manual_assertion
  step_id: count_qty_distinct
  expected: 实际M内建COUNT输出BIGINT；两行qty均NULL不是空表，星号计2、字段计0，驱动/目录类型仍须校准。
- kind: result_set
  step_id: count_star
  expected:
  - - 2
- kind: manual_assertion
  step_id: count_star
  expected: 实际M内建COUNT输出BIGINT；两行qty均NULL不是空表，星号计2、字段计0，驱动/目录类型仍须校准。
execution_requirements:
- database_authorization
- target_oracle_calibration
- isolated_connection
- close_case_connection
- per_step_oracle
- ownership_scoped_cleanup
