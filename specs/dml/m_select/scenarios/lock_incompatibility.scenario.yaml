schema_version: 1
kind: scenario
id: scenario_m_select_lock_incompatibility
name: scenario_m_select_lock_incompatibility
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_select
fact_refs:
- m_select_fact_lock
fixture_refs:
- fixture_m_select_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: SELECT DISTINCT id FROM m_b01_source FOR UPDATE;
oracles:
- kind: target_error
  expected: DISTINCT 与行锁不兼容；目标 SQLSTATE 待校准
execution_requirements:
- database_authorization
- target_oracle_calibration
