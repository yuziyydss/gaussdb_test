schema_version: 1
kind: scenario
id: scenario_m_select_group_mode
name: scenario_m_select_group_mode
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_select
fact_refs:
- m_select_fact_group_mode
fixture_refs:
- fixture_m_select_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 显式区分 ONLY_FULL_GROUP_BY 开关；补函数依赖和分组列合同
oracles:
- kind: result_set
  expected: 非分组投影的合法性按 M 模式条件判断，不沿用通用模式一律拒绝
execution_requirements:
- database_authorization
- target_oracle_calibration
