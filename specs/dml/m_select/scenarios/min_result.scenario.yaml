schema_version: 1
kind: scenario
id: scenario_m_select_min_result
name: scenario_m_select_min_result
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_select
fact_refs:
- m_select_fact_min_result
- m_select_fact_min_gap
fixture_refs:
- fixture_m_select_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
- function_resolution=m_builtin_min；实际函数身份与驱动返回类型待环境验证
steps:
- sql: SELECT MIN(qty) AS result FROM m_b01_source;
oracles:
- kind: result_set
  expected:
  - - 10
execution_requirements:
- database_authorization
- target_oracle_calibration
