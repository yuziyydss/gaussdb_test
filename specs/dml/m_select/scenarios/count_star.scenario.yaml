schema_version: 1
kind: scenario
id: scenario_m_select_count_star
name: scenario_m_select_count_star
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_select
fact_refs:
- m_select_fact_count_result
- m_select_fact_count_gap
fixture_refs:
- fixture_m_select_count_source
preconditions:
- M-Compatibility；独立隔离测试空间
- function_resolution=m_builtin_count；复用六行含NULL/重复值源表，只验证独立COUNT(*)，不推导ALL *、DISTINCT *或窗口形式。
steps:
- id: count_star
  sql: SELECT COUNT(*) AS result FROM m_select_count_ns.source_table;
oracles:
- kind: result_set
  step_id: count_star
  expected:
  - - 6
- kind: manual_assertion
  step_id: count_star
  expected: 实际M内建COUNT(*)输出BIGINT；须校准驱动/目录类型及六行seed，含NULL行不能按COUNT(qty)=3计算。
execution_requirements:
- database_authorization
- target_oracle_calibration
- isolated_connection
- close_case_connection
- per_step_oracle
- ownership_scoped_cleanup
