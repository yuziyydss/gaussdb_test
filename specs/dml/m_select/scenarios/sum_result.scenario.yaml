schema_version: 1
kind: scenario
id: scenario_m_select_sum_result
name: scenario_m_select_sum_result
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_select
fact_refs:
- m_select_fact_sum_result
fixture_refs:
- fixture_m_select_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
- function_resolution=m_builtin_sum；实际函数身份待环境验证
steps:
- sql: SELECT SUM(qty) AS total FROM m_b01_source;
oracles:
- kind: result_set
  expected:
  - - 60
execution_requirements:
- database_authorization
- target_oracle_calibration
