schema_version: 1
kind: scenario
id: scenario_delete_cte_and_hint
name: DELETE CTE 物化与 Hint 行为
status: planned
description: 静态清单已生成有限 CTE 形态；本场景验证数据修改副作用、MATERIALIZED/NOT MATERIALIZED 计划和第一个 hint 块生效规则。
factor_ref: delete
fixture_refs: [fixture_delete_target, fixture_delete_aux]
fact_refs: [delete_fact_with_declaration, delete_fact_with_recursive, delete_fact_with_body_grammar, delete_fact_with_materialization, delete_fact_plan_hint_position, delete_fact_plan_hint_first_block, delete_open_plan_hint_profile, delete_open_recursive_cte_depth, delete_example_with]
preconditions: [取得 SQL 调优指南中的具体 hint, 准备可观察的 CTE 引用次数和 DML 副作用]
steps: [SELECT/VALUES/DML CTE 回放, 递归 CTE 回放, MATERIALIZED 对比, NOT MATERIALIZED 对比, 多 hint 注释块对比]
oracles: [EXPLAIN 计划节点, 目标行与 CTE 副作用, 实际生效 Hint]
execution_requirements: [EXPLAIN, 可用具体 Plan Hint, 数据库行为回放]
