schema_version: 1
kind: scenario
id: scenario_delete_view_subquery
name: DELETE 视图与子查询保留键矩阵
status: planned
description: 回放本章单表、READ ONLY、连接、CHECK OPTION 和重复基表示例；引用 CREATE VIEW 已导出的保留键和只读定义，完整行为矩阵仍待验证。
factor_ref: delete
fixture_refs: [fixture_delete_view, fixture_delete_subquery]
fact_refs: [delete_fact_subquery_grammar, delete_fact_from_item_grammar, delete_fact_subquery_target, delete_fact_view_target, delete_fact_view_subquery_restrictions, delete_fact_supported_table_types, delete_fact_join_target_behavior, delete_fact_duplicate_base_check_restriction, delete_fact_system_view_restriction, delete_fact_read_only_error, delete_fact_key_preserved_error, delete_open_preserved_key_definition, delete_open_subquery_depth, delete_example_subquery, delete_example_view, "create_view::cv_fact_key_preserved_definition", "create_view::cv_fact_read_only_behavior"]
preconditions: [创建 READ ONLY/CHECK OPTION/连接/重复基表目标, 按引用的 CREATE VIEW 保留键规则构造数据]
steps: [单表视图与子查询, READ ONLY, 单保留键连接, 多保留键连接, 重复基表 CHECK OPTION, 系统视图, 文档所列不可更新顶层结构]
oracles: [被删基表身份与行数, READ ONLY 精确错误文本, no key-preserved table 精确错误文本, 目标不可更新错误]
execution_requirements: [视图生命周期, 多表主键 Fixture, 结果行身份校验]
