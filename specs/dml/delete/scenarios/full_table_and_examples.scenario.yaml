schema_version: 1
kind: scenario
id: scenario_delete_full_table_and_examples
name: DELETE 全表行为、condition 与文档示例
status: planned
description: 顺序回放基础、WITH、子查询和视图示例，另将全表 DELETE 与 TRUNCATE 建议作为行为/性能对比而非语法等价。
factor_ref: delete
fixture_refs: [fixture_delete_target, fixture_delete_aux, fixture_delete_view, fixture_delete_subquery]
fact_refs: [delete_fact_purpose, delete_fact_delete_all_preserves_structure, delete_fact_condition_behavior, delete_fact_truncate_recommendation, delete_example_core, delete_example_with, delete_example_subquery, delete_example_view]
preconditions: [按示例创建对象和种子数据, 准备可识别的表结构和行数]
steps: [按 WHERE 删除, 省略 WHERE 删除全部行, 数值 condition 风险对照, WITH 示例, 子查询示例, 视图示例, 全表 DELETE 与 TRUNCATE 性能对比]
oracles: [文档展示的行集, 省略 WHERE 后零行且表仍存在, condition 布尔转换结果, 性能指标仅作建议证据]
execution_requirements: [示例对象生命周期, 查询结果捕获, 性能基准环境]
