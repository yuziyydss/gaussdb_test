schema_version: 1
kind: scenario
id: scenario_delete_stream_concurrency
name: DELETE Stream 计划与同行 UPDATE
status: planned
description: 在可产生 Stream 子查询计划的部署中验证被删行同时 UPDATE 的禁止边界。
factor_ref: delete
fixture_refs: [fixture_delete_target]
fact_refs: [delete_fact_stream_update_restriction]
preconditions: [可产生 Stream 计划的部署, 两并发会话和可观测同一行]
steps: [确认 DELETE 子查询 Stream 计划, 并发 UPDATE 同一行, 提交与回滚分支]
oracles: [不支持操作的目标错误类别, 最终行状态, 无非目标部分写入]
execution_requirements: [Stream 计划环境, 并发会话, 事务隔离控制]
