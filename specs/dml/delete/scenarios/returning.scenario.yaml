schema_version: 1
kind: scenario
id: scenario_delete_returning
name: DELETE RETURNING 删除后输出
status: planned
description: 验证整行、多表达式与输出别名在行删除后计算，并与实际被删行对齐。
factor_ref: delete
fixture_refs: [fixture_delete_target]
fact_refs: [delete_fact_returning_grammar, delete_fact_returning_behavior]
preconditions: [准备包含可区分值的目标行]
steps: [RETURNING *, RETURNING id, note AS deleted_note, 对比删除前后数据]
oracles: [返回值来自被删行, 输出列名, 返回行数与删除行数一致]
execution_requirements: [结果集与元数据捕获]
