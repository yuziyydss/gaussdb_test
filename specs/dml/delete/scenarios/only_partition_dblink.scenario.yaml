schema_version: 1
kind: scenario
id: scenario_delete_only_partition_dblink
name: DELETE ONLY、分区、基表类型与 DBLINK
status: planned
description: 验证 ONLY 继承边界、PARTITION/SUBPARTITION/partitions_clause、视图基表类型全域与远端表；当前缺少必要 Fixture 及跨章子语法。
factor_ref: delete
fixture_refs: [fixture_delete_target]
fact_refs: [delete_fact_only_semantics, delete_fact_partition_grammar, delete_fact_partitions_grammar, delete_fact_partitions_environment, delete_fact_supported_table_types, delete_open_partition_fixture, delete_open_table_type_fixtures, delete_open_dblink_profile]
preconditions: [继承表族, 一级与二级分区表, B 兼容数据库, 各种存储/临时表类型, DATABASE LINK 远端环境]
steps: [ONLY 与省略 ONLY, PARTITION 名称/FOR, SUBPARTITION 名称/FOR, B 模式多分区, 逐个基表类型, 远端 DELETE]
oracles: [父表和子表数据差异, 目标分区数据, 环境不匹配时 skip, 远端数据变化]
execution_requirements: [继承表 Fixture, 二级分区 Fixture, 多兼容模式, 远端 GaussDB]
