schema_version: 1
kind: scenario
id: scenario_delete_permissions
name: DELETE 权限矩阵
status: planned
description: 分别验证目标 DELETE 权限、DELETE ANY TABLE/模式边界、USING 和 condition 引用表 SELECT 权限。
factor_ref: delete
fixture_refs: [fixture_delete_target, fixture_delete_lookup]
fact_refs: [delete_fact_permissions]
preconditions: [创建具有不同 DELETE 和 SELECT 权限组合的用户, 切换三权分立开关]
steps: [基础 DELETE, DELETE USING, condition 引用关联表, DELETE ANY TABLE 路径]
oracles: [按文档权限组合成功或以目标权限错误拒绝]
execution_requirements: [多用户, GRANT/REVOKE, 系统管理员和三权分立配置]
