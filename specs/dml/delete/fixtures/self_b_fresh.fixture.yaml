schema_version: 1
kind: fixture
id: fixture_delete_self_b_fresh
name: B DELETE self-USING fresh普通表
status: ready
factor_ref: delete
description: 显式CREATE及三行seed，无预清理；每case独占生命周期，不与旧通用表复用。
provides:
  tables:
  - name: t_delete_self_b
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
    - name: note
      type: VARCHAR(64)
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE t_delete_self_b (id INTEGER, note VARCHAR(64));
  - INSERT INTO t_delete_self_b VALUES (1,'one'),(2,'two'),(3,'three');
  teardown_sqls:
  - DROP TABLE t_delete_self_b;
  note: 未来执行须验证独占用户模式、对象名未占用与本case创建成功；setup失败不授权清理。无DROP OWNED或CASCADE。
