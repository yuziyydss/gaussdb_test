schema_version: 1
kind: fixture
id: fixture_delete_subquery
name: DELETE 单表子查询基表
status: ready
description: 为本章已展示成功的单表子查询目标提供最小基表，不表示保留键全域。
factor_ref: delete
provides:
  tables:
    - name: t_delete_subquery_base
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: note, type: VARCHAR(64), nullable: true}
seed:
  required: true
  rows:
    - {id: 1, note: keep}
    - {id: 2, note: delete_me}
execution:
  status: ready
  mode: auto
  note: 由 Fixture 编译器生成单表子查询目标的幂等生命周期 SQL。
