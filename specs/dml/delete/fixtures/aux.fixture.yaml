schema_version: 1
kind: fixture
id: fixture_delete_aux
name: DELETE 第二目标与 DML CTE 表
status: ready
description: 为多表目标列表和 INSERT/UPDATE/DELETE CTE 代表形态提供独立表。
factor_ref: delete
provides:
  tables:
    - name: t_delete_aux
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: note, type: VARCHAR(64), nullable: true}
seed:
  required: true
  rows:
    - {id: 2, note: paired}
    - {id: 9, note: cte_row}
execution:
  status: ready
  mode: auto
  note: 由 Fixture 编译器生成幂等生命周期 SQL。
