schema_version: 1
kind: fixture
id: fixture_delete_target
name: DELETE 基础目标表
status: ready
description: 为单表、ONLY/*、别名、排序、LIMIT 与 RETURNING 提供最小两列表及可删除数据。
factor_ref: delete
provides:
  tables:
    - name: t_delete_target
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: note, type: VARCHAR(64), nullable: true}
seed:
  required: true
  rows:
    - {id: 1, note: keep}
    - {id: 2, note: delete_me}
    - {id: 3, note: cte_source}
execution:
  status: ready
  mode: auto
  note: 由 Fixture 编译器生成幂等 DROP、CREATE、INSERT 与 teardown。
