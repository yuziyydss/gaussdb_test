schema_version: 1
kind: "fixture"
id: "fixture_select_into_target"
name: "SELECT INTO target"
status: "ready"
description: "独占测试对象或会话状态；依赖先 setup、后 teardown。"
factor_ref: "select_into"
requires_fixture_refs:
  - "fixture_select_into_source"
provides: {}
seed:
  required: false
  rows: []
execution:
  status: "ready"
  mode: "explicit"
  setup_sqls:
    - "BEGIN;"
  teardown_sqls:
    - "ROLLBACK;"
    - "DROP TABLE IF EXISTS t_into_candidate CASCADE;"
  note: "本阶段仅静态编译，禁止在共享业务会话执行。"
