schema_version: 1
kind: scenario
id: scenario_select_pagination_behavior
name: SELECT LIMIT/OFFSET/FETCH 分页行为
status: planned
description: 对齐 1.13.19.3.4 的 LIMIT count/ALL、offset,count 和 FETCH FIRST/NEXT ... ONLY [OFFSET]。
factor_ref: select
fixture_refs: [fixture_select_source]
fact_refs:
  - select_fact_limit_offset_fetch
  - select_example_pagination
  - select_fact_limit_count_all
  - select_fact_limit_offset_count
  - select_fact_limit_rownum_restriction
  - select_fact_offset_form
  - select_fact_offset_start
  - select_fact_fetch_form_default
preconditions: [存在稳定 ORDER BY 键与足够多的 seed 行]
steps: [LIMIT count, LIMIT offset_count, LIMIT ALL, OFFSET ROWS, FETCH FIRST, FETCH NEXT 后置 OFFSET]
oracles: [返回行数正确, 起始偏移正确, FETCH 未给 count 时返回 1 行]
execution_requirements: [扩充 seed 到至少 20 行]
