schema_version: 1
kind: scenario
id: scenario_select_hint_plan
name: SELECT indexscan/rows Hint 计划
status: planned
description: 只验证 SELECT 章末明示的 indexscan(t1 it1) 和 rows(t1 t2 #5) 两个代表 Hint。
factor_ref: select
fixture_refs: [fixture_select_hint_source]
fact_refs: [select_fact_plan_hint, select_example_hint]
preconditions: [索引 it_select_hint1 存在, 可执行 EXPLAIN]
steps: [对 indexscan profile 执行 EXPLAIN, 对 rows profile 执行 EXPLAIN, 比较无 Hint 计划]
oracles: [第一个计划使用目标索引, rows 计划估算行数反映 5, 不将未展开 Hint 推广为已覆盖]
execution_requirements: [EXPLAIN 权限]
