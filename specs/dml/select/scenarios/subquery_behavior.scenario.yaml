schema_version: 1
kind: scenario
id: scenario_select_subquery_behavior
name: SELECT 单行、多行与关联子查询
status: planned
description: 对齐 1.13.19.3.7 的单行比较、IN/ANY/ALL/SOME、关联子查询和派生表改写。
factor_ref: select
fixture_refs: [fixture_select_source, fixture_select_right_source]
fact_refs: [select_fact_subquery, select_example_subquery]
preconditions: [两表列数与数值类型契约已确认]
steps: [单行比较, IN, ANY, ALL, SOME, 关联查询, 派生表等价改写]
oracles: [结果行集与子查询语义一致, ANY/ALL 与 MIN/MAX 代表改写一致]
execution_requirements: [数据库行为执行]
