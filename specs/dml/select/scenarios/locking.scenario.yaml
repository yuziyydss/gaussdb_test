schema_version: 1
kind: scenario
id: scenario_select_locking
name: SELECT 行锁并发行为
status: planned
description: 用两个会话验证四种锁模式、等待、NOWAIT、WAIT 和 SKIP LOCKED。
factor_ref: select
fixture_refs: [fixture_select_source]
fact_refs: [select_fact_locking]
preconditions: [两个并发事务和具备 UPDATE 权限的用户]
steps: [会话 A 加锁, 会话 B 使用各模式和等待选项]
oracles: [阻塞关系、超时、立即错误和跳过结果符合锁兼容矩阵]
execution_requirements: [多会话, 事务控制, 可测时间]
