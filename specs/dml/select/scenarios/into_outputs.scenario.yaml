schema_version: 1
kind: scenario
id: scenario_select_into_outputs
name: SELECT INTO 用户变量与文件
status: planned
description: 验证三处 INTO 位置、B 兼容用户变量、OUTFILE/DUMPFILE 格式和文件安全边界。
factor_ref: select
fixture_refs: [fixture_select_source]
fact_refs: [select_fact_into]
preconditions: [B 兼容模式, enable_set_variables, 隔离临时目录]
steps: [三处用户变量 INTO, OUTFILE, DUMPFILE]
oracles: [变量值正确, 文件字节和分隔符正确, 非法权限或路径被拒绝]
execution_requirements: [文件系统沙箱, B 兼容模式]
