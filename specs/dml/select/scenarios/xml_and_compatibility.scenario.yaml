schema_version: 1
kind: scenario
id: scenario_select_xml_and_compatibility
name: SELECT XMLTABLE/PIVOT/UNPIVOT 兼容模式
status: planned
description: 在 A、PG 和其他模式下验证 XMLTABLE、PIVOT、UNPIVOT 的语法与差异行为。
factor_ref: select
fixture_refs: [fixture_select_source]
fact_refs: [select_fact_xmltable, select_fact_unpivot, select_fact_pivot]
preconditions: [A 兼容数据库, PG 兼容数据库, 保真 XML 示例]
steps: [XMLTABLE 变体, PIVOT, PIVOT XML, UNPIVOT]
oracles: [按兼容模式成功或命中目标错误, PIVOT XML 不产生功能结果]
execution_requirements: [多兼容模式实例, XML/XMLTYPE]
