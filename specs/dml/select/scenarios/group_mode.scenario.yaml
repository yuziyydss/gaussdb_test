schema_version: 1
kind: scenario
id: scenario_select_group_mode
name: SELECT ONLY_FULL_GROUP_BY 行为矩阵
status: planned
description: 验证模式开关、主键/唯一非空键和 JOIN 方向对非聚集列检查的影响。
factor_ref: select
fixture_refs: [fixture_select_source, fixture_select_right_source]
fact_refs: [select_fact_group_by]
preconditions: [带主键、唯一非空键和无键的表变体]
steps: [切换 sql_mode, 执行单表和各类 JOIN 分组查询]
oracles: [按文档依赖关系成功或报错]
execution_requirements: [可设置 sql_mode]
