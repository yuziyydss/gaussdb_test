schema_version: 1
kind: scenario
id: scenario_select_join_behavior
name: SELECT 五种 JOIN 与连续连接
status: planned
description: 对齐 1.13.19.3.6 的 INNER/LEFT/RIGHT/FULL 结果以及连续多次 JOIN 嵌套顺序。
factor_ref: select
fixture_refs: [fixture_select_source, fixture_select_right_source]
fact_refs: [select_fact_join, select_example_join]
preconditions: [左右表同时含匹配行和未匹配行]
steps: [INNER JOIN, LEFT JOIN, RIGHT JOIN, FULL JOIN, CROSS JOIN, NATURAL JOIN, 连续 JOIN]
oracles: [匹配行正确, 外侧未匹配行用 NULL 扩展, CROSS JOIN 行数等于笛卡尔积]
execution_requirements: [数据库行为执行]
