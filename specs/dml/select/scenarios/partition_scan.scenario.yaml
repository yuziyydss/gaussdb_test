schema_version: 1
kind: scenario
id: scenario_select_partition_scan
name: SELECT 指定分区与索引扫描
status: planned
description: 验证分区名、分区键定位、二级分区以及不使用全局索引扫描的计划行为。
factor_ref: select
fixture_refs: [fixture_select_partitioned_source]
fact_refs: [select_fact_partition_global_index]
preconditions: [两级分区表 p1/sp1, 本地索引和全局索引]
steps: [PARTITION, PARTITION FOR, SUBPARTITION, EXPLAIN]
oracles: [结果只来自目标分区, 计划不执行全局索引扫描]
execution_requirements: [可声明分区结构的 Fixture V2]
