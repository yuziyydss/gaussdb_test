schema_version: 1
kind: scenario
id: scenario_select_where_in_plan_cache
name: SELECT WHERE IN 计划缓存压力
status: planned
description: 对比不同候选数量和 SQL 模板复用方式下的计划数量及内存趋势。
factor_ref: select
fixture_refs: [fixture_select_source]
fact_refs: [select_fact_where_in_performance]
preconditions: [开启可观测计划缓存, 构造不同 IN 列表]
steps: [小于等于 100, 大于 100, 参数化与拼接 SQL]
oracles: [记录计划复用率和内存趋势，不把建议阈值误判为语法错误]
execution_requirements: [计划缓存观测接口]
