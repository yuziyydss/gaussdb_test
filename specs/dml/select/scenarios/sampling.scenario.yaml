schema_version: 1
kind: scenario
id: scenario_select_sampling
name: SELECT TABLESAMPLE 行为
status: planned
description: 验证三种采样方式的范围以及 REPEATABLE 的可重复性。
factor_ref: select
fixture_refs: [fixture_select_source]
fact_refs: [select_fact_tablesample]
preconditions: [准备足够多的数据块和行]
steps: [SYSTEM, BERNOULLI, HYBRID, 相同与不同 seed]
oracles: [结果规模在合法范围, 相同 seed 在表未变化时结果一致]
execution_requirements: [大样本数据]
