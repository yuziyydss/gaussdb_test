schema_version: 1
kind: scenario
id: scenario_select_flashback
name: SELECT TIMECAPSULE 闪回
status: planned
description: 制造历史版本后验证 TIMESTAMP、真实 CSN、过旧快照和 DDL 跨越限制。
factor_ref: select
fixture_refs: [fixture_select_flashback_source]
fact_refs: [select_fact_flashback]
preconditions: [Ustore, undo retention 大于零, 记录真实 CSN 和时间点]
steps: [插入旧值, 记录快照, 更新, 两种闪回查询, DDL 后重试]
oracles: [返回历史值, 过旧或跨 DDL 时命中目标错误]
execution_requirements: [Ustore, gs_txn_snapshot, 可控时间]
