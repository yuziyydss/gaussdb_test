schema_version: 1
kind: fixture
id: fixture_select_source
name: SELECT 基础三列源表
status: ready
description: 为基础过滤、聚合、排序、采样、PIVOT/UNPIVOT 和锁定查询提供最小对象能力。
factor_ref: select
provides:
  tables:
    - name: t_select_source
      persistence: permanent
      table_kind: regular
      columns:
        - {name: col_1, type: INTEGER, nullable: false}
        - {name: col_2, type: INTEGER, nullable: true}
        - {name: name, type: VARCHAR(64), nullable: true}
seed:
  required: true
  rows:
    - {col_1: 1, col_2: 10, name: alpha}
    - {col_1: 2, col_2: 20, name: Beta}
    - {col_1: 3, col_2: null, name: gamma}
execution:
  status: ready
  mode: auto
  note: 由 Fixture 编译器生成幂等 DROP、CREATE、seed 和 teardown。
