schema_version: 1
kind: fixture
id: fixture_select_flashback_source
name: SELECT Ustore 闪回源表
status: needs_review
description: 为 TIMECAPSULE TIMESTAMP/CSN 查询声明 Ustore 和历史版本能力。
factor_ref: select
provides:
  tables:
    - name: t_select_flashback
      persistence: permanent
      table_kind: ustore_regular
      columns:
        - {name: col_1, type: INTEGER, nullable: false}
        - {name: col_2, type: INTEGER, nullable: true}
seed:
  required: true
  rows:
    - {col_1: 1, col_2: 10}
execution:
  status: ready
  mode: auto
  note: 自动创建 Ustore 表和 seed；有效时间点与真实 CSN 仍由闪回 scenario 注入。
