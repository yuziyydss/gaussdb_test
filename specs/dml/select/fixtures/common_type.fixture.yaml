schema_version: 1
kind: fixture
id: fixture_select_common_type
name: fixture_select_common_type
status: needs_review
factor_ref: select
provides:
  tables:
  - name: g_common_source
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
    - name: qty
      type: INTEGER
      nullable: true
    - name: note
      type: TEXT
      nullable: true
    - name: flag
      type: BOOLEAN
      nullable: true
seed:
  required: true
  rows:
  - id: 1
    qty: 10
    note: a
    flag: true
  - id: 2
    qty: 20
    note: b
    flag: false
  - id: 3
    qty: null
    note: null
    flag: null
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE g_common_source (id INTEGER, qty INTEGER, note TEXT, flag BOOLEAN);
  - INSERT INTO g_common_source VALUES (1,10,'a',TRUE),(2,20,'b',FALSE),(3,NULL,NULL,NULL);
  teardown_sqls:
  - DROP TABLE g_common_source;
  note: 独占新schema、名称须不存在；仅清理本轮确认创建对象。当前不执行数据库。
description: 有限PG类型解析与真实资产；不承诺全章覆盖或数据库结果。
