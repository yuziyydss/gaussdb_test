schema_version: 1
kind: fixture
id: fixture_select_right_source
name: SELECT 连接右表
status: ready
description: 为 JOIN 和集合运算提供列数及类型兼容的第二张表。
factor_ref: select
provides:
  tables:
    - name: t_select_right
      persistence: permanent
      table_kind: regular
      columns:
        - {name: col_1, type: INTEGER, nullable: false}
        - {name: col_2, type: INTEGER, nullable: true}
        - {name: name, type: VARCHAR(64), nullable: true}
seed:
  required: true
  rows:
    - {col_1: 1, col_2: 100, name: one}
    - {col_1: 4, col_2: 400, name: four}
execution:
  status: ready
  mode: auto
  note: 由 Fixture 编译器生成幂等 DROP、CREATE、seed 和 teardown。
