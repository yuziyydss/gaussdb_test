schema_version: 1
kind: manifest
id: manifest_select_environment_positive
name: SELECT 异构环境静态候选 profile
status: needs_review
description: >-
  生成文档确认的条件语法；本清单同时包含 A 模式、闪回历史、分区、编码和
  锁权限等异构前置，因此只是 syntax_only 静态库存，不得作为自动执行清单。
  后续执行前必须按同质 environment requirement 拆分为独立 manifest。
factor_ref: select
syntax_ref: syntax_select_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  query_profile:
    - select_tablesample_system
    - select_tablesample_bernoulli
    - select_tablesample_hybrid
    - select_flashback_timestamp
    - select_flashback_csn
    - select_partition_name
    - select_partition_for
    - select_subpartition
    - select_unpivot
    - select_pivot
    - select_xmltable_a_sequence
    - select_legacy_outer_join
    - select_hierarchy
    - select_hierarchy_nocycle_siblings
    - select_nls_sort
    - select_for_update
    - select_for_no_key_update
    - select_for_share
    - select_for_key_share
    - select_lock_nowait
    - select_lock_wait
    - select_lock_skip_locked
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
