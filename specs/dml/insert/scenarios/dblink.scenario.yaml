schema_version: 1
kind: scenario
id: scenario_insert_dblink
name: INSERT DATABASE LINK 远端目标
status: planned
description: 等被引用章节补齐目标拼写、连接对象与执行环境后验证远端插入。
factor_ref: insert
fixture_refs: []
fact_refs: [insert_fact_target_table, insert_open_dblink_profile]
preconditions: [取得 DATABASE LINK 章节, 配置远端数据库与凭据]
steps: [按补充语法执行远端 INSERT, 验证 ON CONFLICT 对 DBLINK 的限制]
oracles: [远端数据变化, 目标不支持错误类别]
execution_requirements: [远端 GaussDB, DATABASE LINK 配置]
