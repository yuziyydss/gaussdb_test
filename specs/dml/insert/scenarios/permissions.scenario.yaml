schema_version: 1
kind: scenario
id: scenario_insert_permissions
name: INSERT 权限矩阵
status: planned
description: 分别验证普通 INSERT、RETURNING、INSERT SELECT 和冲突更新所需权限。
factor_ref: insert
fixture_refs: [fixture_insert_target, fixture_insert_unique_target, fixture_insert_source]
fact_refs: [insert_fact_permissions]
preconditions: [创建具有 INSERT/SELECT/UPDATE 不同组合权限的测试用户]
steps: [普通 INSERT, INSERT RETURNING, INSERT SELECT, ON DUPLICATE KEY UPDATE]
oracles: [按文档权限矩阵成功或以目标权限错误拒绝]
execution_requirements: [多用户, GRANT 和 REVOKE 管理]

