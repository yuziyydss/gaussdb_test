schema_version: 1
kind: scenario
id: scenario_insert_performance
name: INSERT 批量与索引性能建议
status: planned
description: 对批量 VALUES 与 batch_insert_index_types 的适用条件、退化风险和日志建议做基准，不把性能指导当成功断言。
factor_ref: insert
fixture_refs: []
fact_refs: [insert_fact_performance_values, insert_fact_performance_index, insert_fact_performance_risks]
preconditions: [UBTree RCR 索引, JDBC executeBatch, INSERT SELECT, COPY 数据集]
steps: [合并和逐行 VALUES 对比, 开关 batch_insert_index_types, 递增和分散数据对比, SMP 对比]
oracles: [吞吐量, 延迟, EXPLAIN Batch Insert 标记, BIINDEX 日志]
execution_requirements: [性能环境, JDBC, 文件系统, GUC 与日志权限]
