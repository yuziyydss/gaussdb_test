schema_version: 1
kind: scenario
id: scenario_insert_returning
name: INSERT RETURNING 输出与权限
status: planned
description: 验证 RETURNING 在行插入后的表达式计算、整行输出、别名和 SELECT 权限要求；当前仅生成静态 SQL。
factor_ref: insert
fixture_refs: [fixture_insert_target]
fact_refs: [insert_fact_returning, insert_fact_returning_output, insert_fact_permissions]
preconditions: [准备可观察默认值和表达式的目标表, 准备有无 SELECT 权限的用户]
steps: [RETURNING 星号, 返回目标字段表达式, 输出别名, 撤销 SELECT 权限后重试]
oracles: [插入后实际值, 输出列名, 目标权限错误类别]
execution_requirements: [捕获结果集元数据, 多用户授权管理]
