schema_version: 1
kind: scenario
id: scenario_insert_autoincrement_omitted
name: B INSERT省略自增列的目标校准
status: planned
description: 与显式NULL零值DEFAULT分别重建资产，不把类型映射成功等同计数器行为验证。
factor_ref: insert
fixture_refs: [fixture_insert_autoincrement_fresh]
fact_refs: [insert_fact_column_mapping, 'create_table::ct_fact_auto_increment_semantics', 'create_table::ct_fact_auto_increment_nontransactional']
preconditions: [独立fresh B用户Schema, 无历史写入或并发消费者, 隐式序列和索引的真实归属可核验]
steps:
- id: insert_omitted
  candidate:
    manifest_ref: manifest_insert_autoincrement_omitted
    params: {source_profile: insert_source_autoincrement_omitted}
oracles:
- {kind: manual_assertion, step_id: insert_omitted, expected: 校准实际受影响行数、id自动分配和note为1；核实际计数器身份、缓存与失败非回滚边界。不能以省略列映射静态通过声称精确id值已验证。}
execution_requirements: [database_authorization, target_oracle_calibration, case_owned_cleanup]
