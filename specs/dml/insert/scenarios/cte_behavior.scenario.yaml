schema_version: 1
kind: scenario
id: scenario_insert_cte_behavior
name: INSERT CTE 行数与物化行为
status: planned
description: 验证 DML CTE 回显行数以及 MATERIALIZED/NOT MATERIALIZED 对计划的影响。
factor_ref: insert
fixture_refs: [fixture_insert_target, fixture_insert_source]
fact_refs: [insert_fact_cte_syntax, insert_fact_cte_behavior]
preconditions: [准备可 RETURNING 的 DML CTE 和可重复引用的 SELECT CTE]
steps: [执行 DML CTE INSERT, 比较 MATERIALIZED, 比较 NOT MATERIALIZED, 比较默认策略]
oracles: [命令标签行数, EXPLAIN 计划节点和数据结果]
execution_requirements: [捕获命令标签, EXPLAIN]

