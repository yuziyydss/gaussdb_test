schema_version: 1
kind: scenario
id: scenario_insert_view_subquery
name: INSERT 可更新视图与子查询
status: planned
description: 验证直接基表列、保留键、CHECK OPTION、READ ONLY、触发器和不支持顶层结构。
factor_ref: insert
fixture_refs: [fixture_insert_view_target]
fact_refs: [insert_fact_view_subquery_grammar, insert_fact_subquery_grammar, insert_fact_view_subquery_constraints]
preconditions: [创建单表和多表视图/子查询, CHECK OPTION, READ ONLY, 不同触发器组合]
steps: [插入可更新列, 插入非保留键列, 违反 CHECK OPTION, 写只读目标, 写系统视图]
oracles: [目标基表变化, CHECK OPTION 错误, 只读错误, 不可更新目标错误]
execution_requirements: [视图生命周期, 多表 Fixture]

