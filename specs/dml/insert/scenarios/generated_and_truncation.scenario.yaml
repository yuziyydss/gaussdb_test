schema_version: 1
kind: scenario
id: scenario_insert_generated_and_truncation
name: 生成列与 C 模式截断行为
status: planned
description: 历史扩展场景保留：两个INT相加的STORED生成列已拆到finite_generated独立场景；任意生成表达式及C模式不同编码的截断仍需专用环境，不由有限代表宣布通过。
factor_ref: insert
fixture_refs: []
fact_refs: [insert_fact_generated_column, insert_fact_truncation, insert_fact_type_conversion]
preconditions: [创建生成列表, 创建 C 兼容数据库, 配置 td_compatible_truncation, 准备 UTF8 与字节编码环境]
steps: [直接写生成列, 写 DEFAULT, 插入超长 ASCII, 插入跨截断位置的多字节字符]
oracles: [目标错误类别, 生成值, 截断后长度和内容]
execution_requirements: [多兼容模式数据库, 可修改 GUC]
