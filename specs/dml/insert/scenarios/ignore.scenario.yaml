schema_version: 1
kind: scenario
id: scenario_insert_ignore
name: INSERT IGNORE 错误降级矩阵
status: planned
description: 在限定 B/5.7/s1 环境中验证九类错误降级、对象限制和完整 OID 零值表；静态包不推测执行结果。
factor_ref: insert
fixture_refs: []
fact_refs: [insert_fact_ignore_semantics, insert_fact_ignore_environment, insert_fact_ignore_restrictions, insert_fact_ignore_zero_values, insert_open_ignore_fixture_matrix, insert_example_ignore]
preconditions: [B 兼容数据库, b_format_version=5.7, b_format_dev_version=s1, 各类约束与数据类型 Fixture]
steps: [NOT NULL, 唯一冲突, 无匹配分区, 指定分区不匹配, 多行子查询, 数据过长, 时间溢出, 除零, 非法值]
oracles: [WARNING 类别, INSERT 行数, 实际零值或跳过结果]
execution_requirements: [B 模式, 可修改会话参数, 大型类型矩阵]
