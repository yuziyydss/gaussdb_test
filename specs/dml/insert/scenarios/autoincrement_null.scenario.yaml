schema_version: 1
kind: scenario
id: scenario_insert_autoincrement_null
name: B INSERT NULL自增分配目标校准
status: planned
description: NULL触发身份与NOT NULL赋值不同；不把初值声明当成实测结果，不复用被消耗计数器。
factor_ref: insert
fixture_refs: [fixture_insert_autoincrement_fresh]
fact_refs: [insert_fact_expression, 'create_table::ct_fact_auto_increment_semantics', 'create_table::ct_fact_auto_increment_nontransactional']
preconditions: [独立fresh B用户Schema, 无历史写入或并发消费者, 隐式序列和索引的真实归属可核验]
steps:
- id: insert_null
  candidate:
    manifest_ref: manifest_insert_autoincrement_fresh
    params: {source_profile: insert_source_autoincrement_null}
oracles:
- {kind: manual_assertion, step_id: insert_null, expected: 先校准实际受影响行数、主键自增而非NULL、note为1及真实计数器身份；DEFAULT与零值须分别新建资产。精确分配值、失败非回滚及缓存行为未验证，不按CREATE初值直接标绿。}
execution_requirements: [database_authorization, target_oracle_calibration, case_owned_cleanup]
