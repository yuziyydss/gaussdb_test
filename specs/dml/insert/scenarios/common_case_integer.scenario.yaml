schema_version: 1
kind: scenario
id: scenario_insert_common_case_integer
name: scenario_insert_common_case_integer
status: planned
factor_ref: insert
fixture_refs:
- fixture_insert_common_type
fact_refs:
- insert_fact_storage_exact
steps:
- id: target
  candidate:
    manifest_ref: manifest_insert_common_type_pg
    params:
      target_profile: insert_target_common_type
      source_profile: insert_source_common_case_integer
      with_clause: insert_with_none
      ignore_modifier: insert_ignore_none
      conflict_clause: insert_conflict_none
      returning_clause: insert_returning_none
oracles:
- kind: result_set
  step_id: target
  expected:
  - - 1
  - - 20
  - - null
  sql: SELECT result FROM g_common_target ORDER BY result NULLS LAST;
- kind: manual_assertion
  step_id: target
  expected: 校准真实输出类型/目标错误；当前没有数据库证据。
execution_requirements:
- database_authorization
- physical_pg_connection
- target_oracle_calibration
- owned_asset_cleanup
description: 有限PG类型解析与真实资产；不承诺全章覆盖或数据库结果。
