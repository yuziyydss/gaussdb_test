schema_version: 1
kind: scenario
id: scenario_insert_documented_examples
name: INSERT 文档示例逐例回放
status: planned
description: 按章节顺序回放普通插入、冲突、IGNORE、别名、WITH、子查询和视图示例，保持命令、结果和错误 Oracle 可追溯；不跨入下一章 INSERT ALL。
factor_ref: insert
fixture_refs: []
fact_refs: [insert_example_core, insert_example_duplicate, insert_example_conflict, insert_example_ignore, insert_example_alias, insert_example_with, insert_example_subquery, insert_example_view]
preconditions: [按示例创建兼容模式数据库和对象, 保留示例中的会话与清理边界]
steps: [普通与多行插入, INSERT SELECT, ON DUPLICATE, ON CONFLICT, IGNORE 十例, 表别名, DML CTE, 子查询目标, 视图目标]
oracles: [章节展示的结果行, WARNING 或 ERROR 类别, 每例清理成功]
execution_requirements: [A 或默认实例, PG 兼容数据库, B/5.7/s1 数据库, 多会话数据库生命周期]
