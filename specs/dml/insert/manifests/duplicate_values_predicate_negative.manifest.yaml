schema_version: 1
kind: manifest
id: manifest_insert_duplicate_values_predicate_negative
name: INSERT ON DUPLICATE VALUES IN 负向
status: needs_review
description: 本章明确 VALUES(column) 不能位于 IN/NOT IN，本清单只违反该表达式限制；未执行数据库。
factor_ref: insert
syntax_ref: syntax_insert_v1
suite_type: negative
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  target_profile: [insert_target_unique_columns]
  source_profile: [insert_source_values_one]
  conflict_clause: [insert_duplicate_values_in_invalid]
violates_rule_refs: [insert_rule_duplicate_values_predicate_supported]
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: duplicate_values_predicate_not_supported
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
