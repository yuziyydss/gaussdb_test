schema_version: 1
kind: manifest
id: manifest_insert_ignore_view_negative
name: INSERT IGNORE 视图目标负向
status: needs_review
description: 视图/子查询顶层产生式不含 IGNORE，本清单只违反 IGNORE 目标能力规则；B/5.7/s1 数据库尚未执行。
factor_ref: insert
syntax_ref: syntax_insert_v1
suite_type: negative
strategy: pairwise
fixture_refs: []
environment_requirements:
  - key: compatibility_mode
    allowed_values: [B]
    fact_refs: [insert_fact_ignore_environment]
  - key: b_format_version
    allowed_values: ["5.7"]
    fact_refs: [insert_fact_ignore_environment]
  - key: b_format_dev_version
    allowed_values: [s1]
    fact_refs: [insert_fact_ignore_environment]
identifier_policy: {}
bindings:
  ignore_modifier: [insert_ignore_keyword]
  target_profile: [insert_target_view, insert_target_subquery]
  source_profile: [insert_source_values_one]
violates_rule_refs: [insert_rule_ignore_target_supported]
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: ignore_target_not_supported
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
