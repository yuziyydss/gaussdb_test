schema_version: 1
kind: manifest
id: manifest_insert_partition_positive
name: INSERT 一级分区目标语法
status: needs_review
description: 使用本章 RANGE 建表示例缩减 Fixture，静态覆盖 PARTITION 名称、FOR 和 AS alias；成功预期仅限语法契约，未执行数据库。
factor_ref: insert
syntax_ref: syntax_insert_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  target_profile: [insert_target_partition_name, insert_target_partition_for, insert_target_partition_as_alias]
  source_profile: [insert_source_partition_low]
  returning_clause: [insert_returning_none, insert_returning_all]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
