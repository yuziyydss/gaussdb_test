schema_version: 1
kind: manifest
id: manifest_insert_duplicate_key_update_negative
name: INSERT ON DUPLICATE 更新主键负向
status: needs_review
description: 由本章明确限制构造，只违反主键或唯一索引列不可更新规则；未执行数据库。
factor_ref: insert
syntax_ref: syntax_insert_v1
suite_type: negative
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  target_profile: [insert_target_unique_columns]
  source_profile: [insert_source_values_one]
  conflict_clause: [insert_duplicate_update_key_invalid]
violates_rule_refs: [insert_rule_duplicate_cannot_update_unique_key]
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: duplicate_updates_unique_key
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
