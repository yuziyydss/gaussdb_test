schema_version: 1
kind: manifest
id: manifest_insert_partition_alias_negative
name: INSERT 分区与无 AS 别名负向
status: needs_review
description: 由本章明确限制构造，验证分区表省略 AS 的别名不能与指定分区插入组合；未执行数据库。
factor_ref: insert
syntax_ref: syntax_insert_v1
suite_type: negative
strategy: pairwise
fixture_refs: [fixture_insert_partition_target]
identifier_policy: {}
bindings:
  target_profile: [insert_target_partition_bare_alias_invalid]
  source_profile: [insert_source_partition_low]
violates_rule_refs: [insert_rule_partition_alias_form_supported]
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: partition_alias_form_not_supported
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
