schema_version: 1
kind: manifest
id: manifest_insert_duplicate_positive
name: INSERT ON DUPLICATE KEY UPDATE 正向
status: needs_review
description: 使用本章冲突语义和示例覆盖 NOTHING、VALUES(column) 更新及 RETURNING；成功预期尚待数据库复核。
factor_ref: insert
syntax_ref: syntax_insert_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  target_profile: [insert_target_unique_columns]
  source_profile: [insert_source_values_one, insert_source_values_many]
  conflict_clause: [insert_duplicate_nothing, insert_duplicate_update_values, insert_duplicate_update_excluded, insert_duplicate_update_multiple_where]
  returning_clause: [insert_returning_none, insert_returning_expression]
expected: {default: success, scope: syntax_and_semantics}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
