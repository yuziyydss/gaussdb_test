schema_version: 1
kind: manifest
id: manifest_insert_duplicate_positive
name: INSERT ON DUPLICATE KEY UPDATE 正向
status: ready
description: 使用主键 Fixture 覆盖 NOTHING、VALUES(column) 更新和 RETURNING 组合。
factor_ref: insert
syntax_ref: syntax_insert_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  target_profile: [insert_target_unique_columns]
  source_profile: [insert_source_values_one, insert_source_values_two]
  conflict_clause: [insert_duplicate_nothing, insert_duplicate_update_note]
  returning_clause: [insert_returning_none, insert_returning_columns]
expected: {default: success, scope: syntax_and_semantics}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}

