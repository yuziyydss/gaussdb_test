schema_version: 1
kind: manifest
id: manifest_insert_declared_default_positive
name: INSERT 已声明默认值输入组合
status: needs_review
description: 独立覆盖 DEFAULT VALUES、首列 DEFAULT、多行 VALUE 中尾列 DEFAULT 与 RETURNING 的有限组合。保留原无声明默认值用例；只验证生成绑定，不将默认值求值和实际结果声明为通过。
factor_ref: insert
syntax_ref: syntax_insert_v1
suite_type: positive
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  target_profile: [insert_target_declared_defaults]
  source_profile: [insert_source_default_values, insert_source_values_with_default, insert_source_value_many]
  returning_clause: [insert_returning_none, insert_returning_all, insert_returning_expression]
expected: {default: success, scope: syntax_only}
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
