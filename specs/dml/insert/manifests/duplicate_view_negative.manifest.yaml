schema_version: 1
kind: manifest
id: manifest_insert_duplicate_view_negative
name: INSERT 视图目标 ON DUPLICATE 负向
status: ready
description: 只违反视图和子查询目标不支持 ON DUPLICATE KEY UPDATE 的规则。
factor_ref: insert
syntax_ref: syntax_insert_v1
suite_type: negative
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  target_profile: [insert_target_view, insert_target_subquery]
  source_profile: [insert_source_values_one]
  conflict_clause: [insert_duplicate_update_note]
violates_rule_refs: [insert_rule_duplicate_target_supported]
expected:
  default: error
  scope: syntax_and_semantics
  error_category: duplicate_target_not_supported
  error_message_regex: "(?i)(view|subquery|duplicate|not support)"
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}

