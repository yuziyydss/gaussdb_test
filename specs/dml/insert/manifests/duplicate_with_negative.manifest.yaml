schema_version: 1
kind: manifest
id: manifest_insert_duplicate_with_negative
name: INSERT WITH 和 ON DUPLICATE 互斥负向
status: needs_review
description: 由本章明确限制构造，只违反 WITH 与 ON DUPLICATE KEY UPDATE 不兼容规则；未执行数据库。
factor_ref: insert
syntax_ref: syntax_insert_v1
suite_type: negative
strategy: pairwise
fixture_refs: []
identifier_policy: {}
bindings:
  with_clause: [insert_with_select]
  target_profile: [insert_target_unique_columns]
  source_profile: [insert_source_values_one]
  conflict_clause: [insert_duplicate_update_values]
violates_rule_refs: [insert_rule_duplicate_rejects_with]
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: duplicate_with_not_supported
coverage_requirements: {strength: 2, require_all_feasible_pairs: true}
