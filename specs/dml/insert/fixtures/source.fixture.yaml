schema_version: 1
kind: fixture
id: fixture_insert_source
name: INSERT 查询输入源表
status: ready
description: 为 INSERT ... SELECT 和 CTE 查询输入提供两列兼容数据。
factor_ref: insert
provides:
  tables:
    - name: t_insert_source
      persistence: permanent
      table_kind: regular
      columns:
        - {name: col_1, type: INTEGER, nullable: false}
        - {name: name, type: VARCHAR(64), nullable: true}
seed:
  required: true
  rows:
    - {col_1: 201, name: from_query_a}
    - {col_1: 202, name: from_query_b}
execution:
  status: ready
  mode: auto
  note: 由 Fixture 编译器生成目标表、seed 和 teardown。

