schema_version: 1
kind: fixture
id: fixture_insert_target
name: INSERT 基础目标表
status: ready
description: 为本章普通表目标的隐式列、显式列、DEFAULT VALUES、别名和 RETURNING 提供最小两列结构。
factor_ref: insert
provides:
  tables:
    - name: t_insert_target
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: true}
        - {name: note, type: VARCHAR(64), nullable: true}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: auto
  note: 由 Fixture 编译器生成幂等 DROP、CREATE 和 teardown。
