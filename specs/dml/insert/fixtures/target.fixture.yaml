schema_version: 1
kind: fixture
id: fixture_insert_target
name: INSERT 基础目标表
status: ready
description: 两列可空目标表，支持隐式列、显式列和 DEFAULT VALUES。
factor_ref: insert
provides:
  tables:
    - name: t_insert_target
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: true}
        - {name: note, type: VARCHAR(64), nullable: true}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: auto
  note: 由 Fixture 编译器生成幂等 DROP、CREATE 和 teardown。

