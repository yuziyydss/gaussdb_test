schema_version: 1
kind: fixture
id: fixture_insert_common_type
name: fixture_insert_common_type
status: needs_review
factor_ref: insert
requires_fixture_refs:
- fixture_select_common_type
provides:
  tables:
  - name: g_common_target
    persistence: permanent
    table_kind: regular
    columns:
    - name: result
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE g_common_target (result INTEGER);
  teardown_sqls:
  - DROP TABLE g_common_target;
  note: 独占新目标表；先展开来源fixture，逆序清理且只处理本轮成功创建对象。
description: 有限PG类型解析与真实资产；不承诺全章覆盖或数据库结果。
