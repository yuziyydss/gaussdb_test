schema_version: 1
kind: fixture
id: fixture_insert_pg_conflict_fresh
name: INSERT PG 普通主键新表
status: needs_review
description: 仅本case新表及一条冲突种子；不预删除既有对象，不创建数据库或授权。
factor_ref: insert
requires_fixture_refs: []
provides:
  tables:
    - name: g_insert_pg_conflict
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: note, type: VARCHAR(64), nullable: true}
seed:
  required: true
  rows: [{id: 101, note: existing}]
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - CREATE TABLE g_insert_pg_conflict (id INTEGER PRIMARY KEY, note VARCHAR(64));
    - "INSERT INTO g_insert_pg_conflict (id, note) VALUES (101, 'existing');"
  teardown_sqls:
    - DROP TABLE g_insert_pg_conflict;
  note: 仅PG独占新用户schema；表名须未占用。逐case重新创建种子。setup失败不清理未知归属对象；目标失败后需恢复错误事务再定向清理。静态候选，不是实机执行许可。
