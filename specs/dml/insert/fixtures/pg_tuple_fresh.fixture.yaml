schema_version: 1
kind: fixture
id: fixture_insert_pg_tuple_fresh
name: INSERT PG 两非键列元组更新新表
status: needs_review
description: 独占新schema中的实际三列表及一条冲突种子；不预删除任何既有对象。
factor_ref: insert
requires_fixture_refs: []
provides:
  tables:
    - name: g_insert_pg_tuple
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: false}
        - {name: note, type: VARCHAR(64), nullable: true}
        - {name: aux, type: INTEGER, nullable: true}
seed:
  required: true
  rows: [{id: 101, note: existing, aux: 7}]
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - CREATE TABLE g_insert_pg_tuple (id INTEGER PRIMARY KEY, note VARCHAR(64), aux INTEGER);
    - "INSERT INTO g_insert_pg_tuple (id, note, aux) VALUES (101, 'existing', 7);"
  teardown_sqls:
    - DROP TABLE g_insert_pg_tuple;
  note: 仅PG独占新用户schema；逐case恢复种子，名称须未占用。仅清理本case确认创建的表；setup失败不清理未知归属对象。目标失败须恢复错误事务后定向清理。此声明不是执行授权或实机清理证明。
