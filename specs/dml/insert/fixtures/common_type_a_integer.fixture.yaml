schema_version: 1
kind: fixture
id: fixture_insert_common_type_a_integer
name: fixture_insert_common_type_a_integer
status: needs_review
factor_ref: insert
requires_fixture_refs:
- fixture_select_common_type_a_integer
provides:
  tables:
  - name: a_common_target
    persistence: permanent
    table_kind: regular
    columns:
    - name: result
      type: BIGINT
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE a_common_target (result BIGINT);
  teardown_sqls:
  - DROP TABLE a_common_target;
  note: 来源依赖先创建，逆序清理；不扩大为BIGINT向INTEGER隐式存储转换。
description: A兼容表1-309的int2/int4/int8有限子集；未执行数据库，不代表全章覆盖。
