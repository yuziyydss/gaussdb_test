schema_version: 1
kind: fixture
id: fixture_insert_declared_defaults
name: INSERT 显式默认值目标表
status: ready
description: 为 insert_fact_default 的已声明默认值分支提供真实 DDL；保留原基础表的无声明默认值分支。701 和 declared 是有限测试数据，不是产品规定值。
factor_ref: insert
provides:
  tables:
    - name: t_insert_declared_defaults
      persistence: permanent
      table_kind: regular
      columns:
        - {name: id, type: INTEGER, nullable: true}
        - {name: note, type: VARCHAR(64), nullable: true}
seed: {required: false, rows: []}
execution:
  status: ready
  mode: explicit
  setup_sqls:
    - "BEGIN;"
    - "CREATE TABLE t_insert_declared_defaults (id INTEGER DEFAULT 701, note VARCHAR(64) DEFAULT 'declared');"
  teardown_sqls:
    - "ROLLBACK;"
  note: 仅供独立连接中的单 case 生命周期；无外层事务，setup 失败也须 rollback。对象重名直接失败，不预先删除现有对象。当前只静态生成，未验证默认值实际结果。
