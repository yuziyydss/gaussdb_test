schema_version: 1
kind: scenario
id: scenario_m_select_into_output_contract
name: scenario_m_select_into_output_contract
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_select_into
fact_refs:
- m_select_into_fact_output
fixture_refs:
- fixture_m_select_into_source
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 根据所选投影检查新表列顺序/名称/INTEGER类型与复制行；不额外推导 DEFAULT/NOT NULL 继承。
oracles:
- kind: manual_assertion
  expected: 列元数据和复制结果尚未实机验证
execution_requirements:
- database_authorization
- target_oracle_calibration
