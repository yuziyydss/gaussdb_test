schema_version: 1
kind: scenario
id: scenario_m_update_string_four_byte
name: scenario_m_update_string_four_byte
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_update
fact_refs:
- m_update_fact_string_storage
- m_update_fact_string_encoding
- m_update_fact_default
- m_update_fact_string_remaining
fixture_refs:
- fixture_m_update_string_utf8
preconditions:
- M-Compatibility；独立隔离测试空间
- 先完成generated/m_compat_environment/plan.json的物理M数据库创建或授权复用、重连和模式核验。
- 当前schema独占且UTF8；server_encoding/client_encoding=UTF8；connection/database/results字符集=utf8mb4。
- 每个场景单独fixture；NULL、空字符串与Unicode必须由驱动准确区分，结果Oracle仍待实机校准。
steps:
- id: target
  candidate:
    manifest_ref: manifest_m_update_string_utf8
    params:
      assignments: m_update_assignments_string_four_byte
oracles:
- kind: result_set
  step_id: target
  sql: SELECT id,note FROM m_update_utf8_source ORDER BY id;
  expected:
  - - 2
    - 😀好
execution_requirements:
- database_authorization
- target_oracle_calibration
- isolated_connection
- per_step_oracle
- ownership_scoped_cleanup
