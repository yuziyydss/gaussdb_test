schema_version: 1
kind: scenario
id: scenario_m_update_s2_assignment_order
name: scenario_m_update_s2_assignment_order
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_update
fact_refs:
- m_update_fact_s2_order
fixture_refs:
- fixture_m_update_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- action: 仅在确认 m_format_dev_version=s2 后执行
- sql: UPDATE m_b01_source SET id=id+1, qty=id WHERE id=2;
oracles:
- kind: result_set
  sql: SELECT qty FROM m_b01_source WHERE id=3 ORDER BY qty;
  expected:
  - - 3
  - - 30
execution_requirements:
- database_authorization
- target_oracle_calibration
