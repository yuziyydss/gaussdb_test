schema_version: 1
kind: scenario
id: scenario_m_update_generated_write
name: scenario_m_update_generated_write
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_update
fact_refs:
- m_update_fact_generated_write
fixture_refs:
- fixture_m_update_generated
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- id: target_write
  sql: UPDATE m_b01_update_generated SET g=99 WHERE id=2;
  expected: error
oracles:
- kind: target_error
  step_ref: target_write
  expected:
    error_category: generated_write
  calibration_status: needs_verification
  sqlstates: []
  note: 仅目标 UPDATE 的生成列写入错误可匹配；setup/权限/对象缺失错误不通过。
execution_requirements:
- database_authorization
- target_oracle_calibration
