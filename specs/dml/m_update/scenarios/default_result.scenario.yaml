schema_version: 1
kind: scenario
id: scenario_m_update_default_result
name: scenario_m_update_default_result
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_update
fact_refs:
- m_update_fact_default
fixture_refs:
- fixture_m_update_shared_table
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: UPDATE m_b01_source SET qty=DEFAULT WHERE id=2;
oracles:
- kind: result_set
  sql: SELECT qty FROM m_b01_source WHERE id=2;
  expected:
  - - 9
execution_requirements:
- database_authorization
- target_oracle_calibration
