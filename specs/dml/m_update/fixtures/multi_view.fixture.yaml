schema_version: 1
kind: fixture
id: fixture_m_update_multi_view
name: fixture_m_update_multi_view
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_update
requires_fixture_refs: []
provides:
  tables:
  - name: m_b01_update_multi_base
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
    - name: qty
      type: INTEGER
      nullable: true
  - name: m_b01_update_multi_aux
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: true
    - name: qty
      type: INTEGER
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE m_b01_update_multi_base (id INT, qty INT);
  - INSERT INTO m_b01_update_multi_base VALUES (1,10);
  - CREATE TABLE m_b01_update_multi_aux (id INT, qty INT);
  - INSERT INTO m_b01_update_multi_aux VALUES (1,20);
  - CREATE VIEW m_b01_update_multi_view AS SELECT id,qty FROM m_b01_update_multi_base;
  teardown_sqls:
  - DROP VIEW m_b01_update_multi_view;
  - DROP TABLE m_b01_update_multi_aux PURGE;
  - DROP TABLE m_b01_update_multi_base PURGE;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。
