schema_version: 1
kind: scenario
id: scenario_m_insert_generated_null_write
name: scenario_m_insert_generated_null_write
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_insert
fact_refs:
- m_insert_fact_generated_write
fixture_refs:
- fixture_m_insert_generated
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- id: target_write
  sql: INSERT INTO m_b01_generated VALUES(7,9,NULL);
  expected: error
oracles:
- kind: target_error
  step_ref: target_write
  expected:
    error_category: generated_write
  calibration_status: needs_verification
  sqlstates: []
  note: 只接受目标INSERT的生成列写入错误；setup/权限/对象缺失不通过。
execution_requirements:
- database_authorization
- target_oracle_calibration
