schema_version: 1
kind: scenario
id: scenario_m_insert_string_default
name: scenario_m_insert_string_default
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_insert
fact_refs:
- m_insert_fact_string_storage
- m_insert_fact_string_encoding
- m_insert_fact_default
- m_insert_fact_string_remaining
fixture_refs:
- fixture_m_insert_string_utf8
preconditions:
- M-Compatibility；独立隔离测试空间
- 先完成generated/m_compat_environment/plan.json的物理M数据库创建或授权复用、重连和模式核验。
- 当前schema独占且UTF8；server_encoding/client_encoding=UTF8；connection/database/results字符集=utf8mb4。
- 每个场景单独fixture；NULL、空字符串与Unicode必须由驱动准确区分，结果Oracle仍待实机校准。
steps:
- id: target
  candidate:
    manifest_ref: manifest_m_insert_string_utf8
    params:
      source_profile: m_insert_source_profile_string_default
oracles:
- kind: result_set
  step_id: target
  sql: SELECT id,note FROM m_insert_utf8_source ORDER BY id;
  expected:
  - - 7
    - 默认
execution_requirements:
- database_authorization
- target_oracle_calibration
- isolated_connection
- per_step_oracle
- ownership_scoped_cleanup
