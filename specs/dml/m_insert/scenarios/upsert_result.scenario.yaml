schema_version: 1
kind: scenario
id: scenario_m_insert_upsert_result
name: scenario_m_insert_upsert_result
status: planned
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_insert
fact_refs:
- m_insert_fact_duplicate_effect
- m_insert_fact_duplicate_authority
fixture_refs:
- fixture_m_insert_upsert
preconditions:
- M-Compatibility；独立隔离测试空间
steps:
- sql: INSERT INTO m_b01_upsert VALUES (7,9) ON DUPLICATE KEY UPDATE qty = VALUES(qty);
oracles:
- kind: result_set
  sql: SELECT id,qty FROM m_b01_upsert ORDER BY id;
  expected:
  - - 7
    - 9
execution_requirements:
- database_authorization
- target_oracle_calibration
