schema_version: 1
kind: fixture
id: fixture_m_insert_string_utf8
name: fixture_m_insert_string_utf8
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_insert
requires_fixture_refs: []
provides:
  tables:
  - name: m_insert_utf8_source
    persistence: permanent
    table_kind: regular
    columns:
    - name: id
      type: INTEGER
      nullable: false
    - name: note
      type: VARCHAR(2)
      nullable: true
seed:
  required: false
  rows: []
execution:
  status: ready
  mode: explicit
  setup_sqls:
  - CREATE TABLE m_insert_utf8_source (id INT PRIMARY KEY, note VARCHAR(2) DEFAULT '默认');
  teardown_sqls:
  - DROP TABLE m_insert_utf8_source RESTRICT;
  note: 仅静态产物；将来须独占隔离测试空间、逐 case 生命周期。对象名必须预先不存在；setup 失败不能运行目标或清理他人对象。不依赖 DDL 回滚，不关闭约束，不吞异常。 当前schema必须独占且UTF8；不要借用其他schema的字符集。每个case独立建表，UPDATE的seed必须先成功；只清理本case已成功创建的表。
