schema_version: 1
kind: manifest
id: manifest_m_insert_view_duplicate_negative
name: manifest_m_insert_view_duplicate_negative
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_insert
syntax_ref: syntax_m_insert
suite_type: negative
strategy: pairwise
fixture_refs:
- fixture_m_insert_shared_view
identifier_policy: {}
bindings:
  target_profile:
  - m_insert_target_profile_view
  duplicate:
  - m_insert_duplicate_yes
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_insert_fact_mode
violates_rule_refs:
- m_insert_rule_view_duplicate
expected:
  default: error
  scope: syntax_and_semantics
  oracle_status: needs_verification
  error_category: view_duplicate
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
