schema_version: 1
kind: manifest
id: manifest_m_insert_upsert_conflict
name: manifest_m_insert_upsert_conflict
status: needs_review
description: M 有限模型；不承诺全章覆盖或实机成功。
factor_ref: m_insert
syntax_ref: syntax_m_insert
suite_type: positive
strategy: pairwise
fixture_refs:
- fixture_m_insert_upsert
identifier_policy: {}
bindings:
  into:
  - m_insert_into_yes
  - m_insert_into_none
  target_profile:
  - m_insert_target_profile_upsert
  source_profile:
  - m_insert_source_profile_values
  values_keyword:
  - m_insert_values_keyword_values
  - m_insert_values_keyword_value
  duplicate:
  - m_insert_duplicate_yes
environment_requirements:
- key: compatibility_mode
  allowed_values:
  - M
  fact_refs:
  - m_insert_fact_mode
- key: target_authority
  allowed_values:
  - fixture_table_creator_with_insert_update_select
  fact_refs:
  - m_insert_fact_duplicate_authority
violates_rule_refs: []
expected:
  default: success
  scope: syntax_only
coverage_requirements:
  strength: 2
  require_all_feasible_pairs: true
