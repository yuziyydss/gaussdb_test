GaussDB
集中式版参考                                                                  8 系统表和系统视图


               名称                    类型                           描述

               rcyrecycletime        timestamptz                  删除数据库的系统时间。

               rcytablespaces        oidvector_extend             数据库中数据文件所在的
                                                                  TABLESPACE OID列表。

               rcydbsize             bigint                       删除数据库时的数据库大
                                                                  小。




8.3 系统视图

8.3.1 分区表

8.3.1.1 ADM_IND_PARTITIONS
              ADM_IND_PARTITIONS视图显示数据库中所有一级分区表Local索引的索引分区信息
              （不包含分区表全局索引）。数据库中每个一级分区表的Local索引的索引分区（如果
              存在的话）都会在ADM_IND_PARTITIONS里有一行记录。默认只有系统管理员权限才
              可以访问，普通用户需要授权才可以访问。该视图同时存在于PG_CATALOG和SYS
              Schema下。

              表 8-163 ADM_IND_PARTITIONS 字段
               名称                         类型                      描述

               index_owner                character varying(64)   索引分区所属分区表索引
                                                                  的所有者的名称。

               index_name                 character varying(64)   索引分区所属分区表索引
                                                                  的名称。

               partition_name             character varying(64)   索引分区的名称。

               def_tablespace_name        name                    索引分区的表空间名称。

               high_value                 text                    索引分区所对应分区的上
                                                                  边界。
                                                                  ● 对于范围分区和间隔分
                                                                    区，显示各分区的上边
                                                                    界值。
                                                                  ● 对于列表分区，显示各
                                                                    分区的取值列表。
                                                                  ● 对于哈希分区，显示各
                                                                    分区的编号。




文档版本 01 (2026-04-30)            版权所有 © 华为云计算技术有限公司                                4985
GaussDB
集中式版参考                                                               8 系统表和系统视图


               名称                       类型                      描述

               index_partition_usable   boolean                 索引分区是否可用。
                                                                ● t（true）：可用。
                                                                ● f（false）：不可用。

               schema                   character varying(64)   索引分区所属分区表索引
                                                                的模式。

               high_value_length        integer                 索引分区所对应分区的边
                                                                界的字符长度。

               composite                character varying(3)    索引是否属于二级分区表
                                                                上的本地索引，该表不存
                                                                储二级分区信息，所以该
                                                                值为NO。

               subpartition_count       numeric                 分区中的二级分区数，该
                                                                表不存储二级分区信息，
                                                                所以该值为0。

               partition_position       numeric                 索引分区在索引中的位
                                                                置。

               status                   character varying(8)    索引分区是否可用。

               tablespace_name          name                    分区所在表空间的名称。

               pct_free                 numeric                 块中最小可用空间百分
                                                                比。

               ini_trans                numeric                 初始事务数，默认值为4，
                                                                非USTORE分区表时为
                                                                NULL。

               max_trans                numeric                 最大事务数，默认值为
                                                                128，非USTORE分区表时
                                                                为NULL。

               initial_extent           numeric                 暂不支持，值为NULL。

               next_extent              numeric                 暂不支持，值为NULL。

               min_extent               numeric                 暂不支持，值为NULL。

               max_extent               numeric                 暂不支持，值为NULL。

               max_size                 numeric                 暂不支持，值为NULL。

               pct_increase             numeric                 暂不支持，值为NULL。

               freelists                numeric                 暂不支持，值为NULL。

               freelist_groups          numeric                 暂不支持，值为NULL。

               logging                  character varying(7)    是否记录对索引的更改。




文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                          4986
GaussDB
集中式版参考                                                                  8 系统表和系统视图


               名称                        类型                        描述

               compression               character varying(13)     分区索引是否启用索引压
                                                                   缩。

               blevel                    numeric                   暂不支持，值为NULL。

               leaf_blocks               numeric                   暂不支持，值为NULL。

               distinct_keys             numeric                   暂不支持，值为NULL。

               avg_leaf_blocks_per_key   numeric                   暂不支持，值为NULL。

               avg_data_blocks_per_key   numeric                   暂不支持，值为NULL。

               clustering_factor         numeric                   根据索引的值表示表中行
                                                                   的顺序。需要通过执行
                                                                   analyze进行统计。

               num_rows                  numeric                   分区中的行数。需要通过
                                                                   执行vacuum进行统计。

               sample_size               numeric                   暂不支持，值为NULL。

               last_analyzed             timestamp with time       最近分析此分区的日期。
                                         zone

               buffer_pool               character varying(7)      分区的实际缓冲池。

               flash_cache               character varying(7)      暂不支持，值为NULL。

               cell_flash_cache          character varying(7)      暂不支持，值为NULL。

               user_stats                character varying(3)      暂不支持，值为NULL。

               pct_direct_access         numeric                   暂不支持，值为NULL。

               global_stats              character varying(3)      暂不支持，值为NULL。

               domidx_opstatus           character varying(6)      暂不支持，值为NULL。

               parameters                character varying(1000)   暂不支持，值为NULL。

               interval                  character varying(3)      分区是否在间隔分区表的
                                                                   间隔节中。

               segment_created           character varying(3)      索引分区段是否已创建。

               orphaned_entries          character varying(3)      暂不支持，值为NULL。




8.3.1.2 ADM_IND_SUBPARTITIONS
              ADM_IND_SUBPARTITIONS视图显示数据库中所有二级分区表Local索引的索引分区
              信息（不包含分区表全局索引）。数据库中每个二级分区表的Local索引的索引分区
              （如果存在的话）都会在ADM_IND_SUBPARTITIONS里有一行记录。默认只有系统管
              理员权限才可以访问，普通用户需要授权才可以访问。该视图同时存在于
              PG_CATALOG和SYS Schema下。

文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                          4987
GaussDB
集中式版参考                                                               8 系统表和系统视图

              表 8-164 ADM_IND_SUBPARTITIONS 字段
               名称                       类型                      描述

               index_owner              character varying(64)   索引分区所属分区表索引
                                                                的所有者的名称。

               index_name               character varying(64)   索引分区所属分区表索引
                                                                的名称。

               partition_name           character varying(64)   索引所在一级分区的名
                                                                称。

               subpartition_name        character varying(64)   索引所在二级分区的名
                                                                称。

               def_tablespace_name      name                    索引分区的表空间名称。

               high_value               text                    索引分区所对应分区的边
                                                                界值。
                                                                ● 对于范围分区和间隔分
                                                                  区，显示各分区的上边
                                                                  界值。
                                                                ● 对于列表分区，显示各
                                                                  分区的取值列表。
                                                                ● 对于哈希分区，显示各
                                                                  分区的编号。

               index_partition_usable   boolean                 索引分区是否可用。
                                                                ● t（true）：可用。
                                                                ● f（false）：不可用。

               schema                   character varying(64)   索引分区所属分区表索引
                                                                的模式。

               high_value_length        integer                 索引分区所对应分区的边
                                                                界的字符长度。

               partition_position       numeric                 索引分区在索引中的位
                                                                置。

               subpartition_position    numeric                 二级分区在分区中的位
                                                                置。

               status                   character varying(8)    索引分区是否可用。

               tablespace_name          name                    索引分区的表空间名称。

               pct_free                 numeric                 块中最小可用空间百分
                                                                比。

               ini_trans                numeric                 初始事务数，默认值为4，
                                                                非USTORE分区表时为
                                                                NULL。




文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                          4988
GaussDB
集中式版参考                                                                8 系统表和系统视图


               名称                        类型                      描述

               max_trans                 numeric                 最大事务数，默认值为
                                                                 128，非USTORE分区表时
                                                                 为NULL。

               initial_extent            numeric                 暂不支持，值为NULL。

               next_extent               numeric                 暂不支持，值为NULL。

               min_extent                numeric                 暂不支持，值为NULL。

               max_extent                numeric                 暂不支持，值为NULL。

               max_size                  numeric                 暂不支持，值为NULL。

               pct_increase              numeric                 暂不支持，值为NULL。

               freelists                 numeric                 暂不支持，值为NULL。

               freelist_groups           numeric                 暂不支持，值为NULL。

               logging                   character varying(7)    是否记录对索引的更改。

               compression               character varying(13)   用于二级分区的压缩类
                                                                 型。

               blevel                    numeric                 暂不支持，值为NULL。

               leaf_blocks               numeric                 暂不支持，值为NULL。

               distinct_keys             numeric                 暂不支持，值为NULL。

               avg_leaf_blocks_per_key   numeric                 暂不支持，值为NULL。

               avg_data_blocks_per_key   numeric                 暂不支持，值为NULL。

               clustering_factor         numeric                 根据索引的值表示表中行
                                                                 的顺序。需要通过执行
                                                                 analyze进行统计。

               num_rows                  numeric                 二级分区中的行数。需要
                                                                 通过执行vacuum进行统
                                                                 计。

               sample_size               numeric                 暂不支持，值为NULL。

               last_analyzed             timestamp with time     最近分析此分区的日期。
                                         zone

               buffer_pool               character varying(7)    二级分区的缓冲池。

               flash_cache               character varying(7)    暂不支持，值为NULL。

               cell_flash_cache          character varying(7)    暂不支持，值为NULL。

               user_stats                character varying(3)    暂不支持，值为NULL。

               global_stats              character varying(3)    暂不支持，值为NULL。



文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                         4989
GaussDB
集中式版参考                                                                  8 系统表和系统视图


               名称                       类型                        描述

               interval                 character varying(3)      分区是否在间隔分区表的
                                                                  间隔节中。

               segment_created          character varying(3)      索引分区段是否已创建。

               domidx_opstatus          character varying(6)      暂不支持，值为NULL。

               parameters               character varying(1000)   暂不支持，值为NULL。




8.3.1.3 ADM_PART_COL_STATISTICS
              ADM_PART_COL_STATISTICS视图显示数据库中所有表分区的列统计信息和直方图信
              息。默认只有系统管理员权限才可以访问，普通用户需要授权才可以访问。该视图同
              时存在于PG_CATALOG和SYS Schema下。


              表 8-165 ADM_PART_COL_STATISTICS 字段

               名称                 类型                              描述

               owner              character varying(128)          分区表的所有者。

               table_name         character varying(128)          表名。

               partition_name     character varying(128)          表分区名称。

               column_name        character varying(4000)         列名。

               num_distinct       numeric                         列中不同值的数量。

               low_value          raw                             列中的低值。

               high_value         raw                             列中的高值。

               density            numeric                         ● 如果列上有直方图，则
                                                                    该字段将显示直方图中
                                                                    跨越少于2个端点的值的
                                                                    选择性。它不代表跨越2
                                                                    个或更多端点的值的选
                                                                    择性。
                                                                  ● 如果列上没有可用的直
                                                                    方图，则该字段的值为
                                                                    1/NUM_DISTINCT。

               num_nulls          numeric                         列中空值数。

               num_buckets        numeric                         列的直方图的桶数。

               sample_size        numeric                         用于分析此列的样本量。

               last_analyzed      timestamp(0) without time       最近分析此列的日期。数
                                  zone                            据库重启后，数据会丢
                                                                  失。


文档版本 01 (2026-04-30)             版权所有 © 华为云计算技术有限公司                              4990
GaussDB
集中式版参考                                                                   8 系统表和系统视图


               名称                      类型                           描述

               global_stats            character varying(3)         暂不支持，值为NULL。

               user_stats              character varying(3)         暂不支持，值为NULL。

               notes                   character varying(63)        暂不支持，值为NULL。

               avg_col_len             numeric                      列的平均长度（以字节为
                                                                    单位）。

               histogram               character varying(15)        直方图是否存在以及存在
                                                                    的类型：
                                                                    ● NONE：不存在直方
                                                                      图。
                                                                    ● FREQUENCY：频率直
                                                                      方图。
                                                                    ● EQUI-WIDTH：等宽直
                                                                      方图。

               schema                  character varying(64)        列所属的名称空间的名
                                                                    称。




8.3.1.4 ADM_PART_INDEXES
              ADM_PART_INDEXES视图显示数据库中所有分区表索引的信息（不包含分区表全局索
              引）。默认只有系统管理员权限才可以访问，普通用户需要授权才可以访问。该视图
              同时存在于PG_CATALOG和SYS Schema下。

              表 8-166 ADM_PART_INDEXES 字段

               名称                        类型                    描述

               def_tablespace_name       name                  分区表索引的表空间名称。

               index_owner               character             分区表索引的所有者名称。
                                         varying(64)

               index_name                character             分区表索引的名称。
                                         varying(64)

               partition_count           bigint                分区表索引的索引分区的个数。

               partitioning_key_coun     integer               分区表的分区键个数。
               t

               partitioning_type         text                  分区表的分区策略。
                                                               说明
                                                                当前分区表策略支持范围见CREATE
                                                                TABLE PARTITION。

               schema                    character             分区表索引所属模式的名称。
                                         varying(64)


文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                              4991
GaussDB
集中式版参考                                                               8 系统表和系统视图


               名称                       类型             描述

               table_name               character      分区表索引所属的分区表名称。
                                        varying(64)

               subpartitioning_type     text           二级分区表的分区策略。如果分区
                                                       表是一级分区表，则显示NONE。
                                                       说明
                                                        当前二级分区表策略支持范围见
                                                        CREATE TABLE SUBPARTITION。

               def_subpartition_coun    integer        默认创建二级分区的个数。二级分
               t                                       区表为1，一级分区表为0。

               subpartitioning_key_c    integer        分区表二级分区键的个数。
               ount




8.3.1.5 ADM_PART_TABLES
              ADM_PART_TABLES视图显示数据库中所有分区表的信息。默认只有系统管理员权限
              才可以访问，普通用户需要授权才可以访问。该视图同时存在于PG_CATALOG和SYS
              Schema下。


              表 8-167 ADM_PART_TABLES 字段

               名称                        类型              描述

               table_owner               character       分区表的所有者名称。
                                         varying(64)

               table_name                character       分区表的名称。
                                         varying(64)

               partitioning_type         text            分区表的分区策略。
                                                         说明
                                                          当前分区表策略支持范围见
                                                          CREATE TABLE PARTITION。

               partition_count           bigint          分区表的分区个数。

               partitioning_key_count    integer         分区表的分区键个数。

               def_tablespace_name       name            分区表的表空间名称。

               schema                    character       分区表的模式。
                                         varying(64)

               subpartitioning_type      text            二级分区表的分区策略。如果分
                                                         区表是一级分区表，则显示
                                                         NONE。
                                                         说明
                                                          当前二级分区表策略支持范围见
                                                          CREATE TABLE
                                                          SUBPARTITION。


文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                            4992
GaussDB
集中式版参考                                                                       8 系统表和系统视图


               名称                        类型                     描述

               def_subpartition_count    integer                默认创建二级分区的个数，二级
                                                                分区表为1，一级分区表为0。

               subpartitioning_key_cou   integer                分区表二级分区键的个数。
               nt

               status                    character varying(8)   暂不支持，值为valid。

               def_pct_free              numeric                添加分区时使用的PCTFREE默认
                                                                值。

               def_pct_used              numeric                暂不支持，值为NULL。

               def_ini_trans             numeric                添加分区时使用的INITRANS默
                                                                认值。

               def_max_trans             numeric                添加分区时使用的MAXTRANS
                                                                默认值。

               def_initial_extent        character              暂不支持，值为NULL。
                                         varying(40)

               def_next_extent           character              暂不支持，值为NULL。
                                         varying(40)

               def_min_extents           character              暂不支持，值为NULL。
                                         varying(40)

               def_max_extents           character              暂不支持，值为NULL。
                                         varying(40)

               def_max_size              character              暂不支持，值为NULL。
                                         varying(40)

               def_pct_increase          character              暂不支持，值为NULL。
                                         varying(40)

               def_freelists             numeric                暂不支持，值为NULL。

               def_freelist_groups       numeric                暂不支持，值为NULL。

               def_logging               character varying(7)   暂不支持，值为NULL。

               def_compression           character varying(8)   添加分区时使用的默认压缩：
                                                                ● NONE
                                                                ● ENABLED
                                                                ● DISABLED

               def_compress_for          character              添加分区时使用的默认压缩。
                                         varying(30)            说明
                                                                 可用的压缩方法和压缩级别见
                                                                 WITH ( { storage_parameter =
                                                                 value } [, ... ] )。

               def_buffer_pool           character varying(7)   暂不支持，值为DEFAULT。


文档版本 01 (2026-04-30)                 版权所有 © 华为云计算技术有限公司                                    4993
GaussDB
集中式版参考                                                                8 系统表和系统视图


               名称                       类型                     描述

               def_flash_cache          character varying(7)   暂不支持，值为NULL。

               def_cell_flash_cache     character varying(7)   暂不支持，值为NULL。

               ref_ptn_constraint_nam   character              暂不支持，值为NULL。
               e                        varying(128)

               interval                 character              区间值字符串。
                                        varying(1000)

               autolist                 text                   一级列表分区是否开启自动扩展
                                                               功能。
                                                               ● YES：表示开启自动扩展功
                                                                 能。
                                                               ● NO：表示不开启自动扩展功
                                                                 能。

               interval_subpartition    character              二级间隔分区区间值字符串。
                                        varying(1000)

               autolist_subpartition    text                   二级列表分区是否开启自动扩展
                                                               功能。
                                                               ● YES：表示开启自动扩展功
                                                                 能。
                                                               ● NO：表示不开启自动扩展功
                                                                 能。

               is_nested                character varying(3)   暂不支持，值为NO。

               def_segment_creation     character varying(4)   暂不支持段页式设置，当启用
                                                               segment时，值为YES。

               def_indexing             character varying(3)   暂不支持，值为ON。

               def_inmemory             character varying(8)   暂不支持，值为NONE。

               def_inmemory_priority    character varying(8)   暂不支持，值为NULL。

               def_inmemory_distribut   character              暂不支持，值为NULL。
               e                        varying(15)

               def_inmemory_compres     character              暂不支持，值为NULL。
               sion                     varying(17)

               def_inmemory_duplicat    character              暂不支持，值为NULL。
               e                        varying(13)

               def_read_only            character varying(3)   暂不支持，值为NO。

               def_cellmemory           character              暂不支持，值为NULL。
                                        varying(24)

               def_inmemory_service     character              暂不支持，值为NULL。
                                        varying(12)


文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                          4994
GaussDB
集中式版参考                                                                 8 系统表和系统视图


               名称                         类型                    描述

               def_inmemory_service_      character             暂不支持，值为NULL。
               name                       varying(1000)




8.3.1.6 ADM_SUBPART_COL_STATISTICS
              ADM_SUBPART_COL_STATISTICS视图显示数据库中所有子分区的列统计信息和直方
              图信息。默认只有系统管理员权限才可以访问，普通用户需要授权才可以访问。该视
              图同时存在于PG_CATALOG和SYS Schema下。

              表 8-168 ADM_SUBPART_COL_STATISTICS 字段

               名称                   类型                           描述

               owner                character varying(128)       表的所有者。

               table_name           character varying(128)       表名。

               subpartition_name    character varying(128)       子分区名称。

               column_name          character varying(4000)      列名。

               num_distinct         numeric                      列中不同值的数量。

               low_value            raw                          列中的低值。

               high_value           raw                          列中的高值。

               density              numeric                      ● 如果列上有直方图，则该
                                                                   字段将显示直方图中跨越
                                                                   少于2个端点的值的选择
                                                                   性。它不代表跨越2个或
                                                                   更多端点的值的选择性。
                                                                 ● 如果列上没有可用的直方
                                                                   图，则该字段的值为1/
                                                                   NUM_DISTINCT。

               num_nulls            numeric                      列中空值数。

               num_buckets          numeric                      列的直方图的桶数。

               sample_size          numeric                      用于分析此列的样本量。

               last_analyzed        timestamp(0) without time    最近分析此列的日期。数据
                                    zone                         库重启后，数据会丢失。

               global_stats         character varying(3)         暂不支持，值为NULL。

               user_stats           character varying(3)         暂不支持，值为NULL。

               notes                character varying(41)        暂不支持，值为NULL。

               avg_col_len          numeric                      列的平均长度（以字节为单
                                                                 位）。


文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                           4995
GaussDB
集中式版参考                                                             8 系统表和系统视图


               名称                   类型                      描述

               histogram            character varying(15)   直方图是否存在以及存在的
                                                            类型：
                                                            ● NONE：不存在直方图。
                                                            ● FREQUENCY：频率直方
                                                              图。
                                                            ● EQUI-WIDTH：等宽直方
                                                              图。

               schema               character varying(64)   列所属的名称空间的名称。




8.3.1.7 ADM_SUBPART_KEY_COLUMNS
              ADM_SUBPART_KEY_COLUMNS视图显示了数据库中所有的二级分区表或分区索引的
              分区键列的相关信息。默认只有系统管理员权限才可以访问，普通用户需要授权才可
              以访问。该视图同时存在于PG_CATALOG和SYS Schema下。

              表 8-169 ADM_SUBPART_KEY_COLUMNS 字段
               名称                  类型               描述

               owner               character        二级分区表或索引的拥有者。
                                   varying(128)

               name                character        二级分区表名或索引名。
                                   varying(128)

               object_type         character(5)     对象类型。
                                                    ● 若分区为二级分区表，此列为table。
                                                    ● 若分区为二级分区索引，此列为index。

               column_name         character        二级分区表的二级分区键列名。
                                   varying(4000)

               column_position     numeric          列在分区中的位置。

               collated_column_i   numeric          暂不支持，值为NULL。
               d




8.3.1.8 ADM_TAB_PARTITIONS
              ADM_TAB_PARTITIONS视图显示数据库下所有的一级分区信息（包括二级分区表）。
              默认只有系统管理员权限才可以访问，普通用户需要授权才可以访问。该视图同时存
              在于PG_CATALOG和SYS Schema下。




文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                      4996
GaussDB
集中式版参考                                                                8 系统表和系统视图

              表 8-170 ADM_TAB_PARTITIONS 字段
               名称                   类型                      描述

               table_owner          character varying(64)   表的所有者。

               table_name           character varying(64)   关系表名称。

               partition_name       character varying(64)   分区名称。

               high_value           text                    分区的边界值。
                                                            ● 对于范围分区和间隔分区，显
                                                              示各分区的上边界值。
                                                            ● 对于列表分区，显示各分区的
                                                              取值列表。
                                                            ● 对于哈希分区，显示各分区的
                                                              编号。

               tablespace_name      name                    分区表的表空间名称。

               schema               character varying(64)   名称空间的名称。

               subpartition_cou     bigint                  二级分区的个数。
               nt

               high_value_lengt     integer                 分区边界值表达式的长度。
               h

               composite            character varying(3)    表是否为二级分区表。

               partition_position   numeric                 分区在表中的位置。

               pct_free             numeric                 块中可用空间的最小百分比。

               pct_used             numeric                 暂不支持，值为NULL。

               ini_trans            numeric                 初始事务数，默认值为4，非
                                                            USTORE分区表时为NULL。

               max_trans            numeric                 最大事务数，默认值为128，非
                                                            USTORE分区表时为NULL。

               initial_extent       numeric                 暂不支持，值为NULL。

               next_extent          numeric                 暂不支持，值为NULL。

               min_extent           numeric                 暂不支持，值为NULL。

               max_extent           numeric                 暂不支持，值为NULL。

               max_size             numeric                 暂不支持，值为NULL。

               pct_increase         numeric                 暂不支持，值为NULL。

               freelists            numeric                 暂不支持，值为NULL。

               freelist_groups      numeric                 暂不支持，值为NULL。

               logging              character varying(7)    是否记录对表的更改。


文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                         4997
GaussDB
集中式版参考                                                               8 系统表和系统视图


               名称                 类型                       描述

               compression        character varying(8)     表分区的实际压缩属性。

               compress_for       character varying(30)    暂不支持，值为NULL。

               num_rows           numeric                  分区中的行数。

               blocks             numeric                  暂不支持，值为NULL。

               empty_blocks       numeric                  暂不支持，值为NULL。

               avg_space          numeric                  暂不支持，值为NULL。

               chain_cnt          numeric                  暂不支持，值为NULL。

               avg_row_len        numeric                  暂不支持，值为NULL。

               sample_size        numeric                  暂不支持，值为NULL。

               last_analyzed      timestamp with time      最近分析此分区的日期。
                                  zone

               buffer_pool        character varying(7)     用于分区块的缓冲池。

               flash_cache        character varying(7)     暂不支持，值为NULL。

               cell_flash_cache   character varying(7)     暂不支持，值为NULL。

               global_stats       character varying(3)     暂不支持，值为NULL。

               user_stats         character varying(3)     暂不支持，值为NULL。

               is_nested          character varying(3)     是否是嵌套表分区。

               parent_table_par   character varying(128)   暂不支持，值为NULL。
               tition

               interval           character varying(3)     分区是否在间隔分区表的间隔节
                                                           中。

               segment_created    character varying(4)     表分区是否创建了段。

               indexing           character varying(4)     暂不支持，值为NULL。

               read_only          character varying(4)     暂不支持，值为NULL。

               inmemory           character varying(8)     暂不支持，值为NULL。

               inmemory_priorit   character varying(8)     暂不支持，值为NULL。
               y

               inmemory_distrib   character varying(15)    暂不支持，值为NULL。
               ute

               inmemory_compr     character varying(17)    暂不支持，值为NULL。
               ession

               inmemory_duplic    character varying(13)    暂不支持，值为NULL。
               ate


文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                        4998
GaussDB
集中式版参考                                                               8 系统表和系统视图


               名称                 类型                       描述

               cellmemory         character varying(24)    暂不支持，值为NULL。

               inmemory_servic    character varying(12)    暂不支持，值为NULL。
               e

               inmemory_servic    character varying(100)   暂不支持，值为NULL。
               e_name

               memoptimize_re     character varying(8)     暂不支持，值为NULL。
               ad

               memoptimize_wr     character varying(8)     暂不支持，值为NULL。
               ite




8.3.1.9 ADM_TAB_SUBPARTITIONS
              ADM_TAB_SUBPARTITIONS视图显示数据库下所有的二级分区信息。默认只有系统管
              理员权限才可以访问，普通用户需要授权才可以访问。该视图同时存在于
              PG_CATALOG和SYS Schema下。

              表 8-171 ADM_TAB_SUBPARTITIONS 字段
               名称                 类型                       描述

               table_owner        character varying(64)    表的所有者。

               table_name         character varying(64)    关系表名称。

               partition_name     character varying(64)    分区名称。

               subpartition_nam   character varying(64)    二级分区名称。
               e

               high_value         text                     二级分区的边界值。
                                                           ● 对于范围分区和间隔分区，显
                                                             示各分区的上边界值。
                                                           ● 对于列表分区，显示各分区的
                                                             取值列表。
                                                           ● 对于哈希分区，显示各分区的
                                                             编号。

               tablespace_name    name                     二级分区表的表空间名称。

               schema             character varying(64)    名称空间的名称。

               high_value_lengt   integer                  二级分区的边界值的字符长度。
               h




文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                        4999
GaussDB
集中式版参考                                                               8 系统表和系统视图


8.3.1.10 DB_IND_PARTITIONS
              DB_IND_PARTITIONS视图显示当前用户所能访问的一级分区表Local索引的索引分区
              信息（不包含分区表全局索引）。所有用户都可以访问。该视图同时存在于
              PG_CATALOG和SYS Schema下。

              表 8-172 DB_IND_PARTITIONS 字段
               名称                       类型                      描述

               index_owner              character varying(64)   索引分区所属分区表索引
                                                                的所有者的名称。

               index_name               character varying(64)   索引分区所属分区表索引
                                                                的名称。

               partition_name           character varying(64)   索引分区的名称。

               def_tablespace_name      name                    索引分区的表空间名称。

               high_value               text                    索引分区所对应分区的上
                                                                边界。

               index_partition_usable   boolean                 索引分区是否可用。
                                                                ● t（true）：表示可用。
                                                                ● f（false）：表示不可
                                                                  用。

               schema                   character varying(64)   索引分区所属分区表索引
                                                                的模式。

               high_value_length        integer                 索引分区所对应分区的边
                                                                界的字符长度。

               composite                character varying(3)    索引是否属于二级分区表
                                                                上的本地索引，该表不存
                                                                储二级分区信息，所以该
                                                                值为NO。

               subpartition_count       numeric                 分区中的二级分区数，该
                                                                表不存储二级分区信息，
                                                                所以该值为0。

               partition_position       numeric                 索引分区在索引中的位
                                                                置。

               status                   character varying(8)    索引分区是否可用。

               tablespace_name          name                    分区所在表空间的名称。

               pct_free                 numeric                 块中最小可用空间百分
                                                                比。

               ini_trans                numeric                 初始事务数，默认值为4，
                                                                非USTORE分区表时为
                                                                NULL。



文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                       5000
GaussDB
集中式版参考                                                                8 系统表和系统视图


               名称                        类型                      描述

               max_trans                 numeric                 最大事务数，默认值为
                                                                 128，非USTORE分区表时
                                                                 为NULL。

               initial_extent            numeric                 暂不支持，值为NULL。

               next_extent               numeric                 暂不支持，值为NULL。

               min_extent                numeric                 暂不支持，值为NULL。

               max_extent                numeric                 暂不支持，值为NULL。

               max_size                  numeric                 暂不支持，值为NULL。

               pct_increase              numeric                 暂不支持，值为NULL。

               freelists                 numeric                 暂不支持，值为NULL。

               freelist_groups           numeric                 暂不支持，值为NULL。

               logging                   character varying(7)    是否记录对索引的更改。

               compression               character varying(13)   分区索引是否启用索引压
                                                                 缩。

               blevel                    numeric                 暂不支持，值为NULL。

               leaf_blocks               numeric                 暂不支持，值为NULL。

               distinct_keys             numeric                 暂不支持，值为NULL。

               avg_leaf_blocks_per_key   numeric                 暂不支持，值为NULL。

               avg_data_blocks_per_key   numeric                 暂不支持，值为NULL。

               clustering_factor         numeric                 根据索引的值表示表中行
                                                                 的顺序。需要通过执行
                                                                 analyze进行统计。

               num_rows                  numeric                 分区中的行数。需要通过
                                                                 执行vacuum进行统计。

               sample_size               numeric                 暂不支持，值为NULL。

               last_analyzed             timestamp with time     最近分析此分区的日期。
                                         zone

               buffer_pool               character varying(7)    分区的实际缓冲池。

               flash_cache               character varying(7)    暂不支持，值为NULL。

               cell_flash_cache          character varying(7)    暂不支持，值为NULL。

               user_stats                character varying(3)    暂不支持，值为NULL。

               pct_direct_access         numeric                 暂不支持，值为NULL。

               global_stats              character varying(3)    暂不支持，值为NULL。


文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                         5001
GaussDB
集中式版参考                                                                 8 系统表和系统视图


               名称                       类型                        描述

               domidx_opstatus          character varying(6)      暂不支持，值为NULL。

               parameters               character varying(1000)   暂不支持，值为NULL。

               interval                 character varying(3)      分区是否在间隔分区表的
                                                                  间隔节中。

               segment_created          character varying(3)      索引分区段是否已创建。

               orphaned_entries         character varying(3)      暂不支持，值为NULL。




8.3.1.11 DB_IND_SUBPARTITIONS
              DB_IND_SUBPARTITIONS视图显示当前用户所能访问的二级分区表Local索引的索引
              分区信息（不包含分区表全局索引）。所有用户都可以访问。该视图同时存在于
              PG_CATALOG和SYS Schema下。

              表 8-173 DB_IND_SUBPARTITIONS 字段
               名称                       类型                        描述

               index_owner              character varying(64)     索引分区所属分区表索引
                                                                  的所有者的名称。

               index_name               character varying(64)     索引分区所属分区表索引
                                                                  的名称。

               partition_name           character varying(64)     索引所在一级分区的名
                                                                  称。

               subpartition_name        character varying(64)     索引所在二级分区的名
                                                                  称。

               def_tablespace_name      name                      索引分区的表空间名称。

               high_value               text                      索引分区所对应分区的边
                                                                  界值。

               index_partition_usable   boolean                   索引分区是否可用。
                                                                  ● t（true）：可用。
                                                                  ● f（false）：不可用。

               schema                   character varying(64)     索引分区所属分区表索引
                                                                  的模式。

               high_value_length        integer                   索引分区所对应分区的边
                                                                  界的字符长度。

               partition_position       numeric                   索引分区在索引中的位
                                                                  置。




文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                            5002
GaussDB
集中式版参考                                                                8 系统表和系统视图


               名称                        类型                      描述

               subpartition_position     numeric                 二级分区在分区中的位
                                                                 置。

               status                    character varying(8)    索引分区是否可用。

               tablespace_name           name                    索引分区的表空间名称。

               pct_free                  numeric                 块中最小可用空间百分
                                                                 比。

               ini_trans                 numeric                 初始事务数，默认值为4，
                                                                 非USTORE分区表时为
                                                                 NULL。

               max_trans                 numeric                 最大事务数，默认值为
                                                                 128，非USTORE分区表时
                                                                 为NULL。

               initial_extent            numeric                 暂不支持，值为NULL。

               next_extent               numeric                 暂不支持，值为NULL。

               min_extent                numeric                 暂不支持，值为NULL。

               max_extent                numeric                 暂不支持，值为NULL。

               max_size                  numeric                 暂不支持，值为NULL。

               pct_increase              numeric                 暂不支持，值为NULL。

               freelists                 numeric                 暂不支持，值为NULL。

               freelist_groups           numeric                 暂不支持，值为NULL。

               logging                   character varying(7)    是否记录对索引的更改。

               compression               character varying(13)   用于二级分区的压缩类
                                                                 型。

               blevel                    numeric                 暂不支持，值为NULL。

               leaf_blocks               numeric                 暂不支持，值为NULL。

               distinct_keys             numeric                 暂不支持，值为NULL。

               avg_leaf_blocks_per_key   numeric                 暂不支持，值为NULL。

               avg_data_blocks_per_key   numeric                 暂不支持，值为NULL。

               clustering_factor         numeric                 根据索引的值表示表中行
                                                                 的顺序。需要通过执行
                                                                 analyze进行统计。

               num_rows                  numeric                 二级分区中的行数。需要
                                                                 通过执行vacuum进行统
                                                                 计。




文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                         5003
GaussDB
集中式版参考                                                                   8 系统表和系统视图


               名称                        类型                        描述

               sample_size               numeric                   暂不支持，值为NULL。

               last_analyzed             timestamp with time       最近分析此分区的日期。
                                         zone

               buffer_pool               character varying(7)      二级分区的缓冲池。

               flash_cache               character varying(7)      暂不支持，值为NULL。

               cell_flash_cache          character varying(7)      暂不支持，值为NULL。

               user_stats                character varying(3)      暂不支持，值为NULL。

               global_stats              character varying(3)      暂不支持，值为NULL。

               interval                  character varying(3)      分区是否在间隔分区表的
                                                                   间隔节中。

               segment_created           character varying(3)      索引分区段是否已创建。

               domidx_opstatus           character varying(6)      暂不支持，值为NULL。

               parameters                character varying(1000)   暂不支持，值为NULL。




8.3.1.12 DB_PART_COL_STATISTICS
              DB_PART_COL_STATISTICS视图显示当前用户可访问的表分区的列统计信息和直方图
              信息。所有用户都可以访问该视图。该视图同时存在于PG_CATALOG和SYS Schema
              下。

              表 8-174 DB_PART_COL_STATISTICS 字段
               名称                  类型                              描述

               owner               character varying(128)          分区表的所有者。

               table_name          character varying(128)          表名。

               partition_name      character varying(128)          表分区名称。

               column_name         character varying(4000)         列名。

               num_distinct        numeric                         列中不同值的数量。

               low_value           raw                             列中的低值。

               high_value          raw                             列中的高值。




文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                            5004
GaussDB
集中式版参考                                                           8 系统表和系统视图


               名称               类型                          描述

               density          numeric                     ● 如果列上有直方图，则
                                                              该字段将显示直方图中
                                                              跨越少于2个端点的值的
                                                              选择性。它不代表跨越2
                                                              个或更多端点的值的选
                                                              择性。
                                                            ● 如果列上没有可用的直
                                                              方图，则该字段的值为
                                                              1/NUM_DISTINCT。

               num_nulls        numeric                     列中空值数。

               num_buckets      numeric                     列的直方图的桶数。

               sample_size      numeric                     用于分析此列的样本量。

               last_analyzed    timestamp(0) without time   最近分析此列的日期。数
                                zone                        据库重启后，数据会丢
                                                            失。

               global_stats     character varying(3)        暂不支持，值为NULL。

               user_stats       character varying(3)        暂不支持，值为NULL。

               notes            character varying(63)       暂不支持，值为NULL。

               avg_col_len      numeric                     列的平均长度（以字节为
                                                            单位）。

               histogram        character varying(15)       直方图是否存在以及存在
                                                            的类型：
                                                            ● NONE：不存在直方
                                                              图。
                                                            ● FREQUENCY：频率直
                                                              方图。
                                                            ● EQUI-WIDTH：等宽直
                                                              方图。

               schema           character varying(64)       列所属的名称空间的名
                                                            称。




8.3.1.13 DB_PART_INDEXES
              DB_PART_INDEXES视图显示当前用户所能访问的分区表索引的信息（不包含分区表全
              局索引）。该视图同时存在于PG_CATALOG和SYS Schema下。




文档版本 01 (2026-04-30)           版权所有 © 华为云计算技术有限公司                          5005
GaussDB
集中式版参考                                                                    8 系统表和系统视图

              表 8-175 DB_PART_INDEXES 字段

               名称                        类型                 描述

               def_tablespace_name       name               分区表索引的表空间名称。

               index_owner               character          分区表索引的所有者名称。
                                         varying(64)

               index_name                character          分区表索引的名称。
                                         varying(64)

               partition_count           bigint             分区表索引的索引分区的个数。

               partitioning_key_coun     integer            分区表的分区键个数。
               t

               partitioning_type         text               分区表的分区策略。
                                                            说明
                                                             当前分区表策略支持范围见CREATE
                                                             TABLE PARTITION。

               schema                    character          分区表索引所属模式名称。
                                         varying(64)

               table_name                character          分区表索引所属的分区表名称。
                                         varying(64)

               subpartitioning_type      text               二级分区表的分区策略。如果分区
                                                            表是一级分区表，则显示NONE。
                                                            说明
                                                             当前二级分区表策略支持范围见
                                                             CREATE TABLE SUBPARTITION。

               def_subpartition_coun     integer            默认创建二级分区的个数，二级分
               t                                            区表为1，一级分区表为0。

               subpartitioning_key_c     integer            分区表二级分区键的个数。
               ount




8.3.1.14 DB_PART_KEY_COLUMNS
              DB_PART_KEY_COLUMNS视图显示了当前用户可访问的分区表或分区索引的分区键列
              的相关信息。该视图所有用户可访问，显示当前用户可访问的所有信息。该视图同时
              存在于PG_CATALOG和SYS Schema下。

              表 8-176 DB_PART_KEY_COLUMNS 字段

               名称                     类型               描述

               owner                  character        分区表或索引的拥有者。
                                      varying(128)

               name                   character        分区表名或索引名。
                                      varying(128)


文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                                 5006
GaussDB
集中式版参考                                                                    8 系统表和系统视图


               名称                     类型                 描述

               object_type            character(5)       对象类型。
                                                         ● 若分区为分区表，此列为table。
                                                         ● 若分区为分区索引，此列为index。

               column_name            character          分区表或索引的键列名。
                                      varying(4000)

               column_position        numeric            列在分区中的位置。

               collated_column_i      numeric            暂不支持，值为NULL。
               d




8.3.1.15 DB_PART_TABLES
              DB_PART_TABLES视图显示当前用户所能访问的分区表的信息。该视图同时存在于
              PG_CATALOG和SYS Schema下。

              表 8-177 DB_PART_TABLES 字段
               名称                          类型                 描述

               table_owner                 character          分区表的所有者名称。
                                           varying(64)

               table_name                  character          分区表的名称。
                                           varying(64)

               partitioning_type           text               分区表的分区策略。
                                                              说明
                                                               当前分区表策略支持范围见
                                                               CREATE TABLE PARTITION。

               partition_count             bigint             分区表的分区个数。

               partitioning_key_count      integer            分区表的分区键个数。

               def_tablespace_name         name               分区表的表空间名称。

               schema                      character          分区表的模式。
                                           varying(64)

               subpartitioning_type        text               二级分区表的分区策略。如果分
                                                              区表是一级分区表，则显示
                                                              NONE。
                                                              说明
                                                               当前二级分区表策略支持范围见
                                                               CREATE TABLE SUBPARTITION。

               def_subpartition_count      integer            默认创建二级分区的个数，二级
                                                              分区表为1，一级分区表为0。




文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                                    5007
GaussDB
集中式版参考                                                                       8 系统表和系统视图


               名称                        类型                     描述

               subpartitioning_key_cou   integer                分区表二级分区键的个数。
               nt

               status                    character varying(8)   暂不支持，值为valid。

               def_pct_free              numeric                添加分区时使用的PCTFREE默认
                                                                值。

               def_pct_used              numeric                暂不支持，值为NULL。

               def_ini_trans             numeric                添加分区时使用的INITRANS默
                                                                认值。

               def_max_trans             numeric                添加分区时使用的MAXTRANS
                                                                默认值。

               def_initial_extent        character              暂不支持，值为NULL。
                                         varying(40)

               def_next_extent           character              暂不支持，值为NULL。
                                         varying(40)

               def_min_extents           character              暂不支持，值为NULL。
                                         varying(40)

               def_max_extents           character              暂不支持，值为NULL。
                                         varying(40)

               def_max_size              character              暂不支持，值为NULL。
                                         varying(40)

               def_pct_increase          character              暂不支持，值为NULL。
                                         varying(40)

               def_freelists             numeric                暂不支持，值为NULL。

               def_freelist_groups       numeric                暂不支持，值为NULL。

               def_logging               character varying(7)   暂不支持，值为NULL。

               def_compression           character varying(8)   添加分区时使用的默认压缩：
                                                                ● NONE
                                                                ● ENABLED
                                                                ● DISABLED

               def_compress_for          character              添加分区时使用的默认压缩。
                                         varying(30)            说明
                                                                 可用的压缩方法和压缩级别见
                                                                 WITH ( { storage_parameter =
                                                                 value } [, ... ] )。

               def_buffer_pool           character varying(7)   暂不支持，值为DEFAULT。

               def_flash_cache           character varying(7)   暂不支持，值为NULL。




文档版本 01 (2026-04-30)                 版权所有 © 华为云计算技术有限公司                                    5008
GaussDB
集中式版参考                                                                8 系统表和系统视图


               名称                       类型                     描述

               def_cell_flash_cache     character varying(7)   暂不支持，值为NULL。

               ref_ptn_constraint_nam   character              暂不支持，值为NULL。
               e                        varying(128)

               interval                 character              区间值字符串。
                                        varying(1000)

               autolist                 text                   一级列表分区是否开启自动扩展
                                                               功能。
                                                               ● YES：表示开启自动扩展功
                                                                 能。
                                                               ● NO：表示不开启自动扩展功
                                                                 能。

               interval_subpartition    character              二级间隔分区区间值字符串。
                                        varying(1000)

               autolist_subpartition    text                   二级列表分区是否开启自动扩展
                                                               功能。
                                                               ● YES：表示开启自动扩展功
                                                                 能。
                                                               ● NO：表示不开启自动扩展功
                                                                 能。

               is_nested                character varying(3)   暂不支持，值为NO。

               def_segment_creation     character varying(4)   暂不支持段页式设置，当启用
                                                               segment时，值为YES。

               def_indexing             character varying(3)   暂不支持，值为ON。

               def_inmemory             character varying(8)   暂不支持，值为NONE。

               def_inmemory_priority    character varying(8)   暂不支持，值为NULL。

               def_inmemory_distribut   character              暂不支持，值为NULL。
               e                        varying(15)

               def_inmemory_compres     character              暂不支持，值为NULL。
               sion                     varying(17)

               def_inmemory_duplicat    character              暂不支持，值为NULL。
               e                        varying(13)

               def_read_only            character varying(3)   暂不支持，值为NO。

               def_cellmemory           character              暂不支持，值为NULL。
                                        varying(24)

               def_inmemory_service     character              暂不支持，值为NULL。
                                        varying(12)




文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                          5009
GaussDB
集中式版参考                                                                8 系统表和系统视图


               名称                         类型                  描述

               def_inmemory_service_      character           暂不支持，值为NULL。
               name                       varying(1000)




8.3.1.16 DB_SUBPART_COL_STATISTICS
              DB_SUBPART_COL_STATISTICS视图显示当前用户可访问的分区对象的子分区的列统
              计信息和直方图信息。所有用户都可以访问该视图。该视图同时存在于PG_CATALOG
              和SYS Schema下。

              表 8-178 DB_SUBPART_COL_STATISTICS 字段

               名称                   类型                          描述

               owner                character varying(128)      表的所有者。

               table_name           character varying(128)      表名。

               subpartition_name    character varying(128)      子分区名称。

               column_name          character varying(4000)     列名。

               num_distinct         numeric                     列中不同值的数量。

               low_value            raw                         列中的低值。

               high_value           raw                         列中的高值。

               density              numeric                     ● 如果列上有直方图，则该
                                                                  字段将显示直方图中跨越
                                                                  少于2个端点的值的选择
                                                                  性。它不代表跨越2个或
                                                                  更多端点的值的选择性。
                                                                ● 如果列上没有可用的直方
                                                                  图，则该字段的值为1/
                                                                  NUM_DISTINCT。

               num_nulls            numeric                     列中空值数。

               num_buckets          numeric                     列的直方图的桶数。

               sample_size          numeric                     用于分析此列的样本量。

               last_analyzed        timestamp(0) without time   最近分析此列的日期。数据
                                    zone                        库重启后，数据会丢失。

               global_stats         character varying(3)        暂不支持，值为NULL。

               user_stats           character varying(3)        暂不支持，值为NULL。

               notes                character varying(41)       暂不支持，值为NULL。

               avg_col_len          numeric                     列的平均长度（以字节为单
                                                                位）。


文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                          5010
GaussDB
集中式版参考                                                             8 系统表和系统视图


               名称                   类型                      描述

               histogram            character varying(15)   直方图是否存在以及存在的
                                                            类型：
                                                            ● NONE：不存在直方图。
                                                            ● FREQUENCY：频率直方
                                                              图。
                                                            ● EQUI-WIDTH：等宽直方
                                                              图。

               schema               character varying(64)   列所属的名称空间的名称。




8.3.1.17 DB_SUBPART_KEY_COLUMNS
              DB_SUBPART_KEY_COLUMNS视图显示了当前用户可访问的二级分区表或分区索引的
              分区键列的相关信息。该视图所有用户可访问，显示当前用户可访问的所有信息。该
              视图同时存在于PG_CATALOG和SYS Schema下。

              表 8-179 DB_SUBPART_KEY_COLUMNS 字段
               名称                  类型               描述

               owner               character        二级分区表或索引的拥有者。
                                   varying(128)

               name                character        二级分区表名或索引名。
                                   varying(128)

               object_type         character(5)     对象类型。
                                                    ● 若分区为分区表，此列为table。
                                                    ● 若分区为分区索引，此列为index。

               column_name         character        二级分区表的二级分区键列名。
                                   varying(4000)

               column_position     numeric          列在分区中的位置。

               collated_column_i   numeric          暂不支持，值为NULL。
               d




8.3.1.18 DB_TAB_PARTITIONS
              DB_TAB_PARTITIONS视图显示当前用户所能访问的一级分区信息（包括二级分区
              表）。所有用户都可以访问。该视图同时存在于PG_CATALOG和SYS Schema下。




文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                      5011
GaussDB
集中式版参考                                                                8 系统表和系统视图

              表 8-180 DB_TAB_PARTITIONS 字段
               名称                   类型                      描述

               table_owner          character varying(64)   表的所有者。

               table_name           character varying(64)   关系表名称。

               partition_name       character varying(64)   分区名称。

               high_value           text                    分区的边界值。
                                                            ● 对于范围分区和间隔分区，显
                                                              示各分区的上边界值。
                                                            ● 对于列表分区，显示各分区的
                                                              取值列表。
                                                            ● 对于哈希分区，显示各分区的
                                                              编号。

               tablespace_name      name                    分区表的表空间名称。

               schema               character varying(64)   名称空间的名称。

               subpartition_cou     bigint                  二级分区的个数。
               nt

               high_value_lengt     integer                 分区边界值表达式的长度。
               h

               composite            character varying(3)    表是否为二级分区表。

               partition_position   numeric                 分区在表中的位置。

               pct_free             numeric                 块中可用空间的最小百分比。

               pct_used             numeric                 暂不支持，值为NULL。

               ini_trans            numeric                 初始事务数，默认值为4，非
                                                            USTORE分区表时为NULL。

               max_trans            numeric                 最大事务数，默认值为128，非
                                                            USTORE分区表时为NULL。

               initial_extent       numeric                 暂不支持，值为NULL。

               next_extent          numeric                 暂不支持，值为NULL。

               min_extent           numeric                 暂不支持，值为NULL。

               max_extent           numeric                 暂不支持，值为NULL。

               max_size             numeric                 暂不支持，值为NULL。

               pct_increase         numeric                 暂不支持，值为NULL。

               freelists            numeric                 暂不支持，值为NULL。

               freelist_groups      numeric                 暂不支持，值为NULL。

               logging              character varying(7)    是否记录对表的更改。


文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                         5012
GaussDB
集中式版参考                                                               8 系统表和系统视图


               名称                 类型                       描述

               compression        character varying(8)     表分区的实际压缩属性。

               compress_for       character varying(30)    暂不支持，值为NULL。

               num_rows           numeric                  分区中的行数。

               blocks             numeric                  暂不支持，值为NULL。

               empty_blocks       numeric                  暂不支持，值为NULL。

               avg_space          numeric                  暂不支持，值为NULL。

               chain_cnt          numeric                  暂不支持，值为NULL。

               avg_row_len        numeric                  暂不支持，值为NULL。

               sample_size        numeric                  暂不支持，值为NULL。

               last_analyzed      timestamp with time      最近分析此分区的日期。
                                  zone

               buffer_pool        character varying(7)     用于分区块的缓冲池。

               flash_cache        character varying(7)     暂不支持，值为NULL。

               cell_flash_cache   character varying(7)     暂不支持，值为NULL。

               global_stats       character varying(3)     暂不支持，值为NULL。

               user_stats         character varying(3)     暂不支持，值为NULL。

               is_nested          character varying(3)     这是否是嵌套表分区。

               parent_table_par   character varying(128)   暂不支持，值为NULL。
               tition

               interval           character varying(3)     分区是否在间隔分区表的间隔节
                                                           中。

               segment_created    character varying(4)     表分区是否创建了段。

               indexing           character varying(4)     暂不支持，值为NULL。

               read_only          character varying(4)     暂不支持，值为NULL。

               inmemory           character varying(8)     暂不支持，值为NULL。

               inmemory_priorit   character varying(8)     暂不支持，值为NULL。
               y

               inmemory_distrib   character varying(15)    暂不支持，值为NULL。
               ute

               inmemory_compr     character varying(17)    暂不支持，值为NULL。
               ession

               inmemory_duplic    character varying(13)    暂不支持，值为NULL。
               ate


文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                        5013
GaussDB
集中式版参考                                                                 8 系统表和系统视图


               名称                   类型                       描述

               cellmemory           character varying(24)    暂不支持，值为NULL。

               inmemory_servic      character varying(12)    暂不支持，值为NULL。
               e

               inmemory_servic      character varying(100)   暂不支持，值为NULL。
               e_name

               memoptimize_re       character varying(8)     暂不支持，值为NULL。
               ad

               memoptimize_wr       character varying(8)     暂不支持，值为NULL。
               ite




8.3.1.19 DB_TAB_SUBPARTITIONS
              DB_TAB_SUBPARTITIONS视图显示当前用户所能访问的二级分区表的信息。所有用户
              都可以访问。该视图同时存在于PG_CATALOG和SYS Schema下。


              表 8-181 DB_TAB_SUBPARTITIONS 字段

               名称                   类型                       描述

               table_owner          character varying(64)    表的所有者。

               table_name           character varying(64)    关系表名称。

               partition_name       character varying(64)    分区名称。

               subpartition_nam     character varying(64)    二级分区名称。
               e

               high_value           text                     二级分区的边界值。
                                                             ● 对于范围分区和间隔分区，显
                                                               示各分区的上边界值。
                                                             ● 对于列表分区，显示各分区的
                                                               取值列表。
                                                             ● 对于哈希分区，显示各分区的
                                                               编号。

               tablespace_name      name                     二级分区表的表空间名称。

               schema               character varying(64)    名称空间的名称。

               high_value_lengt     integer                  二级分区边界值的字符长度。
               h

               partition_position   numeric                  分区在表中的位置。

               subpartition_posi    numeric                  二级分区在分区中的位置。
               tion


文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                        5014
GaussDB
集中式版参考                                                             8 系统表和系统视图


               名称                 类型                      描述

               pct_free           numeric                 块中可用空间的最小百分比。

               pct_used           numeric                 暂不支持，值为NULL。

               ini_trans          numeric                 初始事务数，默认值为4，非
                                                          USTORE分区表时为NULL。

               max_trans          numeric                 最大事务数，默认值为128，非
                                                          USTORE分区表时为NULL。

               initial_extent     numeric                 暂不支持，值为NULL。

               next_extent        numeric                 暂不支持，值为NULL。

               min_extent         numeric                 暂不支持，值为NULL。

               max_extent         numeric                 暂不支持，值为NULL。

               max_size           numeric                 暂不支持，值为NULL。

               pct_increase       numeric                 暂不支持，值为NULL。

               freelists          numeric                 暂不支持，值为NULL。

               freelist_groups    numeric                 暂不支持，值为NULL。

               logging            character varying(3)    是否记录对表的更改。

               compression        character varying(8)    表分区的实际压缩属性。

               compress_for       character varying(30)   暂不支持，值为NULL。

               num_rows           numeric                 二级分区中的行数。

               blocks             numeric                 暂不支持，值为NULL。

               empty_blocks       numeric                 暂不支持，值为NULL。

               avg_space          numeric                 暂不支持，值为NULL。

               chain_cnt          numeric                 暂不支持，值为NULL。

               avg_row_len        numeric                 暂不支持，值为NULL。

               sample_size        numeric                 暂不支持，值为NULL。

               last_analyzed      timestamp with time     最近分析此分区的日期。
                                  zone

               buffer_pool        character varying(7)    二级分区的缓冲池。

               flash_cache        character varying(7)    暂不支持，值为NULL。

               cell_flash_cache   character varying(7)    暂不支持，值为NULL。

               global_stats       character varying(3)    暂不支持，值为NULL。

               user_stats         character varying(3)    暂不支持，值为NULL。



文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                         5015
GaussDB
集中式版参考                                                                8 系统表和系统视图


               名称                 类型                        描述

               interval           character varying(3)      分区是否在间隔分区表的间隔节
                                                            中。

               segment_created    character varying(3)      表二级分区是否创建了段。

               indexing           character varying(3)      暂不支持，值为NULL。

               read_only          character varying(3)      暂不支持，值为NULL。

               inmemory           character varying(8)      暂不支持，值为NULL。

               inmemory_priorit   character varying(8)      暂不支持，值为NULL。
               y

               inmemory_distrib   character varying(15)     暂不支持，值为NULL。
               ute

               inmemory_compr     character varying(17)     暂不支持，值为NULL。
               ession

               inmemory_duplic    character varying(13)     暂不支持，值为NULL。
               ate

               inmemory_servic    character varying(12)     暂不支持，值为NULL。
               e

               inmemory_servic    character varying(1000)   暂不支持，值为NULL。
               e_name

               cellmemory         character varying(24)     暂不支持，值为NULL。

               memoptimize_re     character varying(8)      暂不支持，值为NULL。
               ad

               memoptimize_wr     character varying(8)      暂不支持，值为NULL。
               ite




8.3.1.20 MY_IND_PARTITIONS
              MY_IND_PARTITIONS视图显示当前用户下一级分区表Local索引的索引分区信息（不
              包含分区表全局索引）。所有用户都可以访问。该视图同时存在于PG_CATALOG和SYS
              Schema下。

              表 8-182 MY_IND_PARTITIONS 字段
               名称                        类型                      描述

               index_owner               character varying(64)   索引分区所属分区表索引
                                                                 的所有者的名称。

               index_name                character varying(64)   索引分区所属分区表索引
                                                                 的名称。



文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                         5016
GaussDB
集中式版参考                                                               8 系统表和系统视图


               名称                       类型                      描述

               partition_name           character varying(64)   索引分区的名称。

               def_tablespace_name      name                    索引分区的表空间名称。

               high_value               text                    索引分区所对应分区的上
                                                                边界。

               index_partition_usable   boolean                 索引分区是否可用：
                                                                ● t（true）：表示可用。
                                                                ● f（false）：表示不可
                                                                  用。

               schema                   character varying(64)   索引分区所属分区表索引
                                                                的模式。

               high_value_length        integer                 索引分区所对应分区的边
                                                                界的字符长度。

               composite                character varying(3)    索引是否属于二级分区表
                                                                上的本地索引，该表不存
                                                                储二级分区信息，所以该
                                                                值为NO。

               subpartition_count       numeric                 分区中的二级分区数，该
                                                                表不存储二级分区信息，
                                                                所以该值为0。

               partition_position       numeric                 索引分区在索引中的位
                                                                置。

               status                   character varying(8)    索引分区是否可用。

               tablespace_name          name                    分区所在表空间的名称。

               pct_free                 numeric                 块中最小可用空间百分
                                                                比。

               ini_trans                numeric                 初始事务数，默认值为4，
                                                                非USTORE分区表时为
                                                                NULL。

               max_trans                numeric                 最大事务数，默认值为
                                                                128，非USTORE分区表时
                                                                为NULL。

               initial_extent           numeric                 暂不支持，值为NULL。

               next_extent              numeric                 暂不支持，值为NULL。

               min_extent               numeric                 暂不支持，值为NULL。

               max_extent               numeric                 暂不支持，值为NULL。

               max_size                 numeric                 暂不支持，值为NULL。




文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                       5017
GaussDB
集中式版参考                                                                  8 系统表和系统视图


               名称                        类型                        描述

               pct_increase              numeric                   暂不支持，值为NULL。

               freelists                 numeric                   暂不支持，值为NULL。

               freelist_groups           numeric                   暂不支持，值为NULL。

               logging                   character varying(7)      是否记录对索引的更改。

               compression               character varying(13)     分区索引是否启用索引压
                                                                   缩。

               blevel                    numeric                   暂不支持，值为NULL。

               leaf_blocks               numeric                   暂不支持，值为NULL。

               distinct_keys             numeric                   暂不支持，值为NULL。

               avg_leaf_blocks_per_key   numeric                   暂不支持，值为NULL。

               avg_data_blocks_per_key   numeric                   暂不支持，值为NULL。

               clustering_factor         numeric                   根据索引的值表示表中行
                                                                   的顺序。需要通过执行
                                                                   analyze进行统计。

               num_rows                  numeric                   分区中的行数。需要通过
                                                                   执行vacuum进行统计。

               sample_size               numeric                   暂不支持，值为NULL。

               last_analyzed             timestamp with time       最近分析此分区的日期。
                                         zone                      数据库重启后，数据会丢
                                                                   失。

               buffer_pool               character varying(7)      分区的实际缓冲池。

               flash_cache               character varying(7)      暂不支持，值为NULL。

               cell_flash_cache          character varying(7)      暂不支持，值为NULL。

               user_stats                character varying(3)      暂不支持，值为NULL。

               pct_direct_access         numeric                   暂不支持，值为NULL。

               global_stats              character varying(3)      暂不支持，值为NULL。

               domidx_opstatus           character varying(6)      暂不支持，值为NULL。

               parameters                character varying(1000)   暂不支持，值为NULL。

               interval                  character varying(3)      分区是否在间隔分区表的
                                                                   间隔节中。

               segment_created           character varying(3)      索引分区段是否已创建。

               orphaned_entries          character varying(3)      暂不支持，值为NULL。




文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                          5018
GaussDB
集中式版参考                                                               8 系统表和系统视图


8.3.1.21 MY_IND_SUBPARTITIONS
              MY_IND_SUBPARTITIONS描述了当前用户拥有的二级分区表Local索引的索引分区信
              息（不包含分区表全局索引）。所有用户都可以访问。该视图同时存在于
              PG_CATALOG和SYS Schema下。

              表 8-183 MY_IND_SUBPARTITIONS 字段
               名称                       类型                      描述

               index_owner              character varying(64)   索引分区所属分区表索引
                                                                的所有者的名称。

               index_name               character varying(64)   索引分区所属分区表索引
                                                                的名称。

               partition_name           character varying(64)   索引所在一级分区的名
                                                                称。

               subpartition_name        character varying(64)   索引所在二级分区的名
                                                                称。

               def_tablespace_name      name                    索引分区的表空间名称。

               high_value               text                    索引分区所对应分区的边
                                                                界值。

               index_partition_usable   boolean                 索引分区是否可用：
                                                                ● t（true）：表示可用。
                                                                ● f（false）：表示不可
                                                                  用。

               schema                   character varying(64)   索引分区所属分区表索引
                                                                的模式。

               high_value_length        integer                 索引分区所对应分区的边
                                                                界的字符长度。

               partition_position       numeric                 索引分区在索引中的位
                                                                置。

               subpartition_position    numeric                 二级分区在分区中的位
                                                                置。

               status                   character varying(8)    指示索引分区是否可用。

               tablespace_name          name                    索引分区的表空间名称。

               pct_free                 numeric                 块中最小可用空间百分
                                                                比。

               ini_trans                numeric                 初始事务数，默认值为4，
                                                                非USTORE分区表时为
                                                                NULL。




文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                       5019
GaussDB
集中式版参考                                                                8 系统表和系统视图


               名称                        类型                      描述

               max_trans                 numeric                 最大事务数，默认值为
                                                                 128，非USTORE分区表时
                                                                 为NULL。

               initial_extent            numeric                 暂不支持，值为NULL。

               next_extent               numeric                 暂不支持，值为NULL。

               min_extent                numeric                 暂不支持，值为NULL。

               max_extent                numeric                 暂不支持，值为NULL。

               max_size                  numeric                 暂不支持，值为NULL。

               pct_increase              numeric                 暂不支持，值为NULL。

               freelists                 numeric                 暂不支持，值为NULL。

               freelist_groups           numeric                 暂不支持，值为NULL。

               logging                   character varying(7)    指示是否记录对索引的更
                                                                 改。

               compression               character varying(13)   用于二级分区的压缩类
                                                                 型。

               blevel                    numeric                 暂不支持，值为NULL。

               leaf_blocks               numeric                 暂不支持，值为NULL。

               distinct_keys             numeric                 暂不支持，值为NULL。

               avg_leaf_blocks_per_key   numeric                 暂不支持，值为NULL。

               avg_data_blocks_per_key   numeric                 暂不支持，值为NULL。

               clustering_factor         numeric                 根据索引的值指示表中行
                                                                 的顺序。需要通过执行
                                                                 analyze进行统计。

               num_rows                  numeric                 二级分区中的行数。需要
                                                                 通过执行vacuum进行统
                                                                 计。

               sample_size               numeric                 暂不支持，值为NULL。

               last_analyzed             timestamp with time     最近分析此分区的日期。
                                         zone                    数据库重启后，数据会丢
                                                                 失。

               buffer_pool               character varying(7)    二级分区的缓冲池。

               flash_cache               character varying(7)    暂不支持，值为NULL。

               cell_flash_cache          character varying(7)    暂不支持，值为NULL。

               user_stats                character varying(3)    暂不支持，值为NULL。



文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                         5020
GaussDB
集中式版参考                                                                  8 系统表和系统视图


               名称                       类型                         描述

               global_stats             character varying(3)       暂不支持，值为NULL。

               interval                 character varying(3)       指示分区是否在间隔分区
                                                                   表的间隔节中。

               segment_created          character varying(3)       指示索引分区段是否已创
                                                                   建。

               domidx_opstatus          character varying(6)       暂不支持，值为NULL。

               parameters               character varying(1000)    暂不支持，值为NULL。




8.3.1.22 MY_PART_COL_STATISTICS
              MY_PART_COL_STATISTICS视图显示当前用户拥有的表分区的列统计信息和直方图信
              息。所有用户可以访问。该视图同时存在于PG_CATALOG和SYS Schema下。

              表 8-184 MY_PART_COL_STATISTICS 字段
               名称                 类型                              描述

               table_name         character varying(128)          表名。

               partition_name     character varying(128)          表分区名称。

               column_name        character varying(4000)         列名。

               num_distinct       numeric                         列中不同值的数量。

               low_value          raw                             列中的低值。

               high_value         raw                             列中的高值。

               density            numeric                         ● 如果列上有直方图，则该
                                                                    字段将显示直方图中跨越
                                                                    少于2个端点的值的选择
                                                                    性。它不代表跨越2个或
                                                                    更多端点的值的选择性。
                                                                  ● 如果列上没有可用的直方
                                                                    图，则该字段的值为1/
                                                                    NUM_DISTINCT。

               num_nulls          numeric                         列中空值数。

               num_buckets        numeric                         列的直方图的桶数。

               sample_size        numeric                         用于分析此列的样本量。

               last_analyzed      timestamp(0) without time       最近分析此列的日期。数据
                                  zone                            库重启后，数据会丢失。

               global_stats       character varying(3)            暂不支持，值为NULL。



文档版本 01 (2026-04-30)             版权所有 © 华为云计算技术有限公司                              5021
GaussDB
集中式版参考                                                                   8 系统表和系统视图


               名称                     类型                           描述

               user_stats             character varying(3)         暂不支持，值为NULL。

               notes                  character varying(63)        暂不支持，值为NULL。

               avg_col_len            numeric                      列的平均长度（以字节为单
                                                                   位）。

               histogram              character varying(15)        直方图是否存在以及存在的
                                                                   类型：
                                                                   ● NONE：不存在直方图。
                                                                   ● FREQUENCY：频率直方
                                                                     图。
                                                                   ● EQUI-WIDTH：等宽直方
                                                                     图。

               schema                 character varying(64)        列所属的名称空间的名称。




8.3.1.23 MY_PART_INDEXES
              MY_PART_INDEXES视图显示当前用户下分区表索引的信息。该视图同时存在于
              PG_CATALOG和SYS Schema下。

              表 8-185 MY_PART_INDEXES 字段
               名称                      类型                     描述

               def_tablespace_name     name                   分区表索引的表空间名称。

               index_owner             character              分区表索引的所有者名称。
                                       varying(64)

               index_name              character              分区表索引的名称。
                                       varying(64)

               partition_count         bigint                 分区表索引的索引分区的个数。

               partitioning_key_cou    integer                分区表的分区键个数。
               nt

               partitioning_type       text                   分区表的分区策略。
                                                              说明
                                                               当前分区表策略支持范围见CREATE
                                                               TABLE PARTITION。

               schema                  character              分区表索引的模式。
                                       varying(64)

               table_name              character              分区表索引所属的分区表名称。
                                       varying(64)




文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                               5022
GaussDB
集中式版参考                                                                 8 系统表和系统视图


               名称                       类型                  描述

               subpartitioning_type     text                二级分区表的分区策略。如果分区表
                                                            是一级分区表，则显示NONE。
                                                            说明
                                                             当前二级分区表策略支持范围见CREATE
                                                             TABLE SUBPARTITION。

               def_subpartition_cou     integer             默认创建二级分区的个数，二级分区
               nt                                           表为1，一级分区表为0。

               subpartitioning_key_c    integer             分区表二级分区键的个数。
               ount




8.3.1.24 MY_PART_KEY_COLUMNS
              MY_PART_KEY_COLUMNS视图显示了当前用户拥有的分区表或分区索引的分区键列的
              相关信息。该视图所有用户可访问，仅可查看当前用户所属信息。该视图同时存在于
              PG_CATALOG和SYS Schema下。


              表 8-186 MY_PART_KEY_COLUMNS 字段

               名称                     类型               描述

               name                   character        分区表名或索引名。
                                      varying(128)

               object_type            character(5)     对象类型。
                                                       ● 若分区为分区表，此列为table。
                                                       ● 若分区为分区索引，此列为index。

               column_name            character        分区表或索引的键列名。
                                      varying(4000)

               column_position        numeric          列在分区中的位置。

               collated_column_i      numeric          暂不支持，值为NULL。
               d




8.3.1.25 MY_PART_TABLES
              MY_PART_TABLES视图显示当前用户下分区表的信息。该视图同时存在于
              PG_CATALOG和SYS Schema下。


              表 8-187 MY_PART_TABLES 字段

               名称                        类型                      描述

               table_owner               character varying(64)   分区表的所有者名称。


文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                          5023
GaussDB
集中式版参考                                                                     8 系统表和系统视图


               名称                      类型                      描述

               table_name              character varying(64)   分区表的名称。

               partitioning_type       text                    分区表的分区策略。
                                                               说明
                                                                当前分区表策略支持范围见
                                                                CREATE TABLE PARTITION。

               partition_count         bigint                  分区表的分区个数。

               partitioning_key_coun   integer                 分区表的分区键个数。
               t

               def_tablespace_name     name                    分区表的表空间名称。

               schema                  character varying(64)   分区表的模式。

               subpartitioning_type    text                    二级分区表的分区策略。如果分
                                                               区表是一级分区表，则显示
                                                               NONE。
                                                               说明
                                                                当前二级分区表策略支持范围见
                                                                CREATE TABLE SUBPARTITION。

               def_subpartition_coun   integer                 默认创建二级分区的个数，二级
               t                                               分区表为1，一级分区表为0。

               subpartitioning_key_c   integer                 分区表二级分区键的个数。
               ount

               status                  character varying(8)    暂不支持，值为valid。

               def_pct_free            numeric                 添加分区时使用的PCTFREE默认
                                                               值。

               def_pct_used            numeric                 暂不支持，值为NULL。

               def_ini_trans           numeric                 添加分区时使用的INITRANS默
                                                               认值。

               def_max_trans           numeric                 添加分区时使用的MAXTRANS
                                                               默认值。

               def_initial_extent      character varying(40)   暂不支持，值为NULL。

               def_next_extent         character varying(40)   暂不支持，值为NULL。

               def_min_extents         character varying(40)   暂不支持，值为NULL。

               def_max_extents         character varying(40)   暂不支持，值为NULL。

               def_max_size            character varying(40)   暂不支持，值为NULL。

               def_pct_increase        character varying(40)   暂不支持，值为NULL。

               def_freelists           numeric                 暂不支持，值为NULL。

               def_freelist_groups     numeric                 暂不支持，值为NULL。


文档版本 01 (2026-04-30)                 版权所有 © 华为云计算技术有限公司                                   5024
GaussDB
集中式版参考                                                                       8 系统表和系统视图


               名称                      类型                       描述

               def_logging             character varying(7)     暂不支持，值为NULL。

               def_compression         character varying(8)     添加分区时使用的默认压缩：
                                                                ● NONE
                                                                ● ENABLED
                                                                ● DISABLED

               def_compress_for        character varying(30)    添加分区时使用的默认压缩。
                                                                说明
                                                                 可用的压缩方法和压缩级别见
                                                                 WITH ( { storage_parameter =
                                                                 value } [, ... ] )。

               def_buffer_pool         character varying(7)     暂不支持，值为DEFAULT。

               def_flash_cache         character varying(7)     暂不支持，值为NULL。

               def_cell_flash_cache    character varying(7)     暂不支持，值为NULL。

               ref_ptn_constraint_na   character varying(128)   暂不支持，值为NULL。
               me

               interval                character                区间值字符串。
                                       varying(1000)

               autolist                text                     一级列表分区是否开启自动扩展
                                                                功能。
                                                                ● YES：表示开启自动扩展功
                                                                  能。
                                                                ● NO：表示不开启自动扩展功
                                                                  能。

               interval_subpartition   character                二级间隔分区区间值字符串。
                                       varying(1000)

               autolist_subpartition   text                     二级列表分区是否开启自动扩展
                                                                功能。
                                                                ● YES：表示开启自动扩展功
                                                                  能。
                                                                ● NO：表示不开启自动扩展功
                                                                  能。

               is_nested               character varying(3)     暂不支持，值为NO。

               def_segment_creation    character varying(4)     暂不支持段页式设置，当启用
                                                                segment时，值为YES。

               def_indexing            character varying(3)     暂不支持，值为ON。

               def_inmemory            character varying(8)     暂不支持，值为NONE。

               def_inmemory_priorit    character varying(8)     暂不支持，值为NULL。
               y


文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                                       5025
GaussDB
集中式版参考                                                                 8 系统表和系统视图


               名称                      类型                       描述

               def_inmemory_distrib    character varying(15)    暂不支持，值为NULL。
               ute

               def_inmemory_compr      character varying(17)    暂不支持，值为NULL。
               ession

               def_inmemory_duplic     character varying(13)    暂不支持，值为NULL。
               ate

               def_read_only           character varying(3)     暂不支持，值为NO。

               def_cellmemory          character varying(24)    暂不支持，值为NULL。

               def_inmemory_service    character varying(12)    暂不支持，值为NULL。

               def_inmemory_service    character                暂不支持，值为NULL。
               _name                   varying(1000)




8.3.1.26 MY_SUBPART_COL_STATISTICS
              MY_SUBPART_COL_STATISTICS视图显示了当前用户拥有的分区对象的子分区的列统
              计信息和直方图信息。所有用户都可以访问该视图。该视图同时存在于PG_CATALOG
              和SYS Schema下。


              表 8-188 MY_SUBPART_COL_STATISTICS 字段

               名称                     类型                         描述

               table_name             character varying(128)     表名。

               subpartition_name      character varying(128)     子分区名称。

               column_name            character varying(4000)    列名。

               num_distinct           numeric                    列中不同值的数量。

               low_value              raw                        列中的低值。

               high_value             raw                        列中的高值。

               density                numeric                    ● 如果列上有直方图，则该
                                                                   字段将显示直方图中跨越
                                                                   少于2个端点的值的选择
                                                                   性。它不代表跨越2个或
                                                                   更多端点的值的选择性。
                                                                 ● 如果列上没有可用的直方
                                                                   图，则该字段的值为1/
                                                                   NUM_DISTINCT。

               num_nulls              numeric                    列中空值数。

               num_buckets            numeric                    列的直方图的桶数。


文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                          5026
GaussDB
集中式版参考                                                                8 系统表和系统视图


               名称                   类型                          描述

               sample_size          numeric                     用于分析此列的样本量。

               last_analyzed        timestamp(0) without time   最近分析此列的日期。数据
                                    zone                        库重启后，数据会丢失。

               global_stats         character varying(3)        暂不支持，值为NULL。

               user_stats           character varying(3)        暂不支持，值为NULL。

               notes                character varying(41)       暂不支持，值为NULL。

               avg_col_len          numeric                     列的平均长度（以字节为单
                                                                位）。

               histogram            character varying(15)       直方图是否存在以及存在的
                                                                类型：
                                                                ● NONE：不存在直方图。
                                                                ● FREQUENCY：频率直方
                                                                  图。
                                                                ● EQUI-WIDTH：等宽直方
                                                                  图。

               schema               character varying(64)       列所属的名称空间的名称。




8.3.1.27 MY_SUBPART_KEY_COLUMNS
              MY_SUBPART_KEY_COLUMNS视图显示了当前用户所拥有的二级分区表或分区索引的
              分区键列的相关信息。该视图所有用户可访问，仅可查看当前用户所属信息。该视图
              同时存在于PG_CATALOG和SYS schema下。

              表 8-189 MY_SUBPART_KEY_COLUMNS 字段
               名称                  类型               描述

               name                character        二级分区表名或索引名。
                                   varying(128)

               object_type         character(5)     对象类型。
                                                    ● 若分区为分区表，此列为table。
                                                    ● 若分区为分区索引，此列为index。

               column_name         character        二级分区表的二级分区键列名。
                                   varying(4000)

               column_position     numeric          列在分区中的位置。

               collated_column_i   numeric          暂不支持，值为NULL。
               d




文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                          5027
GaussDB
集中式版参考                                                                8 系统表和系统视图


8.3.1.28 MY_TAB_PARTITIONS
              MY_TAB_PARTITIONS视图显示当前用户下所有一级分区信息。当前用户下每个分区表
              的一级分区都会在MY_TAB_PARTITIONS中有一条记录。所有用户都可以访问。该视图
              同时存在于PG_CATALOG和SYS Schema下。


              表 8-190 MY_TAB_PARTITIONS 字段

               名称                   类型                      描述

               table_owner          character varying(64)   表的所有者。

               table_name           character varying(64)   关系表名称。

               partition_name       character varying(64)   分区名称。

               high_value           text                    分区的边界值。
                                                            ● 对于范围分区和间隔分区，显
                                                              示各分区的上边界值。
                                                            ● 对于列表分区，显示各分区的
                                                              取值列表。
                                                            ● 对于哈希分区，显示各分区的
                                                              编号。

               tablespace_name      name                    分区表的表空间名称。

               schema               character varying(64)   名称空间的名称。

               subpartition_cou     bigint                  二级分区的个数。
               nt

               high_value_lengt     integer                 分区边界值表达式的长度。
               h

               composite            character varying(3)    表是否为二级分区表。

               partition_position   numeric                 分区在表中的位置。

               pct_free             numeric                 块中可用空间的最小百分比。

               pct_used             numeric                 暂不支持，值为NULL。

               ini_trans            numeric                 初始事务数，默认值为4，非
                                                            USTORE分区表时为NULL。

               max_trans            numeric                 最大事务数，默认值为128，非
                                                            USTORE分区表时为NULL。

               initial_extent       numeric                 暂不支持，值为NULL。

               next_extent          numeric                 暂不支持，值为NULL。

               min_extent           numeric                 暂不支持，值为NULL。

               max_extent           numeric                 暂不支持，值为NULL。

               max_size             numeric                 暂不支持，值为NULL。


文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                         5028
GaussDB
集中式版参考                                                               8 系统表和系统视图


               名称                 类型                       描述

               pct_increase       numeric                  暂不支持，值为NULL。

               freelists          numeric                  暂不支持，值为NULL。

               freelist_groups    numeric                  暂不支持，值为NULL。

               logging            character varying(7)     是否记录对表的更改。

               compression        character varying(8)     表分区的实际压缩属性。

               compress_for       character varying(30)    暂不支持，值为NULL。

               num_rows           numeric                  分区中的行数。

               blocks             numeric                  暂不支持，值为NULL。

               empty_blocks       numeric                  暂不支持，值为NULL。

               avg_space          numeric                  暂不支持，值为NULL。

               chain_cnt          numeric                  暂不支持，值为NULL。

               avg_row_len        numeric                  暂不支持，值为NULL。

               sample_size        numeric                  暂不支持，值为NULL。

               last_analyzed      timestamp with time      最近分析此分区的日期。
                                  zone

               buffer_pool        character varying(7)     用于分区块的缓冲池。

               flash_cache        character varying(7)     暂不支持，值为NULL。

               cell_flash_cache   character varying(7)     暂不支持，值为NULL。

               global_stats       character varying(3)     暂不支持，值为NULL。

               user_stats         character varying(3)     暂不支持，值为NULL。

               is_nested          character varying(3)     指示这是否是嵌套表分区。

               parent_table_par   character varying(128)   暂不支持，值为NULL。
               tition

               interval           character varying(3)     分区是否在间隔分区表的间隔节
                                                           中。

               segment_created    character varying(4)     表分区是否创建了段。

               indexing           character varying(4)     暂不支持，值为NULL。

               read_only          character varying(4)     暂不支持，值为NULL。

               inmemory           character varying(8)     暂不支持，值为NULL。

               inmemory_priorit   character varying(8)     暂不支持，值为NULL。
               y




文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                        5029
GaussDB
集中式版参考                                                                   8 系统表和系统视图


               名称                   类型                           描述

               inmemory_distrib     character varying(15)        暂不支持，值为NULL。
               ute

               inmemory_compr       character varying(17)        暂不支持，值为NULL。
               ession

               inmemory_duplic      character varying(13)        暂不支持，值为NULL。
               ate

               cellmemory           character varying(24)        暂不支持，值为NULL。

               inmemory_servic      character varying(12)        暂不支持，值为NULL。
               e

               inmemory_servic      character varying(100)       暂不支持，值为NULL。
               e_name

               memoptimize_re       character varying(8)         暂不支持，值为NULL。
               ad

               memoptimize_wr       character varying(8)         暂不支持，值为NULL
               ite




8.3.1.29 MY_TAB_SUBPARTITIONS
              MY_TAB_SUBPARTITIONS视图显示当前用户可访问的所有子分区表信息。该视图所有
              用户可访问，仅可查看当前用户所属信息。该视图同时存在于PG_CATALOG和SYS
              Schema下。

              表 8-191 MY_TAB_SUBPARTITIONS 字段

               名称                    类型                    描述

               table_owner           character             表的所有者。
                                     varying(64)

               schema                character             模式名称。
                                     varying(64)

               table_name            character             表名。
                                     varying(64)

               partition_name        character             分区名称。
                                     varying(64)

               subpartition_name     character             子分区名称。
                                     varying(64)

               high_value            text                  子分区边界值表达式。

               high_value_length     integer               子分区边界值表达式的长度。

               partition_position    numeric               分区在表内的位置。


文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                          5030
GaussDB
集中式版参考                                                              8 系统表和系统视图


               名称                     类型               描述

               subpartition_positi    numeric          子分区在分区中的位置。
               on

               tablespace_name        name             子分区所在的表空间名称。

               pct_free               numeric          块中最小可用空间百分比。

               pct_used               numeric          暂不支持，值为NULL。

               ini_trans              numeric          初始事务数。

               max_trans              numeric          最大事务数。

               initial_extent         numeric          暂不支持，值为NULL。

               next_extent            numeric          暂不支持，值为NULL。

               min_extent             numeric          暂不支持，值为NULL。

               max_extent             numeric          暂不支持，值为NULL。

               max_size               numeric          暂不支持，值为NULL。

               pct_increase           numeric          暂不支持，值为NULL。

               freelists              numeric          暂不支持，值为NULL。

               freelist_groups        numeric          暂不支持，值为NULL。

               logging                character        是否记录对表的更改：
                                      varying(7)       ● YES
                                                       ● NO

               compression            character        子分区是否压缩。取值范围：
                                      varying(8)       ENABLED、DISABLED。

               compress_for           character        暂不支持，值为NULL。
                                      varying(30)

               num_rows               numeric          暂不支持，值为NULL。

               blocks                 numeric          暂不支持，值为NULL。

               empty_blocks           numeric          暂不支持，值为NULL。

               avg_space              numeric          暂不支持，值为NULL。

               chain_cnt              numeric          暂不支持，值为NULL。

               avg_row_len            numeric          暂不支持，值为NULL。

               sample_size            numeric          暂不支持，值为NULL。

               last_analyzed          timestamp with   最近分析此表的日期。数据库重启后，数
                                      time zone        据会丢失。




文档版本 01 (2026-04-30)                 版权所有 © 华为云计算技术有限公司                    5031
GaussDB
集中式版参考                                                           8 系统表和系统视图


               名称                   类型              描述

               buffer_pool          character       子分区的缓冲池：
                                    varying(7)      ● DEFAULT
                                                    ● KEEP
                                                    ● RECYCLE
                                                    ● NULL

               flash_cache          character       暂不支持，值为NULL。
                                    varying(7)

               cell_flash_cache     character       暂不支持，值为NULL。
                                    varying(7)

               global_stats         character       暂不支持，值为NULL。
                                    varying(3)

               user_stats           character       暂不支持，值为NULL。
                                    varying(3)

               interval             character       分区是在间隔分区表的间隔节中
                                    varying(3)      （YES），还是在范围节中（NO）。

               segment_created      character       表子分区段是否已创建：
                                    varying(3)      ● YES：已创建。
                                                    ● NO：未创建。
                                                    ● N/A：此表没有子分区。

               indexing             character       暂不支持，值为NULL。
                                    varying(3)

               read_only            character       暂不支持，值为NULL。
                                    varying(5)

               inmemory             character       暂不支持，值为NULL。
                                    varying(8)

               inmemory_priority    character       暂不支持，值为NULL。
                                    varying(8)

               inmemory_distribu    character       暂不支持，值为NULL。
               te                   varying(15)

               inmemory_compre      character       暂不支持，值为NULL。
               ssion                varying(17)

               inmemory_duplica     character       暂不支持，值为NULL。
               te                   varying(13)

               inmemory_service     character       暂不支持，值为NULL。
                                    varying(12)

               inmemory_service_    character       暂不支持，值为NULL。
               name                 varying(1000)



文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                    5032
GaussDB
集中式版参考                                                          8 系统表和系统视图


               名称                   类型              描述

               cellmemory           character       暂不支持，值为NULL。
                                    varying(24)

               memoptimize_rea      character       暂不支持，值为NULL。
               d                    varying(8)

               memoptimize_writ     character       暂不支持，值为NULL。
               e                    varying(8)




8.3.1.30 GS_STATIO_ALL_PARTITIONS
              GS_STATIO_ALL_PARTITIONS视图包含当前数据库中每个分区表分区的I/O统计信息，
              此视图信息由gs_statio_get_all_partitions_stats()函数查询得到。

              表 8-192 GS_STATIO_ALL_PARTITIONS 字段
               名称                  类型             描述

               partition_oid       oid            分区OID。

               schemaname          name           该分区模式名。

               relname             name           该分区所在表的表名。

               partition_name      name           该分区所在一级分区名。

               sub_partition_nam   name           该分区所在二级分区名。
               e

               heap_blks_read      bigint         从该分区中读取的磁盘块数。

               heap_blks_hit       bigint         该分区缓存命中数。

               idx_blks_read       bigint         从分区中所有索引读取的磁盘块数。

               idx_blks_hit        bigint         分区中所有索引命中缓存数。

               toast_blks_read     bigint         该分区的TOAST表分区读取的磁盘块数（如果
                                                  存在）。

               toast_blks_hit      bigint         该分区的TOAST表分区命中缓冲区数（如果存
                                                  在）。

               tidx_blks_read      bigint         该分区的TOAST表分区索引读取的磁盘块数
                                                  （如果存在）。

               tidx_blks_hit       bigint         该分区的TOAST表分区索引命中缓冲区数（如
                                                  果存在）。




文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                  5033
GaussDB
集中式版参考                                                         8 系统表和系统视图


8.3.1.31 GS_STAT_XACT_ALL_PARTITIONS
              GS_STAT_XACT_ALL_PARTITIONS视图显示命名空间中所有分区表分区的事务状态信
              息，此视图信息通过gs_stat_get_xact_all_partitions_stats()函数查询。

              表 8-193 GS_STAT_XACT_ALL_PARTITIONS 字段

               名称                 类型       描述

               partition_oid      oid      分区的OID。

               schemaname         name     该分区的模式名。

               relname            name     该分区所在表的表名。

               partition_name     name     该分区所在一级分区名。

               sub_partition_na   name     该分区所在二级分区名。
               me

               seq_scan           bigint   该分区发起的顺序扫描数。

               seq_tup_read       bigint   顺序扫描抓取的活跃行数。

               idx_scan           bigint   该分区发起的索引扫描数。

               idx_tup_fetch      bigint   索引扫描抓取的活跃行数。

               n_tup_ins          bigint   插入行数。

               n_tup_upd          bigint   更新行数。

               n_tup_del          bigint   删除行数。

               n_tup_hot_upd      bigint   热更新行数（比如没有更新所需的单独索引）。




8.3.1.32 GS_STAT_ALL_PARTITIONS
              GS_STAT_ALL_PARTITIONS视图包含当前数据库中所有分区表每个分区的信息，每个
              分区各占一行（二级分区表则只显示其二级分区），显示该分区访问情况的统计信
              息。此视图信息通过gs_stat_get_all_partitions_stats()函数查询。

              表 8-194 GS_STAT_ALL_PARTITIONS 字段

               名称                 类型              描述

               partition_oid      oid             分区的OID。

               schemaname         name            该分区所在表的模式名。

               relname            name            该分区所在表的表名。

               partition_name     name            该分区所在一级分区名。

               sub_partition_na   name            该分区所在二级分区名。
               me


文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                  5034
GaussDB
集中式版参考                                                          8 系统表和系统视图


               名称                 类型               描述

               seq_scan           bigint           该分区发起的顺序扫描数。

               seq_tup_read       bigint           顺序扫描抓取的活跃行数。

               idx_scan           bigint           该分区发起的索引扫描数。

               idx_tup_fetch      bigint           索引扫描抓取的活跃行数。

               n_tup_ins          bigint           插入行数。

               n_tup_upd          bigint           更新行数。

               n_tup_del          bigint           删除行数。

               n_tup_hot_upd      bigint           热更新行数（比如没有更新所需的单独索
                                                   引）。

               n_live_tup         bigint           估计活跃行数。

               n_dead_tup         bigint           估计非活跃行数。在Ustore表中仅代表死
                                                   行指针数量。

               last_vacuum        timestamp with   该分区最后一次被清理的时间。
                                  time zone

               last_autovacuum    timestamp with   该分区最后一次被autovacuum守护线程
                                  time zone        清理的时间。

               last_analyze       timestamp with   该分区最后一次被分析的时间。
                                  time zone

               last_autoanalyze   timestamp with   该分区最后一次被autovacuum守护线程
                                  time zone        分析的时间。

               vacuum_count       bigint           该分区被清理的次数。

               autovacuum_cou     bigint           该分区被autovacuum守护线程清理的次
               nt                                  数。

               analyze_count      bigint           该分区被分析的次数。

               autoanalyze_cou    bigint           该分区被autovacuum守护线程分析的次
               nt                                  数。




8.3.2 负载管理

8.3.2.1 GS_WLM_CGROUP_INFO
              GS_WLM_CGROUP_INFO视图显示当前执行作业的控制组的信息。

                  说明

                 当查询视图无数据显示时，请联系华为工程师提供技术支撑。


文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                   5035
