GaussDB
集中式版参考                                                              8 系统表和系统视图


               名称                  类型            描述

               inherited_from      character     当前策略是继承哪一个对象上的策略，取值范
                                   varying(20)   围：
                                                 ● TABLE：表。
                                                 ● TABLE PARTITION：分区。
                                                 ● TABLE SUBPARTITION：二级分区。
                                                 ● INDEX：普通索引。
                                                 ● POLICY NOT INHERITED：不继承。

               tbs_inherited_fro   character     当前策略是继承哪一个TS上的策略。当前版本
               m                   varying(30)   值为null。

               enabled             character     该策略对当前对象是否开启。
                                   varying(7)

               deleted             character     标识该对象上的ILM策略被删除，实际操作结
                                   varying(7)    果是删除分区或者是删除分区上的策略，该条
                                                 记录都会被删除（ILMOBJ）。




8.3.3.13 GS_MY_ILMPOLICIES
              GS_MY_ILMPOLICIES视图反映ILM策略的概要信息，包含策略名称、类型、启用禁用
              状态、删除状态。

              表 8-211 GS_MY_ILMPOLICIES 字段
               名称               类型                描述

               policy_name      character         ADO策略名称，系统自动生成，规则为：p
                                varying(128)      +策略ID。

               policy_type      character         策略类型。
                                varying(13)

               tablespace       character         表空间名称，当该策略制定在表空间上时此
                                varying(30)       处有值。当前版本值为null。

               enabled          character         策略是否开启。
                                varying(6)

               deleted          character         策略是否被删除。
                                varying(7)




8.3.4 通信




文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                          5047
GaussDB
集中式版参考                                                      8 系统表和系统视图


8.3.4.1 GS_COMM_LISTEN_ADDRESS_EXT_INFO
              GS_COMM_LISTEN_ADDRESS_EXT_INFO描述了查询连接扩展IP的相关线程、会话、
              socket等DFX信息。当前不支持查询该视图。

              表 8-212 GS_COMM_LISTEN_ADDRESS_EXT_INFO 字段
               名称               类型           描述

               node_name        text         当前实例名。

               app              text         当前连接DN的客户端。

               tid              bigint       当前线程的线程号。

               lwtid            integer      当前线程的轻量级线程号。

               query_id         bigint       当前线程的查询ID。

               socket           integer      当前物理连接的socket fd。

               remote_ip        text         当前连接对端IP。

               remote_port      text         当前连接对端port。

               local_ip         text         当前连接本端IP。

               local_port       text         当前连接本端port。




8.3.4.2 GS_GET_LISTEN_ADDRESS_EXT_INFO
              GS_GET_LISTEN_ADDRESS_EXT_INFO视图描述了查询当前实例扩展IP配置信息。当前
              不支持查询该视图。

              表 8-213 GS_GET_LISTEN_ADDRESS_EXT_INFO 字段
               名称              类型            描述

               node_name       text          当前实例名。

               host            text          当前实例侦听IP地址。

               port            bigint        当前实例侦听的port。

               ext_listen_ip   text          当前实例配置扩展IP地址。




8.3.4.3 PG_COMM_DELAY
              PG_COMM_DELAY视图展示单个DN的通信库时延状态。




文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司               5048
GaussDB
集中式版参考                                                    8 系统表和系统视图

              表 8-214 PG_COMM_DELAY 字段

               名称             类型         描述

               node_name      text       节点名称。

               remote_name    text       连接对端节点名称。

               remote_host    text       连接对端IP地址。

               stream_num     integer    当前物理连接使用的stream逻辑连接数量。

               min_delay      integer    当前物理连接一分钟内探测到的最小时延，单
                                         位微秒。
                                         说明
                                          负数结果无效，请重新等待时延状态更新后再执行。

               average        integer    当前物理连接一分钟内探测时延的平均值，单
                                         位微秒。

               max_delay      integer    当前物理连接一分钟内探测到的最大时延，单
                                         位微秒。




8.3.4.4 PG_COMM_RECV_STREAM
              PG_COMM_RECV_STREAM视图展示单个DN上所有的通信库接收流状态。

              表 8-215 PG_COMM_RECV_STREAM 字段

               名称             类型         描述

               node_name      text       节点名称。

               local_tid      bigint     使用此通信流的线程ID。

               remote_name    text       连接对端节点名称。

               remote_tid     bigint     连接对端线程ID。

               idx            integer    通信对端DN在本DN内的标识编号。

               sid            integer    通信流在物理连接中的标识编号。

               tcp_sock       integer    通信流所使用的tcp通信socket。

               state          text       通信流当前的状态。
                                         ● UNKNOWN：表示当前逻辑连接状态未
                                           知。
                                         ● READY：表示逻辑连接已就绪。
                                         ● RUN：表示逻辑连接发送报文正常。
                                         ● HOLD：表示逻辑连接发送报文等待中。
                                         ● CLOSED：表示关闭逻辑连接。
                                         ● TO_CLOSED：表示将会关闭逻辑连接。


文档版本 01 (2026-04-30)         版权所有 © 华为云计算技术有限公司                    5049
GaussDB
集中式版参考                                                    8 系统表和系统视图


               名称             类型        描述

               query_id       bigint    通信流对应的debug_query_id编号。

               pn_id          integer   通信流所执行查询的plan_node_id编号。

               send_smp       integer   通信流所执行查询send端的smpid编号。

               recv_smp       integer   通信流所执行查询recv端的smpid编号。

               recv_bytes     bigint    通信流接收的数据总量，单位Byte。

               time           bigint    通信流当前生命周期使用时长，单位ms。

               speed          bigint    通信流的平均接收速率，单位Byte/s。

               quota          bigint    通信流当前的通信配额值，单位Byte。

               buff_usize     bigint    通信流当前缓存的数据大小，单位Byte。




8.3.4.5 PG_COMM_SEND_STREAM
              PG_COMM_SEND_STREAM视图展示单个DN上所有的通信库发送流状态。

              表 8-216 PG_COMM_SEND_STREAM 字段

               名称             类型        描述

               node_name      text      节点名称。

               local_tid      bigint    使用此通信流的线程ID。

               remote_name    text      连接对端节点名称。

               remote_tid     bigint    连接对端线程ID。

               idx            integer   通信对端DN在本DN内的标识编号。

               sid            integer   通信流在物理连接中的标识编号。

               tcp_sock       integer   通信流所使用的tcp通信socket。

               state          text      通信流当前的状态。
                                        ● UNKNOWN：表示当前逻辑连接状态未
                                          知。
                                        ● READY：表示逻辑连接已就绪。
                                        ● RUN：表示逻辑连接发送报文正常。
                                        ● HOLD：表示逻辑连接发送报文等待中。
                                        ● CLOSED：表示关闭逻辑连接。
                                        ● TO_CLOSED：表示将会关闭逻辑连接。

               query_id       bigint    通信流对应的debug_query_id编号。

               pn_id          integer   通信流所执行查询的plan_node_id编号。


文档版本 01 (2026-04-30)         版权所有 © 华为云计算技术有限公司                    5050
GaussDB
集中式版参考                                                            8 系统表和系统视图


               名称                 类型        描述

               send_smp           integer   通信流所执行查询send端的smpid编号。

               recv_smp           integer   通信流所执行查询recv端的smpid编号。

               send_bytes         bigint    通信流发送的数据总量，单位Byte。

               time               bigint    通信流当前生命周期使用时长，单位ms。

               speed              bigint    通信流的平均发送速率，单位Byte/s。

               quota              bigint    通信流当前的通信配额值，单位Byte。

               wait_quota         bigint    通信流等待quota值产生的额外时间开销，单位
                                            ms。




8.3.4.6 PG_COMM_STATUS
              PG_COMM_STATUS视图展示单个DN的通信库状态。

              表 8-217 PG_COMM_STATUS 字段
               名称                 类型        描述

               node_name          text      节点名称。

               rxpck_rate         integer   节点通信库接收速率，单位Byte/s。

               txpck_rate         integer   节点通信库发送速率，单位Byte/s。

               rxkbyte_rate       bigint    节点通信库接收速率，单位KByte/s。

               txkbyte_rate       bigint    节点通信库发送速率，单位KByte/s。

               buffer             bigint    cmailbox的buffer大小。

               memkbyte_libcom    bigint    libcomm线程通信内存大小，单位Byte。
               m

               memkbyte_libpq     bigint    libpq线程通信内存大小，单位Byte。

               used_pm            integer   postmaster线程实时使用率。

               used_sflow         integer   gs_sender_flow_controller线程实时使用率。

               used_rflow         integer   gs_receiver_flow_controller线程实时使用率。

               used_rloop         integer   多个gs_receivers_loop线程中高的实时使用
                                            率。

               stream             integer   当前使用的逻辑连接总数。




文档版本 01 (2026-04-30)             版权所有 © 华为云计算技术有限公司                          5051
GaussDB
集中式版参考                                                        8 系统表和系统视图


8.3.4.7 PG_GET_INVALID_BACKENDS
              不支持查询该视图，报错提示：

              “Un-support feature”。

8.3.5 段页式存储

8.3.5.1 GS_SEG_DATAFILES
              GS_SEG_DATAFILES查看所有表空间的数据文件信息。只支持管理员权限用户查询。


              表 8-218 GS_SEG_DATAFILES 字段

               名称              类型           描述

               node_name       text         节点名称。

               file_name       text         数据文件名，例如，base/
                                            17467/2_fsm。

               file_id         integer      数据文件标识。取值范围：[1,5]的int4
                                            值。

               bucketnode      integer      ● 0~1023表示hashbucket表的
                                              bucketnode（当前集中式版本暂不
                                              支持hashbucket表）。
                                            ● 1024表示段页式普通表的
                                              bucketnode。
                                            ● 1025表示段页式全局临时表的
                                              bucketnode。
                                            ● 1026表示段页式unlogged表的
                                              bucketnode。
                                            ● 1027表示段页式本地临时表的
                                              bucketnode。

               forknum         integer      段对象的分支类型。取值范围：
                                            ● 0表示main fork。
                                            ● 1表示fsm fork。
                                            ● 2表示vm fork。

               tablespace_na   name         数据文件所属的表空间名称。
               me

               contents        text         数据文件的存储内容。取值范围：
                                            ● permanent：永久。
                                            ● unlogged：不记录日志。
                                            ● temporary：全局临时。
                                            ● temporary2：本地临时。


文档版本 01 (2026-04-30)            版权所有 © 华为云计算技术有限公司                   5052
