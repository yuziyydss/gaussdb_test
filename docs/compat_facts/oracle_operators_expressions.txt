GaussDB
集中式版参考                                                            4 兼容性说明


4.3.3 伪列
分层查询伪列

              表 4-20 分层查询伪列
               Oracle数据库                      GaussDB数据库

               connect_by_iscycle             支持

               connect_by_isleaf              支持

               level伪列                        支持




序列伪列

              表 4-21 序列
               Oracle数据      GaussDB数据库       差异
               库

               currval       支持，有差异           GaussDB以函数形式实现。兼容Oracle调用方
                                              式。

               nextval       支持，有差异           GaussDB以函数形式实现。兼容Oracle调用方
                                              式。




rowid 伪列

              表 4-22 rowid
               Oracle数据      GaussDB数    差异
               库             据库

               rowid         支持，有差异      Oracle在创建表时支持rowid伪列；GaussDB在创建
                                         表时使用hasrowid选项支持rowid伪列。




文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                 3098
GaussDB
集中式版参考                                                              4 兼容性说明


rownum 伪列

表 4-23 rownum

Oracle数据库       GaussDB数据库     差异

rownum          支持，有差异         Oracle在left、right、full join的条件中使用rownum进行过滤
                               时，不同的条件下表现不尽相同，可能存在忽略或部分忽略
                               rownum条件的现象，而GaussDB在此情况下则表现为对left、
                               right、full join后的结果进行过滤。




4.3.4 操作符
                GaussDB数据库基本兼容Oracle数据库的运算符。


SQL 运算符

                表 4-24 SQL 运算符

                Oracle数据库                   GaussDB数据库

                一元运算符和二元运算符                 支持

                运算符优先级                      支持




算术运算符

                表 4-25 算术运算符

                Oracle数据库                   GaussDB数据库

                正负（+-）一元运算符                 支持

                加减（+-）二元运算符                 支持

                乘除（*/）二元运算符                 支持




COLLATE 运算符

                表 4-26 COLLATE 运算符

                Oracle数据库                   GaussDB数据库

                COLLATE collation_name      支持



文档版本 01 (2026-04-30)             版权所有 © 华为云计算技术有限公司                      3099
GaussDB
集中式版参考                                                                     4 兼容性说明


连接运算符

              表 4-27 连接运算符

               Oracle数据库                        GaussDB数据库

               ||                               支持




分层查询运算符

              表 4-28 分层查询运算符

               Oracle数据库             GaussDB数   差异
                                     据库

               prior                 支持，有差      GaussDB中仅支持对普通列调用，不支持对
                                     异          函数等调用。

               connect_by_root       支持，有差      GaussDB中，connect_by_root调用时，用括
                                     异          号修饰操作值时，行为与Oracle一致；不使
                                                用括号时，仅支持对普通列调用此运算符。

               connect_by_iscycle    支持，有差      GaussDB中可以单独使用，Oracle中必须搭
                                     异          配NOCYCLE一起使用。

               start with，connect    支持，有差      GaussDB中start with，connect by，level可
               by，level              异          以作为列别名，Oracle不支持。

               start with            支持，有差      GaussDB中start with后面支持空字符查询，
                                     异          查询结果为空，支持对伪列的操作；Oracle
                                                会抛出异常。

               connect by            支持，有差      GaussDB中connect by支持对NULL值操作，
                                     异          Oracle会抛出异常。




集合运算符

              表 4-29 集合运算符

               Oracle数据库                        GaussDB数据库

               union                            支持

               union all                        支持

               intersect                        支持

               minus                            支持




文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                           3100
GaussDB
集中式版参考                                                              4 兼容性说明


多集合运算符

              表 4-30 多集合运算符
               Oracle数据库                         GaussDB数据库

               multiset except                   支持

               multiset intersect                支持

               multiset union                    支持




用户自定义运算符

              表 4-31 用户自定义运算符
               Oracle数据库         GaussDB数   差异
                                 据库

               CREATE            支持，有差      ● Oracle中提供的CONTEXT_CLAUSE支持自定义
               OPERATOR          异            功能评估函数，和GaussDB约束选择性评估函数
                                              不同。GaussDB不支持自定义功能评估函数。
                                            ● Oracle和GaussDB可选参数差异较大。GaussDB
                                              具体请参见参数说明。




比较操作符
               Oracle数据库                     GaussDB数据库

               <=                            支持

               <>                            支持

               >=                            支持

               ^=                            支持

               !=                            不支持，!=中间存在空格时，!会被识别为阶乘。




              当比较操作符（<=、<>、>=、^=）中间存在空格时，也可以识别成没有空格进行正常
              操作。!=中间存在空格时，!会被识别为阶乘，可能会导致结果与预期不一致。

4.3.5 表达式
              GaussDB数据库兼容大部分Oracle数据库表达式。




文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                   3101
GaussDB
集中式版参考                                                                4 兼容性说明

              表 4-32 表达式
               Oracle数据库   GaussDB数据   差异
                           库

               简单表达式       支持          -

               分析视图表达      不支持         -
               式

               复合表达式       支持          -

               case表达式     支持          -

               列表示式        支持          -

               cursor表达式   不支持         -

               日期时间表达      支持，有差异      GaussDB的输出结果中不会带时区信息，而
               式                       Oracle会带有类似“PM AMERICA/
                                       LOS_ANGELES”的时区信息。

               函数表达式       支持          -

               区间表达式       部分支持        GaussDB支持形如SELECT INTERVAL
                                       '999999999 23:59:59.999' day(9) to second
                                       FROM DUAL;的语句，而不支持形如
                                       SELECT(SYSDATE- SYSDATE) DAY TO SECOND
                                       FROM DUAL;的语句。Oracle均支持。

               json对象访问表   部分支持，有      ● GaussDB支持通过“->'key'”的方式从JSON
               达式          差异            对象中提取value，而Oracle支持通过
                                         “.key”方式来提取value。
                                       ● 对于JSON数组对象而言，Oracle支持通过
                                         “.key”方式一次性提取所有key对应的
                                         value，但GaussDB目前不支持。

               模型表达式       不支持         -

               对象表达式       不支持         -

               占位符表达式      部分支持        对于形如“:var”的一般占位符表达式，
                                       GaussDB支持，但不支持通过INDICATOR关键字
                                       将两个一般占位符表达式结合起来。

               标量子查询表      支持          -
               达式

               类型构造器表      部分支持        GaussDB不支持在类型构造器前指定New关键
               达式                      字，而Oracle支持。

               表达式list     支持          -




文档版本 01 (2026-04-30)        版权所有 © 华为云计算技术有限公司                               3102
GaussDB
集中式版参考                                                                                4 兼容性说明


               Oracle数据库   GaussDB数据   差异
                           库

               含(+)操作符表    部分支持，有      ● 对于使用OR操作符连接的外连接表达式，
               达式          差异            GaussDB仅支持OR操作符左右两侧均为同一
                                         单表的外连接表达式。
                                         在(+)和OR操作符混用的场景下，GaussDB与
                                         Oracle的行为存在差异，以下是与Oracle 19c
                                         的对比说明：
                                        公共语句
                                        -- 执行以下建表语句并插入数据
                                        CREATE TABLE a(id int, b int);
                                        CREATE TABLE a2(id int, c int);
                                        INSERT INTO a VALUES(1,1);
                                        INSERT INTO a VALUES (null, 11);
                                        INSERT INTO a VALUES (2, null);
                                        INSERT INTO a VALUES (123,123);
                                        INSERT INTO a VALUES (5, 500);
                                        INSERT INTO a VALUES (null, 600);
                                        INSERT INTO a2 VALUES(1,2);
                                        INSERT INTO a2 VALUES (3,null);
                                        INSERT INTO a2 VALUES (null, 22);
                                        INSERT INTO a2 VALUES (234,234);
                                        INSERT INTO a2 VALUES (5, 700);
                                        INSERT INTO a2 VALUES (null, 800);
                                        示例一
                                        SELECT * FROM a,a2 WHERE (a.id(+) = 1 AND a.id(+) <=
                                        2) OR a2.id(+) = 1 ORDER BY 1,2,3,4;
                                        GaussDB：
                                        -- 执行公共语句后再执行查询语句
                                        gaussdb=# SELECT * FROM a,a2 WHERE (a.id(+) = 1
                                        AND a.id(+) <= 2) OR a2.id(+) = 1 ORDER BY 1,2,3,4;
                                        ERROR: Operator "(+)" is not allowed used with "OR"
                                        together
                                        -- 清理本次示例新建的数据表（需按照实际情况修改表名）
                                        gaussdb=# DROP TABLE a;
                                        gaussdb=# DROP TABLE a2;
                                        Oracle：
                                        -- 执行公共语句后再执行查询语句
                                        SQL> SELECT * FROM a,a2 WHERE (a.id(+) = 1 AND
                                        a.id(+) <= 2) OR a2.id(+) = 1 ORDER BY 1,2,3,4;

                                        ID|       B|         ID|       C
                                        ----------|----------|----------|----------
                                        1|      1|           1|      2
                                        1|      1|           3|
                                        1|      1|           5| 700
                                        1|      1|         234| 234
                                        1|      1|       |      22
                                        1|      1|       | 800
                                        2|       |          1|       2
                                        5|     500|            1|      2
                                        123|      123|            1|     2
                                        |     11|           1|      2
                                        |    600|           1|       2

                                        11 rows selected.
                                        -- 清理本次示例新建的数据表（需按照实际情况修改表名）
                                        SQL> DROP TABLE a;
                                        SQL> DROP TABLE a2;




文档版本 01 (2026-04-30)        版权所有 © 华为云计算技术有限公司                                            3103
GaussDB
集中式版参考                                                                                4 兼容性说明


               Oracle数据库   GaussDB数据   差异
                           库

                                        GaussDB因不满足OR操作符左右两侧为同一
                                        单表的外连接表达式的限制，报错；Oracle不
                                        报错。
                                        示例二
                                        SELECT * FROM a,a2 WHERE a.id(+) = a2.id AND a.id(+) !
                                        = 2 OR a.id(+) IS NULL AND a.id(+) = a2.id ORDER BY
                                        1,2,3,4;
                                        GaussDB：
                                        -- 执行公共语句后再执行查询语句
                                        gaussdb=# SELECT * FROM a,a2 WHERE a.id(+) = a2.id
                                        OR a.id IS NULL AND a.id(+) = a2.id ORDER BY 1,2,3,4;
                                        id | b | id | c
                                        ----+-----+-----+-----
                                        1| 1| 1| 2
                                        5 | 500 | 5 | 700
                                        |    | 3|
                                        |    | 234 | 234
                                        |    |   | 22
                                        |    |   | 800
                                        (6 rows)
                                        -- 提取公共表达式后，执行转化后的SQL语句
                                        gaussdb=# SELECT * FROM a,a2 WHERE a.id(+) = a2.id
                                        AND (a.id(+) != 2 OR a.id(+) IS NULL) ORDER BY 1,2,3,4;
                                        id | b | id | c
                                        ----+-----+-----+-----
                                        1| 1| 1| 2
                                        5 | 500 | 5 | 700
                                        |    | 3|
                                        |    | 234 | 234
                                        |    |   | 22
                                        |    |   | 800
                                        (6 rows)
                                        -- 清理本次示例新建的数据表（需按照实际情况修改表名）
                                        gaussdb=# DROP TABLE a;
                                        gaussdb=# DROP TABLE a2;
                                        Oracle：
                                        -- 执行公共语句后再执行查询语句
                                        SQL> SELECT * FROM a,a2 WHERE a.id(+) = a2.id OR
                                        a.id IS NULL AND a.id(+) = a2.id ORDER BY 1,2,3,4;
                                        ERROR at line 1:
                                        ORA-01719: outer join operator (+) not allowed in
                                        operand of OR or IN
                                        -- 提取公共表达式后，执行转化后的SQL语句
                                        SQL> SELECT * FROM a,a2 WHERE a.id(+) = a2.id AND
                                        (a.id(+) != 2 OR a.id(+) IS NULL) ORDER BY 1,2,3,4;

                                        ID|        B|       ID|      C
                                        ----------|----------|----------|----------
                                        1|        1|        1|     2
                                        5|     500|          5| 700
                                        |       |        3|
                                        |       |       234| 234
                                        |       |         |    22
                                        |       |         | 800

                                        6 rows selected.
                                        -- 清理本次示例新建的数据表（需按照实际情况修改表名）
                                        SQL> DROP TABLE a;
                                        SQL> DROP TABLE a2;




文档版本 01 (2026-04-30)        版权所有 © 华为云计算技术有限公司                                             3104
GaussDB
集中式版参考                                                                               4 兼容性说明


               Oracle数据库   GaussDB数据   差异
                           库

                                        GaussDB因满足OR操作符左右两侧均为同一
                                        单表的外连接表达式，因此不报错；Oracle则
                                        直接报错（ORA-01719）。
                                        通过提取公共表达式a.id(+) = a2.id，转化后
                                        的SQL语句为
                                        SELECT * FROM a,a2 WHERE a.id(+) = a2.id AND
                                        (a.id(+) != 2 OR a.id(+) IS NULL) ORDER BY 1,2,3,4;
                                        在两种数据库中执行结果一致。
                                       ● GaussDB不支持在MERGE INTO语句的
                                         WHERE子句中使用带(+)的外连接表达式，而
                                         Oracle支持。
                                         以下是与Oracle 19c的对比用例：
                                        GaussDB：
                                        -- 执行MERGE INTO，其中WHEN MATCHED THEN后的
                                        SQL子句中WHERE含(+)操作符表达式
                                        gaussdb=# MERGE INTO test_tb2
                                        gaussdb-# USING test_tb1
                                        gaussdb-# ON (test_tb1.c1 = test_tb2.c2)
                                        gaussdb-# WHEN MATCHED THEN
                                        gaussdb-# UPDATE SET test_tb2.c1 = test_tb1.c2
                                        WHERE test_tb1.c3 = test_tb2.c3(+);
                                        ERROR: Operator "(+)" can only be used in
                                        WhereClause of Select-Statement or Subquery.
                                        LINE 5: ...set test_tb2.c1 = test_tb1.c2 where test_tb1.c3 =
                                        test_tb2.c...
                                                                                    ^
                                        Oracle：
                                        -- 执行MERGE INTO，其中WHEN MATCHED THEN后的
                                        SQL子句中WHERE含(+)操作符表达式
                                        SQL> MERGE INTO test_tb2
                                        USING test_tb1
                                        ON (test_tb1.c1 = test_tb2.c2)
                                        WHEN MATCHED THEN
                                        UPDATE SET test_tb2.c1 = test_tb1.c2 WHERE test_tb1.c3
                                        = test_tb2.c3(+);

                                        Affected rows:10




4.3.6 条件
              本章节描述常见的条件兼容项，包含比较条件、浮点条件、逻辑条件、模型条件、多
              集合条件、模式匹配条件、NULL值条件、XML条件、SQL/JSON条件、复合条件、
              BETWEEN条件、EXISTS条件、IN条件、IS OF TYPE条件。详情请参见表4-33。




文档版本 01 (2026-04-30)        版权所有 © 华为云计算技术有限公司                                                 3105
GaussDB
集中式版参考                                                               4 兼容性说明

                  表 4-33 条件

                  Oracle数据库     GaussDB数据   差异
                                库

                  比较条件          支持，有差异      语句中存在ANY、SOME、ALL操作符时存在差
                                            异，Oracle支持对list对象进行操作，而GaussDB
                                            中需要将list对象转换成数组表达式的形式后再
                                            进行操作。

                  浮点条件          不支持         -

                  逻辑条件          支持          -

                  模型条件          不支持         -

                  多集合条件         不支持         -

                  模式匹配条件        支持          -

                  NULL值条件       支持          -

                  XML条件         不支持         -

                  SQL/JSON条     部分支持，有差     ● GaussDB不支持IS JSON条件和
                  件             异             JSON_TEXTCONTAINS条件。
                                            ● GaussDB中JSONB_EQ条件等同于Oracle中
                                              JSON_EQUAL条件，但GaussDB不支持
                                              ERROR子句。
                                            ● GaussDB中JSONB_EXISTS条件等同于Oracle
                                              中JSON_EXISTS条件，但GaussDB不支持
                                              ERROR子句、EMPTY子句和PASSING子句。

                  复合条件          支持          -

                  BETWEEN条      支持          -
                  件

                  EXISTS条件      支持          -

                  IN条件          支持          -

                  IS OF TYPE条   不支持         -
                  件




4.3.7 驱动

4.3.7.1 JDBC

4.3.7.1.1 Array
                  本章节描述在JDBC驱动使用java.sql.Array类型时，Oracle与GaussDB的差异。


文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                       3106
