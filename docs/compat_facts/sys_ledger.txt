GaussDB
集中式版参考                                                             8 系统表和系统视图


               名称                     类型        描述

               nodeis_central         boolean   表明当前节点是否为中心控制节点，对DN无效。
                                                ● t（true）：表示是。
                                                ● f（false）：表示不是。

               nodeis_active          boolean   表明当前节点是否是正常状态，对DN无效。
                                                ● t（true）：表示是。
                                                ● f（false）：表示不是。

               cluster_name           text      数据库实例名称。

               slice_order            integer   节点归属分片的顺序序号。




8.2.6 账本数据库

8.2.6.1 GS_GLOBAL_CHAIN
              GS_GLOBAL_CHAIN系统表记录用户对防篡改用户表的修改操作信息，每条记录对应
              一次表级修改操作。具有审计管理员权限的用户可以查询此系统表，所有用户均不允
              许修改此系统表。


              表 8-23 GS_GLOBAL_CHAIN 字段

               名称              类型                描述

               blocknum        bigint            区块号，当前用户操作在账本中记录的序号。

               dbname          name              数据库名称。被修改的防篡改用户表所属的
                                                 database。

               username        name              用户名，执行用户表修改操作的用户名。

               starttime       timestamp with    用户执行操作的时间。
                               time zone

               relid           oid               用户表oid，被修改的防篡改用户表oid。

               relnsp          name              模式名，被修改的防篡改用户表所属的模式名。

               relname         name              用户表名，被修改的防篡改用户表名。

               relhash         hash16            当前操作产生的表级别hash变化量。

               globalhash      hash32            全局摘要，由当前行信息与前一行globalhash计
                                                 算而来，将整个表串联起来，用于验证
                                                 GS_GLOBAL_CHAIN数据完整性。

               txcommand       text              被记录操作的SQL语句。




文档版本 01 (2026-04-30)                   版权所有 © 华为云计算技术有限公司                 4855
GaussDB
集中式版参考                                                              8 系统表和系统视图


8.2.7 SPM 计划管理

8.2.7.1 GS_SPM_SQL
              GS_SPM_SQL系统表是用于存储SPM SQL相关信息的系统表，具备sysadmin权限的用
              户可以对该系统表进行读操作，但只有初始用户才可以对该系统表进行写操作。

              表 8-24 GS_SPM_SQL 字段
               名称              类型                描述

               sql_namespace   oid               schema oid。

               sql_hash        bigint            SPM中SQL的唯一标识。

               sql_text        text              SQL的文本串。

               param_num       integer           SQL执行所需参数的数量。

               user            oid               登录用户的oid。

               creation_time   timestamp         本记录的创建时间。
                               with time
                               zone




8.2.7.2 GS_SPM_BASELINE
              GS_PLAN_BASELINE系统表是用于存储baseline相关信息的系统表，具备sysadmin权
              限的用户可以对该系统表进行读操作，但只有初始用户才可以对该系统表进行写操
              作。

              表 8-25 GS_SPM_BASELINE 字段
               名称                    类型            描述

               sql_namespace         oid           schema oid。

               sql_hash              bigint        SPM中SQL的唯一标识。

               plan_hash             bigint        当前SQL下的plan的唯一标识。

               outline               text          outline文本，可固定当前计划的一组Hint。

               cost                  double        计划的总代价。
                                     precision

               user                  oid           创建baseline的用户。




文档版本 01 (2026-04-30)            版权所有 © 华为云计算技术有限公司                             4856
