GaussDB
集中式版参考                                                               8 系统表和系统视图


               名称                     类型          描述

               userid                 oid         用户的oid。

               baseline_creation_t    timestam    baseline的创建时间。
               ime                    p with
                                      time zone

               history_creation_ti    timestam    跳变历史的创建时间。
               me                     p with
                                      time zone

               modification_time      timestam    baseline的修改时间。
                                      p with
                                      time zone

               namespace_oids         text        当前search_path中的OID列表，其中OID满足以
                                                  下特征：
                                                  ● 用户所拥有权限的Schema的OID。
                                                  ● 存在的Schema的OID。
                                                  ● 非本地临时表所在Schema的OID。




8.3.7 审计

8.3.7.1 ADM_AUDIT_OBJECT
              ADM_AUDIT_OBJECT显示数据库中所有对象的审计跟踪记录, 该视图同时存在于
              pg_catalog和sys schema下。仅拥有AUDITADMIN属性及ADM_AUDIT_OBJECT的
              SELECT权限的用户才可以查看审计信息；三权分立未开启时，拥有SYSADMIN属性的
              用户也可以查看审计信息。

              表 8-232 ADM_AUDIT_OBJECT 字段

               名称                    类型           描述

               os_username           character    暂不支持，值为NULL。
                                     varying(25
                                     5)

               username              character    操作被审计的用户的名称，不是用户ID。
                                     varying(12
                                     8)

               userhost              character    暂不支持，值为NULL。
                                     varying(12
                                     8)

               terminal              character    暂不支持，值为NULL。
                                     varying(25
                                     5)


文档版本 01 (2026-04-30)                 版权所有 © 华为云计算技术有限公司                      5067
GaussDB
集中式版参考                                                           8 系统表和系统视图


               名称             类型           描述

               timestamp      timestam     在本地数据库会话时区中创建审计跟踪条目的日
                              p(0)         期和时间（审计会话创建的条目的用户登录日期
                              without      和时间）。
                              time zone    说明
                                            在初始化数据库时，若指定GUC参数
                                            mapping_date_to_datea的值为on，则该字段的类型
                                            会变更为datea。

               owner          character    受操作影响的对象的创建者。
                              varying(12
                              8)

               obj_name       character    受操作影响的对象的名称。
                              varying(12
                              8)

               action_name    character    DBA_AUDIT_TRAIL中的“操作”列中的数字代码
                              varying(28   对应的动作类型名称。
                              )            说明
                                            GaussDB的action_name字段与A数据库审计动作不一
                                            致。

               new_owner      character    暂不支持，值为NULL。
                              varying(12
                              8)

               new_name       character    暂不支持，值为NULL。
                              varying(12
                              8)

               ses_actions    character    暂不支持，值为NULL。
                              varying(19
                              )

               comment_text   character    暂不支持，值为NULL。
                              varying(40
                              00)

               sessionid      numeric      暂不支持，值为NULL。

               entryid        numeric      暂不支持，值为NULL。

               statementid    numeric      暂不支持，值为NULL。

               returncode     numeric      暂不支持，值为NULL。

               priv_used      character    暂不支持，值为NULL。
                              varying(40
                              )

               client_id      character    暂不支持，值为NULL。
                              varying(12
                              8)




文档版本 01 (2026-04-30)          版权所有 © 华为云计算技术有限公司                            5068
GaussDB
集中式版参考                                                                 8 系统表和系统视图


               名称                 类型           描述

               econtext_id        character    暂不支持，值为NULL。
                                  varying(64
                                  )

               session_cpu        numeric      暂不支持，值为NULL。

               extended_timesta   timestam     在UTC（协调世界时）时区创建审计跟踪条目的
               mp                 p(6) with    时间戳（审计会话创建的条目的用户登录时间
                                  time zone    戳）。

               proxy_sessionid    numeric      暂不支持，值为NULL。

               global_uid         character    暂不支持，值为NULL。
                                  varying(32
                                  )

               instance_number    numeric      暂不支持，值为NULL。

               os_process         character    暂不支持，值为NULL。
                                  varying(16
                                  )

               transactionid      text         访问或修改对象事务的事务标识符。
                                               说明
                                                GaussDB的transactionid字段与A数据库中
                                                transactionid数据的类型保持一致。

               scn                numeric      暂不支持，值为NULL。

               sql_bind           nvarchar2(   暂不支持，值为NULL。
                                  2000)

               sql_text           character    查询的SQL文本。
                                  varying      说明
                                                GaussDB的sql_text字段为解析后的SQL描述语句，不
                                                完全与执行的SQL语句相同。

               obj_edition_nam    character    暂不支持，值为NULL。
               e                  varying(12
                                  8)




8.3.7.2 ADM_AUDIT_SESSION
              ADM_AUDIT_SESSION显示所有连接断开数据库审计信息，GaussDB审计信息主要通
              过gs_query_audit函数查询，该视图同时存在于PG_CATALOG和SYS schema下。仅拥
              有AUDITADMIN属性及ADM_AUDIT_SESSION的SELECT权限的用户才可以查看审计信
              息；三权分立未开启时，拥有SYSADMIN属性的用户也可以查看审计信息。




文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                            5069
GaussDB
集中式版参考                                                            8 系统表和系统视图

              表 8-233 ADM_AUDIT_SESSION 字段
               名称              类型           描述

               os_username     character    暂不支持，值为NULL。
                               varying(25
                               5)

               username        character    操作被审计的用户的名称，不是用户ID。
                               varying(12
                               8)

               userhost        character    暂不支持，值为NULL。
                               varying(12
                               8)

               terminal        character    暂不支持，值为NULL。
                               varying(25
                               5)

               timestamp       timestam     创建审核跟踪条目的日期和时间(用户登录创建的
                               p(0)         条目的日期和时间AUDIT SESSION)。
                               without      说明
                               time zone     在初始化数据库时，若指定GUC参数
                                             mapping_date_to_datea的值为on，则该字段的类型
                                             会变更为datea。

               action_name     character    DBA_AUDIT_TRAIL中的ACTION列中的数字代码
                               varying(28   对应的动作类型的名称。
                               )            说明
                                             GaussDB的action_name字段与A数据库审计动作不一
                                             致。

               logoff_time     timestam     暂不支持，值为NULL。
                               p(0)
                               without
                               time zone

               logoff_lread    numeric      暂不支持，值为NULL。

               logoff_pread    numeric      暂不支持，值为NULL。

               logoff_lwrite   numeric      暂不支持，值为NULL。

               logoff_dlock    character    暂不支持，值为NULL。
                               varying(40
                               )

               sessionid       numeric      暂不支持，值为NULL。

               returncode      numeric      暂不支持，值为NULL。

               client_id       character    暂不支持，值为NULL。
                               varying(12
                               8)

               session_cpu     numeric      暂不支持，值为NULL。



文档版本 01 (2026-04-30)           版权所有 © 华为云计算技术有限公司                            5070
GaussDB
集中式版参考                                                               8 系统表和系统视图


               名称                 类型           描述

               extended_timesta   timestam     创建审核跟踪条目的时间戳(创建的条目的用户登
               mp                 p(6) with    录时间戳AUDIT SESSION)在UTC（协调世界
                                  time zone    时）时区。

               proxy_sessionid    numeric      暂不支持，值为NULL。

               global_uid         character    暂不支持，值为NULL。
                                  varying(32
                                  )

               instance_number    numeric      暂不支持，值为NULL。

               os_process         character    暂不支持，值为NULL。
                                  varying(16
                                  )




8.3.7.3 ADM_AUDIT_STATEMENT
              ADM_AUDIT_STATEMENT显示所有GRANT、REVOKE审计跟踪条目，GaussDB审计
              信息主要通过gs_query_audit函数查询，该视图同时存在于PG_CATALOG和SYS
              schema下。仅拥有AUDITADMIN属性及ADM_AUDIT_STATEMENT的SELECT权限的用
              户才可以查看审计信息；三权分立未开启时，拥有SYSADMIN属性的用户也可以查看
              审计信息。

              表 8-234 ADM_AUDIT_STATEMENT 字段
               名称                 类型           描述

               os_username        character    暂不支持，值为NULL。
                                  varying(25
                                  5)

               username           character    操作被审计的用户的名称，不是用户ID。
                                  varying(12
                                  8)

               userhost           character    暂不支持，值为NULL。
                                  varying(12
                                  8)

               terminal           character    暂不支持，值为NULL
                                  varying(25
                                  5)

               timestamp          timestam     创建审核跟踪条目的日期和时间（用户登录创建
                                  p(0)         条目的日期和时间AUDIT SESSION）。
                                  without      说明
                                  time zone     在初始化数据库时，若指定GUC参数
                                                mapping_date_to_datea的值为on，则该字段的类型
                                                会变更为datea。




文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                            5071
GaussDB
集中式版参考                                                               8 系统表和系统视图


               名称              类型           描述

               owner           character    暂不支持，值为NULL。
                               varying(12
                               8)

               obj_name        character    受操作影响的对象的名称。
                               varying(12
                               8)

               action_name     character    DBA_AUDIT_TRAIL中的ACTION列中的数字代码
                               varying(28   对应的动作类型的名称。
                               )            说明
                                             GaussDB的action_name字段与A数据库审计动作不一
                                             致，transactionid字段与A数据库中transactionid数据
                                             的类型保持一致。

               new_name        character    暂不支持，值为NULL。
                               varying(12
                               8)

               obj_privilege   character    暂不支持，值为NULL。
                               varying(32
                               )

               sys_privilege   character    暂不支持，值为NULL。
                               varying(40
                               )

               admin_option    character    暂不支持，值为NULL。
                               varying(1)

               grantee         character    暂不支持，值为NULL。
                               varying(12
                               8)

               audit_option    character    暂不支持，值为NULL。
                               varying(40
                               )

               ses_actions     character    暂不支持，值为NULL。
                               varying(19
                               )

               comment_text    character    暂不支持，值为NULL。
                               varying(40
                               00)

               sessionid       numeric      暂不支持，值为NULL。

               entryid         numeric      暂不支持，值为NULL。

               statementid     numeric      暂不支持，值为NULL。

               returncode      numeric      暂不支持，值为NULL。




文档版本 01 (2026-04-30)           版权所有 © 华为云计算技术有限公司                               5072
GaussDB
集中式版参考                                                                 8 系统表和系统视图


               名称                 类型           描述

               priv_used          character    暂不支持，值为NULL。
                                  varying(40
                                  )

               client_id          character    暂不支持，值为NULL。
                                  varying(12
                                  8)

               econtext_id        character    暂不支持，值为NULL。
                                  varying(64
                                  )

               session_cpu        numeric      暂不支持，值为NULL。

               extended_timesta   timestam     在UTC（协调世界时）时区创建审核跟踪条目的
               mp                 p(6) with    时间戳（创建条目的用户登录时间戳AUDIT
                                  time zone    SESSION）。

               proxy_sessionid    numeric      暂不支持，值为NULL。

               global_uid         character    暂不支持，值为NULL。
                                  varying(32
                                  )

               instance_number    numeric      暂不支持，值为NULL。

               os_process         character    暂不支持，值为NULL。
                                  varying(16
                                  )

               transactionid      text         访问或修改对象事务的事务标识符。
                                               说明
                                                GaussDB的transactionid字段与A数据库中
                                                transactionid数据的类型保持一致。

               scn                numeric      暂不支持，值为NULL。

               sql_bind           nvarchar2(   暂不支持，值为NULL。
                                  2000)

               sql_text           character    查询的SQL文本。
                                  varying(20   说明
                                  00)           GaussDB的sql_text字段为解析后的SQL描述语句，不
                                                完全与执行的SQL语句相同。

               obj_edition_nam    character    暂不支持，值为NULL。
               e                  varying(12
                                  8)




8.3.7.4 ADM_AUDIT_TRAIL
              ADM_AUDIT_TRAIL显示所有标准审计跟踪条目，GaussDB审计信息主要通过
              gs_query_audit函数查询，该视图同时存在于PG_CATALOG和SYS schema下。仅拥有

文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                            5073
GaussDB
集中式版参考                                                           8 系统表和系统视图

              AUDITADMIN属性及ADM_AUDIT_TRAIL的SELECT权限的用户才可以查看审计信息；
              三权分立未开启时，拥有SYSADMIN属性的用户也可以查看审计信息。

              表 8-235 ADM_AUDIT_TRAIL 字段
               名称              类型             描述

               os_username     character      暂不支持，值为NULL。
                               varying(255)

               username        character      操作被审计的用户的名称，不是用户ID。
                               varying(128)

               userhost        character      暂不支持，值为NULL。
                               varying(128)

               terminal        character      暂不支持，值为NULL。
                               varying(255)

               timestamp       timestamp(0)   在本地数据库会话时区中创建审计跟踪条
                               without time   目的日期和时间（审计会话创建的条目的
                               zone           用户登录日期和时间）。
                                              说明
                                               在初始化数据库时，若指定GUC参数
                                               mapping_date_to_datea的值为on，则该字段
                                               的类型会变更为datea。

               owner           character      受操作影响的对象的创建者。
                               varying(128)

               obj_name        character      受操作影响的对象的名称。
                               varying(128)

               action          numeric        暂不支持，值为NULL。

               action_name     character      action列中的数字代码对应的action类型名
                               varying(28)    称。
                                              说明
                                               GaussDB的action_name字段与A数据库审计动
                                               作不一致。

               new_owner       character      暂不支持，值为NULL。
                               varying(128)

               new_name        character      暂不支持，值为NULL。
                               varying(128)

               obj_privilege   character      暂不支持，值为NULL。
                               varying(32)

               sys_privilege   character      暂不支持，值为NULL。
                               varying(40)

               admin_option    character      暂不支持，值为NULL。
                               varying(1)




文档版本 01 (2026-04-30)           版权所有 © 华为云计算技术有限公司                           5074
GaussDB
集中式版参考                                                            8 系统表和系统视图


               名称                 类型               描述

               grantee            character        暂不支持，值为NULL。
                                  varying(128)

               audit_option       character        暂不支持，值为NULL。
                                  varying(40)

               ses_actions        character        暂不支持，值为NULL。
                                  varying(19)

               logoff_time        timestamp(0)     暂不支持，值为NULL。
                                  without time
                                  zone

               logoff_lread       numeric          暂不支持，值为NULL。

               logoff_pread       numeric          暂不支持，值为NULL。

               logoff_lwrite      numeric          暂不支持，值为NULL。

               logoff_dlock       character        暂不支持，值为NULL。
                                  varying(40)

               comment_text       character        暂不支持，值为NULL。
                                  varying(4000)

               sessionid          numeric          暂不支持，值为NULL。

               entryid            numeric          暂不支持，值为NULL。

               statementid        numeric          暂不支持，值为NULL。

               returncode         numeric          暂不支持，值为NULL。

               priv_used          character        暂不支持，值为NULL。
                                  varying(40)

               client_id          character        暂不支持，值为NULL。
                                  varying(128)

               econtext_id        character        暂不支持，值为NULL。
                                  varying(64)

               session_cpu        numeric          暂不支持，值为NULL。

               extended_timesta   timestamp(6)     创建审计跟踪条目的时间戳（创建条目的
               mp                 with time zone   用户登录时间戳UTC（协调通用）中的审
                                                   计会话时间）时区。

               proxy_sessionid    numeric          暂不支持，值为NULL。

               global_uid         character        暂不支持，值为NULL。
                                  varying(32)

               instance_number    numeric          暂不支持，值为NULL。

               os_process         character        暂不支持，值为NULL。
                                  varying(16)


文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                     5075
GaussDB
集中式版参考                                                                8 系统表和系统视图


               名称                类型                描述

               transactionid     text              访问或修改对象事务的事务标识符。
                                                   说明
                                                    GaussDB的transactionid字段与A数据库中
                                                    transactionid数据的类型保持一致。

               scn               numeric           暂不支持，值为NULL。

               sql_bind          nvarchar2(2000)   暂不支持，值为NULL。

               sql_text          nvarchar2         查询的SQL文本。
                                                   说明
                                                    GaussDB的sql_text字段为解析后的SQL描述语
                                                    句，不完全与执行的SQL语句相同。

               obj_edition_nam   character         暂不支持，值为NULL。
               e                 varying(128)

               dbid              numeric           暂不支持，值为NULL。

               rls_info          clob              暂不支持，值为NULL。

               current_user      character         暂不支持，值为NULL。
                                 varying(128)




8.3.7.5 GS_AUDITING
              GS_AUDITING视图显示对数据库相关操作的所有审计信息。默认只有系统管理员权限
              或安全策略管理员权限才可以访问，普通用户需要授权才可以访问。


              表 8-236 GS_AUDITING 字段

               名称                类型          描述

               polname           name        策略名称，需要唯一，不可重复。

               pol_type          text        审计策略类型。取值范围：
                                             ● access：审计DML操作。
                                             ● privilege：审计DDL操作。

               polenabled        boolean     用来表示策略启动开关。
                                             ● t（true）：启动。
                                             ● f（false）：不启动。

               access_type       name        DML数据库操作相关类型，例如SELECT、INSERT、
                                             DELETE等；或是DDL数据库操作相关类型，例如
                                             CREATE、ALTER、DROP等。

               label_name        name        资源标签名称。对应系统表gs_auditing_policy中的
                                             polname字段。

               priv_object       text        数据库资产的路径。


文档版本 01 (2026-04-30)             版权所有 © 华为云计算技术有限公司                                 5076
GaussDB
集中式版参考                                                        8 系统表和系统视图


               名称              类型        描述

               filter_name     text      过滤条件的逻辑字符串。




8.3.7.6 GS_AUDITING_ACCESS
              GS_AUDITING_ACCESS视图显示对数据库ACCESS相关操作的所有审计信息。默认只
              有系统管理员权限或安全策略管理员权限才可以访问，普通用户需要授权才可以访
              问。

              表 8-237 GS_AUDITING_ACCESS 字段
               名称              类型        描述

               polname         name      策略名称，需要唯一，不可重复。

               pol_type        text      审计策略类型，值为‘access’，表示审计DML操
                                         作。

               polenabled      boolean   策略是否启动。
                                         ● t（true）：启动。
                                         ● f（false）：不启动。

               access_type     name      ACCESS数据库操作相关类型。例如SELECT、
                                         INSERT、DELETE等。

               label_name      name      资源标签名称。对应系统表gs_auditing_policy中的
                                         polname字段。

               access_object   text      数据库资产的路径。

               filter_name     text      过滤条件的逻辑字符串。




8.3.7.7 GS_AUDITING_PRIVILEGE
              GS_AUDITING_PRIVILEGE视图显示对数据库PRIVILEGE相关操作的所有审计信息。默
              认只有系统管理员权限或安全策略管理员权限才可以访问，普通用户需要授权才可以
              访问。

              表 8-238 GS_AUDITING_PRIVILEGE 字段
               名称              类型        描述

               polname         name      策略名称，需要唯一，不可重复。

               pol_type        text      审计策略类型，值为‘privilege’，表示审计DDL
                                         操作。




文档版本 01 (2026-04-30)           版权所有 © 华为云计算技术有限公司                      5077
GaussDB
集中式版参考                                                                8 系统表和系统视图


               名称             类型          描述

               polenabled     boolean     策略是否启动。
                                          ● t（true）：启动。
                                          ● f（false）：不启动。

               access_type    name        PRIVILEGE数据库操作相关类型。例如CREATE、
                                          ALTER、DROP等。

               label_name     name        资源标签名称。对应系统表gs_auditing_policy中的
                                          polname字段。

               priv_object    text        包含数据库对象的全称域名。

               filter_name    text        过滤条件的逻辑字符串。




8.3.8 用户和权限管理

8.3.8.1 ADM_COL_PRIVS
              ADM_COL_PRIVS视图显示所有的列权限授予信息。默认只有系统管理员权限才可以访
              问此系统视图，普通用户需要授权才可以访问。该视图同时存在于PG_CATALOG和SYS
              Schema下。

              表 8-239 ADM_COL_PRIVS 字段

               名称                       类型                       描述

               grantor                  character varying(128)   执行授权的用户名。

               owner                    character varying(128)   对象的所有者。

               grantee                  character varying(128)   被授予权限的用户或角色
                                                                 的名称。

               table_schema             character varying(128)   对象的schema。

               table_name               character varying(128)   对象的名称。

               column_name              character varying(128)   列的名称。

               privilege                character varying(40)    列的权限。

               grantable                character varying(3)     是否授予特权。
                                                                 ● YES：授予。
                                                                 ● NO：不授予。

               common                   character varying(3)     暂不支持，值为NULL。

               inherited                character varying(3)     暂不支持，值为NULL。



文档版本 01 (2026-04-30)          版权所有 © 华为云计算技术有限公司                              5078
