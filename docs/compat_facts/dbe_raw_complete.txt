GaussDB
集中式版参考                                                        3 存储过程

                  trunc
                 -------
                    26
                 (1 row)


3.12.2.16 DBE_RAW
              DBE_RAW包提供了用于操作RAW类型的接口。

                  说明

                 RAW类型的外部表现形式是十六进制，内部存储形式是二进制。例如一个RAW类型的数据
                 11001011的表现形式为‘CB’，即在实际的类型转换中输入的是‘CB’。


权限介绍
              DBE_RAW模式属于系统目录，DBE_RAW模式除了系统管理员用户在初始化数据库时
              拥有创建权限，模式及模式下的所有接口，所有用户仅具有使用权限。


接口介绍
              高级功能包DBE_RAW支持的接口请参见表3-269。

              表 3-269 DBE_RAW

               接口名称                   描述

               DBE_RAW.CAST_FROM_BI   将INTEGER类型值转换为二进制表示形式（即RAW类
               NARY_INTEGER_TO_RAW    型）。

               DBE_RAW.CAST_FROM_R    将RAW类型值转换为INTEGER类型。
               AW_TO_BINARY_INTEGER

               DBE_RAW.GET_LENGTH     获取RAW类型值的长度。

               DBE_RAW.CAST_FROM_V    将VARCHAR2类型值转换为RAW类型。
               ARCHAR2_TO_RAW

               DBE_RAW.CAST_TO_VARC   将RAW类型值转换为VARCHAR2类型。
               HAR2

               DBE_RAW.BIT_OR         返回RAW类型值按位或计算后的值。

               DBE_RAW.SUBSTR         返回RAW类型值取子串后的值。

               DBE_RAW.BIT_AND        返回RAW类型按位与计算后的值。

               DBE_RAW.BIT_COMPLEM    返回RAW类型按位取反计算后的值。
               ENT

               DBE_RAW.BIT_XOR        返回RAW类型按位异或计算后的值。

               DBE_RAW.CAST_FROM_BI   将BINARY_DOUBLE类型值转换为RAW类型。
               NARY_DOUBLE_TO_RAW

               DBE_RAW.CAST_FROM_R    将RAW类型值转换为BINARY_DOUBLE类型。
               AW_TO_BINARY_DOUBLE


文档版本 01 (2026-04-30)            版权所有 © 华为云计算技术有限公司                 2766
GaussDB
集中式版参考                                                               3 存储过程


               接口名称                         描述

               DBE_RAW.CAST_FROM_R          将RAW类型值转换为FLOAT4类型。
               AW_TO_BINARY_FLOAT

               DBE_RAW.CAST_FROM_BI         将FLOAT4类型值转换为RAW类型。
               NARY_FLOAT_TO_RAW

               DBE_RAW.CAST_FROM_R          将RAW类型值转换为NUMERIC类型。
               AW_TO_NUMBER

               DBE_RAW.CAST_FROM_N          将NUMERIC类型值转换为RAW类型。
               UMBER_TO_RAW

               DBE_RAW.CAST_FROM_R          将RAW类型值转换为NVARCHAR2类型。
               AW_TO_NVARCHAR2

               DBE_RAW.COMPARE              返回两个RAW类型值首个不相同的位置。

               DBE_RAW.CONCAT               将最多12个RAW类型值连接到一个新的RAW类型值
                                            中，并返回。

               DBE_RAW.CONVERT              将一个RAW类型值从原编码方式from_charset转换为
                                            目标编码方式to_charset。

               DBE_RAW.COPIES               将一个RAW类型值复制n次，串联在一起，并返回连
                                            接后的结果。

               DBE_RAW.OVERLAY              用一个RAW类型数据对另一个RAW类型数据进行覆
                                            盖，可以指定起始覆盖位置和覆盖长度。

               DBE_RAW.REVERSE              将RAW类型数据按字节进行翻转。

               DBE_RAW.TRANSLATE            转换或舍弃RAW类型值的指定字节。

               DBE_RAW.TRANSLITERAT         转换RAW类型值的指定字节。
               E

               DBE_RAW.XRANGE               从一个字节到另一个字节的所有字节拼接后返回。




              ●   DBE_RAW.CAST_FROM_BINARY_INTEGER_TO_RAW
                  CAST_FROM_BINARY_INTEGER_TO_RAW将INTEGER类型值转换为RAW类型。
                  DBE_RAW.CAST_FROM_BINARY_INTEGER_TO_RAW原型为：
                  DBE_RAW.CAST_FROM_BINARY_INTEGER_TO_RAW(
                    value   IN BIGINT,
                    endianess IN INTEGER DEFAULT 1)
                  RETURN RAW;




文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                    2767
GaussDB
集中式版参考                                                                      3 存储过程

                  表 3-270 DBE_RAW.CAST_FROM_BINARY_INTEGER_TO_RAW 接口参数说明

                   参数          描述

                   value       待转成RAW类型的INTEGER类型值。
                               说明
                                DBE_RAW.CAST_FROM_BINARY_INTEGER_TO_RAW预期处理INTEGER类
                                型值，为了兼容性考虑，该参数被定义为BIGINT类型，BIGINT类型范围比
                                INTEGER类型范围更大，不影响接口的使用。

                   endianess   字节序，取值范围：整型值1、2或3。
                               ● 1表示BIG_ENDIAN。默认值为BIG_ENDIAN。
                               ● 2表示LITTLE_ENDIAN。
                               ● 3表示MACHINE_ENDIAN。
                               如果机器默认为BIG_ENDIAN，则函数执行结果
                               MACHINE_ENDIAN和BIG_ENDIAN一致，如果机器默认为
                               LITTLE_ENDIAN，则函数执行结果MACHINE_ENDIAN和
                               LITTLE_ENDIAN一致。


              ●   DBE_RAW.CAST_FROM_RAW_TO_BINARY_INTEGER
                  CAST_FROM_RAW_TO_BINARY_INTEGER将RAW类型值转换为INTEGER类型。
                  DBE_RAW.CAST_FROM_RAW_TO_BINARY_INTEGER原型为：
                  DBE_RAW.CAST_FROM_RAW_TO_BINARY_INTEGER(
                    value   IN RAW,
                    endianess IN INTEGER DEFAULT 1)
                  RETURN INTEGER;


                  表 3-271 DBE_RAW.CAST_FROM_RAW_TO_BINARY_INTEGER 接口参数说明

                   参数            描述

                   value         待转成INTEGER类型的RAW类型值。

                   endianess     字节序，取值范围：整型值1、2或3。
                                 ● 1表示BIG_ENDIAN。默认值为BIG_ENDIAN。
                                 ● 2表示LITTLE_ENDIAN。
                                 ● 3表示MACHINE_ENDIAN。
                                 如果机器默认为BIG_ENDIAN，则函数执行结果
                                 MACHINE_ENDIAN和BIG_ENDIAN一致，如果机器默认为
                                 LITTLE_ENDIAN，则函数执行结果MACHINE_ENDIAN和
                                 LITTLE_ENDIAN一致。


              ●   DBE_RAW.GET_LENGTH
                  GET_LENGTH返回RAW类型值的长度。
                  DBE_RAW.GET_LENGTH原型为：
                  DBE_RAW.GET_LENGTH(
                    value IN RAW)
                  RETURN INTEGER;



文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                            2768
GaussDB
集中式版参考                                                                3 存储过程

                  表 3-272 DBE_RAW.GET_LENGTH 接口参数说明

                   参数               描述

                   value            RAW类型值。


              ●   DBE_RAW.CAST_FROM_VARCHAR2_TO_RAW
                  CAST_FROM_VARCHAR2_TO_RAW将VARCHAR2类型值转换为RAW类型。
                  DBE_RAW.CAST_FROM_VARCHAR2_TO_RAW原型为：
                  DBE_RAW.CAST_FROM_VARCHAR2_TO_RAW(
                    str IN VARCHAR2)
                  RETURN RAW;


                  表 3-273 DBE_RAW.CAST_FROM_VARCHAR2_TO_RAW 接口参数说明

                   参数               描述

                   str              待转成RAW类型的VARCHAR2类型值。


              ●   DBE_RAW.CAST_TO_VARCHAR2
                  CAST_TO_VARCHAR2将RAW类型值转换为VARCHAR2类型。
                  DBE_RAW.CAST_TO_VARCHAR2原型为：
                  DBE_RAW.CAST_TO_VARCHAR2(
                    str IN RAW)
                  RETURN VARCHAR2;


                  表 3-274 DBE_RAW.CAST_TO_VARCHAR2 接口参数说明

                   参数               描述

                   str              待转成VARCHAR2类型的RAW类型值。


              ●   DBE_RAW.BIT_OR
                  BIT_OR返回两个RAW类型值按位或计算后的值。
                  DBE_RAW.BIT_OR原型为：
                  DBE_RAW.BIT_OR(
                    str1 IN TEXT,
                    str2 IN TEXT)
                  RETURN TEXT;


                  表 3-275 DBE_RAW.BIT_OR 接口参数说明

                   参数               描述

                   str1             按位或的第一个字符串。
                                    说明
                                     由于历史原因，该参数被定义为TEXT类型，但是DBE_RAW.BIT_OR接口
                                     预期并处理RAW类型值，这里定义为TEXT类型不影响对RAW类型值的处
                                     理。


文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                    2769
GaussDB
集中式版参考                                                              3 存储过程


                   参数             描述

                   str2           按位或的第二个字符串。
                                  说明
                                   由于历史原因，该参数被定义为TEXT类型，但是DBE_RAW.BIT_OR接口
                                   预期并处理RAW类型值，这里定义为TEXT类型不影响对RAW类型值的处
                                   理。



              ●   DBE_RAW.SUBSTR
                  函数SUBSTR将RAW类型值按起始位off_set和长度amount截取。
                  DBE_RAW.SUBSTR原型为：
                  DBE_RAW.SUBSTR(
                    IN lob_loc BLOB,
                    IN off_set INTEGER DEFAULT 1,
                    IN amount INTEGER DEFAULT 32767)
                  RETURN RAW;


                  表 3-276 DBE_RAW.SUBSTR 接口参数说明
                   参数             描述

                   lob_loc        待截取的RAW类型值。
                                  说明
                                   由于历史原因，该参数被定义为BLOB类型，但是DBE_RAW.SUBSTR接口
                                   预期并处理RAW类型值，这里定义为BLOB类型不影响对RAW类型值的处
                                   理。

                   off_set        子串的起始位置，默认值1。

                   amount         子串的长度，默认值32767。


              ●   DBE_RAW.BIT_AND
                  BIT_AND求两个RAW类型值按位与的结果。
                  DBE_RAW.BIT_AND原型为：
                  DBE_RAW.BIT_AND(
                    r1 IN RAW,
                    r2 IN RAW)
                  RETURN RAW;


                  表 3-277 DBE_RAW.BIT_AND 接口参数说明
                   参数             描述

                   r1             与r2进行按位与的RAW类型值，最大长度不超过32767。

                   r2             与r1进行按位与的RAW类型值，最大长度不超过32767。


              ●   DBE_RAW.BIT_COMPLEMENT
                  BIT_COMPLEMENT求一个RAW类型值按位取反的结果。
                  DBE_RAW.BIT_COMPLEMENT原型为：
                  DBE_RAW.BIT_COMPLEMENT(
                    r IN RAW)
                  RETURN RAW;


文档版本 01 (2026-04-30)                 版权所有 © 华为云计算技术有限公司                 2770
GaussDB
集中式版参考                                                                 3 存储过程

                  表 3-278 DBE_RAW.BIT_COMPLEMENT 接口参数说明

                   参数                描述

                   r                 进行按位取反的RAW类型值，最大长度不超过32767。


              ●   DBE_RAW.BIT_XOR
                  BIT_XOR求两个RAW类型值按位异或的结果。
                  DBE_RAW.BIT_XOR原型为：
                  DBE_RAW.BIT_XOR(
                    r1 IN RAW,
                    r2 IN RAW)
                  RETURN RAW;


                  表 3-279 DBE_RAW.BIT_XOR 接口参数说明

                   参数                描述

                   r1                与r2进行按位异或的RAW类型值，最大长度不超过32767。

                   r2                与r1进行按位异或的RAW类型值，最大长度不超过32767。


              ●   DBE_RAW.CAST_FROM_BINARY_DOUBLE_TO_RAW
                  CAST_FROM_BINARY_DOUBLE_TO_RAW将BINARY_DOUBLE类型值转换为RAW
                  类型。
                  DBE_RAW.CAST_FROM_BINARY_DOUBLE_TO_RAW原型为：
                  DBE_RAW.CAST_FROM_BINARY_DOUBLE_TO_RAW (
                    n      IN BINARY_DOUBLE,
                    endianess IN INTEGER DEFAULT 1)
                  RETURN RAW;


                  表 3-280 DBE_RAW.CAST_FROM_BINARY_DOUBLE_TO_RAW 接口参数说明

                   参数                描述

                   n                 需要进行转换的BINARY_DOUBLE类型值。

                   endianess         字节序，取值范围：整型值1、2或3。
                                     ● 1表示BIG_ENDIAN。默认值为BIG_ENDIAN。
                                     ● 2表示LITTLE_ENDIAN。
                                     ● 3表示MACHINE_ENDIAN。
                                     如果机器默认为BIG_ENDIAN，则函数执行结果
                                     MACHINE_ENDIAN和BIG_ENDIAN一致，如果机器默认为
                                     LITTLE_ENDIAN，则函数执行结果MACHINE_ENDIAN和
                                     LITTLE_ENDIAN一致。


              ●   DBE_RAW.CAST_FROM_RAW_TO_BINARY_DOUBLE
                  CAST_FROM_RAW_TO_BINARY_DOUBLE将RAW类型值转换为BINARY_DOUBLE
                  类型。
                  DBE_RAW.CAST_FROM_RAW_TO_BINARY_DOUBLE原型为：

文档版本 01 (2026-04-30)                 版权所有 © 华为云计算技术有限公司                     2771
GaussDB
集中式版参考                                                             3 存储过程

                  DBE_RAW.CAST_FROM_RAW_TO_BINARY_DOUBLE(
                    r     IN RAW,
                    endianess IN INTEGER DEFAULT 1)
                  RETURN BINARY_DOUBLE;


                  表 3-281 DBE_RAW.CAST_FROM_RAW_TO_BINARY_DOUBLE 接口参数说明

                   参数            描述

                   r             需要进行转换的RAW类型值，最短长度不低于8，最大长度不超
                                 过32767。

                   endianess     字节序，取值范围：整型值1、2或3。
                                 ● 1表示BIG_ENDIAN。默认值为BIG_ENDIAN。
                                 ● 2表示LITTLE_ENDIAN。
                                 ● 3表示MACHINE_ENDIAN。
                                 如果机器默认为BIG_ENDIAN，则函数执行结果
                                 MACHINE_ENDIAN和BIG_ENDIAN一致，如果机器默认为
                                 LITTLE_ENDIAN，则函数执行结果MACHINE_ENDIAN和
                                 LITTLE_ENDIAN一致。


              ●   DBE_RAW.CAST_FROM_RAW_TO_BINARY_FLOAT
                  CAST_FROM_RAW_TO_BINARY_FLOAT将RAW类型值转换为FLOAT4类型。
                  DBE_RAW.CAST_FROM_RAW_TO_BINARY_FLOAT原型为：
                  DBE_RAW.CAST_FROM_RAW_TO_BINARY_FLOAT(
                    r     IN RAW,
                    endianess IN INTEGER DEFAULT 1)
                  RETURN FLOAT4;


                  表 3-282 DBE_RAW.CAST_FROM_RAW_TO_BINARY_FLOAT 接口参数说明

                   参数            描述

                   r             需要进行转换的RAW类型值，最短长度不低于4，最大长度不超
                                 过32767。

                   endianess     字节序，取值范围：整型值1、2或3。
                                 ● 1表示BIG_ENDIAN。默认值为BIG_ENDIAN。
                                 ● 2表示LITTLE_ENDIAN。
                                 ● 3表示MACHINE_ENDIAN。
                                 如果机器默认为BIG_ENDIAN，则函数执行结果
                                 MACHINE_ENDIAN和BIG_ENDIAN一致，如果机器默认为
                                 LITTLE_ENDIAN，则函数执行结果MACHINE_ENDIAN和
                                 LITTLE_ENDIAN一致。


              ●   DBE_RAW.CAST_FROM_BINARY_FLOAT_TO_RAW
                  CAST_FROM_BINARY_FLOAT_TO_RAW将FLOAT4类型值转换为RAW类型。
                  DBE_RAW.CAST_FROM_BINARY_FLOAT_TO_RAW原型为：
                  DBE_RAW.CAST_FROM_BINARY_FLOAT_TO_RAW(
                    n     IN FLOAT4,


文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                     2772
GaussDB
集中式版参考                                                               3 存储过程

                    endianess IN INTEGER DEFAULT 1)
                  RETURN RAW;


                  表 3-283 DBE_RAW.CAST_FROM_BINARY_FLOAT_TO_RAW 接口参数说明

                   参数              描述

                   n               需要进行转换的FLOAT4类型值。

                   endianess       字节序，取值范围：整型值1、2或3。
                                   ● 1表示BIG_ENDIAN。默认值为BIG_ENDIAN。
                                   ● 2表示LITTLE_ENDIAN。
                                   ● 3表示MACHINE_ENDIAN。
                                   如果机器默认为BIG_ENDIAN，则函数执行结果
                                   MACHINE_ENDIAN和BIG_ENDIAN一致，如果机器默认为
                                   LITTLE_ENDIAN，则函数执行结果MACHINE_ENDIAN和
                                   LITTLE_ENDIAN一致。


              ●   DBE_RAW.CAST_FROM_RAW_TO_NUMBER
                  CAST_FROM_RAW_TO_NUMBER将RAW类型值转换为NUMERIC类型。
                       说明

                       由于A数据库与GaussDB的NUMBER数据类型底层实现不同，而RAW类型为数据的底层实
                       现的二进制流的十六进制表示，故该函数在A数据库与GaussDB的表现不同。用户不能将A
                       数据库中的一个NUMBER类型数据对应的RAW类型在GaussDB中得到相同的NUMBER类型
                       数据，在GaussDB中的表现可参考示例。
                  DBE_RAW.CAST_FROM_RAW_TO_NUMBER原型为：
                  DBE_RAW.CAST_FROM_RAW_TO_NUMBER(
                    r IN RAW)
                  RETURN NUMERIC;


                  表 3-284 DBE_RAW.CAST_FROM_RAW_TO_NUMBER 接口参数说明

                   参数              描述

                   r               需要进行转换的RAW类型值，最短长度不低于6，最大长度不超
                                   过32767。


              ●   DBE_RAW.CAST_FROM_NUMBER_TO_RAW
                  CAST_FROM_NUMBER_TO_RAW将NUMERIC类型值转换为RAW类型。
                       说明

                       由于A数据库与GaussDB的NUMBER数据类型底层实现不同，而RAW类型为数据的底层实
                       现的二进制流的十六进制表示，故该函数在A数据库与GaussDB的表现不同。用户不能将A
                       数据库的NUMBER类型数据的RAW类型表示在GaussDB数据库中还原成A数据库原
                       NUMBER类型数值，该函数在GaussDB中的表现可参考示例。
                  DBE_RAW.CAST_FROM_NUMBER_TO_RAW原型为：
                  DBE_RAW.CAST_FROM_NUMBER_TO_RAW(
                    n IN NUMERIC)
                  RETURN RAW;


文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                    2773
GaussDB
集中式版参考                                                             3 存储过程

                  表 3-285 DBE_RAW.CAST_FROM_NUMBER_TO_RAW 接口参数说明

                   参数             描述

                   n              需要进行转换的NUMERIC类型。


              ●   DBE_RAW.CAST_FROM_RAW_TO_NVARCHAR2
                  CAST_FROM_RAW_TO_NVARCHAR2将RAW类型值转换为NVARCHAR2类型。
                  DBE_RAW.CAST_FROM_RAW_TO_NVARCHAR2原型为：
                  DBE_RAW.CAST_FROM_RAW_TO_NVARCHAR2(
                    r IN RAW)
                  RETURN NVARCHAR2;


                  表 3-286 DBE_RAW.CAST_FROM_RAW_TO_NVARCHAR2 接口参数说明

                   参数             描述

                   r              需要进行转换的RAW类型值，最大长度不超过32767。


              ●   DBE_RAW.COMPARE
                  COMPARE返回两个RAW类型值首个不相同的位置。
                  DBE_RAW.COMPARE原型为：
                  DBE_RAW.COMPARE(
                    r1 IN RAW,
                    r2 IN RAW,
                    pad IN RAW DEFAULT NULL)
                  RETURN INTEGER;


                  表 3-287 DBE_RAW.COMPARE 接口参数说明

                   参数             描述

                   r1             要比较的第一个数据，可以为NULL或长度为0，最大长度不超过
                                  32767。

                   r2             要比较的第二个数据，可以为NULL或长度为0，最大长度不超过
                                  32767。

                   pad            取pad的首字节，用于扩展r1或r2中较短的一个，最大长度不超
                                  过32767。当该参数为NULL、长度为0或缺省时，扩展值为
                                  0x00。


              ●   DBE_RAW.CONCAT
                  CONCAT将最多12个RAW类型值连接到一个新的RAW类型值中，并返回。如果连
                  接后的长度超过32767，则会报错。
                  DBE_RAW.CONCAT原型为：
                  DBE_RAW.CONCAT(
                    r1 IN RAW DEFAULT NULL,
                    r2 IN RAW DEFAULT NULL,
                    r3 IN RAW DEFAULT NULL,
                    r4 IN RAW DEFAULT NULL,
                    r5 IN RAW DEFAULT NULL,
                    r6 IN RAW DEFAULT NULL,


文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                 2774
GaussDB
集中式版参考                                                            3 存储过程

                    r7 IN RAW DEFAULT NULL,
                    r8 IN RAW DEFAULT NULL,
                    r9 IN RAW DEFAULT NULL,
                    r10 IN RAW DEFAULT NULL,
                    r11 IN RAW DEFAULT NULL,
                    r12 IN RAW DEFAULT NULL)
                  RETURN RAW;


                  表 3-288 DBE_RAW.CONCAT 接口参数说明

                   参数               描述

                   r1...r12         需要进行连接的RAW类型值。


              ●   DBE_RAW.CONVERT
                  CONVERT将一个RAW类型值从原编码方式from_charset转换为目标编码方式
                  to_charset。
                  如果原编码格式到目标编码格式的转化规则不存在，则参数r不进行任何转换直接
                  返回，如GBK和LATIN1之间的转换规则是不存在的，具体转换规则可以通过查看
                  系统表PG_CONVERSION获得。
                  DBE_RAW.CONVERT原型为：
                  DBE_RAW.CONVERT(
                    r        IN RAW,
                    to_charset IN VARCHAR2,
                    from_charset IN VARCHAR2)
                  RETURN RAW;


                  表 3-289 DBE_RAW.CONVERT 接口参数说明

                   参数               描述

                   r                需要进行转换的RAW类型值，最大长度不超过32767。

                   to_charset       目标编码字符集名称。

                   from_chars       原编码字符集名称，在该编码下，r必须是合法的。
                   et


              ●   DBE_RAW.COPIES
                  COPIES将一个RAW类型值复制n次，串联在一起，并返回连接后的结果。如果复
                  制后的长度超过32767，则会报错。
                  DBE_RAW.COPIES原型为：
                  DBE_RAW.COPIES(
                    r IN RAW,
                    n IN NUMERIC)
                  RETURN RAW;


                  表 3-290 DBE_RAW.COPIES 接口参数说明

                   参数               描述

                   r                需要进行复制的RAW类型值。

                   n                复制的次数（必须为正数，如果是小数会进行四舍五入）。


文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司               2775
GaussDB
集中式版参考                                                             3 存储过程



              ●   DBE_RAW.OVERLAY
                  OVERLAY用一个RAW类型值对另一个RAW类型值进行覆盖，可以指定起始覆盖位
                  置和覆盖长度。
                  DBE_RAW.OVERLAY原型为：
                  DBE_RAW.OVERLAY(
                    overlay_str IN RAW,
                    target     IN RAW,
                    pos       IN BINARY_INTEGER DEFAULT 1,
                    len      IN BINARY_INTEGER DEFAULT NULL,
                    pad       IN RAW        DEFAULT NULL)
                  RETURN RAW;


                  表 3-291 DBE_RAW.OVERLAY 接口参数说明

                   参数              描述

                   overlay_str     用于覆盖的字节，不能为NULL。

                   target          被覆盖的字节串，RAW类型，最大长度不超过32767字节，不能
                                   为NULL。

                   pos             从第几个字节开始覆盖，第一个字节位置为1，限制pos>=1且len
                                   +pos<=32767，缺省值为1。

                   len             要覆盖的长度，限制len>=0且len+pos<=32767，缺省值为覆盖
                                   字节（overlay_str）的长度。

                   pad             填充字节，只有第一个字节有效，缺省值为NULL，为NULL时默
                                   认为0x00。


              ●   DBE_RAW.REVERSE
                  REVERSE将RAW类型值按字节进行翻转。
                  DBE_RAW.REVERSE原型为：
                  DBE_RAW.REVERSE(
                    r IN RAW
                  )
                  RETURN RAW;


                  表 3-292 DBE_RAW.REVERSE 接口参数说明

                   参数              描述

                   r               需要进行翻转的RAW类型值，最大长度不超过32767字节，如果
                                   为NULL则返回NULL。


              ●   DBE_RAW.TRANSLATE
                  TRANSLATE将转换或舍弃RAW类型值的指定字节。
                  DBE_RAW.TRANSLATE原型为：
                  DBE_RAW.TRANSLATE(
                    r     IN RAW,
                    from_set IN RAW,
                    to_set IN RAW)
                  RETURN RAW;


文档版本 01 (2026-04-30)                 版权所有 © 华为云计算技术有限公司               2776
GaussDB
集中式版参考                                                                 3 存储过程

                  表 3-293 DBE_RAW.TRANSLATE 接口参数说明

                   参数              描述

                   r               要转换的原字节串，RAW类型，最大长度不超过32767字节，不
                                   能为NULL。

                   from_set        原字节串中要转换的字节码，RAW类型，不能为NULL。原字节
                                   串中存在于from_set中的字节都将被替换成to_set中对应位置的
                                   字节，假如from_set中有多个相同的字节，只会替换成第一个该
                                   字节对应的字节。比如：r[x]=from_set[n]，那么r[x]将被替换成
                                   to_set[n]；如果from_set[n]对应的to_set[n]不存在（也就是
                                   to_set字节数不超过n）时，r[x]将被舍弃。

                   to_set          对应from_set字节转换为的字节码，RAW类型，不能为NULL。


              ●   DBE_RAW.TRANSLITERATE
                  TRANSLITERATE将RAW类型值转换为NUMERIC类型。
                  DBE_RAW.TRANSLITERATE原型为：
                  DBE_RAW.TRANSLITERATE(
                    r     IN RAW,
                    from_set IN RAW DEFAULT NULL,
                    to_set IN RAW DEFAULT NULL,
                    pad     IN RAW DEFAULT NULL)
                  RETURN RAW;


                  表 3-294 DBE_RAW.TRANSLITERATE 接口参数说明

                   参数              描述

                   r               要转换的字节串，RAW类型，最大长度不超过32767字节，不能
                                   为NULL。

                   to_set          对应from_set字节转换为的字节码，RAW类型，缺省值为
                                   NULL。如果为NULL，则r中的所有存在于from_set中的字节都将
                                   被转换成pad。

                   from_set        字节串r中要转换的字节码，RAW类型，缺省值为NULL。如果
                                   from_set为NULL，则字节串r的所有字节将被转换成由pad填充
                                   的等长数据。否则字节串r中存在于from_set中的字节都将被替换
                                   成to_set中对应位置的字节，比如r[x]=from_set[n]，那么r[x]将
                                   被转换成to_set[n]，如果to_set[n]不存在，r[x]将被转换成
                                   pad。

                   pad             填充字节，只有第一个字节有效，缺省值为NULL，为NULL时默
                                   认为0x00。


              ●   DBE_RAW.XRANGE
                  XRANGE返回包含以指定字节码开始和结束的连续一字节编码的RAW类型值。
                  DBE_RAW.XRANGE原型为：
                  DBE_RAW.XRANGE(
                    start_byte IN RAW,
                    end_byte IN RAW)
                  RETURN RAW;


文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                    2777
GaussDB
集中式版参考                                                                                        3 存储过程

                  表 3-295 DBE_RAW.XRANGE 接口参数说明
                    参数             描述

                    start_byte     开始字节，只有第一个字节有效，如果为NULL则默认为0x00。

                    end_byte       结束字节，只有第一个字节有效，如果为NULL则默认为0xFF。
                                   如果end_byte<start_byte，则会从end_byte拼接到0XFF，再从
                                   0x00拼接到start_byte。



示例
              DECLARE
              v_raw RAW;
              v_int INTEGER;
              v_length INTEGER;
              v_str VARCHAR2;
              v_double BINARY_DOUBLE;
              v_float FLOAT4;
              v_numeric NUMERIC;
              v_nvarchar2 NVARCHAR2;
              BEGIN
              -- INTEGER类型值转RAW类型。
              SELECT DBE_RAW.CAST_FROM_BINARY_INTEGER_TO_RAW(170,1) INTO v_raw; -- 000000AA
              SELECT DBE_RAW.CAST_FROM_BINARY_INTEGER_TO_RAW(170,2) INTO v_raw; -- AA000000
              SELECT DBE_RAW.CAST_FROM_BINARY_INTEGER_TO_RAW(170,3) INTO v_raw; -- AA000000
              -- RAW类型值转INTEGER类型。
              SELECT
              DBE_RAW.CAST_FROM_RAW_TO_BINARY_INTEGER(DBE_RAW.CAST_FROM_BINARY_INTEGER_TO_RAW(170,
              1),1) INTO v_int; -- 170
              SELECT
              DBE_RAW.CAST_FROM_RAW_TO_BINARY_INTEGER(DBE_RAW.CAST_FROM_BINARY_INTEGER_TO_RAW(170,
              2),2) INTO v_int; -- 170
              SELECT
              DBE_RAW.CAST_FROM_RAW_TO_BINARY_INTEGER(DBE_RAW.CAST_FROM_BINARY_INTEGER_TO_RAW(170,
              3),3) INTO v_int; -- 170
              -- 求RAW类型值长度。
              SELECT DBE_RAW.GET_LENGTH(DBE_RAW.CAST_FROM_BINARY_INTEGER_TO_RAW(170,1)) INTO v_length;
              -- 4
              -- VARCHAR2类型值转RAW类型。
              SELECT DBE_RAW.CAST_FROM_VARCHAR2_TO_RAW('AA') INTO v_raw; -- 4141
              -- RAW类型值转VARCHAR2类型。
              SELECT DBE_RAW.CAST_TO_VARCHAR2('4141') INTO v_str; -- AA
              -- RAW类型按位或。
              SELECT DBE_RAW.BIT_OR('0000', '1111') INTO v_raw; -- 1111
              -- RAW类型取字串。
              SELECT DBE_RAW.SUBSTR('ABCD', 1, 2) INTO v_raw; -- ABCD
              -- RAW类型按位与。
              SELECT DBE_RAW.BIT_AND('AFF', 'FF0B') INTO v_raw; -- 0A0B
              -- RAW类型按位取反。
              SELECT DBE_RAW.BIT_COMPLEMENT('0AFF') INTO v_raw; -- F500
              -- RAW类型按位异或。
              SELECT DBE_RAW.BIT_XOR('AFF', 'FF0B') INTO v_raw; -- F5F4
              -- BINARY_DOUBLE类型值转RAW类型。
              SELECT DBE_RAW.CAST_FROM_BINARY_DOUBLE_TO_RAW(1.0001,1) INTO v_raw; -- 3FF00068DB8BAC71
              -- RAW类型值转BINARY_DOUBLE类型。
              SELECT DBE_RAW.CAST_FROM_RAW_TO_BINARY_DOUBLE('3FF00068DB8BAC7',1) INTO v_double; -- 1.0001
              -- RAW类型转FLOAT4类型。
              SELECT DBE_RAW.CAST_FROM_RAW_TO_BINARY_FLOAT('40200000',1) INTO v_float; -- 2.5
              -- FLOAT4类型转RAW类型。
              SELECT DBE_RAW.CAST_FROM_BINARY_FLOAT_TO_RAW('2.5',1) INTO v_raw; -- 40200000
              -- RAW类型转NUMERIC类型。
              SELECT DBE_RAW.CAST_FROM_RAW_TO_NUMBER('808002008813') INTO v_numeric; -- 2.5
              -- NUMERIC类型转RAW类型。
              SELECT DBE_RAW.CAST_FROM_NUMBER_TO_RAW('2.5') INTO v_raw; -- 808002008813
              -- RAW类型转NVARCHAR2类型。


文档版本 01 (2026-04-30)                版权所有 © 华为云计算技术有限公司                                              2778
GaussDB
集中式版参考                                                                                                3 存储过程

              SELECT DBE_RAW.CAST_FROM_RAW_TO_NVARCHAR2('12345678') INTO v_nvarchar2; -- \x124Vx
              -- RAW类型COMPARE。
              SELECT DBE_RAW.COMPARE('ABCD','AB') INTO v_numeric; -- 2
              -- RAW类型CONCAT。
              SELECT DBE_RAW.CONCAT('ABCD','AB') INTO v_raw; -- ABCDAB
              -- RAW类型CONVERT。
              SELECT DBE_RAW.CONVERT('E695B0', 'GBK','UTF8') INTO v_raw; -- CAFD
              -- RAW类型COPIES。
              SELECT DBE_RAW.COPIES('ABCD',2) INTO v_raw; -- ABCDABCD
              -- RAW类型指定位置和长度进行覆盖。
              SELECT DBE_RAW.OVERLAY('abcef', '12345678123456', 2, 5, '9966') INTO v_raw; -- 120ABCEF999956
              -- RAW类型按字节翻转。
              SELECT DBE_RAW.REVERSE('12345678') INTO v_raw; -- 78563412
              -- RAW类型字节转换（无填充码）。
              SELECT DBE_RAW.TRANSLATE('1122112233', '1133','55') INTO v_raw; -- 55225522
              -- RAW类型字节转换（有填充码）。
              SELECT DBE_RAW.TRANSLITERATE('1122112233', '55','1133','FFEE') INTO v_raw; -- 55225522FF
              -- RAW类型两个字节间的所有字节。
              SELECT DBE_RAW.XRANGE('00','03') INTO v_raw; -- 00010203
              END;
              /
              ANONYMOUS BLOCK EXECUTE


3.12.2.17 DBE_SCHEDULER

接口介绍
              高级功能包DBE_SCHEDULER支持通过调度（schedule）和程序（program）更加灵活
              地创建定时任务。支持的接口请参见表3-296。

              表 3-296 DBE_SCHEDULER

               接口名称                                                    描述

               DBE_SCHEDULER.CREATE_JOB                                创建定时任务。

               DBE_SCHEDULER.DROP_JOB                                  删除定时任务。

               DBE_SCHEDULER.DROP_SINGLE_JOB                           删除单个定时任务。

               DBE_SCHEDULER.SET_ATTRIBUTE                             设置对象属性。

               DBE_SCHEDULER.RUN_JOB                                   运行定时任务。

               DBE_SCHEDULER.RUN_BACKEND_JOB                           后台运行定时任务。

               DBE_SCHEDULER.RUN_FOREGROUND_JO                         前台运行定时任务。
               B

               DBE_SCHEDULER.STOP_JOB                                  停止定时任务。

               DBE_SCHEDULER.STOP_SINGLE_JOB                           停止单个定时任务。

               DBE_SCHEDULER.GENERATE_JOB_NAME                         生成定时任务名。

               DBE_SCHEDULER.CREATE_PROGRAM                            创建程序。

               DBE_SCHEDULER.DEFINE_PROGRAM_ARG                        定义程序参数。
               UMENT

               DBE_SCHEDULER.DROP_PROGRAM                              删除程序。


文档版本 01 (2026-04-30)                  版权所有 © 华为云计算技术有限公司                                                      2779
