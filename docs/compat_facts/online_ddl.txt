GaussDB
集中式版参考                                                    7 数据库运行参数说明


                  说明

                  ● 当GUC参数enable_opfusion开启时，该参数设置才会生效。
                  ● 自增列的类型限制为：smallint、int、bigint。
                  ● 继承SQLBYPASS插入语句的约束。
                  ● 只支持INSERT场景下的序列自增，不支持UPDATE等语句。

              参数类型：布尔型
              参数单位：无
              取值范围：

              ●   on：表示支持SQLBYPASS优化。
              ●   off：表示不支持SQLBYPASS优化。
              默认值：on
              设置方式：该参数属于USERSET类型参数，请参见表7-1中对应设置方法进行设置。
              设置建议：推荐使用默认值。
              设置不当的风险与影响：若关闭该参数，含自增列表的INSERT语句将无法使用
              SQLBYPASS优化，从而会降低INSERT语句的执行效率。

7.3.49 在线 DDL
enable_online_ddl
              参数说明：该参数与ONLINE/OFFLINE关键字（关键字详情请参见ALTER TABLE和
              VACUUM）结合使用，控制是否开启在线DDL或在线VACUUM。
              参数类型：布尔型
              参数单位：无
              取值范围：

              ●   on：表示启用在线DDL，当前版本不生效。
              ●   off：表示不启用在线DDL，当前版本不生效。
              默认值：off
              设置方式：该参数属于USERSET类型参数，请参见表7-1中对应设置方法进行设置。
              设置建议：建议关闭此参数，此参数当前版本不生效。需要使用在线DDL新增特性可
              在语法里加ONLINE关键字。
              设置不当的风险与影响：请在充分理解参数含义，并经过测试验证后进行修改。

7.3.50 数据生命周期管理-OLTP 表压缩
enable_ilm
              参数说明：控制是否开启数据生命周期管理-OLTP表压缩特性。
              参数类型：布尔型

文档版本 01 (2026-04-30)          版权所有 © 华为云计算技术有限公司                  4754
