# 运行参数 — behavior_compat_options SQL示例 — 结构化参考事实
# 来源: gaussdb-rf-cent.pdf 7 behavior_compat_options 表7-24
document: "集中式版参考 / 7 数据库运行参数 / behavior_compat_options SQL示例"
version: "V2.0-10.0.0"
parent_pdf_sha256: "716ab36bb4410cb823c76cd331d06f06a43ae81ce6b3a267ffe17085b3d3acbe"
extraction_date: "2026-09-14"
extraction_method: pdftotext -layout 逐页精读含SQL示例
verification_status: confirmed

facts:
  # display_leading_zero
  - id: bc_example_display_leading_zero
    type: behavior_oracle
    statement: "display_leading_zero SQL示例：SELECT 0.1231243 → 不设置显示.1231243（长度8），设置显示0.1231243（长度9）。影响numeric/integer转换和length()。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4531）

  # end_month_calculate
  - id: bc_example_end_month_calculate
    type: behavior_oracle
    statement: "end_month_calculate SQL示例：SELECT add_months('2018-02-28',3) → 不设置返回2018-05-28（保持日期），设置返回2018-05-31（取月末）。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4531-4532）

  # correct_to_number
  - id: bc_example_correct_to_number
    type: behavior_oracle
    statement: "correct_to_number SQL示例：SELECT to_number('34,50','999,99') → 不设置报错invalid data，设置返回3450（与pg11一致）。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4533）

  # unbind_divide_bound
  - id: bc_example_unbind_divide_bound
    type: behavior_oracle
    statement: "unbind_divide_bound SQL示例：SELECT (-2147483648)::int4 / (-1)::int4 → 不设置报错integer out of range，设置返回2147483648。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4533）

  # convert_string_digit_to_numeric
  - id: bc_example_convert_string_digit
    type: behavior_oracle
    statement: "convert_string_digit_to_numeric SQL示例：varchar列存'1.1'与int列join时 → 不设置报错invalid input syntax，设置自动转换匹配。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4533-4534）

  # return_null_string
  - id: bc_example_return_null_string
    type: behavior_oracle
    statement: "return_null_string SQL示例：SELECT length(lpad('123',0,'*')) → 不设置返回NULL（长度为空），设置返回0（空字符串长度0）。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4533）

  # compat_concat_variadic
  - id: bc_example_concat_variadic
    type: behavior_oracle
    statement: "compat_concat_variadic SQL示例：SELECT concat(variadic NULL::int[]) → 不设置返回空，设置返回NULL。控制concat对variadic NULL的处理。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4533）

  # hide_tailing_zero / truncate_numeric_tail_zero
  - id: bc_example_hide_tailing_zero
    type: behavior_oracle
    statement: "hide_tailing_zero SQL示例：SELECT cast(123.123 as numeric(15,10)) → 不设置显示123.1230000000，设置显示123.123（隐藏尾零）。to_char结果同样受影响。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4534）

  - id: bc_example_truncate_numeric
    type: behavior_oracle
    statement: "truncate_numeric_tail_zero SQL示例：SELECT cast(123.123 as numeric(15,10)) → 不设置保留尾零，设置截断尾零。与hide_tailing_zero的区别是truncate实际修改数值而非仅显示。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4534）

  # char_coerce_compat
  - id: bc_example_char_coerce
    type: behavior_oracle
    statement: "char_coerce_compat SQL示例：varchar(3)列与char(3)列比较 → 不设置时隐式转换可能丢失尾部空格导致结果不一致，设置时保留空格比较。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含建表和查询（页4534-4535）

  # rownum_type_coerce
  - id: bc_example_rownum_type
    type: behavior_oracle
    statement: "rownum_type_coerce SQL示例：控制ROWNUM在CTAS/CREATE VIEW中的投影类型（varchar2 vs integer）。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含建表和查询（页4534）

  # aformat_null_test
  - id: bc_example_aformat_null_test
    type: behavior_oracle
    statement: "aformat_null_test SQL示例：SELECT r, r is null FROM (values (NULL::text)) t(r) → 不设置r is null返回t，设置后可能返回不同结果。控制A模式NULL测试行为。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4535）

  # current_sysdate
  - id: bc_example_current_sysdate
    type: behavior_oracle
    statement: "current_sysdate SQL示例：SELECT sysdate → 不设置返回语句级时间（同事务内不变），设置返回当前系统时间。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4537）

  # enable_funcname_with_argsname
  - id: bc_example_funcname_argsname
    type: behavior_oracle
    statement: "enable_funcname_with_argsname SQL示例：SELECT power(2,3) → 不设置投影别名显示?column?，设置显示power(2,3)完整函数名。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4536）

  # proc_outparam_transfer_length
  - id: bc_example_outparam_transfer
    type: behavior_oracle
    statement: "proc_outparam_transfer_length SQL示例：存储过程出参传值 → 不设置可能截断长度，设置后完整传递。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含存储过程示例（页4537）

  # enable_case_when_alias
  - id: bc_example_case_when_alias
    type: behavior_oracle
    statement: "enable_case_when_alias SQL示例：SELECT CASE WHEN...END → 不设置投影别名显示?column?或case，设置显示CASE WHEN表达式的别名。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4538）

  # enable_bpcharlikebpchar_compare
  - id: bc_example_bpcharlike_compare
    type: behavior_oracle
    statement: "enable_bpcharlikebpchar_compare SQL示例：SELECT bpcharlikebpchar('455'::BPCHAR(10), '455 '::BPCHAR) → 不设置不考虑尾部空格差异，设置后严格比较。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4538）

  # show_full_error_lineno
  - id: bc_example_show_error_lineno
    type: behavior_oracle
    statement: "show_full_error_lineno SQL示例：编译错误 → 不设置显示部分行号，设置显示完整行号信息。需配合plsql_compile_check_options使用。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整错误输出（页4539）

  # enable_crosstype_integer_operator
  - id: bc_example_crosstype_int
    type: behavior_oracle
    statement: "enable_crosstype_integer_operator SQL示例：int1类型列的索引 → 不设置跨类型整数操作不走索引，设置后支持。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含建索引示例（页4539）

  # enable_use_ora_timestamptz
  - id: bc_example_ora_timestamptz
    type: behavior_oracle
    statement: "enable_use_ora_timestamptz SQL示例：SELECT timestamp '2024-03-20 01:30:00' at time zone 'Europe/...' → 不设置和设置返回不同的时区处理结果。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4539-4540）

  # sys_function_without_brackets
  - id: bc_example_sys_func_no_brackets
    type: behavior_oracle
    statement: "sys_function_without_brackets SQL示例：SELECT systimestamp → 不设置需括号systimestamp()，设置后支持无括号调用。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4541）

  # array_count_compat
  - id: bc_example_array_count
    type: behavior_oracle
    statement: "array_count_compat SQL示例：控制array_count函数对空数组和NULL的处理行为。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含类型定义和查询（页4541）

  # plsql_rollback_keep_user
  - id: bc_example_plsql_rollback_user
    type: behavior_oracle
    statement: "plsql_rollback_keep_user SQL示例：存储过程rollback → 不设置回滚后用户上下文可能丢失，设置后保留。需创建两个用户并grant测试。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含用户创建和授权（页4538-4539）

  # allow_function_procedure_replace
  - id: bc_example_func_proc_replace
    type: behavior_oracle
    statement: "allow_function_procedure_replace SQL示例：create or replace procedure → 不设置可能不允许函数和过程互相替换，设置后允许。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含\\df+输出（页4538）

  # proc_uncheck_default_param
  - id: bc_example_uncheck_default
    type: behavior_oracle
    statement: "proc_uncheck_default_param SQL示例：function test(f1 int, f2 int default 20, f3 int, f4 int default 40) → 不设置报错默认参数后有非默认参数，设置后允许。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整错误输出（页4536-4537）

  # time_constexpr_compact
  - id: bc_example_time_constexpr
    type: behavior_oracle
    statement: "time_constexpr_compact SQL示例：SELECT timestamp '1999-03-15 8:00:00 -8:00:00' → 不设置和设置返回不同的时区解析结果。"
    source_anchor: "7 表7-24"
    status: confirmed
    verification_method: 逐页精读含完整输入输出（页4539）
