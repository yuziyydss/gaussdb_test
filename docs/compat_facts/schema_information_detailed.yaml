# Schema — Information Schema — 结构化参考事实
document: "集中式版参考 / 10.1 Information Schema"
version: "V2.0-10.0.0"
parent_pdf_sha256: "716ab36bb4410cb823c76cd331d06f06a43ae81ce6b3a267ffe17085b3d3acbe"
extraction_date: "2026-09-14"
extraction_method: pdftotext -layout
verification_status: confirmed

facts:
  - id: info_schema_overview
    type: environment
    statement: "information_schema是自动存在于所有数据库中的模式，由一组视图构成，包含当前数据库中定义对象的信息。模式拥有者为初始用户，所有用户仅有使用权限。"
    source_anchor: "10.1"
    status: confirmed
    verification_method: pdftotext双模式交叉验证通过（页5433）

  - id: info_schema_pg_compatible_views
    type: metadata_oracle
    statement: "信息模式兼容PG，包含约60+个视图：tables、columns、views、routines、parameters、schemata、sequences、table_constraints、collations、domains、foreign_tables、user_mappings等。"
    source_anchor: "10.1"
    status: confirmed
    verification_method: pdftotext双模式交叉验证通过（页5433）

  - id: info_schema_tables
    type: metadata_oracle
    statement: "INFORMATION_SCHEMA.TABLES显示当前数据库中的表信息：table_catalog/table_schema/table_name/table_type等。"
    source_anchor: "10.1 PG兼容视图列表"
    status: confirmed
    verification_method: pdftotext双模式交叉验证通过（页5433）

  - id: info_schema_columns
    type: metadata_oracle
    statement: "INFORMATION_SCHEMA.COLUMNS显示列信息：table_schema/table_name/column_name/data_type/character_maximum_length/numeric_precision/numeric_scale/is_nullable/column_default等。"
    source_anchor: "10.1 PG兼容视图列表"
    status: confirmed
    verification_method: pdftotext双模式交叉验证通过（页5433）

  - id: info_schema_views
    type: metadata_oracle
    statement: "INFORMATION_SCHEMA.VIEWS显示视图信息：table_schema/table_name/view_definition等。"
    source_anchor: "10.1 PG兼容视图列表"
    status: confirmed
    verification_method: pdftototext双模式交叉验证通过（页5433）

  - id: info_schema_fdw_views
    type: metadata_oracle
    statement: "INFORMATION_SCHEMA._PG_FOREIGN_DATA_WRAPPERS显示外部数据封装器信息（仅sysadmin可查看）：oid/fdwowner/fdwoptions/foreign_data_wrapper_name/authorization_identifier/foreign_data_wrapper_language。"
    source_anchor: "10.1.1 表10-2"
    status: confirmed
    verification_method: pdftotext双模式交叉验证通过（页5434）

  - id: info_schema_foreign_servers
    type: metadata_oracle
    statement: "INFORMATION_SCHEMA._PG_FOREIGN_SERVERS显示外部服务器信息（仅sysadmin可查看）：oid/srvoptions等。"
    source_anchor: "10.1.2 表10-3"
    status: confirmed
    verification_method: pdftotext双模式交叉验证通过（页5435）

  - id: info_schema_security_rebuild
    type: constraint
    statement: "只有初始用户可通过CREATE OR REPLACE重建information_schema下的视图，可能产生越权行为。重建时绑定自定义函数可能被用于提权。"
    source_anchor: "10.1 须知"
    status: confirmed
    verification_method: pdftotext双模式交叉验证通过（页5433）

  - id: info_schema_dbe_sql_util
    type: environment
    statement: "dbe_sql_util Schema提供SQL运维功能，包含SQL Patch和Plan Management的运维接口。"
    source_anchor: "10 Schema概览"
    status: confirmed
    verification_method: pdftotext双模式交叉验证通过（页5433）
