GaussDB
集中式版参考                                                                 8 系统表和系统视图


               名称                 类型                 描述

               column_name        name               加密列的名称。

               column_key_id      oid                外键，列加密密钥的OID。

               encryption_type    tinyint            加密类型，取值为2（DETERMINISTIC）或者
                                                     1（RANDOMIZED）。

               data_type_origi    oid                加密列的原始数据类型id，参考系统表
               nal_oid                               PG_TYPE中的oid。

               data_type_origi    integer            加密列的原始数据类型修饰符，参考系统表
               nal_mod                               PG_ATTRIBUTE中的atttypmod。其值对那
                                                     些不需要的类型data_type_original_mod通
                                                     常为-1。

               create_date        timestamp          创建加密列的时间。
                                  without time
                                  zone




8.2.4.6 GS_ENCRYPTED_PROC
              GS_ENCRYPTED_PROC系统表提供了密态函数/存储过程函数参数、返回值的原始数据
              类型，加密列等信息。

              表 8-21 GS_ENCRYPTED_PROC 字段
               名称                类型                    描述

               oid               oid                   行标识符（隐含字段）。

               func_id           oid                   function的oid，对应系统表PG_PROC中
                                                       的oid行标识符。

               prorettype_ori    integer               返回值的原始数据类型。
               g

               last_change       timestamp without     密态函数信息上次修改的时间。
                                 time zone

               proargcached      oidvector             函数INPUT参数对应的加密列的oid，对
               col                                     应系统表GS_ENCRYPTED_COLUMNS中
                                                       的oid行标识符。

               proallargtypes    oid[]                 所有函数参数的原始数据类型。
               _orig




8.2.5 通信



文档版本 01 (2026-04-30)               版权所有 © 华为云计算技术有限公司                            4853
GaussDB
集中式版参考                                                         8 系统表和系统视图


8.2.5.1 PGXC_NODE
              PGXC_NODE系统表存储数据库实例节点信息。集中式只能查询该表定义。

              表 8-22 PGXC_NODE 字段
               名称                类型        描述

               oid               oid       行标识符（隐含字段，必须明确选择）。

               node_name         name      节点名称。

               node_type         "char"    节点类型。
                                           ● D：数据节点。
                                           ● S：数据节点的备节点。

               node_port         integer   节点的端口号。

               node_host         name      节点的主机名称或者IP（如配置为虚拟IP，则为虚
                                           拟IP）。

               node_port1        integer   复制节点的端口号。

               node_host1        name      复制节点的主机名称或者IP（如配置为虚拟IP，则
                                           为虚拟IP）。

               hostis_primary    boolean   表明当前节点是否发生主备切换。
                                           ● t（true）：表示发生。
                                           ● f（false）：表示不发生。

               nodeis_primary    boolean   在replication表下，是否优选当前节点作为优先执
                                           行的节点进行非查询操作。
                                           ● t（true）：表示优选。
                                           ● f（false）：表示不优选。

               nodeis_preferre   boolean   在replication表下，是否优选当前节点作为首选的
               d                           节点进行查询。
                                           ● t（true）：表示优选。
                                           ● f（false）：表示不优选。

               node_id           integer   节点标识符。由node_name经过hash函数计算后
                                           得到。

               sctp_port         integer   主节点使用TCP代理通信库的数据通道侦听端口
                                           （当前版本已经不再支持SCTP通信库）。

               control_port      integer   主节点使用TCP代理通信库的控制通道侦听端口。

               sctp_port1        integer   备节点使用TCP代理通信库的数据通道侦听端口
                                           （当前版本已经不再支持SCTP通信库）。

               control_port1     integer   备节点使用TCP代理通信库的控制通道侦听端口。




文档版本 01 (2026-04-30)              版权所有 © 华为云计算技术有限公司                  4854
GaussDB
集中式版参考                                                             8 系统表和系统视图


               名称                     类型        描述

               nodeis_central         boolean   表明当前节点是否为中心控制节点，对DN无效。
                                                ● t（true）：表示是。
                                                ● f（false）：表示不是。

               nodeis_active          boolean   表明当前节点是否是正常状态，对DN无效。
                                                ● t（true）：表示是。
                                                ● f（false）：表示不是。

               cluster_name           text      数据库实例名称。

               slice_order            integer   节点归属分片的顺序序号。




8.2.6 账本数据库

8.2.6.1 GS_GLOBAL_CHAIN
              GS_GLOBAL_CHAIN系统表记录用户对防篡改用户表的修改操作信息，每条记录对应
              一次表级修改操作。具有审计管理员权限的用户可以查询此系统表，所有用户均不允
              许修改此系统表。


              表 8-23 GS_GLOBAL_CHAIN 字段

               名称              类型                描述

               blocknum        bigint            区块号，当前用户操作在账本中记录的序号。

               dbname          name              数据库名称。被修改的防篡改用户表所属的
                                                 database。

               username        name              用户名，执行用户表修改操作的用户名。

               starttime       timestamp with    用户执行操作的时间。
                               time zone

               relid           oid               用户表oid，被修改的防篡改用户表oid。

               relnsp          name              模式名，被修改的防篡改用户表所属的模式名。

               relname         name              用户表名，被修改的防篡改用户表名。

               relhash         hash16            当前操作产生的表级别hash变化量。

               globalhash      hash32            全局摘要，由当前行信息与前一行globalhash计
                                                 算而来，将整个表串联起来，用于验证
                                                 GS_GLOBAL_CHAIN数据完整性。

               txcommand       text              被记录操作的SQL语句。




文档版本 01 (2026-04-30)                   版权所有 © 华为云计算技术有限公司                 4855
