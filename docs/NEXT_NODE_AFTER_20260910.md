# 下一节点：按真实缺口验收，不再用候选数代表完成度

依据09-10 08:26最终冻结回归和08:35只读缺口导出。当前317个登记包、261个有活跃清单的包、817个清单、5187条case；215模块1682项静态测试通过。数据库执行为0。本文是下阶段交接，不授权数据库操作，也没有改变任何包状态。

机器可读逐包清单：`work/project_evolution_20260910_0900/handoff_gaps.json`。它直接保留当前生成报告中的来源、取值、规则、特性、场景、Oracle缺口，并附56个无普通manifest包的路径、SHA、来源和场景引用。它不是一次新的全文精审；不同缺口类别可能重叠，不能相加成完成率。

## 1. 首选静态任务：复核生成模型未完整的10个包

这10包均能生成候选；现有报告中的生成异常、缺pair、重复case ID均为空。`generation_model_complete=false`不能解释为“生成器运行失败”。

| 分组 | 包及直接证据 | 下一步 |
| --- | --- | --- |
| 已声明取值尚未覆盖 | ALTER PACKAGE的四种COMPILE取值；CREATE/ALTER RESOURCE POOL的dop_one；CREATE DATABASE的一组编码取值 | 先核原文、所需资产、模式及实际profile绑定，再决定补代表或记录限制；不改valid属性消除缺口 |
| 其他取值与规则缺口 | ALTER TABLE 13、CREATE INDEX 6、CREATE SEQUENCE 2、DELETE 2、INSERT 4、UPDATE 1条取值缺口；CREATE SEQUENCE另有1条规则未被目标负例覆盖 | 各自复核模式、资产和规则消费者。尤其B兼容、加密、游标前置不能仅靠追加绑定放行 |

这些才是当前生成模型结论的直接阻断：已按`core/factor_coverage_auditor.py`中的实际计算链核对。Oracle未校准不直接使`generation_model_complete`变false，而是阻止更高层的`static_coverage_complete`；不要混淆两层。其他来源与场景缺口仍按逐包清单验收。

另列错误Oracle：全261活跃包中，72包共有136份负向manifest仍为`needs_verification`。其中上述六包有37份，并非全库只有37份。按错误类别与目标阶段整理最小对照；缺SQLSTATE或环境校准就保持未闭合，不以任意错误通过。当前不执行数据库，也不把这136份全部改成固定SQLSTATE。

既有运行记录已指出ALTER PACKAGE的COMPILE注意事项与语法存在冲突，不能把这四条简单当成漏绑后补成正向，更不应重复进行同样的泛读。先沿已记录冲突定位需要的确证；无新增依据时保留缺口，转做已有前置的项。CREATE SEQUENCE系统列也已有ROWID函数章节不足以证明列身份的记录。后续选择任务必须先读这些已做结论，不能用无效包名或空fixture制造可生成状态。此处没有新增SQL或执行。

## 2. 共性语义问题：16条视图/派生DEFAULT

逐case明细已导出：一般INSERT 8条、M INSERT 4条、M UPDATE 2条、一般UPDATE 2条。全部为`default_unknown`，并非16种独立根因。

先把直接视图与派生目标、显式DEFAULT与省略输入、一般与M模式分开。已有基表列血缘只能证明列来源，不能证明DEFAULT应用或继承。一般ALTER VIEW的SET/DROP DEFAULT注明“暂无实际意义”，不能借它放行M或任何视图目标。

下次有实机授权时，用最小独立环境对照：基表有显式常量默认、无默认可空、无默认非空；比较直接基表与受支持视图目标的结果和错误。真实结果与版本/模式绑定，再决定是否需要共享合同；不先编造求值规则，不逐条手改SQL。若来源不能闭合且没有执行授权，这16条继续保留待审即可，不阻塞别的静态任务。

## 3. 56个无普通清单包：按前置资产拆开，不整体标unsupported

导出清单已确认56个包确实没有`manifest_refs`，不是生成运行崩溃。这些包涉及密钥/加密、外部连接/外表、语言处理器/转换/操作符类、数据库与表空间、系统管理、数据导入恢复、模型等不同前置条件。

下一批先选能在隔离环境中定义实际资产的少量代表，读取其已有source与scenario，再判断是否能安全生成。依赖服务、文件、处理器或物理数据库时，记录具体所缺资产及权限；“本机未准备”不等于产品不支持。不要根据命令名字自动生成通用CREATE TABLE fixture，也不把存在scenario文件算成已执行。

M PREPARE仍有5个文档语句族缺profile：ALTER DATABASE、ALTER USER、CREATE USER、DROP USER、权限语句。已有10个族只是代表，不是族内全组合。此批涉及身份与管理权限，优先级不应高于可复用的普通对象缺口。

## 4. 跨阶段依赖是独立设计问题

17章、21正文的引用和副本失效传播已复核，25个失效探针通过；真实任务仍全部needs_review。CREATE SCHEMA与DROP SCHEMA的创建/清理关系暴露了阶段区分需求，不能把所有来源引用都当作同一种调度依赖，也不能关闭循环检测。

只在下一真实消费者被这一问题阻断时，先用该消费者构造失败回归，区分事实来源边、创建前置边和清理阶段边，再做最小适配。不要为清掉图上的环删除事实来源，不在这轮验收后立即建设通用工作流引擎。

## 5. 实机阶段必须单独授权和验收

当前写入审计的305条checked只是有限静态检查；330条transaction_scoped只是生命周期形状，不证明DDL可回滚或清理安全。旧fixture仍存在需要复核的CASCADE等文本，不能直接执行全5187条。

授权后先选少量普通对象代表，逐case检查目标数据库与兼容模式、专属连接、对象归属、setup失败处理、目标语句错误与清理错误的分离。M模式必须先确认实际M物理数据库并重连，不能在一般数据库里通过会话参数假装M。清理只能作用于本case确认创建的对象。

验收保留原始目标SQL、setup、expected、actual、SQLSTATE、Oracle结果、teardown结果；setup失败不得算目标负例通过，清理失败不得掩盖原始错误。本夜没有进行这一步。

## 每批固定交付

1. 精确来源与待解决缺口，说明是否需要新的外部授权。
2. 一个可复用规则的真实失败回归及最小修正；不改审计口径凑绿。
3. 受影响包重新生成，新旧case完整字段、SQL、ID、pair和Oracle对账。
4. 对应模块验证及必要的全项目冻结验证；保留所有失败历史。
5. 分开展示登记、有限生成、静态覆盖和实机验证；未完成原因可定位。

以上是下一阶段计划。09-10这一轮的最终测试输入保持不变，未自动开始新功能、数据库执行或Git推送。
