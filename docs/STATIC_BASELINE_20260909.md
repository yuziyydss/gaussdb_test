# 2026-09-09 双模式离线基线

以本地 V2.0-10.0.0 集中式 PDF 为来源，固定统计 224 个一般章节包和 93 个 M 章节包。原始 PDF、拆分正文和本地运行证据不随代码发布。

| 指标 | 一般章节 | M 章节 |
| --- | --- | --- |
| 登记包 | 224 | 93 |
| 有普通 SQL 候选的包 | 169 | 92 |
| 有限生成模型通过 | 159 | 92 |
| manifest / SQL 快照 | 544 | 228 |
| 候选用例 | 3952 | 1108 |
| 全章静态覆盖通过 | 3 | 0 |
| 本轮数据库执行 | 0 | 0 |

合计 772 份快照、5060 个唯一 case ID。一般章节候选包含明确的 B/PG 等环境条件，不能当作同一环境可直接执行的集合。

最后两份互不重叠的离线回归收据分别为 96 模块 / 772 项和 57 模块 / 394 项，退出码均为 0；合计覆盖全部 153 个测试模块、1166 项，8849 个产品、来源及生成输入端点哈希一致。该结论不代表数据库行为验证或 PDF 全文语义覆盖。

夜间窗口净增 60 个候选、30 份快照。原 742 份快照中 741 份字节不变；M SET 时区的 1 份因增加分支维度而迁移 9 个 ID，SQL、预期及生命周期不变。原 5000 个 ID 中 4991 个保留、9 个有明确映射，迁移不计作新增用例。

主要改进：共享列 RENAME/CHANGE/ADD 合同，M PREPARE 的 12 个有限语句代表，M 变量/EXPLAIN/REPLACE/序列分支，一般 GRANT/INDEX/INSERT/SEQUENCE/DELETE/LOAD DATA 有界候选，以及失败 CREATE 的保守清理门。

剩余缺口：一般全部 224 包共 192 个取值缺口，其中已有候选的包占 76 个；一般 55 包、M 1 包没有普通候选。M 仍有 2565 个未映射 source unit、3249 个原子性待审 unit。一般/M planned 场景分别 530/177，均不是实机通过。完整对象所有权及目标错误 Oracle 仍需独立验证。

下一轮先盘点并完善已有 DEFAULT 与写入列合同，使用一般/M INSERT、UPDATE、ALTER TABLE 的真实消费者；数据库验证须单独授权。

详细设计见 [共享合同集成记录](SHARED_CONTRACT_INTEGRATION.md)；生成结果见 `generated/factor_packages/generation_report.json`。重现来源检查需要用户自行提供匹配哈希的本地 PDF 与正文，不能用空文件或旧环境报告代替。
