[[PDF_PAGE physical=1448 printed=1399]]
1.13.9.24 CREATE GLOBAL INDEX

功能描述
            在指定的表上创建全局二级索引（Global Secondary Index，简称GSI）。
            全局二级索引允许用户定义与基表分布不一致的索引，从而实现基表非分布列查询的
            单节点计划和基表非分布列上的UNIQUE/主键约束。

               说明

              当前形态不支持此功能。
