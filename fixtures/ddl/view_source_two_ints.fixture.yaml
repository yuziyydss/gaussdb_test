# 静态 fixture：只声明对象能力；本阶段不生成或执行 setup SQL。
id: fixture_view_source_two_ints
name: "CREATE VIEW 两整数列源表"
description: "为基础 CREATE VIEW 语法生成提供确定存在的两列源表。"
doc_ref: "SQL参考/DDL/CREATE-VIEW"

tables:
  - name: t_view_source
    columns:
      - name: col_1
        type: INTEGER
      - name: col_2
        type: INTEGER
